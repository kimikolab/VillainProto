# AGENTS.md

**このファイルには規約を書かない。** 内容は `CLAUDE.md` と `CONTRIBUTING.md` が唯一の正。
作業でもレビューでも、手を動かす前に必ずその2つを読むこと。

- **`CLAUDE.md`** — 構成・絶対のルール・アーキテクチャ・検証コマンド（`compare` / `chain` /
  `ablate` / `pulse` / `replay` など）。まずここ
- **`CONTRIBUTING.md`** — 作業手順と**レビューのチェックリスト**。
  レビューを頼まれたらそのチェックリストを使う
- **`README.md`** — 調整の理由と過去の失敗例。数値や特性をいじる前に読む
  （例: 増幅は必ず加算にする — 乗算にしたら毒が発散して戦闘が30ターン上限に張り付いた）
- **`docs/`** — BattleSim の生成物。手で編集しない

## なぜ空なのか

以前ここに `CLAUDE.md` の写しを置いていたが、更新されずに陳腐化した。
発見時点で `chain` / `ablate` / `pulse` / `replay` / `BattleEvent` / `InstanceId` が
どれも載っておらず、`docs/` も「2ファイル」のまま（実際は6ファイル）だった。

**二重管理は必ずずれる。** このリポジトリは「説明文と挙動のズレ」で既に4回事故を起こしていて、
そのたびに検出器を増やしてきた（`dump` の吐き直し、`compare` の差分確認）。
同じ穴を文書側で開け直さないために、ここは参照だけに留める。

内容を書き足したくなったら、それは `CLAUDE.md` か `CONTRIBUTING.md` に書くべきもの。
