# 第138期 報告書 —— 破片に「出口」を作る（新規駒・礫のガレ）

指示書は `design/PHASE138_SHRAPNEL_SPEC.md`。**HEAD 前提: 第137期 `70bca99`。**

---

## 段1 —— Phase 0（コード変更なし）

### Q0-0. origin との同期

`git fetch` 済み。`## master...origin/master`（ahead / behind の表示なし ＝ **同期**）。
作業ツリーは未追跡2件（本指示書・`design/art/hota/compare-validation.txt`）のみで、
**第137期が記録した「未コミットの `BattleEventKind.Parry`」は既にコミット済み**
（`BattleCore/Models.cs:1732` に列挙子として入っている）。**この期も触らない。**

### Q0-1. 破片の在庫の時系列 → **段2 に繰り越す**

**現行の計数では出せない。** `ShatterGiven` は配布の総量、`ScaleWornTurns` は
ウロの纏い率（二値）で、**どちらも「ある時点で1体が纏っている量」を持っていない。**
`ScaleLeftover` は決着時の残量だが、これも**ウロだけ**を合計している（`BattleEngine.cs:6447`）。

指示書の commit 計画どおり、**段2 でターン頭の在庫センサス（計数のみ）を足してから実測する。**
紙からは導かない（第133期・第137期）。

### Q0-2. ループ検査（**最優先**）—— **閉ループは発生しない**

礫が味方を砕いたダメージで、破片を作る側が再発火しないか。3経路すべて確認した。

| 経路 | 発火口 | 礫のダメージで走るか | 根拠 |
|---|---|---|---|
| 砕け（ヒビ・`ShatterTrait`） | `OnDamaged` | **走らない** | `Traits.cs:6646` が `source.CurrentPattern == AttackPattern.Single` で早期 return。ガレの `Def.Pattern` は **単体**で、`ModifyPattern` を持つ札を1枚も載せないので `CurrentPattern` は常に `Single` |
| 集約（ウケ・`BearTrait`） | `BattleContext.Dull` の中（`BattleEngine.cs:5547`） | **走らない** | 破片を書くのは `Dull` の内側 1 箇所だけ。ガレは `Dull` を1度も呼ばない |
| 鱗（ウロ・`ScaleTrait`） | `OnAllyDeath`（供給）／ `OnAfterAttack`（消費） | **走らない** | `OnAfterAttack` の呼び出し元は `PerformAttack` の 2 箇所（`BattleEngine.cs:4278` / `4337`）だけ。`ActionKind.Skill` は `PerformAttack` を通らない（`TakeTurnCore` の `Skill` 分岐は `t.OnAction` を流すだけ） |

**`source.CurrentPattern` を読む規則は盤面に1本しかない**（`Traits.cs:6646` の砕けだけ。
grep で全数：他の 11 件は `PerformAttack` / `SelectTarget` / `ResolvePierce` の
`patternOverride ?? ...` と、台本の表示用）。**したがってループの入口は砕けの1本に限られ、
そこが単体で閉じている以上、破片 → 砕く → ダメージ → 破片 の閉ループは構造的に立たない。**

副次の確認:

- **棘（カド）・仇討ち（ザン）は味方の刃に反撃しない**（`Traits.cs:4588` / `4605` が
  `source.TeamId == self.TeamId` で return）。味方を砕いても反撃は返ってこない
- **`ApplyDamage` に渡す `pattern` は砕けが読まない。** 砕けが見るのは
  `source.CurrentPattern` であって `HitFrame.Pattern` ではない（Q0-3 の判断に効く）

### Q0-3. `Skill` で敵全体に `ApplyDamage` する前例

**味方側の前例は破裂（ゾト・`BomberTrait.OnDeath`・`Traits.cs:1357-1361`）1本だけ。**

```
foreach (UnitState foe in ctx.LivingMembersShuffled(ctx.Opponent(self.TeamId)))
    ctx.ApplyDamage(foe, EnemyBlast, self);
```

`pattern:` を**渡していない**（＝ `DamageRoute.Other` に落ちる）。
敵の詠唱兵は `Charge → 全体` だが、あれは `PerformAttack(patternOverride: All)` なので別経路。
**手番（`OnAction`）から敵全体へ回す前例**は瘴気（グザ・`MiasmaTrait.Spread`）で、
こちらは `ctx.LivingMembers(Opponent)`（**スロット昇順・乱数を1つも引かない**）を使っている。

**決めたこと（2つとも既存の形に揃える）:**

1. **列挙は `LivingMembers`（非シャッフル）を使う。** 破裂の `LivingMembersShuffled` ではない
   ——`Shuffle` は乱数を消費するので、火選り（第58期）が「`FavorRule(0,0)` が素体と1セルも違わない」を
   検算に使えなくなったのと同じ穴が開く。効果は全員一律なので順序に意味が無く、瘴気・火選りと同じ判断。
2. **`pattern: AttackPattern.All` を渡す。** 渡しても**規則は1本も動かない**——
   `HitFrame.Pattern` の読み手は `NoteNourishPath`（糧・保持者 **0 枚**）と
   `NoteHarm` の経路分類（`docs/harm.md` の帳簿）と台本の表示だけで、**分岐する規則は 0 件**
   （`BattleEngine.cs:2818` / `2696-2703` / `BattleEvent.Pattern`）。
   渡さないと `DamageRoute.Other` に落ちて**帳簿の上で「型なし」になる**ので、
   害の帳簿と再生の側で全体攻撃として読めなくなる。
   **砕かれた味方への自傷には渡さない**（`isFriendlyFire: true` ＝ `DamageRoute.Friendly`）
   ——あれは攻撃ではなく代金である。

### Q0-4. `CanAct` 偽 → `IdleTurn` → 手番市場

`BattleEngine.cs:5165-5182`（`TakeTurnCore`）で確認した。

- `CanAct` がどれか1つでも偽 → **`actor.SetCounter(StatusKeys.IdleTurn, Turn)` を立てて `Stalled` で返る**。
  理解は正しい
- **買い手が通すのは `Trait.SurrenderedTurn`**（`Traits.cs:296`）で、これは
  **`CanAct(ctx, u, ActionKind.Attack)` を偽にした札のうち `SurrendersTurnIn` が偽のものがあるか**を見る。
  ガレの `CanAct` は **`kind == Skill` のときだけ**偽になるので、`Attack` で問われると真
  → 否決した札が 0 → **`SurrenderedTurn` は真** → **号令（ガン）・据え（バン）が買い取る**
- 号令側のゲートは `idle == ctx.Turn - 1`（`Traits.cs:5659`）なので、
  **実際に潰れたターンの翌ターンだけ**払われる。無償の毎ターン収入にはならない
- **`SurrendersTurn` は既定（true）のまま何も上書きしない。** ガレが失うのは
  「本来使えたはずの手番」であって、不動（カド）・追い打ち（ハギ）のような
  「もともと持っていないターン」ではない——**ここを偽にすると市場が消える**（指示書 §1-3 の狙いの逆）
- **`Actions` は `[Skill]` 1要素にする。** `actor.ActionIndex++` は `CanAct` 通過**後**にあるので
  （`BattleEngine.cs:5195`）、周期に永久に実行できない種別を混ぜると**その要素で止まって二度と進まない**
  ——engine のコメントが明示的に警告している穴

### Q0-5. `compare` 61行・交差帯12行に触れるか → **触らない**

**ガレを `Presets` に1行も足さない。** 測定は §4-2 のローカル台で行う。

**ただし `UnitCatalog.All` にも足さない**（指示書 §5「ガレを `UnitCatalog` に残したまま」の解釈）。
理由は2つ:

1. **ロスターの上限は 52 ＝ トランプ1組で、第103期に確定している**（`UnitCatalog.All` の doc）。
   足すと 53 になり、**どの既存駒と差し替えるかは指示書自身が次期へ送っている**（Q0-5 末尾）
2. 測って採らなかった素材（オゴ・ゴウ・ヌキ・オノ・ハリ）と器具の札
   （`Undying` / `Regen` / `Reprieve` / `Tempered` / `Grade*` / `Wildfire`）は
   **全部この形**——`UnitDef` / `Trait` は残すが `All` には載せない。**新しい作法を作らない**

**採否が決まった時点で `All` へ入れる**（差し替えと同時）。段3 で「`All` に入れない」ことを
自己検査の1項目にする（`compare` が 0 件差分であることの構造的な保証にもなる）。

### Q0-6. 過去に同じ実験が無いか（`design/*.md` に限定して走査）

- **「破片を消費する」新機構は 0 件。** 第48期の棚卸しが
  「**破片 `armor` の2枚目の読み手**（書き手3・読み手1）」を**埋まっていない空白 4 件のうちの1つ**として
  名指ししている（`design/PHASE48_CENSUS.md:499` / `:319`）が、
  **第47期にウロ（1枚目）が出てから第138期まで 91 期、2枚目は作られていない。**
  第137期は**供給側**（鍵の出どころ）を触った期で、出口には1行も触れていない
- **「全体攻撃を味方に持たせる」は第127期が測っている**（`GradeAll` / `GradeStep`）。
  結論は「**全体の値段は『介入を抜けること』ではなく『一度に全員へ届くこと』**」
  ——介入の素通りは**薙ぎの時点で既に起きている**（`SelectTargetChain` の早期リターンは
  `pattern != Single` の1本）ので、**全体化の代金として数えてはいけない**
- **指示書の「ロスター初の全体攻撃」は半分外れ。** `Def.Pattern` が全体の味方は
  **いまも 0 枚**だが、第128期に `GradeStep` がドルガへ載ったので
  **条件付き（`AtkBonus >= 10`）の全体は既に盤面にある。**
  ガレが初なのは「**`Def.Pattern` にも `ModifyPattern` にも依らず、`Skill` から敵全体へ能動的に撃つ**」形である

### Q0-7. 敵側で実際に回復する駒（**次期のための下見。実装はしない**）

`ctx.Heal` の呼び出し口は **10 本**（`Traits.cs` を全数走査。CLAUDE.md の記載と一致）:
`Drain` / `Necro` / `Colossus` / `Mender` / `Alms` / `Suture` / `Devour` / `Regen` / `Drifter` / `Forsake`。

このうち**敵の `UnitDef` が持つのは 2 枚だけ**（`EnemyCatalog` 全体を走査）:

| 駒 | 札 | `Stages` の在席 | 実態 |
|---|---|---|---|
| 従軍司祭（`Priest` / `PriestG`） | **なし**（`MakeStill` の素の 40/9/8） | `Priest` は第四波 後3 | **1点も回復しない** |
| 従軍司祭長（`Chaplain`） | `Mender` | **0 波**（定義だけ残置） | 盤面に出ない |
| **施しの司祭長（`Almoner`）** | `Alms`（毎ターン 14） | **第二波 後1 のみ** | **実際に回復する唯一の敵** |
| 渇きの祭司（`Droughter`） | 盤面ルール | 第三波 中央 | 回復を**封じる**側 |

**指示書の表と1件も食い違わない。** 第134期の実測「渇きの味方の取り分 100.0%」の原因
（敵の回復役は第二波にいて第三波の渇きと一度も同席しない）がそのまま出ている。

> **§7-1（場の回復を破片に変える）の分母はこれで確定した**——
> 現状、敵側に回るのは **第二波の Almoner の毎ターン14 だけ**である。

---

## 予測（段1 で書き切った。外れても消さない）

指示書 §2 の 7 本を、実装を1回たどってから確定させたもの。

| # | 予測 | 実装のどこから来るか（たどった結果） |
|---|---|---|
| **P1** | ヒビと同席する台で最も大きく動く | 破片の書き手は **ヒビ（砕け）／ウケ（集約・`Dull` の中）／ウロ（味方の死）** の3本。台にウケが居ない以上、供給はヒビとウロの死拾いだけ |
| **P2** | ウロと同席すると取り合いになり、**符号は負** | ウロの貫きは `ModifyPattern` が `Armor > 0` の**二値**（`Traits.cs:3407`）。ガレが最大保持者を 0 にするので、ウロが最大保持者なら**その場で単体へ戻る**。纏い率（`ScaleWornTurns`）が下がる |
| **P3** | 供給が無い台では手番を捨て続け、`IdleTurn` の回数が礫の生存Tに近づく | `CanAct` 偽 → `IdleTurn` は毎ターン必ず立つ（Q0-4）。例外は痺れ・まどろみが先に潰したターンだけ |
| **P4** | 敵側分岐の発火は **0 件** | 敵の `UnitDef` に `Shatter` / `Bear` / `Scale` が **0 枚**（Q0-7 と同じ走査）。敵に `Armor` を書く経路が盤面に1本も無い |
| **P5** | ガレを含まない 61行 ＋ 12行は **±0.0** | ガレを `Presets` にも `All` にも足さないので、`Formation` から参照されない（Q0-5） |
| **P6** | 第四波（軛・1発 25 上限）で全体攻撃が切られ、`Multiplier` を上げるほど伸びが鈍る | 軛は `ApplyDamage` の **`target.Hp -= amount` の直前**で 1 回のダメージを `YokeTrait.Cap`(25) に切る。ガレの一撃は**敵1体ごとに独立した `ApplyDamage` 呼び出し**なので、**体ごとに独立して切られる**。第132期の実測では**全体が切られた率 100.0%** で、型の中でいちばん上限に食われる |
| **P7** | 纏い率（稼働率）が下がっても勝率は上がる（第137期の逆） | 第137期は「稼働率 47.4% → 62.9% で勝率は落ちた」＝**二値の読み手の稼働率と通貨の総量は逆を向く**。今回は総量を出口に変えるので、**同じ2量が逆向きに動けば §0 の読みが裏を取る** |

**P6 の補足（実装をたどって分かったこと）**: 軛が切るのは 1 回の `ApplyDamage` なので、
**全体攻撃は「1 発 25 × 敵の体数」まで通る。** 「全体化すると第四波で丸ごと潰れる」ではなく
「**1 体あたりの上振れだけが潰れる**」——`shards * Multiplier + 攻9` が 25 を超えた分が捨てられる。
したがって **P6 は「伸びが鈍る」であって「効かなくなる」ではない。**

---

## 段2 —— 破片の在庫の走査（`ArmorRule` / `ArmorLedger`・計数のみ）

Q0-1 は現行の計数では出せなかったので、**ターン頭の在庫を写す走査**を1本足した。
`HarmRule(bool Census)`（第135期）と同じ形・同じ理由で、**既定は数えない**。

写す位置は **`TickStatuses` の後・`OnTurnStart` の前**。前に置くのは、
砕けの自前の鍵（`ShatterTrait.OnTurnStart`）と鱗の纏い率の分母が `OnTurnStart` にあるためで、
**そのターンの供給が乗る前**の在庫でないと「礫（`OnAction` ＝ 行動順ループの中）が見る在庫」と別物になる。

### Q0-1 の答え（第2〜5波 × seed 0..199・味方側・**ガレ抜き**）

| 台／行 | ターン頭/戦 | 盤面 平均 | 盤面 最大 | **最大保持 平均** | 最大保持 最大 | 在庫のあるT率 |
|---|---:|---:|---:|---:|---:|---:|
| 台1 変換 (ヒビ×ウロ) | 5.85 | 4.8 | 22 | **3.7** | 22 | 46.9% |
| 台2 素通り (ヒビ×読み手なし) | 8.41 | 2.1 | 16 | **1.8** | 12 | 43.1% |
| 台3 ガルド (ヒビ×ガルド) | 8.57 | 2.6 | 17 | **1.7** | 12 | 23.6% |
| 台4 肩代わり (ヒビ×ゴルム) | 5.73 | 0.9 | 15 | **0.6** | 10 | 13.7% |
| 範囲耐性 (ヒビ×ボルグ) | 8.57 | 2.6 | 17 | **1.7** | 12 | 23.6% |
| 仇討ち×砕け (ヒビ×ザン) | 5.96 | 2.0 | 20 | **1.1** | 10 | 22.7% |
| 鱗改 (ウロ×ヒビ) | 8.49 | 4.0 | 28 | **2.6** | 18 | 35.3% |
| 弱体×被弾 (ドハ×ワタ×ヒビ) | 9.44 | 0.7 | 10 | **0.6** | 9 | 23.4% |
| 破片×被弾 (ヒビ×ウロ×セロ) | 10.95 | 3.2 | 28 | **2.2** | 20 | 34.8% |

**最大保持者は誰か**（全台・全行の合算。同値は走査順で先着に偏る）:
鱗のウロ 20.4% ／ のろまの巨兵ドルガ 19.3% ／ 焼け残りのボルグ 19.3% ／ 呪詛官ネル 16.6% ／
逃亡兵セロ 6.2% ／ 逆しまのウツ 6.0% ／ 墓守リィカ 3.8% ／ 以下 2.5% 未満。

> **鱗の読み手が最大保持者なのは5回に1回だけ**で、残りの8割は「ただの受け手」である。
> **破片は「読み手に集まる通貨」ではない**——砕けは味方全員へ等量を配るので、
> 在庫は「生き延びた駒」に溜まる。**礫が砕く相手を陣営で絞らなくてよいのはこのため**でもある。

**在庫のあるターンは 13.7〜46.9% しかない。** 平均在庫（0.6〜3.7）を
在庫のあるターンだけで条件付けし直すと **4.2〜7.9** で、これが「1回に砕ける量」の見積もりになる。

---

## 段4 —— ローカル台（**指示書 §4-2 の4台を組み直した**）

**指示書どおりの埋め草（ムド × ドルガ）では 台A が 3.4% / 台D が 23.4% ＝ 床だった。**
第61期（素体の5波平均 40% 以上）・第63期（40〜95% の両側）・第133期（段1 のローカル台にも当てる）。

`gscan probe` で埋め草の組を **42 通り**走査した結果、**4台すべてが 40〜95% に入るのは
ドルガ × ガルドの2通りだけ**（左右を入れ替えた版）で、台D の天井に余裕がある側を採った
（**89.9% 対 94.9%**）。

| 台 | 中央 | 後1 | V0（ガレを素体に落とした版）第2〜5波 |
|---|---|---|---:|
| 台A 取り合い | ヒビ | **ウロ**（読み手） | 48.5 |
| 台B 出口だけ | ヒビ | エグ（読まない） | 46.5 |
| 台C 供給なし | **ネル**（供給なし） | エグ | 49.9 |
| 台D 手番市場 | ヒビ | **ガン**（手番の買い手） | 89.9 |

**4台は「後1 の1枚」と「中央の1枚」だけで分岐する梯子**にしてある（A→B は後1、B→C は中央、B→D は後1）。
前1 ドルガ / 前3 ガルドは4台で共通。**ガルドは受け流し（第136期）を持つが破片を1点も書かず読まない**ので
礫の帰属には入らない——ただし**壁として台を成立させているのはこの1枚**である。

### 掃引点（**実測から引いて先に固定した**）

指示書 Q0-1 の注記どおり「**1回に砕ける量 × 発火回数**」から引く（1戦の総量を発火回数で割る式は使わない）。
**ガレを入れた版で測り直した**——礫は在庫を消費するので、段2 の値（ガレ抜き）は上振れしている。

| 台 | 発火/戦 | 捨て/戦 | 発火率 | **1回に砕ける量** | 条件1 を満たす最小 M |
|---|---:|---:|---:|---:|---:|
| 台A | 2.45 | 2.10 | 53.8% | **3.97** | 3 |
| 台B | 2.39 | 2.54 | 48.5% | **3.87** | 3 |
| 台C | **0.00** | **11.09** | **0.0%** | 0.00 | — |
| 台D | 1.56 | 1.68 | 48.2% | **4.05** | 3 |

（この表は**当時の既定 `Multiplier = 2`** で測った値。段5 の `M=2` の行と一致する
——`砕いた/戦 ÷ 発火/戦` ＝ 9.7 / 2.45 ＝ 3.96。**採用後は既定が 3 になっているので `gscan` の数字は動く。**）

採用条件1（破片由来 > 攻撃力由来 ＝ 攻9）は「1回に砕ける量 × M > 9」と同値なので、**閾値は M = 3**。
**その 3 を跨ぐように 2 / 3 / 4 に置いた。**

> **見積もり（段2 の 4.2〜7.9）と実測（3.87〜4.05）は桁で違わない**ので、指示書 Q0-1 の停止条件には当たらない。
> ずれの向きは予想どおり（礫が在庫を食うので下がる）。

---

## 段5 —— 掃引

第2〜5波 × seed 0..199（規約 (G10)）。**`SelfDamagePercent` は 100 で固定**（指示書 §1-4）。
**V0 は同じ台のガレを素体に落とした版**（席も他の4枚も動かさない）。

| 台 | 版 | 第2〜5波 | 帰属 | 発火/戦 | 捨て/戦 | 砕いた/戦 | 名目/戦 | 実通/戦 | 通し率 | 破片由来 | 決着T |
|---|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 台A | V0 素体 | 48.5 | — | — | — | — | — | — | — | — | 14.94 |
| 台A | M=2 | 77.9 | **+29.4** | 2.45 | 2.10 | 9.7 | 169.1 | 169.1 | 100.0% | 46.9% | 9.88 |
| 台A | M=3 | 82.1 | **+33.6** | 2.24 | 1.97 | 8.7 | 193.0 | 192.7 | 99.8% | 56.5% | 8.85 |
| 台A | M=4 | 84.9 | **+36.4** | 2.10 | 1.86 | 7.9 | 209.7 | 208.3 | 99.3% | 62.5% | 8.01 |
| 台B | V0 素体 | 46.5 | — | — | — | — | — | — | — | — | 14.24 |
| 台B | M=2 | 66.8 | **+20.3** | 2.39 | 2.54 | 9.3 | 166.6 | 166.6 | 100.0% | 46.2% | 10.46 |
| 台B | M=3 | 72.1 | **+25.6** | 2.11 | 2.42 | 8.1 | 186.8 | 186.8 | 100.0% | 56.2% | 9.80 |
| 台B | M=4 | 75.6 | **+29.1** | 2.03 | 2.25 | 7.8 | 201.7 | 201.7 | 100.0% | 63.1% | 9.35 |
| 台C | V0 素体 | 49.9 | — | — | — | — | — | — | — | — | 17.41 |
| 台C | M=2 | 50.4 | **+0.5** | **0.00** | 11.09 | 0.0 | 0.0 | 0.0 | — | — | 17.57 |
| 台C | M=3 | 50.4 | **+0.5** | **0.00** | 11.09 | 0.0 | 0.0 | 0.0 | — | — | 17.57 |
| 台C | M=4 | 50.4 | **+0.5** | **0.00** | 11.09 | 0.0 | 0.0 | 0.0 | — | — | 17.57 |
| 台D | V0 素体 | 89.9 | — | — | — | — | — | — | — | — | 8.22 |
| 台D | M=2 | 92.8 | +2.9 | 1.56 | 1.68 | 6.3 | 271.1 | 202.4 | **74.7%** | 47.4% | 5.55 |
| 台D | M=3 | 94.9 | +5.0 | 1.55 | 1.55 | 6.3 | 297.1 | 223.0 | **75.1%** | 57.6% | 4.81 |
| 台D | M=4 | 94.9 | +5.0 | 1.55 | 1.56 | 6.3 | 313.2 | 233.7 | **74.6%** | 64.5% | 4.77 |

**台D の帰属が小さいのは天井のため**（V0 が既に 89.9%・第2〜4波が全部 100%）。
第24期の「天井・床のセルでは誰に注入しても 0 に潰れる」がそのまま出ている——**M=3 と M=4 が同値**なのもそれ。

---

## 段5 で踏んだ事故 —— `BattleEngine.Run` の引数を2本増やしたら `compare` が落ちた

**`docs/` の再生成で見つかった。** 段5 まで全部通ったあとに `compare` を回したら、
**`ApplyDamage` の再帰でスタックオーバーフロー**（exit 127・途中で死ぬので `docs/balance.md` が壊れる）。

    Stack overflow.
       at BattleCore.BattleContext.ApplyDamageBody(...)
       at BattleCore.BattleContext.ApplyDamageCore(...)
       at BattleCore.BattleContext.ApplyDamage(...)
       at BattleCore.BattleContext.ApplyDamageBody(...)      ← これが数千段
       ...
       at BattleCore.BattleContext.ResolvePierce(...)
       at BattleCore.BattleEngine.Run(... 51 個の引数 ...)

**切り分け**（作業前の `6004cbe` を `git worktree` に出して4回測った）:

| 版 | `compare` |
|---|---|
| 作業前の `BattleCore` ＋ 作業前の `BattleSim` | **通る** |
| **この期の `BattleCore`** ＋ 作業前の `BattleSim` | **落ちる** |
| この期の `BattleCore` から **`Run` の引数2本だけ**を外した版 | **通る** |
| この期の `BattleCore` に **引数を1本だけ**戻した版 | **通る** |

> **原因は機構ではなく引数の本数。** `BattleEngine.Run` は `verbose` と観測子を除いて既に **47 本**の引数を持っていて、
> **その1段下で `ApplyDamage` が数千段の再帰をしている。**
> 引数を1本増やすと `Run` の呼び出しフレームが太り、**再帰に残るスタックがそのぶん減る。**
> **1本なら通り、2本で踏み抜く**——つまり**余裕はもう1本ぶんしか無かった。**

**採った直し方**: `ArmorRule` を独立した型にするのをやめ、`ShrapnelRule` に
`ArmorCensus` の1ビットとして畳んだ（**新しい引数は `shrapnel` の1本だけ**）。

- **`ShatterRule`（第137期）に足さなかった**のは、規約 (G8) の必須3
  「触っていないノブの既定が動いていない」を偽にしないため
- **本来は別の型にしたい**——破片の在庫はこの駒の持ち物ではなく通貨そのもので、
  素直に書けば `HarmRule(bool Census)` と同じ形になる。**畳んだのは引数の本数の都合だけ**であり、
  そのことを `ShrapnelRule.ArmorCensus` の doc に書いてある

**この事故は `shard check` では捕まらなかった。** 同じ 305 セルを同じ seed で回しているのに、
**`compare`（`Main` の直下）では落ち、`shard check`（`ShardDiag.Check` の中）では落ちない**
——**再帰の手前に積まれているフレームの量が呼び出し口で違う**ためである。
**「自己検査が通った」は「生成物が作れる」を意味しない。**

---

## 採否

| 条件 | 内容 | 判定 |
|---|---|---|
| 1 | `ShrapnelDealt` のうち破片由来が攻撃力由来を上回る掃引点が存在する | **○**（M=3 で 56.2〜57.6% ／ M=4 で 62.5〜64.5%） |
| 2 | 供給ありの台で上がり、**供給なしの台で下がる** | **半分 ×**（供給あり +2.9〜+36.4 ／ 供給なし **+0.5**。下がってはいない） |
| 3 | 特異性（ガレを含まない 73 行が ±0.0） | **○**（自己検査 (G-a)(G-e)） |

**条件2 の × の中身は「破片を読んでいない」ではない。**
供給なしの台（台C）は **発火 0.00 回 / 砕いた 0.0 / 名目 0.0** で、**礫は1度も仕事をしていない**
——条件2 が防ごうとしていた失敗（破片を読まない全体攻撃駒になっていること）は起きていない。
+0.5pt の正体は**素体との差**で、**V0 の素体は `Actions` を落としてあるので攻9 で毎ターン殴る**のに対し、
V1 のガレは**何もしない**。その差が第2波 −5.0 / 第5波 +4.0 と波で割れて、通算 +0.5 に落ちている。

> **「何もしない駒」が「攻9 で殴る駒」とほぼ同値だった**、が台C の実体である。
> **条件2 を「下がる」で書いたのが物差しの側の問題**（規約 (G11)）——
> 礫のマイナスは「手番を捨てる」ことであって「編成を弱くする」ことではないので、
> **素体に対して負になる保証は最初から無い。** 次に同じ形を測るなら、
> 条件は「**供給なしの台で帰属が 0 に潰れる**」と書くべきだった。

**採用する。** 指示書 §5 の「壊れ気味は許容する／優先順位は新しい編成・シナジーが生まれるか」に照らして、
- **破片という通貨に初めて量の意味が生まれた**（M=3 で払い出しの過半が破片由来）
- **供給の有無で機構が完全に切り替わる**（台C は発火 0.00 回・台A/B は 2.4 回）
- **既存の 73 行は1セルも動かない**（採用してもロスターに入れないので、次期の差し替えまで盤面は不変）

**採用値は `ShrapnelRule.Default = (3, 100)`。** 条件1 を満たす最小の掃引点で、
**結果を見てから掃引点の外へ出ていない**（第64期）。M=4 のほうが勝率は高いが、
**条件1 の閾値を跨いだ最初の点を採る**のが強度の決め方（第135・136・137期の則）。

---

## 自己検査（規約 (G8) の必須4項目 ＋ この期の (G-a)〜(G-e)）

| 項目 | 結果 |
|---|---|
| 必須1 `compare` 305 セルが `docs/balance.md` と 0 件 | **○ 0 件**（`compare` を回し直して**全文一致**も確認した） |
| 必須2 `docs/` を再生成して差分を報告する | **○** 動いたのは `rules.md`（ノブ1本）と `units.md`（`Shrapnel` の1行）**だけ**。`audit` は「ずれているファイル 0 件」 |
| 必須3 触っていないノブの既定が動いていない | **○** `docs/rules.md` の差分は**追加2行と本数の行だけ**（既存の既定値は1つも動かない） |
| 必須4 `ctx.PickOne` を新たに使っていない | **×（意図的）** 礫の対象選択で**同値のときだけ**使う（指示書 §1-2 が明示）。**保持者が `compare` 61行・交差帯12行に 0 枚**なので、305 セルは 0 件のまま |
| (G-a) ガレが `All` にも `Presets` にもいない | **○** `All` ＝ 52 枚・73 行に在席 0 |
| (G-b) 札と行動の形（`[Skill]` 1要素・`Def.Pattern` 単体・`Advances = false`） | **○** |
| (G-c) ループ検査（砕けの発火が礫で増えない） | **○** 砕け発火 ÷ ヒビ生存T ＝ 0.70〜0.84（1ターンに1回未満） |
| (G-d) 帳簿が閉じる | **○** 撃った ＋ 捨てた ＝ 生存T **− 1.1〜1.3**。痺/眠は 0 なので、残差は**決着したターンに手番が回ってこなかったぶん**（`LastActiveTurn` は生存者に `min(turn, MaxTurns)` を入れるが、そのターンのループは走っていない）。砕いた ≦ 配られた も4台とも ○ |
| (G-e) 73 行が `ShrapnelRule` に依らない | **○** 365 セル 0 件（`M = 2` 対 `M = 8`） |

---

## 予測の答え合わせ（**外れたものも消さない**）

| # | 予測 | 判定 | 実測 |
|---|---|---|---|
| **P1** | ヒビと同席する台で最も大きく動く | **○** | ヒビあり +20.3〜+36.4 ／ ヒビなし（台C）+0.5 |
| **P2** | ウロと同席すると取り合いになり、**符号は負** | **半分 ×** | 取り合いは起きた（纏い率 42.6% → 35.1%・ウロ生存T 8.56 → 5.21）が、**勝率は 48.5 → 84.9 と最も大きく上がった** |
| **P3** | 供給が無い台では手番を捨て続け、`IdleTurn` の回数が礫の生存Tに近づく | **○** | 発火 0.00 / 捨て 11.09 / 生存T 12.32 |
| **P4** | 敵側分岐の発火は 0 件 | **○** | 4台とも `ShrapnelFoeTargets` = **0**（次期の点灯確認に使う回帰値） |
| **P5** | ガレを含まない 61行 ＋ 12行は ±0.0 | **○** | 73 行 / 365 セル 0 件差分（`M = 2` 対 `M = 8`） |
| **P6** | 第四波（軛）で切られ、M を上げるほど伸びが鈍る | **半分 ○** | **台D は通し率 39.8 → 36.3%** と大きく切られるのに、**台A は 100.0 → 97.8%・台B は 3点とも 100.0%** |
| **P7** | 纏い率が下がっても勝率は上がる（第137期の逆） | **○** | 台A で 42.6% → 35.1% / 48.5 → 84.9pt。**第137期は「稼働率↑・勝率↓」、こちらは「稼働率↓・勝率↑」** |

---

## この期に分かったこと

**破片は「読み手に集まる通貨」ではない。生き延びた駒に溜まる。**
最大保持者はウロ 20.4% / ドルガ 19.3% / ボルグ 19.3% / ネル 16.6% と散っていて、
**唯一の読み手（ウロ）が最大保持者なのは5回に1回**しかない。
砕けは味方全員へ**等量**を配るので、在庫の差を作っているのは配り方ではなく**誰が生き残るか**である。
——**「その通貨の読み手が持っているはずだ」を数えずに書くと外す**（第115期「供給の帳簿を見ても
読み手に何点届くかは分からない」の在庫側の版）。

**二値の読み手と量の読み手は、同じ在庫を取り合っても符号が逆に出る。**
礫は最大保持者を選ぶので、破片を溜めたウロが真っ先に砕かれる——
**纏い率 42.6% → 35.1%・ウロ生存T 8.56 → 5.21T** と、読み手としてのウロは明確に痩せる。
それでも**その台の帰属が4台で最大（+36.4pt）**になった。
第137期は「纏い率が上がったのに勝率が落ちた」で、**今回はその完全な裏返し**である。

> **稼働率は機構の健康の指標ではない。** 第137期の「二値で読む機構の稼働率と通貨の総量は逆を向く」に、
> **出口を足すと稼働率と勝率まで逆を向く**という続きが付いた。

**上限（軛）に食われるかどうかを決めているのは強度のノブではなく、同席する強化である。**
第四波の通し率は 台A 97.8〜100.0% / 台B 100.0% に対し **台D は 36.3〜39.8%**。
台D だけが号令（ガン）を含む——**`shards × M ＋ 自分の攻撃力` の後ろの項が号令で膨らむ**ので、
1体あたりの一撃が 25 を越える。**`Multiplier` を 2 → 4 に倍にしても通し率は 39.8 → 36.3% しか動かない**
のに、**号令を1枚足すと 100% → 39.8% に落ちる。**
第132期「上限に課金されるのは平均の大きさではなく分布の上側の厚み」の**式の項の側の版**で、
**掃引したノブではないほうの項が上限を決めていた。**

**全体攻撃は軛に「丸ごと」潰されない。** 軛は1回の `ApplyDamage` を切るので、
全体攻撃は「1発 25 × 敵の体数」まで通る——**捨てられるのは1体あたりの上振れだけ**である。
実測でも台D の第四波は 36% しか通らないのに、その版の第四波の勝率は 100.0% のままだった。

**「供給なしの台で下がること」を採否の条件にしてはいけない。**
礫のマイナスは「手番を捨てる」ことで、**編成を弱くすることではない**。
実測で台C は **+0.5pt**（発火 0.00 回）——「何もしない駒」が「攻9 で毎ターン殴る素体」と**ほぼ同値**だった。
条件は「**帰属が 0 に潰れる**」と書くべきで、**符号を要求すると物差しが機構ではなく素体の強さを測る**
（規約 (G11) の採否側・第126期の `残存` と同じ形）。

**指示書の台は、指示書の埋め草のままでは床に落ちることがある**（第137期 段1 と同じ形で2期連続）。
ムド × ドルガでは台A 3.4% / 台D 23.4%。42 組を走査して**帯に入るのは 2 通りだけ**だった
——**通ったのはどちらも「壁（ガルド）＋ 大打点（ドルガ）」の組**で、
**礫・ヒビ・ウロ・ガンで3〜4枠が埋まる台では、残り2枠に壁と火力を両方入れないと成立しない。**

---

## 次期への持ち越し

- **ガレを既存駒のどれと差し替えるか**（採用が決まったので、これが次期の最初の作業）。
  `checkup` の下位と `docs/harm.md` から選ぶ。**`UnitCatalog.All` に入れるのはそのとき**
- **場の回復を破片に変える対称ルール**（指示書 §7-1）。**これが礫の敵側分岐のイネーブラ**で、
  この期の **`ShrapnelFoeTargets` = 0 が点灯確認の回帰値**になる。
  分母は Q0-7 で確定した——**現状、敵側に回るのは第二波の施しの司祭長の毎ターン14 だけ**
- **`SelfDamagePercent` を振る／`lethal: true` にするか**（この期は 100 / false で固定した）
- **礫を周期（`[Attack, Skill]`）にするか**。溜める時間が生まれ、いつ撃つかの読み合いが立つ
- **号令 × 全体攻撃 × 軛の3者**。台D の通し率 36% は**この期に見つけた穴**で、
  「強化を積んだ全体攻撃は第四波でだけ帳消しになる」が編成の判断として立つかは測っていない
