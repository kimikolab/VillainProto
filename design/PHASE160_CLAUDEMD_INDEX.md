# 第160期 報告 —— `CLAUDE.md` の一般則を索引化した

**文書だけの期。`BattleCore` / `BattleSim` / `docs/` は1行も触っていない。測定も行っていない。**
触ったのは **`CLAUDE.md` 1ファイルと、新規の `design/RULES_001_097.md` / `RULES_098_173.md` / `RULES_174_242.md` の3ファイル**だけ。

---

## 1. 前後の文字数

**節ごと**（`## ` 見出しで切った文字数。バイト数も併記する——§6-1 参照）:

| 節 | 前（文字） | 後（文字） | 差 |
|---|--:|--:|--:|
| **指示書を書くときに当てる一般則** | **84,056** | **20,520** | **−63,536（−75.6%）** |
| コマンド | 56,813 | 56,813 | 0 |
| アーキテクチャ | 32,698 | 32,698 | 0 |
| 現状値 | 29,612 | 29,612 | 0 |
| 知見の索引 | 19,588 | 19,588 | 0 |
| その他5節 | 13,063 | 13,063 | 0 |

**全体**: **235,957 → 172,421 文字**（466,454 → 314,377 バイト）。

`design/RULES_*.md` の3ファイルは **32,801 / 33,180 / 30,068 文字**（指示書の上限 50,000 の内側）。
**分け方は主題ではなく ID 範囲**で、1ファイルがほぼ同じ大きさになる点で機械的に切った。

### 1-1. 目標「150k 未満」には届かない。指示書の §0-1 が単位を混ぜている

**指示書 §0-1 の表はバイト数、「232.7k 文字」は文字数**である
（表の合計 451,834 は作業前のバイト数 466,454 とほぼ一致し、232.7k は文字数 235,957 とほぼ一致する）。
一般則の節は **181,617 バイト ＝ 84,056 文字**で、**全体の 44%（バイト）だが 36%（文字）**。
したがって **§2 の「一般則だけで目標に届く」という前提は成り立たない**
——本文を全部出しても **172,421 文字 / 314,377 バイト**で、どちらの単位でも 150k を超える。

> **則の候補**: **文字数とバイト数を混ぜた表から目標値を引くと、到達不能な目標になる。**
> 規約 (G4)（分母を必ず書く）の**単位の側**で、
> 第159期の (G4) 系「線は値ではなく<u>量の定義</u>で1つに決める」（R238）と同じ形である。

**次に削るなら `コマンド`（56,813 文字・作業後の最大の節）。**
この節も `知見の索引` と同じ形（1モード1行 ＋ 詳細は `design/`）に畳める見込みがある。

---

## 2. ID の総数と、畳み込みで減った数

- **段落ユニット 248**（指示書 §0-1 の「248 段落」と一致。
  引用ブロック・コード表・箇条書きの続きは、直前の則の本文として同じユニットに入れてある）
- **ID 242**（`R001`〜`R242`・欠番なし・昇順）
- **畳み込み 6 件**（ID を新しく作らず、本文側に見出しだけを置いた）

| 本文側の見出し | 種類 | 中身 |
|---|---|---|
| `#### R011 の再発（第149期）` | **再発** | 「計数が版に依らない」の分母がターン数——第113期と**逐語でほぼ同一の則**が第149期に再掲されていた |
| `#### R019 の再発（第135期）` | **再発** | 「二値の制約は出口にしか置けない」——本文自身が「第25期の軛・第126期の猶予に続く**3例目**」と書いている |
| `#### R112 の続き` | 続き | 「同じ駒・同じ通貨・同じ式で符号が逆」（単独では則にならない文） |
| `#### R125 の続き` | 続き | 「どちらも同じ穴」 |
| `#### R233 の続き` | 続き | 「ρ を並べて初めて符号が割れる」 |
| `#### R242 の続き` | 続き | 「重い波を先頭に置くと…帰属そのものが小さくなる」 |

**「再発」と「続き」を見出しの語で分けた**——前者は索引の `期` 欄に期番号が並ぶ（`R011` は `113/149`、`R019` は `126/135`）が、
後者は同じ期の同じ段落の続きなので `期` 欄を増やさない。

**本文の位置は1つも動かしていない**ので、`design/RULES_*.md` を上から読むと
`CLAUDE.md` の元の順序がそのまま出る（畳んだ6件は ID を持たない小見出しとして元の位置にある）。

---

## 3. 畳む候補に挙げたが、畳まなかったもの（次の期の材料）

**§1-4 の手順4「迷うものは畳まない」に従って触っていない。**

### 3-1. 単独で則として読めるが、直前の則の続きでもある段落（5件）

| ID | 直前 | 一行 |
|---|---|---|
| R100 | R099（第129期・密度の表） | 「守れば起動する」と「守っても働きが薄まる」は発火回数では区別が付かない |
| R144 | R143（第133期・上限の課金） | 同じ回数だけ上限に触れているのに、乗算は加算のちょうど 2 倍を捨てている |
| R148 | R147（第134期・3種類の非対称） | 盤面ルールの合否テスト1は、波ごとに数えないと通ってしまう |
| R160 | R159（第140期・`All` の全数） | ハリの側は 32 期ぶん壊れたままだった |
| R168 | R167（第142期・コンパイル代金） | 指示書の予測「最初の10本では…」は外れた |

**5件とも「その段落だけで指示書に引ける文」を持っている**ので、ID を与えたまま残した。

### 3-2. 「N例目」で繋がっている族（畳まない。族の一覧としては使える）

同じ番号の族を1つの ID に潰すと**個々の失敗の形が消える**ので触っていない。
族として引きたいときのために、ここに並べておく。

- **判定式が読む対象が到達不能・見えない**（第59・60・61・107・110・117・118・121・126・155・157期）
  → R013 / R016 / R017 / R063 / R012 / R021 / R194 / R227 ほか
- **実装から引く索引・走査が静かに嘘をつく**（第117・121・123・129・130・132・142・158期）
  → R022 / R034 / R035 / R095 / R105 / R109 / R140 / R170 / R231
- **二値／「殺さない」制約の置き場所**（第25・126・135期）→ R019（畳み込み済み）／ R102
- **生成物の自己参照**（第124・131・132・148期）→ R036 / R113 / R140 / R188

### 3-3. 近いが別の則（畳まない）

- R019「『殺さない』制約は出口にしか置けない」 対 **R102「『殺さない』器具は、出口に置くだけでは足りない」**
  ——後者は「置き場所」ではなく「対にする戻し」の話で、別の則。
- R184「同じ供給量まで絞っても、確率と回数制では値段が違う」（時間軸） 対
  **R211「同じ供給量でも、1ターンの中の束ね方で値段が違う」**（1ターンの中）——本文が明示的に区別している。

---

## 4. 50期以上使われていない則（**数えただけ。この期では何もしない**）

**現在 第159期。本文中に現れる最大の期番号が 109 以下のものを数えた。40 / 242 本（16.5%）。**

**うち 7 本は本文に期番号が1つも無い**（初出は `git log -S` で引いた）:

    R001（第48期）  主表の判定式を Traits.cs だけの grep で書くと5件落ちる
    R005（第47期）  OnAllyDeath は召喚枠の死も通る
    R085（第46期）  驕り（棄却）は All にも CompareBuilds() にも無い
    R086（—）       layout は粗い当たり。採否は reseat → confirm で決める
    R088（—）       長時間ジョブは前景で待ち切ること
    R148（第134期） 盤面ルールの合否テスト1は、波ごとに数えないと通ってしまう
    R168（第142期） 指示書の予測「最初の10本では…」は外れた

R086 / R088 は `reseat` / `confirm` を足したコミット（`0a62d77`）で入った文で、
**当時のコミットメッセージに期番号が無い**ので `期` 欄を `—` にしてある（第45期前後）。

**残り 33 本の最終言及期**: 29 / 38 / 43 / 45 / 46 ×2 / 47 / 50 / 63 / 64 ×2 / 68 / 69 / 75 ×3 /
76 ×2 / 78 / 82 / 83 / 86 / 92 ×2 / 96 / 102 / 103 / 104 ×2 / 107 / 108 / 109 ×2。

**目立つのは席の族**——R002（`reseat` の1位）・R004（発火0の席）・R006（閾値 5.0pt）・R007（床と天井）・
R008（素体を引く期では使わない）は**どれも 第64期以降ほとんど引かれていない**のに、
第148期の席の棚卸し（R192）と第143期の転生（R182）は同じ問題を踏んでいる。
**「使われていない」が「古い」を意味するとは限らない**ので、廃棄は次の期の判断に残す。

---

## 5. 検算

| # | 項目 | 結果 |
|---|---|---|
| (a) | 移す前の一般則の本文（節から見出し・前書き・末尾の1行を除いた 263 ブロック）の sha256 | `8a38de84…dc73ec20` |
| (b) | `design/RULES_*.md` 3ファイルから `### R…` / `#### …` の見出し行を除いた本文の sha256 | `8a38de84…dc73ec20` |
| (c) | **(a) と (b) が一致** | **一致（逐語で1文字も失われていない）** |
| (d) | 則の数 | 段落ユニット **248** ＝ 則見出し **242** ＋ 再発・続き **6** |
| (e) | 索引の行数 | **242**（= ID の数・昇順・`R001`〜`R242`・欠番なし） |
| (f) | `CLAUDE.md` の文字数 | **235,957 → 172,421**（目標 150k には届かない。§1-1） |
| (g) | `compare` 305 セル | **`docs/balance.md` と 0 件差分** |
| (h) | `audit` | **ずれているファイル 0 件** |

**(c) は一度落ちた**——`design/RULES_*.md` の各ファイル末尾の改行1つが本文側に付いていたためで、
検算器の側（ファイル末尾の改行を1つ落とす）を直して通した。**本文は1文字も触っていない。**

**追加で確かめたこと**: `docs/rules.md` を再生成して**差分 0 行**。
第131期の則（最終列が `CLAUDE.md` / `LESSONS_*.md` を走査する）に引っかかる懸念があったが、
**移した型名はすべて `現状値` / `コマンド` 節か `LESSONS_*.md` にも出ている**ので列は動かなかった。
**`design/RULES_*.md` は走査対象に入っていない**——次に則の本文にしか無い型名が現れたら、
そこで初めてこの列が静かに × になる。**走査対象へ `RULES_*.md` を足すかは、次の期の判断に残す。**

---

## 6. この期で決めなかったこと（次の期の論点）

1. **`コマンド` 節（56,813 文字）の整理。** 作業後は**ここが最大の節**で、150k を目指すなら次はここ。
2. **`現状値` の鮮度管理。** 第119期の値が 33 期残っていた件があるが、索引化とは別の課題。
   いまも「その1つ前が…」の連鎖で **29,612 文字**ある。
3. **則の廃棄・倉庫送り**（§4 で数えただけ）。
4. **`derive rules` の走査対象に `design/RULES_*.md` を足すか**（§5 の最後）。
