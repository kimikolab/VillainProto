# 第89期 —— 傷も肩代わりする（浅く広い供給に深さを作る）

> **（結論はここに書く。測定が終わってから。(P1)(P2) と本編の3つを含める。）**

指示書: `design/PHASE89_GATHER_SPEC.md`。起点は第88期（`gauge`・第85期を採用＝`SutureRule.Both` ＋ `SpillWoundRule` が既定 on）。

**この期は baseline が2回動く。順序は (P1) → (P2) → 本編。**

---

## 3. 判定基準（**測る前に固定した。この節は本編の測定より先にコミットしてある**）

第88期で作り替えた規約をそのまま使う。**大きさは線にしない。**
**ノイズ床の定義だけがこの期に変わった**（§1-1。以後の規約）。

### 3-0. 器具

- 2×2 は第81期 `pairs2` の写し。**定数は1つも変えない**（`TableSeed` = 8,100,000 ／ K = 64 ／ S = 2 ／ M = 8 ／ 弱い波 60% ／ 強 7）。
- **A ＝ ガルドに固定**（第85期に A・B の割り当てで添字の意味が変わって一度落ちている。**A を固定してから添字を書く**）。
  TSV の添字は **0 = y11 ／ 1 = y01（ガルド素体）／ 2 = y10（B 素体）／ 3 = y00**。
- 情報帯（第88期 §4-1）: `y00` と `y01` の**両方が 0.0%** または**両方が 100.0%** の台を分母から外す。
  情報帯が 20 台未満の組は「測れていない」と書き、判定に使わない。

### 3-1. ノイズ床（**この期から規約**）

> **増分尺度のノイズ床 ＝ 同じ実験の中の「意図しない相手」の \|Δ相乗\| の 95 パーセンタイル。**
>
> 根拠: **対にした設計（同じ台・同じ席・同じ戦闘 seed）では、真の効果がゼロなら Δ は厳密に 0 になる**ので、
> 「同じ版を2回」では増分の陰性対照が作れない（第88期 §8-2）。
> 増分の物差しは実験の外から持ってこられず、**同じ実験の中の効くはずのない相手から取るしかない。**

**この定義のもとでは Q1-2 は飾りに近い**——床は意図しない相手の分布の 95%tile なので、
**構成上おおよそ 5%（43 体なら 2.2 体）が必ず超える**。線 3 はそこに合わせてある。
**判定の中身は Q1-1、すなわち「意図した相手が意図しない 95% の相手より上に立つか」という順位の検定である。**

### 3-2. 主判定 —— 特異性

> **Q1-1: 意図した相手**（ハリ・ゴルム・ボルグ・カド・ナラ・リィカ・ゾト の7枚）**のうち、
> 2系列とも正で \|Δ\| がノイズ床を超えるものが 1 枚以上。**
> **Q1-2: 意図しない相手**（残り43枚）**で床を超える体数が 3 以下。**

### 3-3. 副判定

- **Q2: 深さ。** 味方の傷の最大の深さと分布（Z0 → Z1・波別）。**ガルド1体に集まった傷の平均と最大**を単独で出す。**これが機構の実体。**
- **Q3: 終端の出力。** ハリが**味方側から糸を引いた回数**と、**そのときの深さ**（Z0 → Z1）。
- **(A) 読み手の発火回数**（第86期から常設。稼働率＝発火 ÷ 決着T を併記）。
- **(B) 持続係数**（第87期から常設。**単独では採否を決められない。決着ターン数を必ず併記**）。
- **(C) 台の乖離**（第87期から常設。**理想台にも情報帯を当ててから**）。
- **情報帯の割合**（第88期 §8-5。**ミオの 56.5% と並べる**）。

### 3-4. 拒否権（**大きさはここにだけ残す**）

1. **主判定19行の第五波平均が `PrimaryFifthFloor` を下回る**（**§1-2 で更新した値を使う**）
2. **`compare` で第五波が 95% を超える行が新たに2行以上生まれる**
3. **主判定19行のいずれかが、いずれかの波で −10.0pt 以上落ちる**

### 3-5. 自己検査

(a) ガルド素体の2セルが Z0 / Z1 で完全一致 ／ (b) 味方の傷の総量が Z0 と Z1 で一致（移すのであって増やさない）／
(c) ガルドの `AtkBonus` が Z0 / Z1 で一致 ／ (d) 敵の殉教者が傷を引き取った回数が 0 ／
(e) ガルドが `MostHurtAlly` の患者になった回数が 0 ／ (f) 引き取り ÷ 庇い成立 ≤ 1.00 ／
(g) 紙のスループットと実測が 1.5 倍以内 ／ (h) ガルドを含まない行が `compare` で全セル ±0.0 ／
(i) 主判定が2系列で同符号 ／ (j) `docs/` 8ファイルを再生成して差分 0 バイト（採用前）。

### 3-6. 紙のスループットの形（**測る前に宣言する。自己検査 (g)**）

**分子は線形である。** 1回の庇いにつき傷は 1 つしか動かず、その 1 つが生む回復は
**ハリが味方側から糸を引いたときに `PerWound` = 3 が1回だけ乗る**——`2L² − L` のような累積は生まれない
（第87期の毒の層とは違い、傷は毎ターン刻まない）。したがって

    増分/戦（上限） = min(引き取り回数/戦, ハリが味方側から糸を引いた回数/戦) × PerWound(3)

**第85期と同じく、実測は下限にぴたり乗るはず**（供給は読まれる前に決着に消える側へ倒れる）。
**停止条件: 増分/戦 ÷ 総被ダメージ/戦 ≥ 5%。落ちたら 2×2 を回さずに閉じる。**

**指示書 §1-3 の式**（庇い成立回数 × 隣に傷のある味方がいた率 × ハリが深い糸を引けた回数 × 3）は
**2つの回数の積になっていて次元が合わない**ので、上の形に読み替えた。**両方を表に並べる。**

---

## 予測（**測る前に書いた**。指示書 §1-3 の写し ＋ この期の追加）

- **意図した相手の1位はハリ。** 味方の傷の深さを読む唯一の駒。
- **2位・3位は供給の密な2枚**（ゴルム＝毎ターン全隣接の吸い／ボルグ＝攻撃ごとの余波）。第85期 表D の順位がそのまま出るはず。
- **リィカ・ゾトは効かない**（開戦1回・死亡時。第67期）。
- **ハリ不在では 0 に近い。** 落ちた場合、**「中継が効かない」のか「終端が1枚しかない」のか**を分けて書く。
- **第二波（粛）は関係ない**（庇いは `SelectTargetChain` の標的選択であってターン外の行動ではない）。
  **第三波（渇き）はハリの繕いを封じる**ので、集めても払い出せない。**第三波だけ効かないはず。**
- **ガルドの `AtkBonus` は変わらない**（引き取りは `dmg` を発生させない）。
- 追加: **情報帯の割合はミオ（56.5%）より厚いはず**——ガルドは攻9・HP100 なので台が床へ落ちにくい。
