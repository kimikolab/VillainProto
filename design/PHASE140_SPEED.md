# 第140期 —— 測定を速くした（器具の期・盤面 0 件差分）

**一文**: **盤面を1ビットも動かさずに、`docs/` 全再生成を 16.8 分 → 4.4 分（−73.9%）にした。**
受け入れ条件は「`docs/` の全生成物がバイト単位で一致すること」で、**14 ファイルすべて一致した。**

**この期は盤面を1ビットも動かしていない。** `docs/` 14 ファイルは**1バイトも変わらない**
（`BattleCore` は1行も触っていない。触ったのは `BattleSim/Program.cs` と `BattleSim/BattleSim.csproj` だけ）。

---

## 0. Phase 0 の答え

### Q0-0. `git fetch`
`master...origin/master` に ahead / behind のマーカー無し ＝ **同期済み**。

### Q0-1. 全モードの時間と exit code —— **この期の作業対象を決めた表**

**マシンのコア数: 32。**

#### (a) `docs/` 全再生成の内訳（変更前・すべて直列）

| # | ファイル | コマンド | 基準 | 占有 | 累積 |
|--:|---|---|--:|--:|--:|
| 1 | `reseat.md` | `0 reseat` | **531.7s** | 52.6% | 52.6% |
| 2 | `layout.md` | `0 layout` | **257.6s** | 25.5% | **78.1%** |
| 3 | `ablation.md` | `0 ablate` | 59.2s | 5.9% | 84.0% |
| 4 | `engage.md` | `0 engage` | 39.8s | 3.9% | 87.9% |
| 5 | `crossing.md` | `0 cross quality` | 33.0s | 3.3% | **91.2%** |
| 6 | `pulse.md` | `0 pulse` | 14.3s | 1.4% | 92.6% |
| 7 | `quality.md` | `0 compare quality` | 14.2s | 1.4% | 94.0% |
| 8 | `chain.md` | `0 chain` | 14.2s | 1.4% | 95.4% |
| 9 | `balance.md` | `0 compare` | 14.1s | 1.4% | 96.8% |
| 10 | `harm.md` | `0 parry harm` | 11.8s | 1.2% | 98.0% |
| 11 | `watch.md` | `0 watch` ＋ `0 watch phase0` | 9.1s | 0.9% | 98.9% |
| 12 | `stock.md` | `0 grade2 stock` | 7.1s | 0.7% | 99.6% |
| 13 | `rules.md` | `0 derive rules` | 2.1s | 0.2% | 99.8% |
| 14 | `units.md` | `0 dump` | 1.9s | 0.2% | 100.0% |
|  | **合計** | | **1,010.1s（16.8 分）** | | |

> **上位5件で 91.2%、`reseat` ＋ `layout` だけで 78.1%。**
> **指示書の初版が名指しした `compare` は 14.1 秒 ＝ 1.4% で、14 ファイル中9位。**
> **訂正2 が実測で確定したので、`compare` は「主犯だから」ではなく
> 「規約 (G8) の必須1 で毎回回すから」という別の理由で触った。**

#### (b) 並列化の有無（変更前）

`BattleSim/Program.cs` の `Parallel.For` / `Parallel.ForEach` は **144 箇所あるが、
すべて 15,000 行目より後ろ**（＝診断のモード）で、**`docs/` の生成器には `layout` を除いて1つも無い。**

- **`reseat` は素の入れ子ループで、並列化が1つも入っていなかった**（最遅なのに）。
- **`layout` は「並列・決定的」と `CLAUDE.md` に書いてあるが、並列なのは粗探索だけ**で、
  **波別最良の測り直しは印字ループの中で完全に直列**だった（61編成 × 5波 × 最大9候補 × 200 seed）。
  しかも `curRate` が `pool` にも入っているので、**同じ配置・同じ波を毎回2度測っていた。**
- `cross quality` は既に並列（`Parallel.For` が2箇所）。

#### (c) 診断モードの走査（exit code の洗い出し）

**第139期の申し送り5**（「`sever` も HEAD では落ちていた。他にもある可能性は残る」）に答えるため、
`CLAUDE.md` に載っている全コマンドから**引数の穴が無い 271 本**を機械的に抜き出して1本ずつ走らせた。
結果は §5 に置く。

> **走査の器具そのものが1度壊れた。** 最初の版はコマンド一覧を Python の text mode で書いたので
> **CRLF になり、全コマンドの末尾に `\r` が付いた**——`0 compare` に `\r` が付いたものは
> 別のモードとして扱われ、**271 本すべてが「未知のモード」の経路に落ちて例外で死ぬ**か、
> `0 compare quality` の `quality` 側に `\r` が付いた場合のように
> **黙って別のモード（素の `compare`）を走らせた。**
> 第123期「走査対象が走査する側のファイル自身なら検索文字列は連結で組む」と同じ族で、
> **症状は「走査が空」ではなく「静かに違うものを測る」。**

### Q0-2. 第139期の直しが入っていること
`BattleSim/Entry.cs` の `Entry.Main` が **256MB のスレッド**で `Prog.Body(args)` を1回呼び、
例外は `ExceptionDispatchInfo.Capture` → `Throw()` で投げ直している。**この期ではスタックに触っていない。**

### Q0-3. 可変の static はゼロ（並列化の安全確認）

`BattleCore/*.cs` 6 ファイルを走査:

    static のフィールドで const でも readonly でもないもの   ... 0 件
    [ThreadStatic]                                        ... 0 件
    Trait 派生クラスの可変インスタンスフィールド              ... 0 件
    TraitCatalog の Map を実行時に書き換える箇所             ... 0 件（`Get` の読みのみ）

**`BattleEngine.cs` / `Traits.cs` のコメントが書いているとおりだった**（第139期の
「器具の地の文が静かに嘘になっていた」の逆で、ここは実装と一致している）。
`static readonly` の配列は 26 本あるがすべて定数の引き表で、実行時に書き換えない。

### Q0-4. 乱数はインスタンスに閉じている
`BattleCore` 全体で `Random` は **`BattleContext._rng = new Random(seed)` の1本だけ。**
`Random.Shared` / static な `Random` / `DateTime.Now` / `Guid.NewGuid` / `Environment.TickCount` は
`BattleCore` にも `BattleSim` にも **0 件**。`BattleSim` の `new Random(...)` はすべて seed を渡す形。

### Q0-5. 全文一致の例外 —— **0 行**

指示書は `docs/crossing.md` の「所要 43.9 秒」を名指ししているが、
**これは第124期 段0 で既に外れている。**
`docs/*.md` 14 ファイルを走査して「秒」「所要」「時刻」「日付」を含む行は **0 件**。

> **変更前に 14 ファイルを全部生成し直して、committed 版と突き合わせた——14/14 が一致。**
> **つまりこの期の受け入れ条件は「例外なしの全文一致」で書ける。**
> 第100〜121期の報告書が毎回「`crossing.md` の所要 1 行だけ既知の差分」と書いていたのは、もう過去の話。

### Q0-6. `docs/` 再生成で同じ戦闘を何度走らせているか

**同じ (編成, 敵, seed) の三つ組を、全再生成の中で 2〜4 回走らせている。**

| 重複している計算 | 走らせているモード | 1回ぶん | 余分 |
|---|---|--:|--:|
| `Presets.Compare` 61行 × 5波 × seed 0..199 | `compare` / `compare quality` / `chain` / `pulse` | 61,000 戦 | **183,000 戦（3重）** |
| 同上（`ablate` のフル編成の行） | `ablate` の `fullRate` | 61,000 戦 | **61,000 戦** |
| 全配置 7,320 通り × 5波 × seed 0..49 | `layout` の粗探索 と `reseat` の粗探索 | 1,830,000 戦 | **1,830,000 戦** |
|  |  | | **計 約 207 万戦** |

**`layout` と `reseat` の粗探索は文字どおり同じ計算である**——どちらも
`builds[b].F.Occupied().Select(x => x.Def)` から `SlotAssignments`（決定的な再帰列挙）で
同じ配置を同じ順に作り、`LayoutSeeds == ScanSeeds == 50`・同じ `EnemyCatalog.Stages` に当てている。

**`docs/watch.md` は2コマンド（`0 watch` と `0 watch phase0` の追記）で作る。**
第139期に**節が丸ごと欠けていた**（`audit` は行数と編成名の整合しか見ないので捕まらない）。

### Q0-7. 過去に速度改善をやった期
`design/*.md` に限定して `並列` / `Parallel` / `所要` / `高速` を走査した結果、
**速度改善そのものを主題にした期は 0 件。**
出てくるのは (i) 「static のノブを置かない理由」として `layout` の並列に言及したもの、
(ii) 各期の報告の「所要 N 秒」の記録、(iii) `crossing.md` の所要 1 行の既知の差分、の3種類だけ。
`CONTRIBUTING.md` の `reseat` のシャード分割は**速度ではなくツール呼び出しの時間制限の回避**である。

---

## 1. やったこと

### 段1 —— `csproj` に ServerGC（コード変更ゼロ）

    <ServerGarbageCollection>true</ServerGarbageCollection>
    <ConcurrentGarbageCollection>false</ConcurrentGarbageCollection>
    <TieredPGO>true</TieredPGO>

**これが並列化の効きを3倍にした。** 段2 の並列化だけを入れた版と、そこへ段1 を足した版:

| モード | 基準（直列） | 段2 のみ | 段2 ＋ 段1 |
|---|--:|--:|--:|
| `reseat` | 531.7s | 256.5s | **93.2s** |
| `layout` | 257.6s | 194.5s | **71.9s** |
| `ablate` | 59.2s | 24.7s | **12.2s** |

**`BattleEngine.Run` は1戦あたり短命オブジェクトを大量に作る**
（`LivingMembers` がスナップショットの `List` を返す作法・`CLAUDE.md`）ので、
32 コアで戦闘を並列に回すと**既定の Workstation GC が律速になる**。

> **ただし直列のモードでは符号が逆になる。**
> 並列化していない時点の `compare` は **14.1 → 14.9 秒（+5.7%）**。
> ヒープをコアごとに分ける代金が、14 秒しか走らない直列の実行では回収できない。

### 段2 —— 上位モードの並列化（`compare` を含む7モード）

**どれも同じ形にした**: 戦闘を**平坦なジョブ表**に畳んで `Parallel.For` で回し、
**各ジョブは自分の添字にしか書かず**（共有の `List` に `Add` しない）、
**印字は従来どおり直列に、同じ順序で**行う。**勝ち数は整数のまま持ち帰って最後に直列で割る。**

| モード | 何をしたか |
|---|---|
| `reseat` | 粗探索と測り直しを**それぞれ1つの平坦な並列パス**に。**全61行ぶんを1つのジョブ表に畳む**（行ごとに `Parallel.For` を立てると1行 120 ジョブで末尾にコアが余る） |
| `layout` | 波別最良の測り直しを印字ループの外へ前出し。**(配置, 波) をキーに重複を潰してから**測る（`curRate` の二度測りが消える） |
| `ablate` | 編成 × 版（フル / 1体抜き）× 波を1つの平坦なジョブ表に |
| `engage` | `Sweep`（編成 × 投入部隊数 1..3）を編成をまたいで1つのジョブ表に |
| `compare` / `chain` | **行（編成）でだけ割る。波では割らない**（下記） |
| `pulse` | 集計（行ごとの `Dictionary<string, UnitTally>`）だけを並列に。表は `formation.Occupied()` の順に引くので辞書の列挙順に依存しない |

> **`compare` / `chain` で波を軸にできないのは、`qWins[bi] += ...` のように
> 波をまたいで足す量があるから。** 同じ行の2つの波を別スレッドに置くと壊れる。
> **割ってよい軸は行だけで、1行の中は従来どおり 波 → seed の順**——だから浮動小数の合算順序も変わらない。

### 段3・段4・段5 —— やらなかった

- **段3（`docs all` に畳む）は次期へ送る。** Q0-6 が重複を測ったので**価値は数字で確定している**
  （約 207 万戦・とくに `layout` / `reseat` の粗探索 183 万戦）が、
  14 ファイルぶんの出力をバイト単位で再現する新しいモードを作る作業は
  この期の残り時間には収まらない。**§4 に持ち越しとして書いた。**
- 段4（`LivingMembers` の割り当て削減）・段5（`Run` の引数を束に畳む）は指示書どおり触っていない。

### 触らなかった生成器 —— `harm` / `watch` / `stock`（合計 26.0s ＝ 新しい合計の 9.9%）

**`parry harm` は行をまたいで共有する台帳に足し込む。** 同じ駒（ガルド等）が複数行に出るので、
`Parry.Collect` の `led` は行で割れない。行ごとの台帳を作って後から合算する形は書けるが、
**台帳には浮動小数の累算が含まれる**ので、**部分和を先に作ると結合順が変わって最後の1ビットが動きうる。**
**この期の受け入れ条件は「バイト単位で一致」なので、9.9% の利得に対して risk が見合わない。**

---

## 2. 結果

| # | ファイル | 基準 | 変更後 | 比 | 全文一致 |
|--:|---|--:|--:|--:|:-:|
| 1 | `reseat.md` | 531.7s | **93.2s** | ×5.70 | ○ |
| 2 | `layout.md` | 257.6s | **71.9s** | ×3.58 | ○ |
| 3 | `crossing.md` | 33.0s | 21.6s | ×1.53 | ○ |
| 4 | `ablation.md` | 59.2s | 12.2s | ×4.85 | ○ |
| 5 | `harm.md` | 11.8s | 11.8s | ×1.00 | ○ |
| 6 | `engage.md` | 39.8s | 11.7s | ×3.40 | ○ |
| 7 | `watch.md` | 9.1s | 9.0s | ×1.01 | ○ |
| 8 | `chain.md` | 14.2s | 6.4s | ×2.22 | ○ |
| 9 | `quality.md` | 14.2s | 5.7s | ×2.49 | ○ |
| 10 | `balance.md` | 14.1s | 5.6s | ×2.52 | ○ |
| 11 | `pulse.md` | 14.3s | 5.3s | ×2.70 | ○ |
| 12 | `stock.md` | 7.1s | 5.2s | ×1.37 | ○ |
| 13 | `rules.md` | 2.1s | 2.2s | ×0.95 | ○ |
| 14 | `units.md` | 1.9s | 1.5s | ×1.27 | ○ |
|  | **合計** | **1,010.1s（16.8 分）** | **263.3s（4.4 分）** | **×3.84** | **14/14** |

**毎回回す3コマンド**（`compare` ＋ `dump` ＋ `audit`・`CONTRIBUTING.md` の「毎回」の段）は
**約 17 秒 → 約 8 秒。**

### 受け入れ条件

| # | 条件 | 結果 |
|--:|---|---|
| 1 | `docs/` 14 ファイルが全文一致（例外行を除く） | **○ 14/14。例外行は 0**（Q0-5） |
| 2 | `audit` のずれ 0 件 | **○** |
| 3 | `derive scan` ／ ビルド警告 0 | **○ `derive scan` 正常。警告は 12 のまま（新規 0）**。12 は HEAD からの持ち越しで、この期は1つも増やしていない |
| 4 | `compare` 305 セルが `docs/balance.md` と 0 件差分 | **○**（`balance.md` の全文一致に含まれる） |
| 5 | `docs/rules.md` が 0 バイト差 | **○** |
| 6 | **並列化した各モードを3回走らせ、出力が互いに全文一致** | **○ 7 モード × 3 回すべてバイト一致**（下表） |

    compare   3回一致 ○   5.6 / 5.0 / 6.1 s
    chain     3回一致 ○   5.3 / 6.5 / 6.4 s
    pulse     3回一致 ○   5.6 / 5.3 / 5.2 s
    engage    3回一致 ○  12.1 / 11.7 / 11.7 s
    ablate    3回一致 ○  12.2 / 12.3 / 12.2 s
    layout    3回一致 ○  73.6 / 71.9 / 71.7 s
    reseat    3回一致 ○  93.4 / 93.2 / 92.8 s

---

## 3. この期に分かったこと（則）

### **速度の作業対象は、実装の形ではなく実測の表が決める。**
指示書の初版は「`compare` が素の `for` で 61,000 戦を1コア」を最大の穴として名指ししたが、
**`compare` は 14 ファイル中9位・全体の 1.4%** だった。
**改訂版が「まず内訳を測る」に書き換えたのが正しく、その表は `reseat` と `layout` を指した。**
第137期「掃引の中心を実測ではなく見積もりから引くと桁を外す」の**器具の側の版。**

### **「並列・決定的」と書いてある実装が、半分しか並列でないことがある。**
`CLAUDE.md` のコマンド表は `layout` を「並列・決定的」と書いているが、
**並列なのは粗探索（1,830,000 戦）だけで、波別最良の測り直し（約 550,000 戦）は
印字ループの中で完全に直列**だった。**説明文は嘘ではない——一部について正しい。**
第122期「規則を降ろすと、その規則を前提に書かれた説明文が静かに嘘になる」の親戚だが、
こちらは**書いた時点から一部にしか当たっていない**形である。

### **並列化の効きは、並列化の質ではなく GC の設定で3倍変わる。**
段2 の並列化だけでは `reseat` 531.7 → 256.5 秒（×2.07）にしかならず、
**32 コアに対して 2 倍**という明らかに鈍い伸びだった。
`csproj` に3行足す（ServerGC）だけで 256.5 → 93.2 秒（さらに ×2.75）。
**律速は「どう割るか」ではなく「割った先が同じヒープを取り合うこと」だった。**
**`BattleEngine.Run` が1戦あたり短命オブジェクトを大量に作る**（`LivingMembers` の
スナップショット作法）という、**盤面の設計判断がそのまま測定の速度に出ている。**

> **系: 直列のモードでは ServerGC は損になる**（`compare` 単体で +5.7%）。
> **同じノブが、割り方によって符号を変える。**

### **重複した計算は、モードの境界に隠れる。**
`docs/` の全再生成は同じ (編成, 敵, seed) の三つ組を **約 207 万戦ぶん重複して走らせている。**
`compare` / `compare quality` / `chain` / `pulse` は**同じ 61,000 戦を4回**、
`layout` と `reseat` は**同じ粗探索 1,830,000 戦を2回**走らせる。
**どれも「1つのモードの中では戦闘を1回も増やしていない」**——
第126期の `compare quality` のコメントは正しく「戦闘は1回も増えない」と書いているが、
**それは別プロセスで2回起動することを勘定に入れていない。**
**「増やしていない」は、モードの中でしか成り立たない主張である。**

### **測定より、いまはコンパイルのほうが遅い。**
`BattleSim` の Release ビルドは **263〜294 秒**で、**`docs/` 全再生成（263 秒）と同じか、それより長い。**
しかも **Roslyn（VBCSCompiler）のピーク作業セットは 22.3 GB**
——**これは HEAD 素の状態でも同じ**（22.3 GB 対 22.1 GB）なので、この期の変更のせいではない。
`BattleSim/Program.cs` の **68,000 行がトップレベル文で1つの `<Main>$` に畳まれる**という
第139期が名指しした構造が、そのままコンパイルの代金になっている。
**1回の変更あたりの待ちを次に縮めるなら、対象は測定ではなくビルドである**（段5 の動機が変わった）。

---

## 5. 診断モードの走査（Q0-1 (c)）

**走査中。結果は別コミットで本節に追記する。**

`CLAUDE.md` のコマンド表から**引数の穴が無い 271 本**を機械的に抜き出し、
1本ずつ **90 秒の上限つき**で走らせて所要と exit code を記録する。
第139期の申し送り5（「`sever` も HEAD では落ちていた。他にもある可能性は残る」）に答えるためのもの。

---

## 4. 次期への持ち越し

1. **段3（`docs all`）—— 価値は Q0-6 で数字になっている。**
   同じ帯の戦闘を1回だけ走らせて複数のファイルを書くモードを足せば、
   **約 207 万戦ぶん**（うち `layout` / `reseat` の粗探索が 183 万戦）が消える。
   既存のコマンドは残し、新しいモードの出力が既存コマンドの出力と全文一致することが受け入れ条件。
   **`watch.md` の2コマンド問題（第139期に節が丸ごと欠けた）もここで塞げる。**
2. **`harm` / `watch` / `stock` の並列化（合計 26.0s ＝ 9.9%）。**
   `harm` は行をまたぐ共有台帳と浮動小数の累算があるので、
   **「部分和の結合順が変わらない形」を先に決めてから触ること。**
3. **ビルドが測定より遅い。** 段5（`Program.cs` の分割 / `Run` の引数を畳む）の動機は
   「`<Main>$` にモードを足し続ける形が穴」から**「1回の変更あたりの待ちの過半がコンパイル」**へ移った。
   `BattleSim/Parry.cs` / `Wall.cs` / `Shard.cs` のように**別ファイルの `static class` へ切り出した診断は
   この問題を持たない**ので、分割の道筋は既にある。
4. **`CONTRIBUTING.md` の生成物の表は、この期に直した**（持ち越しではない）。
   **56 編成・8 ファイル時代のまま**だった（現行は 61 編成・14 ファイル）うえ、
   **所要が第55期の実測だったので、この期の変更がその列を丸ごと嘘にした。**
   14 ファイル全部を載せ、所要をこの期の実測に入れ替え、
   **`reseat` のシャード分割は「もう要らない」と書き足した**（367 秒 → 93 秒）。
   **`watch.md` が2コマンドであること**（第139期に2本目を忘れて節が丸ごと欠けた）と、
   **いま待ち時間の過半はビルドであること**も表の下に書いた。
