# 第143期 報告書 —— ササの転生（身を固めて、はね返した分を撒く）

指示書は `design/PHASE143_BRACE_SPEC.md`。HEAD の前提は第142期 `d036ca6`。

---

## Phase 0（実装前の全問。答えをここに転記する）

### Q0-0. `git fetch` して origin から behind でないことを確認する

`git fetch` 済み。`## master...origin/master`（ahead / behind の表示なし）＝ **behind 0**。

### Q0-1. `ApplyDamage` の中の順序

`ApplyDamageBody`（`BattleCore/BattleEngine.cs:4585`）を頭から読んで、**行番号ごと数え直した**。
指示書の「Web 側が読んだ順序」は**7段ではなく、間に4段（`lethal:false` のクランプ・受け流し・猶予・不死）が入って 11 段**ある。

| # | 段 | 行 | 備考 |
|---|---|---:|---|
| 1 | 棘守り（カド）の肩代わり上限 `ThornGuardTrait.AbsorbCap` | 4605 | **入力の段階**で切り、超過分を守った相手へ素のまま中継 |
| 2 | `ModifyIncomingDamage`（脆弱ほか） | — | 惨禍・据え・散開・萎縮もこの層 |
| 3 | 庇い・肩代わり・巻き込み則 | 4729〜4775 | 主目標の差し替えと分割 |
| 4 | **破片（`StatusKeys.Armor`）が HP の前に削られる** | 4798 | **吸い切ると `return`** ＝ 以降の段も `OnDamaged` も走らない |
| 5 | `lethal: false` のクランプ | 4846 | 「殺さない」制約の1本目 |
| 6 | 受け流し（`ParryTrait`） | 4849 | 第136期・回数制の無効化 |
| 7 | 猶予（`ReprieveTrait`） | 4887 | 「殺さない」制約の2本目 |
| 8 | 不死（`UndyingTrait`・器具） | 4908 | 保持者 0 枚 |
| 9 | **軛（`Yoke.Cap`）の切り落とし** | 4939 | 破片の**後** |
| 10 | `target.Hp -= amount` | 4982 | |
| 11 | `OnDamaged`（ここで `LooseTrait` の弾きが走る） | 5051 | `OnAllyDamaged` はその直後 |

**ササの上限（`Brace`）は 9（軛）の直前に差し込む。** 指示書の推奨どおり。理由もそのまま成立する
——軛の後ろに置くと第四波でだけ軛が先に 25 で切るので、ササの切り落としがその波で細る。

> **ただし第四波では、この懸念自体が実測で成立しない**（下の Q0-2）。
> 前1 に届く一撃は第四波では**最大 16** で、**軛が 25 で切る一撃が 1 発も無い**
> ——`YokeRule(Active: false)` の版と比べて **442 発が 1 発も違わなかった**。
> **第134期の「軛の非対称の理由は<u>数値</u>（第四波の敵の打点が全部 25 以下）」がそのまま再現した。**
> したがって **`YokeCutLost` ほかの軛の計数がササ在席行で減ることも無い**（そもそも 0）。
> 順序の判断は変えない——**懸念が空振りだったことと、置き場所が正しいことは別**である。

確認した残り2点:

- **`OnDamaged` に渡る `dmg` は上限で切った後の値。** 上限 N > 0 なら `dmg ≥ min(amount, N) > 0` なので必ず鳴る。
  **ただし破片が全部吸うと段4 で `return` して `OnDamaged` に到達しない**——この穴は**現行の散開にも既にある**
  （破片は現行でもササに付きうる）。指示書どおり**この期では塞がず、発生回数だけ数える**。
- **軛のコメント「肩代わりの分割は上限を回避する経路になる」はササの上限にもそのまま当てはまる。**
  ゴルム・ドハがササより前で吸うと、ササには小さい一撃しか届かず切り落としが出ない。
  **設計どおりの相互作用**（肩代わり駒とは噛まない）で、予測4 に書いた。

### Q0-2. 敵の攻撃力の分布から N の掃引点を引く

**紙で決めない。走らせて引いた**（`brace phase0`）。器具は
「前1 に**素体のササ**（HP96・攻7・速12・**特性ゼロ**）を置き、`BattleEvent`（`Kind == Damage`・
`TargetId == 0`・`ActorId is not null`・`FriendlyFire == false`）の `Amount` を数える」。
`ActorId == null` を外すのは、**毒・燃焼の刻みは一撃ではない**から（上限が切る対象ではない）。

| 波 | 軛 | 発数 | 平均 | p10 | p25 | 中央値 | p75 | p90 | 最大 | 主な値（多い順） |
|---|---|---:|---:|---:|---:|---:|---:|---:|---:|---|
| 第2波 | 有 | 321 | 16.3 | 7 | 7 | 18 | 24 | 24 | 24 | 24×116 / 7×81 / 18×73 / 11×51 |
| 第3波 | 有 | 328 | 15.6 | 7 | 12 | 15 | 20 | 20 | 28 | 15×122 / 20×76 / 12×67 / 7×38 / 28×25 |
| 第4波 | 有 | 442 | 11.6 | 9 | 9 | 12 | 12 | 16 | 16 | 12×251 / 9×133 / 16×58 |
| 第4波 | **無** | 442 | 11.6 | 9 | 9 | 12 | 12 | 16 | 16 | 12×251 / 9×133 / 16×58 |
| 第5波 | 有 | 399 | 13.0 | 7 | 7 | 12 | 17 | 20 | 30 | 7×103 / 12×79 / 17×66 / 15×64 / 20×55 |

**第2〜5波の合算**（1,490 発・第四波は軛を外した版）:
平均 **13.9** / p10 **7** / p25 **9** / 中央値 **12** / p75 **17** / p90 **20** / 最大 **30**。

**N の3点（表から引く）**:

| 点 | N | 切る発数の割合 | 切り落とす総量／発 | 切り落とす総量／戦（目安） |
|---|---:|---:|---:|---:|
| 最下点 | **7** | 85.1% | 6.88 | 51.2 |
| 中点 | **12** | 44.4% | 2.99 | 22.3 |
| 最上点 | **20** | 9.7% | 0.46 | 3.4 |

> **則: 敵の打点の表を「この編成に届く一撃」で作ってはいけない。**
> 最初の器具は埋め草に **呪詛官ネル** を入れていた。呪詛は `ctx.Dull` で**敵の攻撃力を下げる**ので、
> 第二波の巡礼騎士（攻24）が一度も 24 で殴ってこず、**表の最大が 18 に化けた**
> （その表から引いた N の3点は 3 / 6 / 14 で、**全部が実際の半分**）。
> `docs/units.md` のステージ行（巡礼騎士 攻24 ／ 勇者候補 攻20 ／ 詠唱兵 攻8×200% ＝ 16）と
> 突き合わせて初めて気付いた。**埋め草が敵の数値に触ると、N の掃引点がそのまま埋め草の性能になる。**
> 第117期「土台の選定規則が、測る対象の入力を 0 にしていないか」の**入力ではなく目盛りの側の版**で、
> 埋め草は**特性を1つも持たない素体**に差し替えた。
> **表の値は `docs/units.md` のステージ行と1つずつ照合できる**（24 / 20 / 16 / 30）——これが検算。

### Q0-3. 手番喪失（転倒）の実装をどの鍵で書くか

- **`StatusKeys.Stun`（痺れ）を流用しない。** 読み手（シガの責め苦ほか）がいて帳簿が混ざる。
- **まどろみ（`ColossusTrait` の腹・第36期）とまったく同じ形**にする。engine 側（`TakeTurnCore`）で
  `IdleTurn` を立てて `Stalled` を返す形で、**`CanAct` を1つも false にしない**
  ——だから `Trait.SurrenderedTurn` が true のまま通り、**号令（ガン）・据え（バン）が買い取れる**。
  `CanAct` のオーバーライドで書くと不動（カド）・追い打ち（ハギ）と同じ扱いになって**買い手が消える**
  （engine のコメントに明文で書いてある）。
- 新しい鍵 **`StatusKeys.Stagger`** を1つ足し、**`StatusKeys.All` にも足す**（会戦の境界で消す一覧）。
- **ヨミ（軋み）は割り込みを失わない。** `IdleTurn` は `CanReact` を1ビットも見ない
  ——ターン外の行動を止めるのは痺れ・のろま（`CanReact` を false にする型）と粛だけで、
  転倒は**次の通常の手番だけ**を落とす。

### Q0-4. `LooseTrait` の弾きを共有ヘルパに出す

弾く先の規則（隣接する生存味方のうち席番号最小／保持者に隣接しない席 → 空席 → 席番号小／
召喚枠は使わない／1T1回・`SwapSlots` の**前**に上限を立てる）は**1文字も変えない**。
`LooseTrait` そのものは**残置**（対照）。

### Q0-5. ササの現在の帳簿

`docs/harm.md` 表A のササの行: 含む行 **1**・戦 800・決着T 13.24・**生存T 6.98**・**死亡率 50.9%**・
**被弾/戦 54.1**・回復/戦 11.1。範囲被弾率は 表B で **82.0%**（単体 18.0%）。

走らせて引き直した第106期の弾き（`compare` の `置き去り×分散回復`・第2〜5波・seed 0..199）:

| 行 | 第2〜5波平均 | 被弾/戦 | 生存T | 死亡率 | 弾き/戦 | 上限で弾かれ | 隣に味方なし |
|---|---:|---:|---:|---:|---:|---:|---:|
| 置き去り×分散回復 | 51.6% | 54.1 | 6.98 | 50.9% | **3.01** | 0.95 | 1.11 |

> 指示書の「2.23 回/戦」は**第106期の台の値**。現行の 61 行で数え直すと **3.01 回/戦**。
> **採否の線（§5 の条件1）は、この表の生存T 6.98 と死亡率 50.9% から引く。**

### Q0-6. 測定台

**4台とも `V0`（現行ササ）の第2〜5波平均が 40〜95% に入り、第2〜5波の情報セルが 4/4。**
指示書の「第138期 `shard` の4台にササを差し込んだ版」は**そのままでは床だった**ので、
`brace scan probe`（埋め草の総当たり・帯 40〜95% かつ情報セル 3 以上で絞る）で選び直した。

| 台 | 席順（前1 / 前3 / 中央 / 後1 / 後3） | V0 勝率（1..5波） | 第2〜5波平均 |
|---|---|---|---:|
| 台1 出口 | ササ / ムグ / **ガレ** / **ウロ** / リィカ | 100.0 / 86.5 / 71.0 / 22.0 / 57.0 | 59.1 |
| 台2 壁だけ | ササ / ドルガ / ムグ / ノミ / リィカ | 100.0 / 98.5 / 97.0 / 6.5 / 51.5 | 63.4 |
| 台3 肩代わり | ササ / ドルガ / **ゴルム** / リィカ / ノミ | 100.0 / 98.0 / 64.5 / 69.5 / 30.0 | 65.5 |
| 台4 手番市場 | ササ / ムド / **ガン** / リィカ / ドルガ | 100.0 / 98.5 / 73.5 / 46.0 / 56.5 | 68.6 |

- **ササは全台とも前1。** 前1（スロット0）の隣接は `AdjacencyTable` で `{中央, 後1, ○中1, ○前2}`
  ——**編成枠では 中央 と 後1 の2つだけ**なので、弾きの宛先はこの2席に限られ、席番号最小＝中央が先に選ばれる。
  だから台1 は **ガレを中央・ウロを後1** に置く（どちらも宛先になりうる席）。
- **埋め草の候補から「肩代わり・被ダメ低減を持つ駒」を全部外した**
  （ガルド・ゴルム・ドハ・セッキ・カド・クビ）——ササに届く一撃を横取りするので、
  **測りたい供給が台の側の理由で消える**。ゴルムだけは台3 で**意図して**同席させる。
- **`ネル`（呪詛官）を含む組も外した。** Q0-2 と同じ理由で、敵の攻撃力を下げると切り落としが細る。
- 対照はデータ点ごとに **同じ台の現行ササ版**（散開 −35% ＋ 弾き）。

### Q0-7. 過去に「単発上限を駒に持たせる」「切り落とした分を配る」実験が無いか

`design/*.md` 233 ファイルを `上限` / `Cap` / `切り落と` / `Brace` で走査した。

- **`Brace` は 0 件**（指示書自身を除く）。
- **`切り落と` は 2 件**で、片方は指示書自身、もう片方は `PHASE83_BREADTH.md` の
  「新しい線はそれを**床で切り落とす**」＝**別の意味**（標本の切り捨て）。
- 「単発の上限」は **軛**（盤面ルール・第25期／第132期に地図を取った）と
  **棘守りの `AbsorbCap`**（肩代わりの入力側の上限・上の表の段1）の2つだけ。
  **どちらも切り落とした分を配らない**——軛は捨て、棘守りは**守った相手へ素のまま中継する**（分配ではなく転送）。
- 第135期 §1 Q0-9 の「1発を丸ごと無効化する機構」14 件の全数にも、**切った分を資源にする形は 0 件**。

> **結論: 「駒が自分の一撃に上限を持ち、切り落とした分を味方に配る」は前例ゼロ。**

---

## 予測（実装前に書き切った。外れても消さない）

1. **段A では壁にならない。** HP96 だけでは第136期のガルド HP100 と同じで、生存Tは現行と大差ない
2. **段B で生存Tが大きく伸びる**。最下点（N=7）で最大、ただし**最下点は供給を殺す**
   （段C で切り落としが太るのは高い N ではなく中〜低の N）
3. **段C の切り落とし供給は、単体高倍率の波（第2波の巡礼騎士24・第3波の勇者候補20）で太り、
   第四波（最大16・全弾が 25 未満）で細る**。ヒビ（範囲被弾に比例）とは**逆の波で光る**
4. **肩代わり台（台3・ゴルム）では切り落としがほぼ出ない**（Q0-1 の分割回避の帰結）。
   この台のササは供給役ではなく、ただの前衛
5. **段D は読み手なしの台（台2）で明確に下がり、手番市場（台4）で上がる**（符号反転）。
   ヨミは割り込みが残るので損が小さい
6. **第四波でササの供給は消えない**（上限を軛の**前**に置くので）。
   ——**ただし Q0-2 で、第四波はそもそも軛が 1 発も切っていないことが分かった**ので、
   置き場所に関わらず消えない。**予測6 は Phase 0 の時点で自明になった**（記録のため残す）
7. **ササを含まない 60 行 ＋ 交差帯 12 行は ±0.0**

---

## 段0 —— Phase 0 のコミット

`BattleSim/Modes/Brace.cs`（新規）と `BattleSim/Program.cs` の振り分け1行だけ。
**`BattleCore` は1行も触っていない**ので、`compare` 305 セルは `docs/balance.md` と 0 件差分。

---

## 段A —— 定義の差し替え（`BraceRule(0, false, false)`）

4台 × 第2〜5波 × seed 0..199。**対照 V0 ＝ 転生前のササ**（散開 −35% ＋ 弾き）で、
`UnitCatalog` の側が転生したので**診断のローカルに置くのは「転生前の姿」**（第60期の作法）。

| 台 | V0（第2〜5波平均） | 段A の帰属 | ササ生存T（V0 → 段A） | 死亡率 | 与ダメ/戦 |
|---|---:|---:|---|---|---:|
| 台1 出口 | 59.1 | **−1.6** | 2.13 → 2.99T | 99.9% → 98.5% | 14.1 → 0.0 |
| 台2 壁だけ | 63.4 | **−6.0** | 2.24 → 3.23T | 98.2% → 98.5% | 14.9 → 0.0 |
| 台3 肩代わり | 65.5 | **−2.3** | 1.80 → 2.96T | 100.0% → 99.8% | 11.2 → 0.0 |
| 台4 手番市場 | 68.6 | **+3.4** | 1.89 → 3.14T | 99.9% → 97.1% | 20.2 → 0.0 |

**予測1 は当たり。** HP 58 → 96 は生存Tを 1.4〜1.6 倍にするが、**死亡率は 97〜100% のまま**で
「壁」にはならない（第136期のガルド HP100 と同じ）。
**被弾/戦は 64〜66 → 102〜103 に増える**——固くなったぶん長く狙われる。
攻撃を捨てるので与ダメが 11〜20 → **0.0** になり、台によっては転生前より弱い。

## 段B —— 上限（`BraceRule(N, false, false)`）

| 台 | N=7 | N=12 | N=20 |
|---|---:|---:|---:|
| 台1 出口 | −10.0 | −5.9 | −1.0 |
| 台2 壁だけ | −1.1 | −7.5 | −6.1 |
| 台3 肩代わり | **+6.8** | −3.5 | −3.0 |
| 台4 手番市場 | **+7.6** | +3.6 | +3.8 |

**壁としては N=7 だけが立つ**（台2「壁だけ」で読む）:

| 版 | ササ生存T | 死亡率 | 被弾/戦 | 決着T |
|---|---:|---:|---:|---:|
| V0 転生前 | 2.24 | 98.2% | 64.1 | 5.88 |
| 段A 上限なし | 3.23 | 98.5% | 103.0 | 6.27 |
| **段B N=7** | **5.44** | **68.2%** | 91.9 | 7.17 |
| 段B N=12 | 3.82 | 94.6% | 100.0 | 6.48 |
| 段B N=20 | 3.32 | 98.1% | 102.0 | 6.32 |

**予測2 は半分外れた。**「段B で生存Tが大きく伸びる／最下点の N で最大」は当たり（2.24 → 5.44T ＝ 2.4 倍）だが、
**「最下点は供給を殺す」は逆だった**——切り落とし/戦は **N=7 で 63.7・N=12 で 19.9・N=20 で 3.5** と、
**N が下がるほど単調に太る**。

> **則: 上限を分布の下の端に置くと、供給の律速が「一撃の大きさ」から「殴られる回数」へ移る。**
> N が分布のほぼ全部を下回ると `切り落とし ≒ 総被弾 − N × 回数` になるので、
> **1発あたりの切り落としは小さいのに、回数が掛かって総量が最大になる**。
> **「高い上限のほうが1回あたり多く切り落とすから供給が太る」は、回数を勘定に入れていない。**
> 第137期「『1戦の総量 ÷ 何か』で強度を引く式は、実装の発火周期を勘定に入れていないと桁を外す」の
> **上限側の版**で、こちらは**分子ではなく分母（回数）が効いた**。

**ただし N=7 だけでは 4台中2台で負ける**（台1 −10.0・台2 −1.1）——
**壁になっても、切り落とした分を誰も読まないと攻撃を捨てた代金が回収できない。**

## 段C —— 切り落とした分を配る（`BraceRule(N, true, false)`）**採用**

| 台 | V0 | 段A | 段B N=7 | **段C N=7** | 段C N=12 | 段C N=20 |
|---|---:|---:|---:|---:|---:|---:|
| 台1 出口 | 59.1 | −1.6 | −10.0 | **+7.8** | −3.1 | −1.5 |
| 台2 壁だけ | 63.4 | −6.0 | −1.1 | **+9.4** | −6.6 | −6.4 |
| 台3 肩代わり | 65.5 | −2.3 | +6.8 | **+13.1** | −1.0 | −2.5 |
| 台4 手番市場 | 68.6 | +3.4 | +7.6 | **+16.9** | +7.0 | +4.3 |

**4台すべてで正になるのは N=7 の段C だけ。** 12 と 20 は 4台中3台で負ける。
**「閾値を跨いだ最初の点を採る」（第138期）はここでは効かない**——跨ぐ点が1つしか無い。

帳簿（第2〜5波・/戦）:

| 台 | 版 | 身構え | 切った回数 | 切り落とし | **配った** | 消えた | 弾き | 破片で不発 | ガレ砕き | ウロ纏い率 |
|---|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 台1 出口 | V0 | 0.00 | 0.00 | 0.0 | 0.0 | 0.0 | 1.75 | 0.00 | 2.8 | 31.1% |
| 台1 出口 | 段B N=7 | 4.67 | 9.03 | 68.2 | 0.0 | 56.2 | 3.72 | 0.00 | 1.6 | 27.9% |
| 台1 出口 | **段C N=7** | 4.51 | 8.60 | 65.0 | **52.4** | 7.0 | 3.83 | 0.00 | **8.2** | 27.0% |
| 台2 壁だけ | 段C N=7 | 5.22 | 8.81 | 65.7 | 53.1 | 7.5 | 4.24 | 0.00 | — | — |
| 台3 肩代わり | 段C N=7 | 3.99 | 5.96 | 46.4 | 40.9 | 1.4 | 4.87 | 0.00 | — | — |
| 台4 手番市場 | 段C N=7 | 4.73 | 7.27 | 53.7 | 45.5 | 4.1 | 4.74 | 0.00 | — | — |

- **`配った ≦ 切り落とし` が全行で成立**（受け入れ条件 4-2）。差は `消えた`（宛先が無いまま turn をまたいだ量）と、
  決着時にまだ配られていない保留。
- **`破片で不発` は 4台 × 全版で 0.00**（Q0-1 の穴）。**この期の台では一度も起きていない。**
  ササ以外に破片を書く駒がいないので、ササが破片を持つ経路がそもそも無い。
- **出口に届いている**: 台1 の `ガレ砕き/戦` が **2.8（V0）→ 1.6（段B）→ 8.2（段C）**。
  段B で下がるのは、ササが殴らなくなって戦が長引くのに破片が1点も湧かないため。

### 予測4 は外れた —— 肩代わりは供給を「消す」のではなく 3 割削る

| 台 | 切り落とし/戦 | 配った/戦 |
|---|---:|---:|
| 台2 壁だけ（肩代わりなし） | 65.7 | 53.1 |
| 台3 肩代わり（ゴルム同席） | **46.4（−29%）** | **40.9（−23%）** |

**「ほぼ出ない」ではなく「3割減る」。** 巨躯が飲み込むのは一撃の一部なので、
**残りが N=7 を超えていれば上限は依然として切る**——N が分布の下の端にあるほど、
肩代わりの「分割による上限回避」は効かなくなる。
**しかも台3 は段C の帰属が +13.1 で4台中2位**。
> **則: 「分割は上限を回避する経路」は、上限が分布のどこにあるかで効き方が変わる。**
> 上限が高い（軛の 25 のような）位置にあれば分割でほぼ回避できるが、
> **上限が分布の下の端にあると、分割された各段もなお上限を超えるので回避にならない。**
> 第25期の軛のコメントは**上限が分布の上側にある場合の話**である。

### 予測3 も外れた —— 第四波は細るどころか「切る回数」が最多

| 台 | 第2波 | 第3波 | 第4波 | 第5波 |
|---|---|---|---|---|
| 台1（切った回数/戦） | 8.25 | 5.55 | **13.97** | 6.63 |
| 台1（切り落とし/戦） | **101.5** | 48.5 | 62.2 | 47.8 |
| 台2（切った回数/戦） | 8.09 | 6.22 | **14.00** | 6.92 |
| 台2（切り落とし/戦） | **96.8** | 54.8 | 61.9 | 49.4 |

**第2波（巡礼騎士 攻24×2）が総量で最大**なのは予測どおりだが、
**第四波は「細る」どころか総量が2位で、切った回数は他の波の 2 倍**ある。
第四波の一撃は 9 / 12 / 16 で**全部が N=7 を超える**ので、**長い波＝殴られる回数が多い波**がそのまま供給になる。
**1回あたりの切り落としは第2波 12.3 に対し第四波 4.5** ——**単価は 1/3 だが回数が 1.7 倍**。

> **段B の則がそのまま出た**: 上限を分布の下の端に置くと、供給は「一撃の大きさ」ではなく
> **「殴られる回数」**で決まる。**ヒビ（範囲被弾に比例）と「逆の波で光る」という見立ては成り立たない**
> ——どちらも結局「たくさん殴られる波」で太る。

### 採用と回帰

**採用値は `BraceRule(7, true, false)`。**

| 検査 | 結果 |
|---|---|
| ササを含まない `compare` 60 行 × 5 波 | **±0.0（300 セル 0 件）** |
| 交差帯 12 行 | **±0.0**（`docs/crossing.md` の差分 0 バイト。ササは `Presets.Cross` に 0 行） |
| `audit` | ずれているファイル **0 件** |
| ビルド警告 | **12 のまま・新規 0** |
| 主判定19行（第2〜5波） | 79.5 / 87.6 / 76.9 / 48.5 → **78.4 / 87.0 / 76.5 / 48.7** |
| 拒否権1（第五波が歯止め 33.2% を下回る） | **通る**（48.7%・余裕 **+15.5pt**） |
| 情報セル | 全61行 **139 / 2.28**・主判定19行 **45 / 2.37**（**どちらも第139期から不変**） |
| 動いた `compare` の行 | **1 行だけ**（`置き去り×分散回復`） |

### 拒否権3 は立つ —— ただし「席の寿命が切れた」ぶんである

`置き去り×分散回復` は **100 / 98.0 / 100.0 / 7.5 / 1.0 → 100 / 76.0 / 88.0 / 0.0 / 6.0**
（第2波 **−22.0pt**）で、**主判定19行のメンバーなので拒否権3（いずれかの波で −10.0pt 以上）に触れる。**

(G2) の分解は**ササについては成立しない**——`compare` 61 行で**ササを含む行は 1 行しか無い**ので、
「その駒を含む<u>他の</u>行」が 0 行である（`LooseRule` の doc が第106期に同じ但し書きを残している）。
他の4枚（ガルド・セロ・ドルガ・ナラ）は他の行で ±0.00pt。

**そして、この行の席は第107期の則どおり寿命が切れている。**
`docs/reseat.md`（再生成済み・帯A）を見ると、**狙（ガルドが前列）を満たし情報セル 4 の候補が現行より 20pt 以上上**にある:

| 候補 | 前1/前3 | 中央 | 後1/後3 | 第2〜5波平均 | 情報セル |
|---|---|---|---|---:|---:|
| **現行** | ガルド/セロ ほか（`Presets` のまま） | | | **42.5** | 3 |
| 粗順4（狙 ○） | ササ/ガルド | セロ | ドルガ/ナラ | **64.6** | 4 |
| 粗順5（狙 ○） | ササ/ガルド | セロ | ナラ/ドルガ | 64.4 | 4 |
| 粗順1（狙 ○） | ガルド/ササ | セロ | ドルガ/ナラ | 63.5 | 4 |

**席は動かさない。** 指示書 §1-3「触らないもの」に `Presets` が明記されているため
——**線（+22.1pt ＞ 第46期の閾値 5.0pt）も狙も情報セルも満たしているのに、この期では採れない。**
**次期に `confirm`（seed 200..599）で追試してから差し替えること**（(G16): 線と採る条件を同じ集合で書く）。

> **則: 駒を転生させた期は、その駒を含む行の席が必ず古くなる。**
> 第107期「機構が変わった行の席は寿命が切れる」の**駒そのものを入れ替えた版**で、
> **転生の期に席を触らないと決めたなら、拒否権3 が立つことは最初から織り込んでおくこと。**
