# 第139期 —— 抉りのエグを外し、礫のガレを入れた（＋ 傷の帳簿）

**HEAD 前提: 第138期 `eaec7e6`。** 指示書は `design/PHASE139_SWAP_SPEC.md`。

> **一文**: 第138期で採用が決まっていた礫のガレを `UnitCatalog.All` に入れ（エグと差し替え・52 枚のまま）、
> `compare` に1行載せた。あわせて**傷の帳簿を、巻き込み則を降ろした後の盤面で取り直した**。

---

## §0. Phase 0 —— 指示書の前提が3つ外れていた

### 0-0. `git fetch`（Q0-0）

`origin/master` と HEAD は同じ `eaec7e6`。behind ではない。
未追跡は `design/PHASE139_SWAP_SPEC.md` と `design/art/hota/compare-validation.txt`
（後者は第138期以前の `docs/balance.md` の写しで、この期では触らない）。

### 0-1. **切る駒の根拠の出所が、指示書の書いている期と違う**（Q0-2 の前提）

**数字はどちらも実在するが、期が違う。**

| 指示書の記述 | 実際の出所 | 値 |
|---|---|---|
| 「第126期の全52枚のプラス値」 | **第119期**（`design/PHASE119_CHECKUP2.md` の 52 枚の3分） | ノノ +0.28 / ナタ +0.59 / **エグ +0.66** |
| 「第126期は 52枚中49位（−8.23）で最も強く否定している」 | **第103期**（`design/PHASE103_BETRAY.md:197`） | 号令のガン −8.23・49 位 |

**さらに重い食い違いが1つ**——**第119期の3分そのものは、エグを「差し替え」ではなく「転生」に置いている**
（第82期の「差し替え」から上がった）。理由は**最良の相乗 +5.59 が線 +5.0 の内側**にあること:

| 駒 | 第82期 | 第119期（52 枚） | 単独 | プラス値 | 代金 | 最良の相乗 | 判定 |
|---|---|---|--:|--:|--:|--:|---|
| 抉りのエグ | 差し替え | **転生** | −2.28 | **+0.66** | +2.94 | **+5.59** | 転生 |
| 断ちのナタ | 差し替え | **転生** | +0.59 | +0.59 | +0.00 | +6.10 | 転生 |

第119期の「差し替え」は **ハネ・ノノ の2体だけ**で、**どちらも拒否権1 に当たる**
（＝CLAUDE.md の「**切れる駒は 52 枚でも 0 体**」）。

> **この期はプラス値の側を採った**（指示書が「エグ1枚だけを切る」と明記しているため）。
> **器具の総合判定とは逆を向いていることを消さずに書いておく**——覆すときの根拠は
> プラス値ではなく**相乗の側**にある。

### 0-2. `All` から外して壊れるもの（Q0-2）

**エグの `Presets` 参照は 6 箇所**（指示書の表と一致。**行名2つを埋めた**）:

| 帯 | 行 | エグの席 |
|---|---|---|
| `compare` | `裂き (キリ×エグ)` | 後3 |
| `compare` | `裂き×責め苦 (キリ×エグ×シガ)` | 後3 |
| `compare` | `刻み×抉り (ノミ×エグ)` | 前1 |
| `compare` | `逸らし改 (ソラ×ノミ)` | 前1（**埋め草**） |
| `compare` | `止め (トメ×ソラ)` | 前1（**埋め草**） |
| 交差帯 | `傷×被弾 (カド×ノミ×ノノ)` | 後3 |

ほかに `Baseline.PrimaryRows` は**元から 0 行**、診断は残置した `UnitDef` を直接引くのでそのまま回る
（`BattleSim/Shard.cs` の 台B / 台C の後1 がエグなので、**第138期の台がそのまま再現できる**）。

**`All` は「編成に選べる 52 枚」の定義であって `Presets` が参照できる集合ではない**（第108期）ので、
**差し替えだけでは盤面は 1 ビットも動かない。** §2 で検算した。

### 0-3. **61 という行数への構造的な依存は 0 件**（Q0-3）

`BattleSim/*.cs` の「61」を全数走査した。**すべてレポートの表示文字列**で、
論理は `Presets.Compare.Length` を読んでいる。唯一の比較は

    Program.cs:22110   byAllRows.Length == 61 && byW == 5   → ○/× を表示するだけ

で、分岐も打ち切りも無い。**したがって「行を増やす」道は技術的に開いている**
（指示書が条件付きで許した選択肢）。

### 0-4. 傷の帳簿に新しい引数は 1 本も要らない（Q0-1・Q0-4）

**`BattleEngine.Run` の引数は 49 本**（`verbose` と観測子 `probe` を含む）。第138期の事故のとおり
余裕は無いが、**この期は 1 本も足さずに済む**——第120期の `WoundRule(Enabled, Census)` 一式が
そのまま残っていて、`wound2 run` が **`compare` 61 行 ＋ 交差帯 12 行 × 第2〜5波 × seed 0..199** を回す。

| 指示書 Q0-4 の項目 | 既にある計数 |
|---|---|
| 1. キリ・ノミが書いた傷 | `WoundLedger.WriteAlly` / `WriteFoe`（経路別 `WoundRoute`） |
| 2. 読み手ごとに読んだ量 | `ReadFires` / `ReadWounds` / `ReadNominal` / `ReadEffective`（`WoundReader` 6 役） |
| 3. 読まれずに消えた在庫 | `LossAll` / `LossAlly`（`WoundLoss` 8 種。`Death` / `End` が未読） |
| 4. ナタの「待った手番」 | **無い。しかも現行既定では構造的に 0**（下） |

> **4 は「測っていない」のではなく「起きない」。**
> 第74期に断ちの待ち方を `SeverWait.Yield`（手番を捨てる）から **`SeverWait.Swing`（振るが断てない）**へ
> 変えて既定にしてあり、`AwaitTrait.CanAct` は `Wait == Swing` のとき**無条件に真を返す**。
> 指示書の予測5「書き手2枚が2ターンかけて積む間、刃は下りない」は**刃ではなく断ちの発火にだけ当たる**。
> 測れるのは「**振ったが断てなかった手番**」（ナタの `UnitTally.Attacks` − `ReadFires[Sever]`）で、
> §3 ではそちらに読み替えた。
> 第59・60・107・110・118・121期に続く——**判定の線は、計数の名前ではなく実装が数えている出来事に当てる。**

### 0-5. **ガレを載せる行 —— 指示書の推奨2つがどちらも落ちた**（Q0-3）

指示書は `刻み×澱み (ノミ×ミオ)` を推奨し、`刻み×抉り (ノミ×エグ)` を「鎖の土台なので推奨しない」とした。
**Phase 0 で確かめろと書いてあった走査を当てたら、2つとも落ちた**——しかも落ちた理由が別々である。

**(i) `刻み×澱み` は `woundIgnite` の唯一の観測点**（`ledger unique`・HEAD で実測。61 行 × 5 波 × seed 0..199 × 22 版）:

| 規則 | 動いた行 | 唯一の観測点 |
|---|--:|:-:|
| `shove` | 1 | **○** 突き返し (ハネ×ウツ) |
| `bear` | 1 | **○** 引き受け (ウケ×ドハ) |
| **`woundIgnite`** | **1** | **○ 刻み×澱み (ノミ×ミオ)** |
| `loose` | 1 | **○** 置き去り×分散回復 |
| `taillight` | 1 | **○** 灯×薙ぎ (トモ×ドルガ) |

第111期が**まさにこの基準で落とす行を選び**（`死軸×ホタ` は `blaze` が動かす 9 行の1つなので落とせた）、
**「次に行を落とす期は、規則を切る側の表を先に出すこと」**を則として残している。
**第122期に巻き込み則を降ろした後も、この唯一性は生きている。**

**(ii) `刻み×抉り` は主判定19行のメンバー**（`Baseline.PrimaryRows`）。
落とすと**歯止めの分母が動く**——第60期が第五波の歯止めを全行平均から固定19行へ移した目的そのものを壊す。
（指示書の「鎖の土台」は理由として弱い——エグは `逸らし改` / `止め` に**埋め草として残る**ので、
5枚組の梯子は「1枚違い」のまま。効くのは主判定の側である。）

**(iii) 行を増やす道も閉じていた。** 0-3 のとおり「61」への依存は表示だけだが、
**`305` の決め打ちが3箇所ある**（第78期 `traits` / 第80期 `pairs` の `== 305`、第73/74期 `wound` / `wcost` の予算計算）
——`ledger phase0` の 表F が自分で名指ししている。**行を増やすと器具が3つ壊れる。**

### 0-6. **落とす行は、器具に選ばせた**

第111期の選定規則（`ledger phase0`）を HEAD で回した。**主判定19行を外した 42 行のうち、
線（`r ≥ 0.95` かつ `max|Δ| ≤ 10pt`・第2〜5波）を満たすのは 10 行**で、
**(c)「r が最大＝いちばん冗長な行」でも「情報セル最少」でも同じ行に落ちる**（器具自身が「どちらの経路でも一意に決まる」と書く）:

| 行 | 主判定 | 情報セル | 最も近い相手 | r | max\|Δ\| |
|---|:-:|--:|---|--:|--:|
| **溜め (ガン×ドルガ×カド)** | — | **1** | 耐久 (ガルド×ノノ) | **+1.0000** | **0.0** |

`docs/balance.md` の実値で突き合わせると、**2行は全5波が1ビットも違わない**:

    耐久 (ガルド×ノノ)         100.0 / 100.0 / 100.0 / 100.0 / 90.5
    溜め (ガン×ドルガ×カド)   100.0 / 100.0 / 100.0 / 100.0 / 90.5

**`溜め` は `耐久` を超える情報を1セルも持っていない。** 拒否権も全部通る:

| 検査 | 結果 |
|---|---|
| 主判定19行に入るか | **入らない** |
| 規則の唯一の観測点か（`ledger unique`・実測） | **違う**（上の5行に無い） |
| 特性の唯一の行か（表B・61 行に1行しか無い特性 8 種） | **違う**（`Sever`/`Await`/`Bear`/`Relay`/`Taillight`/`Drifter`/`Shove`/`Loose` のどれも含まない） |
| 他の行のコメントが梯子の土台として引いているか | **引いていない**（`Presets.cs` の参照は自分の定義行 1 箇所だけ） |
| 駒（ガン・ドルガ・カド）が盤面から消えるか | **消えない**（ガンは `溜め改` / `反撃改2`、ドルガは `駆り立て` ほか、カドは `反撃` ほか） |

> **指示書の2候補ではなく、器具が選んだ行を落とした。**
> 指示書は「推奨は `刻み×澱み`。ただし Phase 0 で確認すること」と書いており、
> **確認した結果として推奨が落ちた**——第111期の則（規則を切る側の表を先に出す）がそのまま働いた。

---

## §0'. 段0 —— **HEAD では `compare` が回らなかった**（指示書に無い作業）

段2 の検算（`compare` 305 セル 0 件差分）を取ろうとして**落ちた**。

    exit=127
    | 反撃改2 (ガン×カド) | 100.0% | 0.0% | 100.0% | 95.0% | 74.0% |
    Stack overflow.
       at BattleCore.BattleContext.ApplyDamageBody(...)
       at BattleCore.BattleContext.ApplyDamageCore(...)
       at BattleCore.BattleContext.ApplyDamage(...)
       at BattleCore.ThornsTrait+<>c__DisplayClass6_0.<OnDamaged>b__0()
       at BattleCore.BattleContext.Reaction(System.Action)
       ...（数千段）
       at BattleCore.BattleEngine.Run(... 49 個の引数 ...)
       at Program.<Main>$(...)

**落ちるのは 23 行目 `反撃改3 (カド×ハギ)`**（棘の反撃が数千段に積む行）。
**22 行目までの値は `docs/balance.md` と1文字も違わない。**

### 切り分け（第138期と同じやり方）

| 版 | `compare` |
|---|---|
| この期の作業ツリー（`All` をエグ → ガレ） | **落ちる**（22 行・exit 127） |
| 同じツリーで `git stash`（＝ HEAD と同じソース） | **落ちる**（22 行・exit 127） |
| **`eaec7e6` をクリーンな `git worktree` に出してビルド** | **落ちる**（22 行・exit 127・同じ行） |

> **HEAD そのものが落ちる。** 第138期は `ArmorRule` を `ShrapnelRule.ArmorCensus` に畳んで
> 「1本なら通る」ところまで戻したが、**その余裕は数百バイトしか無く、最終コミットでは足りていなかった。**
> 第138期の「`compare` 305 セルは `docs/balance.md` と 0 件差分」は
> **`compare` そのものでは取れていない**——同じ報告書が
> 「`shard check` では捕まらない。**自己検査が通ったは、生成物が作れるを意味しない**」と書いた穴を、
> **その報告書を書いた期自身がもう一度踏んでいた。**
> `docs/balance.md` の最終更新が第136期のまま（`git log -- docs/balance.md`）なのも辻褄が合う。

### 直し —— **1戦だけを大きなスタックの上で走らせる**（`BattleSim/BigStack.cs`）

**最初にやった形は失敗した。記録として残す。**
`compare` の分岐まるごとをローカル関数にして大きなスタックのスレッドへ渡したところ、
**本体が参照するトップレベル文の局所変数がすべて表示クラスへ巻き上げられ、
Roslyn（VBCSCompiler）がメモリ 52GB を掴んでビルドが終わらなくなった**（実測・タスクマネージャで確認）。

> **トップレベル文の一部をローカル関数に切り出すと、クロージャの範囲はその関数の本体ではなく
> 「参照しているトップレベルの局所変数の集合」で決まる。** 55,000 行のトップレベル文では、
> **400 行を包むだけでスコープ全体が巻き上がる。**

**採った形**は、クロージャを1つも作らない小さな窓口（`BattleSim/BigStack.cs`・新規 1 ファイル）:

- **ワーカーは大きなスタック（256MB 予約）のスレッド1本**を使い回す
  （1戦ごとに立てると 61 行 × 5 波 × 200 seed ＝ 61,000 回の生成になる）
- **受け渡しは static フィールド**（呼び出し側は単一スレッドなので競合しない）
- `Program.cs` で変えたのは **`compare` の中の `BattleEngine.Run(...)` 1 箇所だけ**（+5 行）

**盤面・乱数・順序には1ビットも触っていない。** 同じ引数・同じ戻り値・同じ順で1戦ずつ同期して走り、
変わるのは既定 1MB のスタックが 256MB の**予約**になることだけ
（予約は仮想アドレスで、実際に触ったページしか物理メモリを使わない）。

**検算**: `compare` が**最後まで完走**（exit 0・61 行 ＋ 見出し）し、
**`docs/balance.md` と全文一致**（305 セル 0 件差分）。
**この1回で段2 の受け入れ条件も同時に取れている**——ビルドには `All` のエグ → ガレが入っているため。

**引数の本数を減らす構造的な直し（49 本を1つの束に畳む）はやらない**
——呼び出し口が数百あり、**「計測器と測定対象を同時に動かさない」**に正面から反する。
この期に閉じるべきなのは「**生成物が作れない**」の1点である。

> **則を2つ**:
> **(1) 第138期の「引数を2本足すと踏み抜く」は、既に「もう踏み抜いている」だった。**
> 余裕を数百バイト単位で見積もる設計になっていること自体が穴で、
> **`compare` が通るかどうかが JIT の段（tier-0 / tier-1）のフレーム差で反転しうる。**
> **(2) トップレベル文のプログラムでは、一部をローカル関数に包むとスコープ全体がクロージャになる。**
> **包む前に「その本体がトップレベルの局所変数をいくつ参照しているか」を数えること。**

---

## §2. 段2 —— ロスターの差し替え（エグ → ガレ）

`UnitCatalog.All` の `Egu` を `Gare` に差し替えた**1語**。**52 枚のまま**。

- `Egu` の `UnitDef` / `GougeTrait` / `OverreachTrait` は**削除していない**
- **`Presets` は1行も触っていない**（この段では）——エグは `compare` 5 行と交差帯 1 行に残る
- `All` の doc に第108期と同じ形で根拠を書いた（**第119期のプラス値 +0.66 と、
  同じ期の3分が「転生」と言っていることの両方**を消さずに書いてある）

**受け入れ**: `compare` 305 セルが `docs/balance.md` と **0 件差分・全文一致**（上の検算と同じ実行）。

---

## §3. 段3 —— 傷の帳簿（**巻き込み則を降ろした後の姿**）

**engine にもノブにも1行も足していない。** 第120期の `WoundRule(Enabled, Census)` 一式がそのまま残っていて、
`wound2 run` が **`compare` 61 行 ＋ 交差帯 12 行 × 第2〜5波 × seed 0..199 ＝ 58,400 戦**（27.7 秒）を回す。
**`BattleEngine.Run` の引数は1本も増えていない**（Q0-1）。

### 3-1. 第120期からの変化 —— **通貨がほぼ消えた**

| 量 | 第120期（巻き込み則あり） | **第139期（HEAD）** |
|---|--:|--:|
| 書かれた傷 / 戦 | 9.2 | **0.646** |
| うち engine（巻き込み則）の取り分 | **89.4%** | **0.0%** |
| **味方側の駒に書かれた比率** | **93.1%** | **0.0%** |
| 読まれずに消えた割合 | 94.7% | **91.8%** |
| 傷が1個でも書かれた行 / 73 | （全域） | **10** |
| 傷由来の与ダメが総被ダメに占める割合 | — | **0.22%** |

> **第122期に巻き込み則を降ろした結果、傷は「味方に溜まる通貨」から「敵にだけ薄く載る通貨」へ完全に反転した。**
> 第120期の「味方比 93.1% は供給の 93.1% が engine 側だと言っているだけ」が、
> **供給を止めたらそのまま 0.0% になる**ことで裏を取った形である。

**帳簿は閉じる**（Q1 ○）——書かれた 37,753 ＝ 消えた 37,753、**差 0**。

### 3-2. 消滅の内訳 —— **消費経路は断ち1本・8.2% しかない**

| 経路 | /戦 | 割合 |
|---|--:|--:|
| **読まれず駒が死亡** | 0.536 | **82.9%** |
| **読まれず決着** | 0.057 | **8.8%** |
| 断ちが消費 | 0.053 | 8.2% |
| 繕いの塞ぎ／縫いの塞ぎ／引き取り／深手／会戦の掃除 | 0.000 | **0.0%** |

**在庫の取り合いは起きていない**（第120期の結論がそのまま生きている）。
**深さの分布は 深さ1 が 70.8%**——**断ちの閾値 2 に届かない在庫が7割**である。

### 3-3. 実効単価 —— **設計値は正しい。割っているのは到達率**

| 読み手 | 定数 | 発火/戦 | 実効÷読んだ傷 |
|---|--:|--:|--:|
| 抉り(エグ) | +3 | 0.153 | **2.73** |
| 刻みのなぞり(ノミ) | +2 | 0.226 | **2.51** |
| 断ち(ナタ) | +5 | 0.027 | **3.23** |
| 縫い(ハリ) / 繕い(ノノ) | +3 | **0.000** | — |

**読まれた傷あたり 2.66 点・書かれた傷あたり 2.39 点**（第120期は 2.72 / 0.15 で **18 倍**開いていた）。
**差が縮んだのは単価が上がったからではなく、読まれない供給が消えたから**である。

### 3-4. **ナタの「待った手番」は、現行既定では構造的に 0**

指示書 Q0-4 の4番目・予測5 は測れない。**第74期に断ちの待ち方を
`SeverWait.Yield`（手番を捨てる）から `SeverWait.Swing`（振るが断てない）へ変えて既定にしてある**ので、
`AwaitTrait.CanAct` は `Wait == Swing` のとき**無条件に真を返す**——手番は1つも捨てられていない。

測れるのは「**振ったが断てなかった手番**」のほう。**`刻み×断ち (ノミ×ナタ)` はナタを含む唯一の行**なので、
断ちの発火 0.027/戦 × 73 行 ＝ **その行で 1.97 回/戦**。同じ行の書かれた傷は **7.01/戦**で最多である。

> **ナタの律速は在庫の量ではなく、在庫の深さである。**
> 7.01 個/戦 書かれても、**深さ 1 が 70.8%** なので閾値 2 に届くのは一部で、
> 届く前に **82.9% が「読まれないまま駒が倒れて」消える。**
> **次にナタを触るなら、閾値でも単価でもなく「1体に積み直す速さ」**
> ——これは第120期の「読まれないのは競合ではなく到達しないから」と同じ結論に、
> **供給を 93% 削った後の盤面でも独立に到達した**ことになる。

### 3-5. 器具の中の説明文が1つ嘘になっていた

`wound2 run` の表D-2 が**「巻き込み則が味方に 8 個/戦 書くので、これは驚きではない」**と出す。
**この期の実測では味方側の傷は 0.000/戦**で、文だけが第120期のまま残っていた。
第122期の**「規則を降ろすと、その規則を前提に書かれた説明文が静かに嘘になる」**が
**診断の出力文の側でもう一度出た**（あちらは駒の `PlusText`、こちらは器具の地の文）。

