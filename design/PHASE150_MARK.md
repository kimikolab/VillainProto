# 第150期 報告書 —— 標（`Marked`）の軸を診た（**直さなかった**）

**HEAD 前提: 第149期 `40b3461`。** 指示書は `design/PHASE150_MARK_SPEC.md`。

## 結論 —— **第149期の読みも、指示書の対抗仮説も、どちらも外れた**

> **標の軸の4行が第149期に +15〜+21pt 動いたのは、標とは関係が無い。**
> **あの4行は V0 が 45〜61% ＝ 余地のいちばん広い帯にいただけ**で、
> **余地で割ると帯の平均以下**（標が立つ行 0.114 ／ 立たない行 **0.148**）。
> 帳簿の側でも、**行動順を振っても標の分類は 1 つも動かない**（Δ死亡 −0.04〜+0.12 回/戦）。

指示書 §1 の分岐表の **「分類がほとんど動かない → 標の穴ではない。ここで終わる」** に落ちたので、
**段C（吸い寄せ・速さ・供給のどれかを1点直す）はやっていない。盤面は1ビットも動いていない。**

やったのは2つ:

| 段 | 内容 |
|---|---|
| **A** | 標の一生の帳簿（`MarkLedger`）を engine に足した。**計数専用。`compare` 305 セル 0 件差分** |
| **B** | V0 / `Slowest` / `Strongest` で帳簿を取り、Q0-4 を判定した。**＋ 全61行で分母を数え直した**（表F〜H） |

`docs/` 14 ファイルのうち動いたのは **`rules.md` の `測った診断` 列だけ**（8 行・`mark` が
`DivertRule` / `GoadRule` / `FinisherRule` / `HasteRule` の利用者として付いた）。
**既定値の列は1つも動いていない**（規約 (G8) の必須3）。他の 13 ファイルは**バイト単位で一致**。

---

## 1. Phase 0

### Q0-0. `git fetch`

`origin/master` と同期。behind 0。

### Q0-1. `StatusKeys.Marked` を書く箇所の全数 —— **指示書の表は正しかった**

`mark phase0` が実装から引いた全数（走査は `class` だけでなく `record struct` / `struct` /
`interface` も宣言として数える。第130期の則）:

| ファイル:行 | 値 | 走らせる主体 |
|---|:-:|---|
| `Traits.cs:2528` | 1 | `MarkerTrait`（囃し立て・ヒサ） |
| `Traits.cs:3796` | **0** | `DivertTrait`（ソラが**味方から**剥がす） |
| `Traits.cs:3808` | 1 | `DivertTrait`（**自分**に付ける＝代金） |
| `Traits.cs:3825` | 1 | `DivertTrait`（**敵**に付ける＝焦点） |
| `Traits.cs:3953` | **0** | `GoadTrait`（カリが**前の相手**から消す） |
| `Traits.cs:3972` | 1 | `GoadTrait`（新しい相手に付ける） |
| `Traits.cs:4097` | **0** | `FinisherTrait.OnAfterAttack`（**トメ自身**が殴ったとき） |
| `Engagement.cs`（`StatusKeys.All` の一括消去 2 箇所） | **0** | 会戦の境界 |
| `Traits.cs`（`ScapegoatTrait.Kinds` に `Marked` を含む） | **−1** | 業（`UnitCatalog.All` の保持者 **0 枚**） |

**書き手 4／消し手 3（＋境界と業）。**
**「誰かが殴ると標が外れる」経路は 0 件である**——第149期の報告書が書いた
「**標は書いた次の手番までに殴られると外れる**」は**実装に無い**。
消すのはトメ自身が殴ったときだけで、`FinisherTrait` はトメが持つ特性なので
**他の味方が何回殴っても標は残る。**

**書き手4枚はすべて `OnBattleStart` か `OnTurnStart`。**
行動順ループの中で標が新しく立つ経路は 0 件——これが段A の実装の根拠で、
**区間を開くのはターン頭の走査1箇所でよい**。

### Q0-2. 既にある計数の棚卸し —— **穴は2つあった**

| 計数 | 何を測っているか | 分母の穴 |
|---|---|---|
| `FinisherFires` | トメが標持ちを**単体で**殴った回数（倍率が乗った回数と同じ） | トメがいないと 0 |
| `FinisherCross` | そのうち `TargetPool` の外＝**標が無ければ狙えなかった敵** | 同上 |
| `FinisherIdle` | トメが単体で振ったが相手が標を持っていなかった回数（空振り） | 同上 |
| `FinisherConsumed` | トメが標を消した回数 | 同上 |
| `FinisherWaitSum/Count` | 標が付いてから**トメが殴るまで**のターン数（÷ が遊休） | **殴られなかった標は1件も入らない** |
| `DivertAllySingles` / `DivertAllyOnMarked` | 味方の単体振りのうち標持ちに落ちた割合 | ソラがいないと 0 |
| `DivertMarkedFoeMax` | 同時に標が付いた敵の最大数 | 同上 |

`NoteFinisherMarkAges` は**呼ばれている**が、穴が2つある:

1. **`if (ctx.FinisherActive)` の門が掛かっている**——**トメが盤上にいる行でしか走らない**
2. ループが **`if (u.TeamId == PlayerTeam) continue;`**——**味方に付いた標は1件も見ていない**

> **予測6（「呼ばれているが読み手は `NoteFinisherFire` だけ」）は ○。**
> **ただし実体はもっと強い**——**呼ばれること自体がトメの在席を条件にしている。**

**だから段A の帳簿は門を `MarkActive`（標の<u>書き手</u>が盤上にいるか）に置いた。**
読み手で門を掛けると `駆り立て改` と `仇討ち×砕け` が丸ごと落ちる。

### Q0-5. 測定台（**新しい台は作らない**）

| 行 | 書き手 | 読み手 | 標の向き | V0（第2〜5波） |
|---|---|---|---|--:|
| `駆り立て改 (カリ×死軸)` | 駆り立てのカリ | **なし** | **味方** | 49.9% |
| `止め改 (トメ×薙ぎ)` | 逸らしのソラ | 止めのトメ | **敵**（＋自分） | 61.0% |
| `仇討ち×砕け (ヒビ×ザン)` | 囃し立てのヒサ | 仇討ちのザン | **味方** | 45.6% |
| `止め (トメ×ソラ)` | 逸らしのソラ | 止めのトメ | **敵**（＋自分） | 47.0% |
| `逸らし改 (ソラ×ノミ)` | 逸らしのソラ | **なし** | **敵**（＋自分） | 84.6% |

**5行で「書き手の向き × 読み手の有無」の4象限が埋まっている**（味方×読み手なし／敵×読み手あり×2／
味方×読み手あり／敵×読み手なし）。

### Q0-6. 既存のノブ

| ノブ | 既定 | 何を振れるか |
|---|---|---|
| `FinisherRule` | `{ Multiplier = 2, Consume = True }` | トメの倍率／標を消費するか |
| `DivertRule` | `{ TargetCount = 1, SelfMark = True, Audit = False }` | ソラの焦点の数／自分に刺さるか |
| `GoadRule` | `{ Boost = 4, Mark = True }` | カリの見返り／標を付けるか |
| `MarkPullPercent` | **75**（`const`） | **吸い寄せの強さ。ノブになっていない** |

> **`MarkPullPercent = 75` は測って置いた値ではない。**
> `git log -S "MarkPullPercent = 75"` は **最初のコミット `7f6c191`（「編成プロトタイプ: 攻撃パターンと
> 範囲耐久まで」）の1件しか返さない**——**150 期のあいだ一度も掃引されていない。**
> **庇いの 50% が第136期まで一度も動いていなかったのと同じ形**（第136期 Q0-10）。

### Q0-7. 過去に標を触った期

**第50期（逸らし・採用）**——標の書き手と消し手を駒側に作った期。**engine には規則を1つも足していない**。
この期が確定させたのは4つ: (1) `MarkPullPercent` は確率だが `marked != target` の但し書きがあるので
**実効の被狙撃率は 75% より高い**、(2) **候補は `pool` ではなく `foes`** なので
**標だけが「前列が生きている限り後列は狙われない」という盤面の中核規則を破る**
（＝ `pool` の段に移すと列越えが構造的に消える。第53期が同じ理由でこの段を選んだ）、
(3) 鎖の順序は **標 → 後備え → 庇う → 殉教 → 棘守り**で標がいちばん先、
(4) **単体攻撃にしか効かない**（薙ぎ・全体・貫きは標を1ビットも見ない）。
帰属 +27.2 / +25.6pt で直近最大だった。

**第52期（駆り立て・採用）**——**設計の意図と逆になった**期。標を代金・強化を見返りとして設計したのに、
実測は**標の寄与 +36.8pt（ザンあり）／−10.0pt（ザンなし）・強化の寄与 +3.3〜+4.2pt** で、
**代金として設計した標が価値のほぼ全部を作っていた。**
一般則は「**読み手がいれば標は収入、いなければ代金**」。

**第53期（止め・採用）**——**敵の標を読む初めての駒**。**engine に規則も窓口も足していない**——
3つの効果を既存の3箇所（標の段・`PerformAttack` の `atk` を作った直後・駒側の `OnAfterAttack`）に置いた。
残件に「**供給がソラ1枚のまま**」「`止め (トメ×ソラ)` の第三波が 3.5%」が挙がっている。

---

## 2. 段A —— 標の一生の帳簿（`MarkLedger`）

**計数専用。どの規則も読まない。** 足したのは:

- `BattleContext` の配列12本＋辞書2本と、走査 `ScanMarkLedger`（`NoteFinisherMarkAges` の直後・同じ理由）
- `Add` の短絡フラグ `MarkActive`（標の**書き手**が盤上にいるか）
- 消す側3箇所の `Note*`（`DivertTrait` / `GoadTrait` / `FinisherTrait`）＋ `HandleDeath` ＋ `ApplyDamage`
- `BattleResult.Marks`（`readonly record struct MarkLedger`）

**`Run` の引数は1本も足していない**（`MarkRule` は存在しない）。**`ctx.PickOne` の出現数は 27 で第149期 HEAD と同数。**

**区間の定義**: 標が付いていない駒に標が付いた瞬間に開き、**消費 / 死亡 / 剥がし / 替え / 他 / 残存**の
どれかで閉じる。**駆り立ては毎ターン `prev` を 0 にしてから付け直す**ので、
同じ相手に付け直した場合は走査の時点で標が立っており区間は閉じない
（＝「替え」は宛先が変わったときだけ数える。これが無いと `駆り立て改` の帳簿が
毎ターンの「替え」で埋まって読めなくなる）。

**帳簿は閉じる**——61 行 × 5 波 × seed 0..19 で「付いた ≠ 分類の合計」**0 件**、
**分類できなかった区間（`他`）0 件**（＝ Q0-1 の全数で消える経路を1本も数え落としていない）。

---

## 3. 段B —— 標の一生（V0・第2〜5波・1戦あたり）

| 行 | 側 | 付いた | 消費 | 死亡 | (内)止めが | 替え | 残存 | 寿命T | 最大T |
|---|---|--:|--:|--:|--:|--:|--:|--:|--:|
| 駆り立て改 | 味方 | 1.25 | 0.00 | **1.12** | 0.00 | 0.06 | 0.08 | 1.45 | 3 |
| 止め改 | 敵 | 2.06 | **1.82** | 0.19 | 0.08 | 0.00 | 0.05 | **0.23** | 6 |
| 止め改 | 味方 | 1.00 | 0.00 | 1.00 | 0.00 | 0.00 | 0.00 | 1.11 | 2 |
| 仇討ち×砕け | 味方 | 1.00 | 0.00 | **0.90** | 0.00 | 0.00 | 0.10 | 3.03 | 7 |
| 止め | 敵 | 4.84 | **3.09** | 1.62 | **0.94** | 0.00 | 0.14 | **0.05** | 3 |
| 止め | 味方 | 1.00 | 0.00 | 0.63 | 0.00 | 0.00 | 0.37 | 4.28 | 9 |
| 逸らし改 | 敵 | 3.73 | 0.00 | **3.48** | 0.00 | 0.00 | 0.25 | 1.64 | 6 |
| 逸らし改 | 味方 | 1.00 | 0.00 | 0.28 | 0.00 | 0.00 | 0.72 | 4.48 | 8 |

割合（付いた標を 100% として）:

| 行 | 側 | 消費 | **死亡** | 替え | 残存 |
|---|---|--:|--:|--:|--:|
| 駆り立て改 | 味方 | 0.0% | **89.5%** | 4.4% | 6.1% |
| 止め改 | 敵 | **88.3%** | 9.0% | 0.0% | 2.6% |
| 止め改 | 味方 | 0.0% | **100.0%** | 0.0% | 0.0% |
| 仇討ち×砕け | 味方 | 0.0% | **89.5%** | 0.0% | 10.5% |
| 止め | 敵 | **63.8%** | 33.4% | 0.0% | 2.8% |
| 止め | 味方 | 0.0% | **62.6%** | 0.0% | 37.4% |
| 逸らし改 | 敵 | 0.0% | **93.2%** | 0.0% | 6.8% |
| 逸らし改 | 味方 | 0.0% | 27.6% | 0.0% | **72.4%** |

標が立っているあいだに標持ちが受けた攻撃:

| 行 | 側 | 被弾/戦 | うち止め | 止めの割合 | **被弾/区間** |
|---|---|--:|--:|--:|--:|
| 駆り立て改 | 味方 | 14.02 | 0.00 | 0.0% | **11.19** |
| 止め改 | 敵 | 5.39 | 1.90 | 35.2% | 2.62 |
| 止め改 | 味方 | 7.70 | 0.00 | 0.0% | 7.70 |
| 仇討ち×砕け | 味方 | 17.28 | 0.00 | 0.0% | **17.28** |
| 止め | 敵 | 13.62 | 4.03 | 29.6% | 2.81 |
| 止め | 味方 | 15.06 | 0.00 | 0.0% | 15.06 |
| 逸らし改 | 敵 | 20.30 | 0.00 | 0.0% | 5.44 |
| 逸らし改 | 味方 | 13.46 | 0.00 | 0.0% | 13.46 |

### 3-1. **標の寿命は読み手の有無で2桁違う**

    トメがいる行（敵の標）      0.05T / 0.23T   ← 書いた同じターンに消える
    トメがいない行              1.45 / 1.64 / 3.03 / 4.28 / 4.48T

**トメがいれば標は書かれた瞬間に食われる**（消費 63.8% / 88.3%）。
**対抗仮説（「トメが動く前に標的が死ぬ」）が支配的な分類になった行は1つも無い**
——`止め改` の死亡は **9.0%**、`止め` は 33.4% だが**その 58%（0.94 / 1.62）はトメ自身の撃破**なので、
「**トメが殴る前に他の味方が倒した**」は **0.68 / 4.84 ＝ 14.0%** しかない。

> **予測1（「標的がトメより先に死ぬ」が `止め` / `止め改` で最大の分類になる）は ×。**
> **どちらも最大は「消費」である。** 標の軸は、少なくともトメがいる行では**設計どおり動いている。**

> **予測4（標の平均寿命は短くない）も ×**——ただし外れ方が予測と逆で、
> **短いのはトメがいる行だけ**（0.05〜0.23T）。**読み手のいない行では 1.45〜4.48T と長い。**

### 3-2. 供給が律速になっている行がある

`止め改` は **2.06 標/戦しか付かないのに 88.3% が消費される**——
ソラは `DivertRule.TargetCount = 1` なので**毎ターン敵1体にしか付けられず**、
トメが同じターンに食い切る。**在庫は 0 に張り付いている。**
第53期の残件「**供給がソラ1枚のまま**」が、帳簿の形で出た。

`止め` はゴルムが後列を覆うのでソラが長く生き、**4.84 標/戦**まで伸びる（消費 63.8%）。
**同じ2枚の組でも、供給の厚さが 2.3 倍違う。**

### 3-3. 読み手のいない行では、標は「集中砲火の避雷針」として働いている

`駆り立て改`（1区間あたり **11.19 発**）と `仇討ち×砕け`（**17.28 発**）は、
**標を付けられた味方が区間のあいだに10〜17回殴られ、89.5% / 89.5% がそのまま倒れる。**
`逸らし改` は逆向きで、**敵に付けた標の 93.2% が死ぬ**（＝味方の集中砲火が機能している）。

**engine の吸い寄せ（75%）は、どちらの向きでも設計どおり働いている。**

---

## 4. 段B —— Q0-4（行動順が何を変えたか）

| 行 | Δ(S) | Δ(A) | Δ死亡(S) | Δ消費(S) | Δ付いた(S) | 読み |
|---|--:|--:|--:|--:|--:|---|
| 駆り立て改 | **+21.13** | +17.88 | −0.01 | 0.00 | +0.03 | **分類が動かない** |
| 止め改 | **+18.25** | +11.13 | −0.04 | +0.08 | +0.01 | **分類が動かない** |
| 仇討ち×砕け | +16.13 | **+21.50** | +0.01 | 0.00 | 0.00 | **分類が動かない** |
| 止め | +15.50 | −0.25 | −0.21 | +0.36 | +0.11 | 死亡が減り消費が増えた |
| 逸らし改 | +5.25 | +5.13 | +0.12 | 0.00 | +0.02 | 対抗仮説の形ではない |

**5 行中 4 行で、帳簿の分類が 0.1 回/戦 も動かないまま勝率が +5〜+21pt 動いている。**
唯一動いた `止め` でも消費 3.09 → 3.45（+11.6%）で、+15.50pt の説明にはならない。

**⇒ 指示書 §1 の分岐表の「分類がほとんど動かない ＝ 標の穴ではない。ここで終わる」に落ちた。**

---

## 5. **では何が動いたのか** —— 全61行で分母を数え直した（表F〜H）

指示書は「その場合は第149期の +2.7 の一般効果がたまたま大きく出ただけ」としていたが、
**「たまたま」も仮説である**ので、`compare` 61 行で数えた。
物差しは第144期の**取り分 ＝ Δ ÷ (100 − V0)**（生の pt は余地を測っている）。

| 群 | 行数 | V0 | Δ(S) | Δ(A) | **取り分(S)** | **取り分(A)** |
|---|--:|--:|--:|--:|--:|--:|
| **標が立つ** | 15 | 64.2 | +4.68 | +5.89 | **0.114** | **0.201** |
| **立たない** | 46 | 77.0 | +2.06 | +4.84 | **0.148** | **0.249** |

**生の pt では標が立つ行が 2.3 倍動いているのに、余地で割ると標の無い行のほうが大きい。**
差の実体は **V0 が 64.2% 対 77.0%** ——それだけである。

相関（余地のある 59 行）:

| 量 | 取り分(S) との r | 生の Δ(S) との r |
|---|--:|--:|
| 標が立つか（0/1） | **−0.072** | +0.233 |
| 標/戦（V0） | +0.087 | +0.373 |
| 被弾/区間（V0） | −0.053 | — |
| V0 の勝率 | +0.275 | **−0.389** |

**「標が立つか」と取り分の相関は −0.072 ＝ ゼロ。** 生の Δ で見えていた +0.233 は、
**V0 との −0.389 を写しているだけ**である。

取り分の上位を並べると、**標を1つも持たない行が上に並ぶ**:

| 行 | 標/戦 | V0 | Δ(S) | 取り分(S) |
|---|--:|--:|--:|--:|
| 縛め収入型 (クグ×バン×ガン) | — | 89.5 | +7.88 | **0.750** |
| 惨禍×死の連鎖 | — | 98.1 | +1.38 | 0.733 |
| 死の連鎖+後備え | — | 97.6 | +1.50 | 0.632 |
| 刻み×断ち (ノミ×ナタ) | — | 94.2 | +3.38 | 0.587 |
| 耐久 (ガルド×ノノ) | — | 97.6 | +1.25 | 0.526 |
| 鱗 (ウロ×死軸) | — | 89.4 | +5.00 | 0.471 |
| **止め改 (トメ×薙ぎ)** | 3.06 | 61.0 | +18.25 | 0.468 |
| **駆り立て改 (カリ×死軸)** | 1.25 | 49.9 | +21.13 | **0.421** |

**第149期の表C で「標の軸だけが別の機構で動いている」と見えたのは、
第149期が生の pt で並べたから**である。

**唯一残る差**は V0 が低い帯の中にある——`0〜60%` の帯では **標あり 0.150（6行）／標なし 0.083（9行）**。
**n が 6 対 9 なので、この期はこれを「差がある」と書かない。**
他の帯（`60〜80%` 0.067 対 0.058 ／ `80〜95%` 0.137 対 **0.176**）では逆を向くか差が無い。

---

## 6. 予測の当否（**外れたものも消さずに残す**）

| # | 予測 | 当否 |
|---|---|:-:|
| 1 | 「標的がトメより先に死ぬ」が `止め` / `止め改` で最大の分類になる | **×** —— どちらも最大は**消費**（88.3% / 63.8%）。死亡は 9.0% / 33.4% で、後者も 58% はトメ自身の撃破 |
| 2 | `駆り立て改` は `止め` と同じ分類で説明できない | **○** —— 消費 0.0% / 死亡 89.5% と分類が完全に別。ただし**動いた理由は標では説明できなかった**ので、当たったのは「別だ」の半分だけ |
| 3 | `仇討ち×砕け` は `Strongest` で最大 | **○** —— +21.50 対 +16.13 |
| 4 | 標の平均寿命は短くない | **×**（かつ外れ方が逆）—— **読み手がいる行だけ 0.05〜0.23T** と極端に短く、いない行は 1.45〜4.48T |
| 5 | 戦闘終了時に立ったままの標がそれなりにある | **半分 ○** —— **味方側**は `逸らし改` 72.4% / `止め` 37.4% だが、**敵側は 2.6〜6.8%** しか残らない |
| 6 | `NoteFinisherMarkAges` は呼ばれているが読み手は `NoteFinisherFire` だけ | **○**（実体はもっと強い）—— **呼び出し自体に `FinisherActive` の門があり、さらに味方側を1件も見ていない** |

---

## 7. 受け入れ条件

| # | 条件 | 結果 |
|---|---|:-:|
| 1 | 段A で `compare` 305 セル 0 件差分・`docs/` 14 ファイル全文一致 | **○**（305 セル 0 件／13 ファイルはバイト一致・`rules.md` は `測った診断` 列 8 行のみ・**既定値の列は不変**） |
| 2 | 段C をやった場合、その行以外が ±0.0 | **該当なし**（段C はやっていない） |
| 3 | `audit` ずれ 0／`derive scan` 正常／ビルド警告 12 のまま新規 0 | **○**（ずれているファイル 0 件／過剰 0 件・既知の欠落のみ／警告 12） |
| 4 | 帳簿の合計が合う | **○**（61行 × 5波 × seed 0..19 で不整合 0 件・`他` 0 件） |
| 5 | `GodotApp` / `DemoApp` / `PrototypeApp` がビルドできる | **○**（3つとも 0 警告 0 エラー） |
| 6 | `sweep` は回さない | **○** |

**注**: この期の測定中、別セッションの `0 sweep` が `BattleSim/bin/Release` を掴んでいたので、
**ビルドと実行は `-p:BaseOutputPath=BattleSim/bin150/` の別出力で行った**（走っているジョブに触れないため）。
バイナリの中身は同じで、`docs/` の全文一致がそれを担保している。

---

## 8. 一般則（`CLAUDE.md` に入れたもの）

1. **「その軸だけが動いた」は、生の pt で並べた表からは言えない。**
   第149期の表C の上位4行は全部 V0 が 45〜61% ＝ 余地のいちばん広い帯にいて、
   **取り分で並べ直すと標を1つも持たない行が上に来る**（最上位は `縛め収入型` の 0.750）。
   **標が立つか と 取り分 の r は −0.072 ＝ ゼロ**で、生の Δ の r = +0.233 は
   **V0 との r = −0.389 を写しているだけ**。第144期「波ごとの効き方を生の pt で比べると、
   測っているのはその波に残っている余地」の**行の側の版**。

2. **報告書が書いた機序は、次の期がコードで検算するまで仮説である。**
   第149期の「標は書いた次の手番までに殴られると外れる」は**実装に1行も無い**
   ——`Marked` を 0 にするのは3箇所だけで、そのうち殴打が条件なのは**トメ自身**の一撃だけ。
   `git log -S` で追うと `MarkPullPercent = 75` も**最初のコミット以来1度も動いていない。**
   **「何期も前から書いてある」は「確かめた」ではない。**

3. **「その機構が働いているか」を計数で見るときは、門が読み手に掛かっていないかを見る。**
   `NoteFinisherMarkAges`（第53期・標の遊休）は **`FinisherActive` の門の中にあり、
   さらに味方側を `continue` で飛ばす**ので、**トメがいない行の標は 97 期ぶん1件も観測されていなかった。**
   帳簿の門は**書き手**（`MarkActive`）に掛けること——読み手で掛けると、
   読み手のいない行（＝いちばん知りたい行）が丸ごと落ちる。

4. **標の寿命は読み手の有無で2桁違う。**
   トメがいれば 0.05〜0.23T（書いた同じターンに食われる）、いなければ 1.45〜4.48T。
   **`止め改` は 2.06 標/戦しか付かないのに 88.3% が消費される**＝**在庫が 0 に張り付いている**
   （ソラの `TargetCount = 1` が律速）。第53期の残件「供給がソラ1枚のまま」が帳簿の形で出た。
   **同じ2枚の組でも、供給の厚さは席で 2.3 倍違う**（`止め` は 4.84 標/戦。
   ゴルムが後列を覆ってソラが長く生きる）。

---

## 9. 残件（**この期では決めない**）

1. **`MarkPullPercent` は 150 期のあいだ一度も掃引されていない。** ノブ化していない（対抗仮説が
   外れたので触る理由が無かった）。**掃引するなら「標の軸を直すため」ではなく「盤面の中核規則を
   破る唯一の窓口の強度」として単独で。**
2. **`止め改` の在庫が 0 に張り付いている。** 供給（`DivertRule.TargetCount`）を上げるか、
   **標の3枚目の書き手**を作るか。第50期の残件Aは「まだ要らない」と判断したが、
   **あれはトメ（消費する読み手）が入る前の判断である。**
3. **`NoteFinisherMarkAges` の門と `PlayerTeam` の `continue` は直していない。**
   段A の `MarkLedger` が上位互換なので、**第53期の遊休の計数を畳むかどうかは別に決める。**
4. **標を付けられた味方は 10〜17 発/区間を浴びて 89.5% が倒れる。**
   「矢面に立つ」の値段は帳簿に出たが、**それを買い戻す機構（肩代わり・仇討ち以外）は数えていない。**
