# 第92期 —— 交差帯を作る（軸をまたぐ機構を測れる台を並置する）

指示書: design/PHASE92_CROSS_SPEC.md ／ 起点: 第91期（`soak`・滲み則は毒だけ採用・器具の規約4つ）

---

## 0. 事前登録（**§5 の測定より先にコミットする**。自己検査 (d)）

**この節は、`cross quality` / `cross redo` を1戦も回す前に書いてコミットしてある。**
以下の内容は測定後に一文字も書き換えない（追記が要る場合は §0-9 に「事前登録の訂正」として別立てで書く）。

### 0-1. 再判定の対象（§4-1）

**ノブが残置されていて、かつ軸をまたぐ機構であるもの**は次の2つだけ。**これ以外を再判定の対象にしない。**

| 期 | ノブ | 交差 | 当時の結果 | A（2×2 で固定する側） |
|---|---|---|---|---|
| 第84期 | `ThornRule`（`None` → `Foe`） | 被弾 × 傷 | Δ相乗 +0.04pt。第88期に3つの読みすべてで落ちて閉じた | **棘鎧のカド** |
| 第86期 | `MendRule`（`Plain` → `Wound`） | 傷 × 回復 | **紙の門で停止（2×2 を1戦も回していない）** | **継ぎ当てのノノ** |

第85・87・89・90・91期は採用済み。第79期の処刑は駒の候補でノブが無い。

### 0-2. 意図した相手と予測（§4-2）

| 期 | 意図した相手 | 予測 |
|---|---|---|
| 第84期 | 傷の5枚（**キリ・ノミ・エグ・ナタ・ハリ**） | **通らない。** 第88期に情報帯の有無を含む3つの読みすべてで落ちている |
| 第86期 | 巻き込み則の書き手6枚（**カド・ゴルム・ボルグ・リィカ・ゾト・ナラ**） | **割れる。** 発火 2.10 回/戦 が交差帯で上がるかが分かれ目 |

**第84期が通ったら器具のほうを疑う**（§6）——交差帯が何を測っているのかを先に確かめる。

### 0-3. 判定の手続き（§4-3。**測る前に固定**）

- **主判定**: 第88期 §4 の特異性。**分母 = 50 体の組。2×2 はドラフト台であって交差帯ではない。**
  - Q1-1 意図した相手のうち少なくとも1枚が 2系列とも正 かつ \|Δ\| > ノイズ床
  - Q1-2 意図しない相手で \|Δ\| > ノイズ床 の体数が **3 以下**
- **ノイズ床**: 第89期 §1-1 の規約 —— 同じ実験の中の「意図しない相手」の \|Δ相乗\| の **95 パーセンタイル**
- **情報帯フィルタ**: 第91期 (G3)。`ThornRule` / `MendRule` はどちらも**駒の特性の中**にあるので**当てる**
- **併記**: 交差帯（12 行）での `compare` 差分
- **拒否権**: 第91期 (G1)(G2)。**分母 = `compare` 61 行全体**。
  いずれかの波で −10.0pt 以上落ちた行は、含まれる駒それぞれの「他の行」の全波平均の変化で
  **壊れ（≤ −3.0pt）と制約（\|変化\| < 3.0pt）に分ける**
- **再判定で通っても、61 行の拒否権で「壊れ」と出たら採らない**

### 0-4. 2×2 の定数（**第81期 `pairs2` の写し。1つも変えていない**）

K = 64 台／組／系列・系列 2 本・戦闘 seed 0..7・弱い波 60%・第2〜5波・
情報帯 20 台未満の組は「測れていない」・Q1-2 の線 N3 = 3。
**台の抽選は `TableSeed` = 9,200,000（第92期の標本）**——第90・91期の 9,000,000、第89期の 8,900,000、
第87・88期の 8,100,000 とは別（第89期の作法）。

### 0-5. 門（§4-2。**紙は出力であって門ではない**・第90期）

**(1) その通貨を持つ相手が存在する ／ (2) その相手に書き手が書く ／ (3) 書いたものが実際に払い出される。**
**交差帯 12 行で測る。** どれかが 0 ならそこが切れている場所を書いて閉じる。

### 0-6. 交差帯の判定（§3。**測る前に固定**）

- **Q1**: 12 行のうち、情報セル（`0 < x < 100`）が5波中3波以上ある行が **10 行以上**
- **Q2**: 交差帯の平均の稼働率が、**61 行を同じ器具で測った値**を上回る
- **Q3**: 交差の両側が発火していない行が **0**

**Q2 の「61 行の 35〜40%」について**: 第86期のその値は**読み手の発火 ÷ 決着T**という
別の器具の値なので、**61 行を交差帯とまったく同じ器具で測り直した値**を比べる相手にする。
器具は **`出会い/戦 ÷ 決着T`**、`出会い` は **同じ駒の上で交差の両方のキーのカウンタが動いた延べ体数**
（engine の `NoteCarry` が 11 キーすべてに等しく積むので、12 行を同じ器具で測れる）。

### 0-7. Phase 0 の予測（§1 の「予測」。**測る前に書いた**）

- **空白の組は 55 のうち大半。** 61 行が「各軸の代表を1行ずつ」で育った帰結。
- **傷 × 毒 の行が最も動く**（機構が2本ある。第91期のローカル台で +0.70pt・持続係数 1.11〜1.34）。
- **交差帯の稼働率は 61 行より高い**（第91期のローカル台 51.1 / 55.9% 対 61 行の 35〜40%）。
  **これが交差帯を作る理由そのものなので、外れたら前提が崩れる。**

### 0-8. この期で決めないこと（§7）

交差帯を主判定19行に入れるか（**入れない**）／ 61 行の作り替え（**触らない**）／
交差帯を13行以上に増やすか ／ 致命傷 ／ 滲みの量（+1）を振ること ／
痺れ・破片・標を滲み則の対象にするか ／ 最後の1枠 ／ `AcceptsSupport` の統一 ／ ムドの呪い。

---

（以下、測定後に書く）
