# 知見 第101期〜

description: 第101期〜の知見。会戦の境界・手番という通貨・再行動・背かれ・尾灯と、ロスターの入れ替え（ハリ→トモ）と行の差し替え、ボスの土台と時間を買う機構、52 枚の健康診断とマイナスを外した版の軸。画面の期（戦績パネル・出来事の書き手・演出・手番の外）。段（攻撃型の格上げ）と、指標の直し・延命台。**いま盤面がどうなっているか**を知るために読む。

`CLAUDE.md` の「コマンド」節から**逐語で**移した各期の実測と経緯（第112期）。
**本文は1文字も書き換えていない。**見出し（`### 第nn期`）だけがこの期に足したもの。
則そのものは `CLAUDE.md` の「規約」「指示書を書くときに当てる一般則」に残してある。

| 期 | 診断 |
|---|---|
| 第101期 | `recover` |
| 第102期 | `choice` |
| 第103期 | `betray` |
| 第104期 | `encore` |
| 第105期 | `tempo` |
| 第106期 | `hold` |
| 第107期 | `hold2` |
| 第108期 | `taillight` |
| 第109期 | `tomo` |
| 第110期 | `tomo` |
| 第111期 | `ledger` |
| 第113期 | `lit` |
| 第114期 | `lit`（採否） |
| 第115期 | `reader` |
| 第116期 | `reader load` |
| 第117期 | `boss` |
| 第118期 | `tank` |
| 第119期 | `checkup`（回し直し） |
| 第120期 | `wound2` |
| 第121期 | `wound2 spill` |
| 第122期 | `wound2 spill adopt`（採用） |
| 第123期 | `watch`（見る期） |
| 第124期 | 戦績パネル・書き手・演出（画面の期） |
| 第125期 | `offturn`（手番の外を見せる期） |
| 第126期 | `time`（時間を買う3案・**採用 0 件**） |
| 第127期 | `grade`（段＝攻撃型の格上げ・**段2 で据えのバンに載せた**） |
| 第128期 | `grade2`（段の載せ替え・**ドルガに載せ、`docs/stock.md` を足した**） |
| 第129期 | `survive`（指標の直しと延命台・**ハギの `MaxHp` を 62 → 82**） |
| 第130期 | `ember`（火を配る・**段0 でハギの HP を戻し、段1 は採用 0 件**） |
| 第131期 | 踏み込みの札（`UnitDef.Advances`）と則の取り込み・**盤面は1ビットも動かない** |
| 第132期 | `yoke map`（上限の地図）と器具2本の修理・**盤面は1ビットも動かない** |
| 第133期 | `wildfire`（撒いた火を読む・**段1 で落ちた**）・**盤面は1ビットも動かない** |
| 第134期 | `stacks`（重ね掛けと盤面ルールの対称性・**測定だけ**）・**盤面は1ビットも動かない** |
| 第135期 | `parry`（害の帳簿 `docs/harm.md` と受け流し・**盤面は1ビットも動かない**） |
| 第136期 | `wall`（ガルドを壁にする——庇い 100%・受け流し・中継。**3段で盤面を動かした**） |

### 第101期 — `recover`

**同じ回復が、第2波では代金の 72% を返し、列全体では 9% しか返さない**（第101期）。
単発の第2波は 66.7%・会戦の R0 は 33.8% で代金は 33.0pt。**HP はそのうち 23.7pt（72%）を返す**が、
突破率では 51.1pt の隙間のうち **4.8pt（9%）**しか返さない。
**第2波は「削られた体で入る」ので HP が効き、第3波は「減った頭数で入る」ので効かない。**
**波が進むほど、代金の中身が HP から体へ移る。**

**死が恒久であるあいだ突破率は「残存」に付き、死者が戻ると「勝率」に乗り換える**（第101期・ピアソン）。
r(残存) は R0〜R100 で 0.415〜0.496 対 r(勝率) 0.141〜0.313 だが、**RFull で 0.114 対 0.879 と入れ替わる。**
**ただしスピアマンは R100 で逆を言う**（0.304 対 0.446）——**46/73 行が 0% の同値塊**なので、
順位相関が測っているのはほぼ「0 かどうか」だけ。**同値塊がある版では順位相関を読まない**
（第22・54期「天井は同値塊を作る」の床側）。

**育つ駒の不利は「二重」ではなく、ほぼ全部が体の側だった**（第101期。第100期の申し送りへの部分的な回答）。
ムド・カド・ヨミ・リィカを含む 36 行と含まない 37 行の突破率の**比**は
R0 0.00 → R100 0.06 → **RFull 0.82** で、**単発の連勝率の比 0.65 を上回る。**
**pt の差を版のあいだで比べてはいけない**（分母が 1.9% から 57.2% まで 30 倍動く）。
**この分割は統制されていない**ので、`AtkBonus` の持ち越しの値段はここからは読めない。

**抜けた試行の上でしか定義できない量を、抜けた試行の数が動く比較に使ってはいけない**（第101期・器具の反省）。
Q2（圧勝率が R0 より下がらない）は**下回りようがない線だった**——R0 の圧勝率は **0%** ＝ 下限。
しかも観測された 82 → 50 → 26 → 14% の単調減少は、分母が 241 → 7601 と 32 倍に増えるので
**「泥仕合になった」と「選抜が緩んだ」を区別できない。**
第59期「判定式の条件節が分母の何割を選ぶかを実装前に数える」の**下限側**の再発。

**`RFull` は「各波が単発と同じ条件」ではない。「HP と体だけを単発と同じにした版」である**（第101期）。
73行を持ち越される資源で分けると、単発の連勝率とのずれは完全に説明が付く:

    リィカ            3行  +26.7pt  ← 墓守の層を OnCarryOver が再構成する（単発より強い）
    移動（セロ/バサ/ヨミ） 17行   +5.9pt  ← HasFallenBack と Slot が持ち越される（単発より強い）
    **どれも無い**      **38行**  **−0.8pt**（最大 |差| 6.6pt ＝ 200 seed の 2SE の内側）
    **ヴェル**          **6行** **−36.9pt** ← 継ぎ接ぎの charges / sewn / **MaxHp 半減が会戦スコープ**

**駆動因を1つも持たない 38 行では `RFull` が単発の連勝率を厳密に再現する**
——**ノブも器具も間違っていない**ことの証拠であり、
**指示書 §4-3 が測る前に列挙した 5 つの持ち越しがそのまま出た**（第94期「既知の値を再現できて初めて器具」の裏返し）。
`刻み×断ち (ノミ×ナタ)` は 単発 95.0% に対し RFull **34.0%**（ヴェルの `MaxHp` が 46 → 11 に縮んでいる）、
`駆り立て改 (カリ×死軸)` は 単発 27.0% に対し RFull **98.5%**（リィカの層）。

**境界の回復に `ctx.Heal` を通さない**（第101期の設計判断）。渇き（第三波の波ルール）も
`AcceptsSupport`（支援拒否）も**「戦闘中に味方が配るもの」に対する規則**であって、
**境界の手当ては誰かが配っているのではない**（`Hp` を直接足す。理由はコード側にも書いた）。
**敵側には一切適用しない**——敵は 1 部隊を抜くたび `Materialize` で新品が投入されるので元から全快。
**`ReviveDead` は `current`（その戦闘へ投入した駒）だけを見るので、召喚された駒は復帰しない。**

### 第102期 — `choice`

**選択は成立した。帯には届かなかった —— 境界の3つの罰から1つだけ免除する**
（第102期・調査。**触ったのは会戦の境界の規則 1 つ（`BoundaryRule`）だけ**。駒・特性・数値・戦闘中の判定・
`Presets` / `CompareBuilds()` / `CrossBuilds()` / `Stages` / `Columns` は 1 行も触っていない・
`compare` 305 セル 0 件・`docs/` 10ファイルの差分は **`rules.md` だけ**〈ノブと列挙型が増えたので正しい〉・
`audit` ずれ 0 件・`derive scan` 欠落 0 / 過剰 0・`ctx.PickOne` の追加 0）。
**既定は `BoundaryChoice.None` のまま。採否は決めていない。** 経緯は design/PHASE102_CHOICE.md。

    方策          突破率   中央値  0%の行  抜けた行  第2波クリア率
    現行 None     0.97%    0.0%     72       1        33.8%
    常に蘇生      2.94%    0.0%     61      12        40.5%
    常に回復      5.81%    0.0%     46      27        57.5%   ← 第101期 R100 と完全一致
    常に持ち越し  3.58%    0.0%     63      10        48.5%
    **最良（上限）** **11.59%** 0.5%  **34**  **39**   **68.5%**

**主判定 Q1（最良だった選択肢の分布が1つに 60% 以上偏らない）は通った**（**49.3%**）。
**Q2（帯）は落ちた**（中央値 0.5%・線 5〜50%）。**Q3（選ばせる価値）は +5.78pt で通る**（固定最良の 2.0 倍）。

**偏りが割れる軸は2本ある。合計で通ったのは打ち消しではなく、境界ごとに違う答えが出たから**（第102期）。

    第1戦の後  回復 **75.3%** / 蘇生 17.1% / 持ち越し 7.6%   ← 死者0体が **82%**（選択肢2つ）
    第2戦の後  蘇生 **85.8%** / 回復 11.2% / 持ち越し 3.0%   ← 死者ありが **94%**
    合計       回復 49.3% / 蘇生 45.1% / 持ち越し 5.7%

**境界ごとに見ればどちらも線を超えている。**「選択肢が何個あるか」自体が境界の位置で変わる
——第2波で 4.75 → 2.22 体まで落ちるので、**第1境界には返す体がほとんど無い。**
**編成でも割れる**——固定3方策のうち唯一の最良は **蘇生 7 行 / 回復 21 行 / 持ち越し 6 行**
（1つでも突破率 > 0 の 35 行が分母・同点1）。`後衛特化+後備え` は回復 91.5% で蘇生・持ち越しが **0.0%**、
`死の連鎖 (リィカ軸)` は蘇生 32.0% で回復が **0.0%**、`縛め収入型` は持ち越し 42.0%。

**「列全体で体が効く」と「この境界で体が返せる」は別の量**（第102期。予測を2つ外した原因）。
第101期の「帯までの距離の 91% を死者の側が持っている」を境界ごとの話に持ち込んで
「最良は蘇生に偏る」と予測したが、実測は**第1境界では蘇生 17.1%**。
**Q1 を通したのは「第1波の後は回復が最良」という副次の予測1本だけ**だった。

**「負も一緒に持ち越せば可変コストになる」は成り立たなかった**（第102期）。
`Carry` が現行より**悪くなった試行は 1.88%、良くなった試行は 18.68%** で**10 倍近く得に偏る。**
`Carry` が選ばれない（5.7%）のは災厄だからではなく**回復・蘇生に負けるから**である。
**しかも拾われるのは育つ駒を含む行ではない**——含む行 5.7% 対 **含まない行 9.3%（予測と逆）**。
味方に載る状態異常は 4 種＋巻き込み則の傷しか無く、**盤面に幅が無い**（第49期）のがそのまま出た。

**Q2 は境界のノブでは通らない**（第102期の申し送り）。`RFull`（第101期の上限）で中央値 53.0%、
この期の「最良」で 0.5%。**帯に入れるには難度の側（敵の攻撃力か味方の枠数）を触るしかない**
——第70期「敵のHPを下げる操作は T_kill しか縮めない」がそのまま当たる。

**`UnitState.LastDeathTurn` を足した**（第102期。`HandleDeath` の1行）。
**純粋な記録で、誰も読んで分岐しない**——境界の蘇生が「最後に倒れた駒」を選ぶためだけにある。
`BattleResult.Events` からは取れない（診断は verbose=false で数万戦回す）。

**会戦の持ち越しの代金は HP ではなく体だった —— 境界に回復を置いて掃引した**
（第101期・調査。**触ったのは会戦の境界の規則 1 つ（`RecoverRule`）だけ**。駒・特性・数値・engine の判定・
`Presets` / `CompareBuilds()` / `CrossBuilds()` / `Stages` / `Columns` は 1 行も触っていない・
`compare` 305 セル 0 件・`docs/` 10ファイルの差分は `rules.md` の +1 行〈ノブが増えたので正しい〉と
`crossing.md` の所要時間 1 行〈既知〉だけ・`audit` ずれ 0 件・`derive scan` 欠落 0 / 過剰 0・
`ctx.PickOne` 8 箇所で不変）。**既定は R0 のまま。採否は決めていない。**
経緯は design/PHASE101_RECOVER.md。

    版      HpPercent/ReviveDead   突破率    中央値   0%の行   第2波クリア率   第3波の入場戦力
    R0      0   / ×                0.97%    0.0%     72       33.8%          2.22体 17%（到達 34%）
    R100    100 / ×                5.81%    0.0%     46       57.5%          2.55体 50%（到達 58%）
    RFull   100 / ○               52.06%   53.0%     13       66.7%          5.00体 97%（到達 67%）

**生き残り全員を毎境界まるごと全快にしても、突破率は 0.97 → 5.81% にしかならない**
——**測れる帯までの距離の 91% を死者の側が持っている。**
**`HpPercent` を 0 → 100 に振っても第2波の入場は `4.75体` から 1 ビットも動かない**
（動くのは HP の割合 72 → 95% だけ）。**教習波である第一波で 0.25 体が落ちていて、HP では戻らない。**

### 第103期 — `betray`

**「盤の形が天井を決める」は成立した。決めた天井が 1 だっただけだった**（第103期・Phase 0 の門）。
指示書の擬似コードをそのまま写すと**餌は 1戦に1体しか湧かない**——
**`BattleContext.Summon` の空き判定は生死を問わない**（死者の枠を空きと見なすと蘇生と衝突するため）ので、
**餌の死体が ○前2 を永久に塞ぐ**（塞 3.47 回/戦）。`PlusText` の「毎ターン」が嘘になるので、
**餌にだけ「死者は席を塞がない」を許した**（`BetrayRule.Respawn`。餌は `Ephemeral` で蘇生されないので
元の規則が避けている衝突は起きず、**席を読む箇所は全部 `LivingMembers` で濾している**）。
**湧きは 1.00 → 3.37 回/戦。** `Respawn = false` は対照として残置。
**次に召喚枠へ湧かせる機構を作るときは、湧いた駒の死体がその枠を塞ぎ続けることを実装前に数えること。**

**餌の代金は「敵が1体増えること」ではなく「手番を1つ捨てること」だった**（第103期）。
**ロスター 52 枚のうち、自分の手番で `PerformAttack` を1度も通らない駒は 5 枚しかない**
——ハギ（`CanAct => false`）／カド（`Immobile`）／ヒヨ・ノノ・ミオ（`Actions = [Skill]`）。
**その5枚の Δ相乗の順位は 1 / 2 / 3 / 13 / 16 で全部が正**（一様なら平均順位 26 のところ **7.0**）。

> **手番を持たない駒は代金の払いようが無いので、増えた敵の体（＝伸びた決着ターン）だけを受け取る。**

**逆に「ついで」の読み手として見込んだ薙ぎ持ちは、読み手ではなく最大の支払者だった**
——餌の撃破の内訳は **ボルグ 24.2%（Δ −1.49・32位）／ ドルガ 22.3%（−6.08・46位）／
ソム自身 19.5%** で、**この2枚だけで 46.5%**。
**餌 HP 12 は巻き込み（ボルグ 60% ＝ 10）では落ちない**ので、
**倒すには主目標にするしかない**——「ついでに倒す」が構造的に成立しない設計が、そのとおり働いた。
**ハギの撃破は全体の 3.2% しかないのに Δ相乗は1位。餌を倒す駒と餌で得をする駒は別である。**

**供給者は自分の餌を食べる**（第103期。第96期「ムドは読まないのに最大の受益者」の符号違い）。
ソムは餌を1つも読まないのに**餌の撃破の 19.5% を自分で持っている**——
**単体で殴る駒である以上、自分の置いた餌が自分の標的候補に入る。**
**攻撃型と成長経路を確認しても防げない**（ソムは単体・攻7・育たないので受益者の輪郭には入らなかった）。
**次に「盤面に物を置く」機構を作るときは、置いた物が置いた駒の標的候補に入るかを先に見ること。**

**「単体高倍率は下がる」は正しいが、理由は単体だからではない**（第103期・Q3）。
損の側5枚の実測は **ザン +5.59（4位）／ キリ +5.22（5位）／ ナタ −0.02 ／ ノミ −0.98 ／ トメ −1.95**。
**上がった2枚は「自分の手番を餌に使わない駒」**——ザンはターン外に振る（`CanActOutOfTurn`）、
キリは薄刃で打点が常に 1 ＝ **捨てる手番の値段がロスター最小**。
**傷の書き手が下がるのも、傷が無駄になるからではなく手番が高いから**
（打点 1 のキリが上がり、打点 12 のノミが下がる）。
**「手番を買う駒」は「手番を持つ駒」なので、代金を先に払う**——
号令のガンは 49 位（−8.23）で、指示書の「エグの失った手番をガン・バンが買い取る」を最も強く否定した。

**単発で払える代金でも、持ち越す台では払えない**（第103期・Q4）。
会戦（地点3波）ではソムを含む5行とも突破率 0.00% のまま**突破波数が 1.80 → 1.57 / 1.97 → 1.51 と下がり、
全滅勝ちが 30.0 → 50.9% / 3.6 → 10.7% と上がる**（勝ち方が悪くなる）。
**境界は HP を1点も返さない**（第101期）ので、**餌に使った手番のぶんだけ削られた体で次の波へ入る。**

**紙の「分母を削るか」は、増える側に倒れるとも限らない**（第103期・規約 (G7) の3点目を外した）。
「餌は敵の体を1つ増やすので決着ターンは伸びる ＝ 紙は下限」と先に書いたが、
**20 セルのうち伸びたのは 7・縮んだのは 7・同値が 6。**
`ソム×追い打ち` の第4波は **10.1 → 8.7T と縮み、そこが勝率も 81.0 → 96.0% と最も伸びた波**
——**追い打ちの連鎖が敵の本体まで届く行では、餌が決着を早める。**

**供給側の稼働率は「読み手の 35〜40%」の帯を上回る**（第103期）。
湧き ÷ 決着T は 20 セルで **31〜78%**——供給が `OnTurnStart` にあり、
条件が「席が空いているか」1つしか無いため（第86期の読み手側の帯とは律速が違う）。

### 第104期 — `encore`

**13期かけて供給も単価も動かなかった傷軸が、精算を二値の鍵にした瞬間に動いた**（第104期）。
`刻み (ノミ単騎)` 75.9 → **97.9** ／ `刻み×抉り` 66.0 → **86.5** ／ `止め` 34.5 → **47.0** ／
`逸らし改` 74.1 → **84.5** ／ `刻み×断ち` 75.2 → **81.9** ／ `刻み×縫い` 69.4 → **74.9**（第2〜5波平均）。
**第74期は傷軸の代金の最大の1枚が「読めない間ずっと突っ立っていること」＝手番だと突き止めた。
この期の払い出しはその裏返しで、同じ通貨を返している。**
**第103期（代金は手番で払われる）と合わせて、手番は3期連続で主題に出ている。**

**効くほど測れなくなる**（第104期）。**同じ機構・同じ意図した相手（ドルガ）で、
Δ相乗は キリ +1.59 / ノミ +5.25、ノイズ床は 1.57 / 7.42。**
ノミの表は上位5枚が +3.7〜+5.3 に密集していて、1位でも「他の 46 体より大きく動いた」とは言えない
——第96期の「特異性の物差しは、大きい機構ほど自動的に厳しくなる」が、
**同じ機構を供給者違いで2回測ったときにそのまま出た。**
**床の高さは機構の大きさの関数なので、A を変えると同じ機構が通ったり落ちたりする**
——**A を複数持つ機構では Q1-1 を「A のどちらかで」と書くこと。**

**規約 (G5) の「供給の過半を持つ駒」は「その機構が実際に使う入力の供給者」で読む**（第104期）。
傷の供給の **92.3% は engine の巻き込み則**だが、**巻き込み則は味方にしか傷を書かない**
（`isFriendlyFire` かつ `source` が同陣営）ので、**その 18,172 の傷は1つも再行動を生まない**
（刻み手と死者が同じ陣営になる）。**A をキリ・ノミに固定したのが正しい。**
第93期は供給の 95% を持つ engine の規則を無視して A を選んで落ちたが、
**「過半を持つのは誰か」ではなく「機構の入力になるのは誰の供給か」を数えること。**

    縫いのハリ     A=ノミで **−6.32（48位）**   毎ターン味方の傷を1つ塞ぐだけ。敵を1体も倒さない  ← 予測どおり
    継ぎ当てのノノ A=ノミで −1.66              同上                                            ← 予測どおり
    **断ちのナタ   両 A とも正（+3.54 / +2.86 ＝ キリの表の1位）**                              ← **逆**

**`SeverTrait` は `ApplyDamage` を先に走らせてから `SetCounter(Wound, 0)` する**ので、
**溜めた傷ぶんの一撃が敵を倒せば、記録が消える前に死が来る。**
**第73期の「在庫を食う量と傾きへの効きは相関しない」は、払い出しを手番に変えると崩れる**
——ハリの塞ぎはこの期に初めて大きな負として立った。

**打点が餌の体を上回る駒は、刻む前に倒す**（第104期・Q5。**第103期のソムとの噛み合いは両側から否定された**）。
ノミ（打点 10〜21）は餌（HP 12）を1振りで抜くので `Wound` が `!target.IsAlive` で弾かれ、
**「刻まれたまま倒れた餌」が 0.00 回/戦**。しかも再行動そのものが対照の半分以下に落ちる
（1.53 対 **3.27**）——ソムが枠を1つ食い、執着が餌に食いつく。
**キリでは経路が成立する**（再行動の 75% が餌由来）**のに総量が対照と変わらない**（1.26 対 1.18）
——**餌は再行動の「相手」をすげ替えただけで、量を1つも増やしていない。**

> **踏み台が成立するのは「1振りでは倒せず、かつ倒すのに手番を使わない」駒だけで、そんな駒はロスターにいない。**

**同じ通貨を2枚に持たせても、同席させる理由は生まれなかった**（第104期）。
A ＝ キリの表の**最下位が刻みのノミ（−1.64）**——同じ敵に両方の記録が載るが、
片方の再行動がその敵を倒せばもう片方の機会は次へ移り、執着で目標が固定されたノミは受け取れない。
**「刻み手が複数なら全員が再行動する」は実装したが、編成の判断を1つも生んでいない。**

**手番の中身を切り出すのは、挙動を1つも変えずにできる**（第104期 §1-4）。
`BattleEngine.Run` の行動順ループにインラインで書かれていた
痺れ／まどろみ／`CurrentAction`／`CanAct`／`PerformAttack`／`Charge`／`OnAction` の分岐は
`BattleContext.TakeTurn` に丸ごと移る（`continue` → `return`、ループ変数 `turn` → `Turn`）。
**ループ側に残す番人は2つだけ**——`!actor.IsAlive` の `continue` と、相手チーム全滅の `break`
（後者は**ループを抜ける**判断なので手番の中身ではない）。
**切り出しだけを先に単独コミットし、`compare` 305 セル 0 件を示してから機構を足すこと**
（第90期の毒の窓口・第93期の傷の窓口と同じ作法）。

**餌を敵陣に置いた —— 背かれのソムを採用し、ロスターが 52 枚で確定した**
（第103期。**新しい `TraitId` 1（`Betrayed`）・新しい `UnitDef` 2（ソム・餌）・
engine の窓口 0**〈`Summon` に席を指定する引数を足しただけ〉・
`CompareBuilds()` / `CrossBuilds()` / `Stages` / `Columns` は 1 行も触っていない・
`compare` 305 セル 0 件・交差帯 12 行 0 件・`audit` ずれ 0 件・`derive scan` 欠落 0 / 過剰 0・
`ctx.PickOne` 8 箇所で不変・`docs/` 10ファイルの差分は **`units.md` の 2 行**と
**`rules.md` の 12 行**〈`BetrayRule` の 2 行 ＋ `期` の列の増分〉**だけ**）。
`BetrayRule.Default` = `(Enabled: true, Respawn: true)`。経緯は design/PHASE103_BETRAY.md。

    主判定 Q1-1  **51 体中1位が意図した相手の追い打ちのハギ**（Δ相乗 **+21.95pt**・2系列とも正）  ○
    ノイズ床     **10.26pt**（過去2番目。第96期 11.87 / 第89〜95期 0.37〜0.77）
    Q1-2         **3 体**（カド +12.06 / ムド −10.30 / ネル −11.43）＝**線ちょうど**             ○
    Q2           **1 位 / 51**                                                                  ○
    拒否権1・2   **ソムが 61 行にも交差帯 12 行にも 0 体**なので原理的に立たない（V0 対 V1 が 365 セル 0 件）

**採用しても盤面は1セルも動かない**——ソムはどの代表編成にも入っていない。
第90期の傷の引き取り（`GatherRule`）と同じ形で、**読み手が増えれば後から動き始める。**

### 第105期 — `tempo`

**手番は与ダメージの通貨であって、盤面を書き換える通貨ではない（第105期・調査）**
（**新機構ゼロ・駒ゼロ・`TraitId` ゼロ・engine の判定ゼロ**・足したのは観測専用の計数 30 本と
`TakeTurn` に被せた枠だけ・`compare` 305 セル 0 件・`docs/` は `rules.md` 以外の9ファイルが差分 0 バイト・
`audit` ずれ 0 件・`derive scan` 欠落 0 / 過剰 0・`ctx.PickOne` 26 箇所で不変）。
73行（`compare` 61 ＋ 交差帯 12）× 5波 × seed 0..199 = 73,000 戦・**12.9 秒**。
診断は **`tempo`**（`turn` は第60期で埋まっている）。経緯は design/PHASE105_TURN.md。

    通貨            手番の中   手番の外   誰のものでもない
    与ダメージ       **75.3%**   18.5%      6.2%（毒・燃焼の刻み＝`source` が null）
    回復              9.7%      **90.3%**   0.0%
    状態異常（回数）  17.6%      **76.0%**   6.4%

**回復の 90.3%・状態異常の 76.0% は手番を1つも使わずに生まれている。**
**第103期の代金（手番を1つ捨てる）も第104期の払い出し（手番をもう一度渡す）も、
与ダメージの側にしか届かない**——回復役・状態異常役に再行動を配っても、
彼らの出力の 8 割は手番の外にあるので増えない。

**「手番の中」の定義は `BattleContext.InOwnTurn` の1箇所（3つの積）。**
(1) `TakeTurn` の枠の中 (2) **出どころがその枠の主** (3) `InReaction` / `InInterrupt` の外。
**(2) が本体**——棘の反撃は殴った側の枠の中で走るが出どころが違うので外に落ち、
責め苦の追撃（`OnAfterAttack`）は自分の手番の中で自分が出どころなので中に残る。
**`OnTurnStart` は行動順ループの外側なので外**（第61期「speed = ∞ の席」をそのまま境界にした）。

**駒の3分（手番外の割合 50% で切る）: 手番外型 4 / 混合 16 / 手番型 30 / 出力なし 1。**
**混合 16 枚は全部が「与ダメは手番の中・それ以外は手番の外」で、逆向きは 0 枚。**

    手番外型  追い打ちのハギ / 棘鎧のカド / 爆ぜるゾト / 仇討ちのザン
    出力なし  火選りのヒヨ（**器具の欠落**。強化・弱体を数えていない）

**カドはロスターで最大の手番外の出力を持ち、手番の中では1点も出さない**
（外ダメ 294.0/戦 対 内ダメ 0.0/手番）。**毎ターン術を撃っているのに、その術は出力を1つも動かさない。**
第52期・第63期の「カドは振らない駒だが死蔵の捨て場ではない」を出力の側から言い直すと
**「カドは手番を持っていて、その手番を出力に使っていない」。**

**不動（`Immobile`）は手番を潰さない。** `ImmobileTrait.CanAct` が拒むのは **`ActionKind.Attack` だけ**で、
**カドは `Actions = [Skill]` を持っている**ので問われる種別が `Skill` になり、不動は一度も拒まない
——実測でカドの潰れ 4,480 / 手番 26,868 は**全部が痺れ**、不動は **0 回**。
**「振らない駒」と「手番が潰れる駒」は別**（第105期・手作りのずれの9例目。
指示書の Q1-4 はこれで落ちた）。**手番が全部潰れる駒はロスターに追い打ちのハギ 1 枚だけ。**

**稼働率は「発火 ÷ 決着T」と「手番 ÷ 決着T」で別の量になる**（第105期）。
第86期の 35〜40% 帯は前者で、後者はロスターの**中央 50.7%・最小 24.1%・最大 89.3%**
（35〜40% に入るのは 4 / 50 枚）。**最大は刻みのノミで、再行動 2.11 回/戦 がそのまま上積みになっている。**
**最小の3枚（ムグ 0.24 / ゾト 0.25 / ヒビ 0.26）はどれも「早く落ちる」駒**
——第47期のウロ（前列だと早く落ちて振れない）と同じ形が、駒を単位に測ると分布の下端に出る。

**手番の内訳（第2〜5波・延べ 990,337）: 通常攻撃 84.3% / 術 7.2% / 溜め 0.0% / 潰れた 8.5%。**
**潰れた手番の 81.9% は `CanAct` 偽**（大半がのろまのドルガ 2.09 回/戦）で、
**その 73.1% は号令・据えに売れている**（`Trait.SurrenderedTurn`）。
**ハギは 3.25 回/戦 が全部潰れるのに1回も売れない**（`SurrendersTurn => false`）
——「潰れた手番」と「売れた手番」の差はほぼこの2枚の差である。

**通貨を3本で切ると、強化・弱体だけを書く駒が「出力なし」に落ちる**（第105期の器具の限界）。
火選りのヒヨは与ダメ 0・回復 0・状態異常 0 なので分類できず、Q3 は 4/5 で止まった。
**4本目（強化・弱体）は第56期 `Whet` と第42期 `Dull` の窓口にそのまま足せる**
——どちらも `Mark.Owner` が立った状態で呼ばれるので、帰属の器具は変えなくてよい。
**この器具を次に使うときは、先に通貨を4本にすること。**

**指示書 §6 の規約 (G14)（機構が課税／払い出す相手を手番型か手番外型かで先に見る）は
`CLAUDE.md` に書いていない**——条件は「Q1〜Q3 が揃ったとき」で、**Q1 は 3/5・Q3 は 4/5** だった。
**ただし中身のうち2つは実測がむしろ強めている**（標的プールを触る機構は第103期の順位の 4/5 を説明し、
再行動は与ダメの 75.3% に届く）。**3つ目（被弾・撃破・死亡で発火する機構）はこの期では測っていない。**

**`docs/rules.md` は `design/PHASE*.md` の本文からも引く**（第105期に判明）。
第94期は「`測った診断` の列は `Program.cs` の本文から引く」とだけ書いていたが、
**`期` の列は `design/` の報告書・指示書を全文検索している**ので、
**その期の文書が型名に触れるだけで既定を1つも変えていないノブの行が動く。**
**既定値の列は reflection なので動かない**——差分がそこに出ていなければノブは触っていない。

**傷軸は「手番」で動いた —— 刻んだ獲物が倒れたら、刻み手がもう一度動く（第104期・採用）**
（**新しい `TraitId` ゼロ・新しい駒ゼロ**・engine の窓口は `HandleDeath` の末尾 1 箇所・
`CompareBuilds()` / `CrossBuilds()` / `Stages` / `Columns` / `UnitCatalog.All` の 52 体は 1 行も触っていない・
`audit` ずれ 0 件・`derive scan` 欠落 0 / 過剰 0・`ctx.PickOne` の追加 0・
`docs/` 10ファイルの差分は `balance.md` 20 セル / 7 行 ＋ `units.md` 2 行 ＋ `rules.md` の `EncoreRule` の行と、
その 7 行の下流）。`EncoreRule.Default` = `new(true)`。経緯は design/PHASE104_ENCORE.md。

    主判定 Q1-1  **A ＝ キリでだけ通った**（ドルガ 2位・Δ相乗 **+1.59pt**・床 1.57・2系列とも正）  ○
                 A ＝ ノミは 1位（+5.25）だが**床が 7.42 に上がって届かない**                      ×
    Q1-2 / Q2    **3 体 / 3 体**（線ちょうど）／ **2 位 / 1 位**                                  ○ / ○
    拒否権1・2   −10.0pt 以上落ちた行 **0 件** ／ 主判定19行の第五波 38.0 → **40.9%**（歯止め 33.2）  ○ / ○
    **Q2（本題）** `CompareBuilds()` の傷軸 **8 行中 6 行が動いた**

### 第106期 — `hold`

**「保留」の理由が実測で1つも成り立たなかった駒がある**（第106期・ムド）。指示書が挙げた
「育つのに42点必要／到達点は攻24／育つ前に落ちる」は3つとも誤りで、実測は
**発火 7.03 回/戦・1発あたり 4.52・到達点 攻36・+24 到達が 84.8%・到達 2.69T**（決着は 6〜7T）。
**ムドは育つ。攻36 まで、3ターン以内に。**
指示書の `Count` の +3 はその誤った前提から引かれた較正なので、
**天井を 33 → 22.4 に切って平均 −1.52pt**（到達ターンは 3.31 → 3.30 と1つも早くならない）。
**Phase 0 から引き直した +5 の対照は +2.53pt・拒否権 0 行**だが、
**「数値の掃引はしない」に従って採らなかった**——採るなら較正を先に固定した指示書で1期使う。

> **駒を「保留」にする前に、保留の根拠を1度は走らせて確かめること。**
> 第106期の外れた予測3つは**すべて「指示書が引いた過去の数値を確かめずに使った」**ことから来ている。

**「量で育つ」と「回数で育つ」の差は、1発が大きいと出ない**（第106期）。
`max(1, dmg / 2)` は**1発目から育つ**ので、回数型にしても到達ターンは 3.31 → 3.30 しか動かない。
**速度を変えたいなら、変えるのは1発あたりの量ではなく「何に反応するか」のほう。**

**等価交換の代金は HP ではなく寿命だった**（第106期・ノノ）。繕いの引く量を半分にすると、
**癒す量は +14 のまま**（1戦監査で両版が同じ行）なのに
**生存Tが 3.62 → 4.33・落率が 85.3% → 68.3%・繕いの回数が 2.72 → 3.28 回/戦**。
**増えたのは「1回の癒し」ではなく「癒せる回数」**——自分を削って癒す駒では、
**自分の HP が自分の手番数の関数**になっている。

**移動軸の読み手なら誰でもいいわけではない**（第106期・ササ）。被弾のたびに隣の味方を弾く版を
移動の読み手4枚と同席させたローカル台の実測は **ヨミ +78.9 / シオ +4.5 / バサ −1.4 / ハネ −0.8pt**。

    軋み（ヨミ）  動かされるたび攻撃力が上がり、**その場で割り込む** → 出来事1つが出力1回に直結  +78.9
    喧噪（バサ）  **自分で毎ターン席を混ぜる**ので供給が既に飽和（移動 11.25 回/戦）        −1.4
    突き返し（ハネ）**敵陣しか動かさない**ので味方側の移動を読む口が無い                     −0.8
    怯み（シオ）  動かされた味方を回復＋強化。読むが、出力への変換が1段遠い                  +4.5

**「移動を読む」の中身は、量を読むのか出来事を読むのかで値段が変わる。**

**新しい機構を1行しか持たない駒に足すと、その1行が代金を全部払う**（第106期）。
`CompareBuilds()` でササを含む唯一の行 `置き去り×分散回復` は **27.3 → 12.8**（第2波 −54.0）。
(G2) の分解では他の4枚の「他の行」が全部 ±0.00pt なので**組み合わせ固有＝制約**だが、
**ササ自身は「他の行」が 0 行なので、この分解はササについては成立しない。**
**第45期「新しい隣接機構は最初から2行以上に入れること」は、被弾クロックの機構にもそのまま当たる。**
**採用後の `reseat` でこの行の現行席は 13 位（5波平均 30.2%）に落ちた**——1位 38.7%（+8.5pt）／狙（ガルド前列）を満たす最上位は 5 位 35.4%（+5.6pt）で、**第46期の作法では追試にかける行。第106期では動かしていない**（**第107期 (S1) で差し替えた**）。

**4つ目の通貨（強化・弱体）を足した。観測点は `AtkBonus` の setter 1 箇所**（第106期 (T1)）。
**窓口（`Whet` / `Dull`）ではなく setter に置く**——窓口だけを見ると**自己強化の9本**が
まるごと落ちる。帰属は第94期 (T2) の印で、**符号を問わず絶対量**を数える。
**境界と蘇生の一括消去（`ResetAtkBonus`）は setter を通らないので入らない**（第68期の判断）。
**逆しま（`Perverse`）は `ModifyAttack` なので、この通貨には1点も現れない。**
`AtkBonus` を動かす経路は**観測で 19 本**（印なしを除く）で、**墓守 1 枚が全体の3分の1**を占め、
**両方向に動くのは `Necro` と `Favor` の 2 本だけ。**

    通貨            手番の中   手番の外   誰のものでもない
    与ダメージ       75.3%      18.5%      6.2%
    回復              9.7%      90.3%      0.0%
    状態異常（回数）  17.6%      76.0%      6.4%
    **強化・弱体（量） 5.4%      93.6%      1.0%**   ← 4本目。**回復よりさらに手番から遠い**

**第105期 Q3 は4通貨にしても 4/5 のまま。(G14) は書かない**（第106期 Q0）。
**器具の欠落ではなく、期待のほうが間違っていた**——ヒヨは**手番型**に落ちる。
第60期に火選りを `OnTurnStart` から `OnAction`（手番）へ降ろしたので、出力が全部自分の手番の中にある。
**ヒヨが第103期に 3 位だったのは「手番で殴らない」からであって「手番を使わない」からではない**
——**`Actions = [Skill]` の駒は手番を持ち、手番を使い、ただし `PerformAttack` を通らない。**
分類の分布（4通貨・在席 51 枚）は **手番外型 4 / 混合 26 / 手番型 21 / 出力なし 0。**

**傷軸の払い出しは書き手にしか届かない**（第106期 (T3)・ハリの材料）。
縫いのハリは **手番 4.03 回/戦・稼働率 65.7%・潰れる手番が1回も無い・4通貨とも手番外 0.0%**
（ロスターで最も「手番だけで出来ている」駒の1枚）なのに、
**第104期の再行動は 0.00 回/戦 しか届いていない**——再行動は「傷を刻んだ駒」に渡る機構で、
**ハリは傷を読んで塞ぐ側**だから。**去就はこの期では決めていない。**
なお第85期の律速（振り 2.15 回/戦）は **4.03 回/戦（1.87 倍）**まで動いている。

**散開は「盤面に何も書かない 21 種」から外れて 20 種になった**（第106期）。
**指示書の「第96期の分類 A」は誤り**——第96期は `Loose` を A に入れていない
（条件「隣に味方がいない」が味方の死で戦闘中に成立するため）。A は 5 種 / 4 駒のまま。
散開が移動の供給者になったので `TraitKeyMap` / `TraitEntryMap.Supplies` に1行ずつ足した
（`derive scan` の欠落を 0 件に戻すため）。**キーを1つも持たない駒は 8 → 7 / 52。**
**第78期の入口・発火口と第80〜83期の独立の広さはこの1行で動く。数え直していない。**

### 第107期 — `hold2`

**機構を採用した期に席を測り直しても、席の値段が動くのは「その機構が効き始めた後」である**（第107期 (S1)）。
差し替えた3行はどれも**その行の機構が変わる前の盤面のまま席が残っていた行**だった:

    刻み×断ち   ナタが 前3 → **後3**・中央が ドルガ → ヴェル。第五波 32.5 → **88.0%**
                **第74期に閾値待ちをやめて毎ターン振るようになった時点で、
                第38期の「振らないナタをどこに置くか」の答えは寿命が尽きていた**
    刻み×縫い   ノミが中央（次数4）へ。第104期の再行動で「ノミが長く立つこと」の値段が上がった
    置き去り     第106期の散開の弾きで 13 位に落ちた。**中央はセロのままで次数も動かない**

**作法1（現行が上位5通りなら動かさない）は本来そういう挙動をする**ので、
**数期おきに 61 行を追試し直すこと。** 第107期の内訳は 作法1 で 42 行 / 差 5.0pt 未満で 13 行 / 追試 6 行。

**`reseat` の追試で採る席は3つの条件の積**（第107期に狙を器具へ入れた）——
**情報セル 2 以上**（第59期）／**狙（ガルドが前列・セッキが後列）**（CONTRIBUTING.md）／**差 5.0pt 以上**（第46期）。
**狙を器具に入れ忘れた初版は `置き去り×分散回復` の「ガルドを中央へ上げる」1位（37.6%）を採るところだった**
——**規則は文書に書いてあったのに、器具に写していなかった。**
**参考として「狙を外した最上位」も毎行並べる**（緩めた版ではなく、外した版がどれだけ上にあるかを読むため）。

**保持者が2枚以上いる `TraitId` に「育ち方」のノブを付けるときは、較正の前に保持者を数える**（第107期 (S2)）。
憤怒（`TraitId.Rage`）はムドと**後備えのセッキ**の2枚が持つのに、較正はムド1枚でしかできない:

    ムド    被弾 6.99 回/戦  1発あたり 4.52 → 5.00（1.11 倍）  到達点 32.9 → 36.1（1.10 倍）
    セッキ  被弾 9.43 回/戦  1発あたり **2.46** → 5.00（2.03 倍）到達点 24.9 → **45.5**（1.83 倍）

セッキは肩代わりで**小さい被弾を数多く**受けるので、**同じ「1発あたり +5」が 1.1 倍と 2.0 倍に化ける**
（`逆しま+後備え` は 76.6 → **100.0%**）。**「総量を変えず速度だけを変える」は、保持者が2枚いる時点で
1本のノブでは成立しない。** Q1（+0.75pt）と Q3（到達T 2.68 → 2.45）は通り、拒否権も 0 件だった。
**ムドは保留から外れる**——第106期に「育つ前に落ちる」が、この期に「速度だけを変える」が否定された。
**現行の形（被弾で育つ・攻3 → 36・+24 到達 84.0% / 2.68T）が正しかった。**

**較正は別 seed 帯（200..599）で再現した**——発火 7.03 → **6.99**／1発 4.52 → **4.52**／
到達点 33.0 → **32.9**／+24 到達 84.8 → **84.0%**／到達T 2.69 → **2.68**。
**器具の直し**: 駒ごとの帳簿が第一波を含んでいた（規約 (G10) 違反）ので発火が **11% 下振れ**し、
**そのままだと「較正が再現しない」と読み違えるところだった。**

**同じ動作の宛先が陣営をまたぐと符号が反転する**（第107期 (S3)）。縫いの発火口を
`OnAfterAttack` から **`BattleContext.Wound` が実際に傷を書いたとき**へ移すと、発火は増える
（3.06 → **3.44 回/戦**・稼働率 55.4 → 63.6%）のに、**敵から糸を引く回数が 0.53 → 0.00 になる:**

    傷の書き込みは 6 経路 8.93 回/戦。**巻き込み則（engine）が 8.08 ＝ 90.5% で、全部が味方側**
    → 1ターン1回の枠を味方の傷が先に埋める → **敵の傷を塞ぐ機会が消える**

**`敵から` はハリの `MinusText`「繕うたび、糸を通した敵の傷がひとつ塞がる」が実際に起きる回数**で、
**同じ塞ぎが味方の傷に当たると利得に反転する。** `刻み×縫い` の +3.3pt の正体は
「発火が増えたこと」ではなく**「第74期に切り出した代金（`TraitId.Seal`）が消えたこと」**で、
**繕い量そのものは 23.1 → 20.1 と減っている。採るなら `MinusText` が嘘になる。**
`SutureFireRule` は既定 `Swing`（現行）のまま**対照として残置**。

**engine が特性を直に呼ぶときは第94期 (T2) の印（`BeginTrait`）を立ててから呼ぶ**（第107期）。
立てないと `ctx.Heal` の帰属が「誰のものでもない出力」に落ちて、
**第105期の器具でその駒の回復がまるごと消える**（Q6 が測れなくなった）。
`CanReactProbed` / `CanActProbed` と同じ形で包む。

**ハリの去就はこの期でも決めていない。材料は揃った**——律速は3期とも「手番が来ない」で正しく、
外へ出せば回数は増える（+12%）。**問題は増えた回数の行き先のほうだった。**
**第108期に決めた**——ロスターから外した（実装は残置）。

**保留の4枚を決めた —— ノノとササは採り、ムドは「保留の理由そのもの」が実測で消えた**
（第106期。**新しい `TraitId` ゼロ・新しい駒ゼロ・engine の判定ゼロ**・
足したのはノブ3本（`RageRule` / `MenderCostRule` / `LooseRule`）と4つ目の通貨の観測・
`CompareBuilds()` / `CrossBuilds()` / `Stages` / `UnitCatalog.All` の 52 体は1行も触っていない・
`audit` ずれ 0 件・`derive scan` 欠落 0 / 過剰 0・`ctx.PickOne` の実呼び出しは不変・
`docs/` の差分は `balance.md` **23 セル / 9 行** ＋ `units.md` 2 行 ＋ `rules.md` のノブ3本と、その下流）。
経緯は design/PHASE106_HOLD.md。

    ノノ  MenderCostRule.Default = new(50)   **採用**  9行すべて正・平均 +3.67pt・拒否権 0 行
    ササ  LooseRule.Default      = new(true) **採用**  弾き 2.23 回/戦。ヨミと組ませると 13.5 → 92.4%
    ムド  RageRule.Default       = 現行のまま  **不採用**  平均 −1.52pt・拒否権 3 行
    ハリ  触っていない（材料のみ）

### 第108期 — `taillight`

**通貨の厚みは駒で数える。CLAUDE.md の「毒 6書き/4読み」等は呼び出し箇所の数で、単位が違う。**
駒で数えると 毒 5/4・標 **3/2**（第52期にカリが2枚目の書き手／第53期にトメが2枚目の読み手）・痺 5(+敵3)/1・燃 **2/2**（第58期にヒヨ＝旧フイが2枚目の読み手／**第59期にゾトが2枚目の書き手**）・IdleTurn 0/3・破片 3/1・傷 **4/4**（第108期にハリが抜けて書き 4 / 読み **4**）・
弱体 5(+敵1)/3・位置 4(+敵1)/4・死 9/5。**空白は IdleTurn の1つだけ**（書いているのは engine
なので「駒が供給できない」の意）、**飽和は 毒・痺・弱体・位置・死の5つ**。
**傷の読み手は第92期に 5 枚になった**（エグ・ナタ・ハリ・ミオ・**ノノ**）——
繕いの傷読み（`MendRule`）の採用で、**味方の傷の読み手が 1 枚（ハリ）から 2 枚になった。**
**第108期にハリが抜けて 4 枚**（エグ・ナタ・ミオ・ノノ）。
**味方の傷の読み手はノノ 1 枚だけに戻った**——ハリの両側読み（第85期の `SutureSide.Both`）が
ロスターから消えたので、**「敵と味方の深いほうを自動で選ぶ」読み手は現在いない。**

**「1行にしか入っていない」を数える前に、行の集合が1つとは限らないことを確かめる**（第108期）。
指示書は「ハリは1行にしか入っていない」と書いていたが、実際は
**`Presets.Compare` の 61 行に1行と `Presets.Cross`（交差帯12行）に1行の2箇所**あった。
**交差帯の `傷×被弾 (カド×ハリ×ノノ)` は触っていない**——交差帯は**測定の器具**なので
「計測器と測定対象を同時に動かさない」に従った。
**この不整合は第111期に閉じた**（`docs/units.md` からハリが消えたのに `docs/crossing.md` に残っていた形）
——交差帯の終端をノミへ差し替えた。**行が測る交差（傷 × 被弾）は変えていない。**

**行は下がった。測れる側は良くなった**（第108期・**指示書の予測「その行は上がる」は外れ**）。

    刻み×縫い (ノミ×ハリ)  100.0 / 100.0 / 97.5 / 100.0 / 62.0   平均 91.9%  情報セル 2
    刻み×澱み (ノミ×ミオ)  100.0 / 100.0 / 98.0 /  48.5 /  1.0   平均 69.5%  **情報セル 3**

代金は体で、ハリ（HP54・攻10・**普通に殴る**）がミオ（HP42・攻2・`Actions = [Skill]`）に替わるので
出力がドルガとノミの2枚だけになる。**席は +25.1pt 動いた**（仮置き 19 位 45.5% → 1位 70.6%・
中央が ノミ → ドルガ・ミオが前1へ）——第107期の「**機構が変わった行の席は寿命が切れる**」がそのまま当たった
（旧席は第39期の「傷持ちがいなくても普通に殴るハリをどこに置くか」の答えだった）。
**同じ実行で他の 60 行は1行も動いていない。**
**ただしミオの `ablate` 寄与は −4.0pt しかない**（他の4枚は −37.9〜−49.5pt）——
**入れ得の疑いがあるので第109期に素体差し替えで測り直すこと。**

**通貨の「供給」は窓口で数え、通貨の「量」は setter で数える**（第108期 (T3)・尾灯の実装判断）。
配ったものを後から引き上げる機構は**供給としては1回・量としては2回（+ と −）**現れるのが正しい:

    点灯  `ctx.Whet(…, WhetRoute.Taillight)` を**通す**
          ——他者強化の供給の観測（`NoteCarry` の強化キー）は `Whet` の中の1行にしか無いので、
          直に足すと `derive scan` / `whet` / `carry` / `spend` の**どこからも見えなくなる**
    消灯  `ctx.Dull` を**通さない**（`AtkBonus` を直接引く）
          ——(1) `Dull` は集約（ウケ）と転嫁（ワタ）の**横取りが立っている窓口**なので、通すと
          「自分の灯を消した」が第三者の破片や敵への弱体に化ける ／ (2) 受け手は**素の攻撃力より
          弱くなっていない**ので弱体の供給者として数えるのは事実に反する。
          **前例は墓守の層の引き直し**（`NecroTrait` の `desired - applied`）で、あちらも
          「自分で積んだ自分のボーナスの再計算であって強化ではない」として両方の窓口を通さない。
          **4つ目の通貨の観測点は `AtkBonus` の setter**（第106期）なので、消灯もそこでは数えられている

**`WhetRoute` は 8 本になった**（その他・駆り立て・号令開戦・号令毎T・縛め・移り木・吐き戻し・火選り・**尾灯**）。
**`UnitState.WhetReceived` は消灯で減らさない**——第67期の「閾値は**累積の床**であって在庫ではない」どおり。
**境界とは `OnCarryOver` で灯の累計も捨てて衝突を避ける**（持ち越すと次の戦闘で二重に引く。第102期に墓守が踏んだ形）。
**横流し（ヌキ）とは干渉しない**——保持者が `All` に居ないので `FunnelActive` が立たない。
**戻すなら灯の帳簿を受け手側へ移す必要がある。**

**同じ `UnitDef` を2枚置く台では、駒名をキーにした帳簿が使えない**（第108期・器具の直し）。
自己検査 (c)(c')(d) は最初トモ2枚の台で落ちたが、**器具の限界であって盤面の不整合ではなかった**
（2枚とも同じ「最も遅い味方」を照らすと帳簿に両方の灯が混ざる。1枚の台 600 戦ではずれ 0 件）。
**分離が要るなら `InstanceId` を持つ計数（`UnitTally`）の側で数えること。**
**もう1つの直しは `ctx.Log` が先頭の空白を落として `LogLine.Indent` に分けて持つこと**
——消灯の行から駒名を切り出す位置が4文字ずれていて、直すまで3つの検査が落ちていた。

**`Baseline.PrimaryFifthFloor` は動かなかった**（第108期・**指示書の予測は外れ**）。
`Baseline.PrimaryRows` の 19 行に `刻み×縫い` は入っていないので、
**この期の差し替えは主判定の分母に1行も触れない**（第107期の「19行にナタを含む行が 0 行」と同じ形）。
**歯止め 33.2 は第60期に確定した固定の線**（確定時の値 − 5.0pt）で、測り直して動く量ではない。

**`docs/rules.md` の腐りが1期ぶん見つかった**（第108期）。`Program.cs` を1文字も触っていないのに
`測った診断` の列に `hold2` が5箇所増えた——**第107期が `docs/rules.md` を `hold2` の実装より前に
生成していた。** 第94期の必須4項目はそのままでよいが、**生成の順序（実装を全部入れてから最後に生成）を守ること。**

**手作りのずれは13例目**（第108期）。指示書の「速5以下の味方」は
**ヌキ・オゴという `All` に載っていない棄却駒を2枚含み、4枚の速さがずれていた**
（ヒビ 4→**3** / ドハ 5→**3** / シガ 5→**3** / クビ 5→**4**）。**実測は 15 体**——
バン2 / セッキ2 / ドハ3 / ゴルム3 / ヒビ3 / シガ3 / ガルド4 / カド4 / クビ4 /
グザ5 / ムド5 / ナタ5 / ウケ5 / ヨミ5 / ザン5。
分布は 速2:2 / 3:4 / 4:3 / 5:6 / 6:10 / 7:9 / 8:8 / 9:4 / 10:2 / 11:1 / 12:3（計 52）。

**保留を閉じた —— 席を3行差し替え、ムドとハリは現行のままで確定した**
（第107期。**新しい駒ゼロ・新しい `TraitId` ゼロ**・足したのはノブ1本（`SutureFireRule`・既定は現行）と
席の差し替え3行・`UnitCatalog.All` の 52 体と `CompareBuilds()` の**行**は1行も触っていない・
`audit` ずれ 0 件・`derive scan` 欠落 0 / 過剰 0・`ctx.PickOne` は 26 箇所で第94期以降不変）。
**(S1)(S2)(S3) は別々のコミット**にした（`docs/balance.md` の差分がどれの帰結か読めなくなるため）。
経緯は design/PHASE107_HOLD2.md。**52 枚のうち役割が未確定の駒は 1 枚（ハリ）だけになった。**
**その1枚は第108期に落ちた**——ハリを `UnitCatalog.All` から外し、尾灯のトモを入れた（上の節）。

    (S1) 席    3 行を差し替え  **採用**  置き去り +5.6 / 刻み×断ち +9.0 / 刻み×縫い +15.5pt
    (S2) ムド  `RageRule` 現行  **不採用**  Q2（到達点 ±3 以内）が 32.9 → 36.1 で落ちた
    (S3) ハリ  `SutureFireRule.Default` = `Swing`（現行）  **不採用**  発火は増えるが行き先が変わる

### 第109期 — `tomo`

**尾灯のトモを測った —— 灯は伸びる。譲渡は「撃破を作る駒」と組むと成立しない**
（第109期・測定。**新しい `TraitId` ゼロ・新しい駒ゼロ・engine の判定ゼロ・
`Presets` は1行も触っていない**・足したのは**誰も読んで分岐しない計数 7 本**と診断 `tomo` 1本だけ・
`compare` 305 セル 0 件・`audit` ずれ 0 件・`derive scan` 欠落 0 / 過剰 0・`ctx.PickOne` 26 箇所で不変・
`docs/` 10ファイルの差分は `crossing.md` の所要時間 1 行と `rules.md` の 2 行〈`IgniteRule` の `期` が 7 → 8〉だけ）。
**トモは第110期まで `Presets` に入れない。** 経緯は design/PHASE109_TOMO.md。

    門1 灯      3.37 回/戦   門2 条件成立 1.08 回/戦   門3 譲渡 0.84 回/戦   **3つとも > 0。鎖は繋がっている**
    Q1 帰属      **+40.6 / +2.9pt**（測れた2台）        ○（**ただし4台中2台は床で測っていない**）
    Q2 譲渡の得  1譲渡あたり 13.1〜27.7 対 トモ 0.0     **×**（`トモ×ハギ` だけ 0.0）
    Q3 到達点    平均 10.0〜18.1・最大 **25**            ○

**「撃破を作る側」と「撃破を条件に動く側」は、条件を読む側が遅いと同席させても噛み合わない**（第109期）。
追い打ちのハギは「味方が敵を倒すとターン順を無視して薙ぐ」駒で**譲渡の条件をいちばん作りそう**だったのに、
**門2 が 0.01〜0.26 回/戦 と4台で唯一ほぼ 0** だった。**トモは味方で最も遅い**（速3・行動順の最後）ので、
「撃破が起きた」と「その情報でトモが動く」のあいだに**そのターンの残りの手番が全部入る**
——ハギの追い打ちは撃破を連鎖させるので、**最初の撃破が起きたターンにはたいてい敵が全滅していて、
トモの手番が来る前に戦闘が終わる。** 第53期の「標は溜まらない（供給 → 消費のサイクルが1ターンの中で閉じる）」の
裏返しで、こちらは**サイクルが閉じる前に戦闘のほうが閉じる。**

**`CanAct` が偽の駒に手番を譲っても何も起きない**（第109期）。ハギの譲渡は 0.06 回/戦 しか立たず、
うち **0.04 が潰れた手番**。**第104期の再行動には無かった穴**——再行動は「傷を刻んだ駒」に渡るので
**必ず自分の手番で振る駒**にしか届かないが、**譲渡は「灯した駒」＝最も遅い味方に渡る**ので、
**自分の手番で `PerformAttack` を1度も通らない5枚**（ハギ `CanAct => false` ／ カド `Immobile` ／
ヒヨ・ノノ・ミオ `Actions = [Skill]`）に当たりうる。**そのうち カド（速4）・ヒヨ（速6）・ノノ（速6）は遅い層にいる。**
**「最も遅い味方」を選ぶ機構は、ロスターの遅い層に手番を使わない駒が集まっていることと衝突する。**

**灯の上限を数値で付ける理由は無い**（第109期・第108期の「根拠は第109期に数え直すこと」への回答）。
到達点は 平均 10.0〜18.1・最大 **25** で、第108期の doc が見込んだ「実質の天井は +15〜30」と合う。
**上限を作っているのは数値ではなく受け手が倒れること**——灯量 11.7〜26.9 に対し
**消した量が 0.4〜19.0**（第2〜4波では 60〜100% の戦で消灯が起きる）。

**点灯の稼働率は4台とも 66.8〜67.8% に張り付く**（幅 1.0pt）。第86期の「読み手の稼働率は 35〜40%」の**倍**で、
理由は**条件が「生きた味方がいるか」1つしか無い**こと（第103期の湧き 31〜78% と同じ形）。
**残りの 33% は「トモが倒れた後」で、条件ではなく寿命が律速。**

**`刻み×澱み (ノミ×ミオ)` は傷軸の読み手を測る行として機能していない**（第109期 (B)）。
**ミオの機構の帰属は −0.8pt**（`ablate` の −4.0pt は「体 +5.8pt ／ 機構 0」の合成だった）で、
同じ席をエグ／ナタに替えると **+18.6 / +19.8pt**・ノノで **+10.3pt**。
**ただしその2枚は第108期に確認したとおり5枚組が `刻み×抉り` / `刻み×断ち` と一文字も違わない**ので行としては採れない。
**この期では行を変えていない。**

> **その駒を抜いても勝てる行は、その駒を測れていない**——**測れていないのは駒ではなく機構のほう。**

**着火（`IgniteRule`・第89期）は `刻み×澱み` で初めて盤面で走っている**（第109期）。
**グザ同席の4行は 20 セルすべて 0.00 回/戦**（第87期の「毎ターン敵全体に毒が撒かれるので
『傷を持ち毒を持たない敵』が存在しない」の再現）で、**グザを含まないこの行だけが 0.50〜1.39 回/戦。**
しかも**着火/戦 は「着火できる敵/戦」と全セルで完全に一致する**（＝ 100% の変換）。
**供給も読み手も詰まっていない。詰まっているのは払い出しの単価**（着火由来の毒ダメは最大 4.0 点/戦）。
**`刻み×澱み` は「傷軸の読み手を測る行」ではなく「着火を測る唯一の行」で、
行を差し替えるなら着火を測れる行を別に立てるまで待つこと。**

**速5以下の味方は 16 枚**（第109期に実装から引き直した。**下の第108期の「15 体」はトモ自身を数えていない**）
——速3 が 4 → **5** 枚（ドハ / ゴルム / ヒビ / シガ / **トモ**）。
**手作りのずれは14例目**——**指示書の `刻み×灯` という行はリポジトリに存在しない**
（`CompareBuilds()` の 61 行を全数検索して 0 件。実体は `刻み×澱み (ノミ×ミオ)`）。

**ハリを抜き、尾灯のトモを入れた —— ロスターは 52 枚のまま入れ替わった**
（第108期。**駒の入れ替えまで。トモの機構は第109期に測る**——「行が変わったせい」と
「駒が変わったせい」を切り分けるため。新しい `TraitId` 1（`Taillight`）・新しい `UnitDef` 1（トモ）・
**engine の判定 0**〈`WhetRoute` を1本と再入ガード `BattleContext.Yielding` を足しただけ〉・
`Presets.Compare` は**1 行の差し替えだけ**・`Presets.Cross` / `Stages` は1行も触っていない・
`compare` 305 セルは (T2)(T3) で 0 件・`audit` ずれ 0 件・`derive scan` 欠落 0 / 過剰 0・
`ctx.PickOne` 26 箇所で不変）。経緯は design/PHASE108_SWAP.md。

    (T1) 行  `刻み×縫い (ノミ×ハリ)` → `刻み×澱み (ノミ×ミオ)`。席は +25.1pt で差し替え
    (T3) 機構 `TaillightTrait`（尾灯）。**この時点では誰も持っていない**
    (T2) 駒  `UnitCatalog.All` の `Hari` → `Tomo`。**52 枚のまま**

**指示書のコミット順 (T2) → (T3) は入れ替えた**——`UnitCatalog.Tomo` の `Traits` が
`TraitId.Taillight` を参照するので、特性の実装より先にロスターを入れ替えるとコンパイルが通らない。

**ハリを切った根拠は4期ぶん**——第83期の3分の器具で唯一「切れる」判定／第85期の両側読み
（律速は振り 2.15 回/戦）／第106期の再行動が **0.00 回/戦**（再行動は傷の**書き手**にしか渡らず、
ハリは読み手）／第107期の発火口の移設（傷の書き込みの 90.5% が巻き込み則＝味方側なので
敵の傷を塞ぐ機会が消える）。**3期にわたって律速が同じ場所（振り回数）で、発火口を移しても解けなかった。**

**`UnitCatalog.Hari` の `UnitDef` も `SutureTrait` / `SutureRule` / `SutureFireRule` も削除していない**
——測って採らなかった機構は対照として残す作法。**`census` の「`All` に載っていない味方の駒」は 6 → 7 枚。**

### 第110期 — `tomo`

**条件が成立しないとき、条件の書き方を疑う前に、条件の入力が盤面に何回現れるかを数える**（第110期）。
V2（敵が倒れた瞬間にターン外で譲る）は第109期の「トモが遅いので撃破に間に合わない」を直接埋める設計だったのに、
**`トモ×ハギ` では段1（敵の死の通知）が 0.13〜0.90 回/戦しか来ない**（`トモ×ドルガ` は 2.92〜5.00）。
**ハギは `CanAct => false` で自分の手番に1度も振らず、追い打ちは「味方が倒したら」なので撃破の起点を1つも持たない。**
**第109期の読みは `トモ×ドルガ` では正しく、`トモ×ハギ` では「そもそも撃破が起きていない」だった。**

**条件節を緩めるだけの版は、門の側では下がりようが無い**（第110期・V1 の採用根拠）。
V1（このターンか前のターン）は4台すべてで門2・門3・帰属が V0 以上
（帰属 +41.3 → **+46.0** / +24.1 → **+26.5** / +2.8 → **+3.4** / +0.6 → +0.6）。
**残る危険は「増えた譲渡が損になる」ことだけで、潰れの割合も1譲渡あたりの与ダメも動かない**
（35.0 → 34.0 / 潰れ 0.68 → 0.78）。**Q5 が測れていないまま採ったのはこの単調性と、
トモが `Presets` に1行も入っていない（拒否権が原理的に立たない）ことによる。**

**「即時」は出力を前倒しするだけで総量を増やさない**（第110期・指示書 §2 の予測5 が当たった）。
V2 の1譲渡あたりの与ダメは **35.0 → 51.7** と上がるのに、**譲渡そのものが 1.70 → 1.32 に減る**
（門2 は 1.84 → 2.26 と増えているのに、**1ターン1回の上限と1ホップで削れる**）。
**V0 / V1 は自分の手番が1ターンに1回しか来ないので、その2つが構造的に効かない。**

**「最も遅い味方」を選ぶ機構は、のろまに当たる**（第110期・V2 だけが踏む壁）。
段3（`CanActOutOfTurn` で止まった回数）の内訳で、`トモ×ドルガ` 第2波は
**粛 0.54 / 痺れ 0.00 / `CanReact` 1.59** で、**`CanReact` の 1.59 は全部のろま（ドルガ）**
——`CanReact` を持つ特性はロスターに **`Sluggish` 1 枚だけ**なのに、
遅い層を選ぶ機構はそこへ吸い寄せられる。**V0 / V1 は `CanAct` の側で潰れるのでこの壁に当たらない。**
第109期の「遅い層に手番を使わない駒が集まっている」に**「遅い層にはのろまもいる」**が加わる。

**台の強さを「総攻」で見積もると、打点を書き換える特性のぶんだけ外す**（第110期）。
土台のプール 12 枚から 3 枚を選ぶ **220 通りを全部測った**ところ、
**総攻が最大の組（39 ＝ ガルド/キリ/ボルグ ＝ 第109期の土台）が最下位**（素体平均 1.1%）で、
**採用した組（33 ＝ ガルド/ソラ/ボルグ）の 8.5 分の1**しかない。
**キリの攻 12 は `ModifyAttack => 1` で打点が常に 1** なので総攻の表には出るが盤面には出ない。
採用した組が勝っているのも攻撃力ではなく**逸らし（標）**である。

**ローカル台の床は、味方側をどう組み替えても上がらない**（第110期）。
規則（速さ ≥ 8 ／ `AtkBonus` を他人に書かない ／ 素体版の第2〜5波平均が 40〜95%）の下で
**220 組すべてが 0 / 4 台**（最良でも `トモ×ドルガ` の 37.1%）。
**素体のトモは攻0 で5枠の1つを埋めるので、味方の実質の出力役が3枚に落ちる。**
**帯に入れたければ動かすのは敵側**（第70期「敵のHPを下げる操作は T_kill しか縮めない。
触るなら敵の攻撃力か味方の枚数」）。

**既定を動かす期では、対照の版を `Default` で書いてはいけない**（第110期・器具の反省）。
診断の版の表を `("V0 現行", TaillightRule.Default)` と書いていたので、
**採用で既定が V1 になった瞬間に「V0 の列」が黙って V1 に化けた。**
捕まえたのは自己検査 (h)（**V0 だけが持たないはずの性質**＝窓を1回も読まない）で、
**値を見ているだけでは気づけない**（版が入れ替わっても数字は出る）。
**版を並べる診断では3版とも明示的にモードを渡し、「その版だけが持たない性質」を1つ検査に置くこと。**

**印刷された参照値と突き合わせるときは、同じ書式に落として比べる**（第110期）。
`Math.Abs(実測 − 参照) > 0.005` で書くと **0.8350 対 0.83** がちょうど境界で落ち
（浮動小数では 0.0050000000000000044）、**`Math.Round(0.835, 2)` も偶数丸めで 0.84 を返す**ので直らない。
**`ToString("F2")` どうしで比べる**——参照値は「印刷された文字列」だから。

**計数の載る先は「その機構の保持者」ではなく「その量を生んだ駒」**（第110期）。
`TaillightYieldDamage` は `ApplyDamage` が**殴った側**に積むので、トモの帳簿を読むと常に 0。
**第109期はパートナー1枚から読んでいたので下限になっていた**
（`トモ×ドルガ` 第2波で 25.7 → 味方5枚から集めると **45.3**）——灯はパートナーが倒れれば次に遅い味方へ移る。

**手作りのずれは15例目**——指示書 §0-2 の「V2 は他人をターン外に動かす初めての機構」は誤りで、
**再行動（`EncoreRule`・第104期）が既に `HandleDeath` から他人の `TakeTurn` を呼んでいる。**
V2 が初めてなのは**「他人をターン外に動かし、しかも `CanActOutOfTurn` を通す」**ほうで、
**粛が止めるのは「4本 ＋ 他人を動かす1本」**である。
指示書 §2 の予測4（のろまは V2 なら通る）も誤りで、**`CanReact` の実装を数えれば実装前に分かった。**

### 第111期 — `ledger`

**`All` の47体は全員が `CompareBuilds()` の50行のどこかに出ている**（表D は空）。
**第108期にこれが破れた**——**尾灯のトモは `All` に居るが `Presets.Compare` にも `Presets.Cross` にも
1行も無かった**（機構を測る第109期まで盤面に出さないため。**意図した状態**で、
第90期の傷の引き取りと同じ「採用しても盤面が1セルも動かない」形）。
**第111期に閉じた**——`灯×薙ぎ (トモ×ドルガ)` を `Presets.Compare` に入れたので、**破れは3期で終わった。**
**現在 `All` に居て `Presets` のどこにも出ていない駒は 0 枚。**
事実上リストラされているのは**敵側の21体**（`Stages` に一度も出ていない）。
**敵側の読み手は10通貨すべてで 0**——敵は撒くだけで一度も読まない（ボス設計の論点）。

**尾灯のトモを盤面に出し、交差帯のハリを閉じた —— 61 行のうち「いちばん冗長な1行」と差し替えた**
（第111期。**機構を1つも変えていない**——engine・`Traits.cs`・`UnitCatalog.cs` は1文字も触らず、
**ノブの既定値は 38 本すべて不動**・新しい `TraitId` ゼロ・新しい駒ゼロ・`ctx.PickOne` 26 箇所のまま・
`audit` ずれ 0 件・`derive scan` 欠落 0 / 過剰 0・`compare` 305 セルは差し替えた1行の5セルだけが動く）。
**`docs/` は 10 ファイル中 9 ファイルが動き（`units.md` だけ 0 バイト）、動いたのは差し替えた2行の節だけ。**
経緯は design/PHASE111_LEDGER.md。

    (T1) `Presets.Compare`  `死軸×ホタ (ゾト×熾)` → **`灯×薙ぎ (トモ×ドルガ)`**（**行数は 61 のまま**）
    (T2) `Presets.Cross`    `傷×被弾` の終端 ハリ → **ノミ**。行名は `傷×被弾 (カド×ノミ×ノノ)`
    Baseline                主判定19行の第五波 **41.3%（1ビットも動かない）** ／ 全61行 **42.9%**（歯止め 33.2 との余裕 +8.1pt）

**トモは強化軸の新しい書き手であって、新しい交差ではない**（第111期。**手作りのずれの16例目**）。
第109期 §9-1 は「交差帯なら **強化 × 手番** で、第92期の交差の空白表にまだ無い組になる」と書いたが、
**`Presets.Cross` には `強化×手番 (ガン×ドルガ×バン)` が既にある**（第92期の12行の #8）うえ、
**`TraitEntryMap.Supplies[Taillight]` は 強化（Ally）1つだけ**で**手番（`IdleTurn`）を1ビットも書かない**
——譲渡は他人の手番を動かすが、通貨としての手番を書くわけではない。**だから行き先は `compare` の側。**
**この記述は CLAUDE.md ではなく `design/PHASE109_TOMO.md` §9-1 にあった**
——**ずれの中身だけでなく、ずれの<u>場所</u>も外れた初めての例。**

**情報セルは `compare` が使う帯（seed 0..199）で数えること**（第111期。第77期の一般則の実例）。
落とした `死軸×ホタ` の情報セルは 1 だが、**その 1 は第89期 (P2) の席の差し替えで 3 → 1 に落ちたもの**で、
**第89期は「情報セルを 2 以上に保つ最上位を採る」という正しい作法を正しく当てている:**

    第89期より前の席  帯A 情報セル **3** ／ 帯B **3**
    現行（第89期）の席 帯A 情報セル **1** ／ 帯B **2**   ← 割れているのは第四波の **99.8% 対 100.0%**

**割れ目は 400 試行のうち1試行。** 第77期の「`0 < x < 100` の情報セルは天井の際で不安定」は
seed の揺れの話として書かれていたが、**実際に効いたのは「どの帯で数えたか」のほう**である。
**席の追試は帯B（200..599）でやってよい。情報セルは帯A で数えること**
——`docs/balance.md` に載る値は帯A なので、そこで 1 なら**その行は勝率表の上で1セルしか情報を持たない。**

**「唯一の観測点」は規則を切って数えられる**（第111期。`ledger unique`）。
「その行でしか観測できない機構が無い」はこれまで手で判定していたが、
**規則を1本ずつ「何もしない版」に落として `compare` 61 行の何行が動くかを数えると、同じ判定が機械で出る**
（22 版 × 61 行 × 5 波 × 200 seed ＝ 約 134 万戦・**2分14秒**）。動く行が 1 行なら、その行が唯一の観測点:

    `shove` → 突き返し ／ `bear` → 引き受け ／ `loose` → 置き去り×分散回復 ／ **`woundIgnite` → 刻み×澱み**

**器具の陽性対照が効いた**——**第109期が手で見つけた `刻み×澱み`（着火の唯一の観測点）を、器具が独立に拾い直した。**
**特性の数え上げ（保持者が1行しかない特性）は下限**で、4 行のうち 3 行しか一致しない
——**組み合わせでしか成立しない唯一性は特性の数では引けない**（ミオは 5 行に居るが、他の4行はグザ同席で条件が潰れる）。
**次に行を落とす期は、規則を切る側の表を先に出すこと。**

**ノブを 0 にする前に、その 0 が実装のどこで読まれるかを確かめる**（第111期・器具の反省）。
`RageRule { Mode = Amount, Gain = 3 }` の `Gain` は**`Mode == Count` のときしか読まれない**ので、
`Gain = 0` は「切った版」にならない。初版はこれを並べて**「`rage` は 0 行」＝誰にも効いていない**と出していた。
**「既定を1つ変えて何も動かなかった」を機構の話として読む前に、その引数が読まれる分岐を数えること。**

**軸を手で数えると、engine が書く通貨を駒に帰属させてしまう**（第111期）。指示書は新しい行を
「強化（トモ）・**手番（ドルガ）**・燃（ボルグ）・標（ソラ）の4軸」と読んだが、実測（`TraitKeyMap`）は **5 軸**で
**ドルガの `Sluggish` はキーを1つも持たない**（**手番は engine が書く**。第95期に確認済み）一方、
**ガルドが 傷 / 被弾 の2軸**を持っていた。**行数（`crossing.md` の 2 軸以上が 16 → 17）が当たったのは
数え方が「撒く6軸のうち2つ以上」だったからで、内訳が当たったからではない。**

**差し替えの代金は2つ**（第111期。どちらも (b) には当たらないが記録する）。
**`blaze` の試験行が 2 → 1**——第59期は `死軸×ホタ` / `死軸×ヒヨ` を対にして作ったが、その片側が消えた
（`BlazeRule` 自体は残り 8 行で観測できる）。**`SutureRule` の観測点は盤面から 0 になった**
——ハリが `compare` 61 行にも交差帯 12 行にも居なくなったので、`derive scan` で `Suture` は「台に居ない」に落ちる。
**機構は既定のまま残置してあるので診断は回るが、縫いの対照を回す期はローカル台から始めることになる。**

**交差帯の終端をノミに替えると、交差の品質が上がった**（第111期 (T2)）。
傷の書き手がカド（被弾ごと）1本から**刻み（毎ターン）を足した2本**になり、
**出会い 2.30 → 4.00 回/戦・稼働率 54.6 → 96.1%**（情報セルは 2 のまま・他の 11 行は ±0.0）。
**キリは打点が常に 1 で第92期の規則2（接続子が両方とも出力ゼロの組は入れない）に触れ、
ナタは読み手が3枚になって供給が細る。**

**`pulse` で「置物」に見える駒がもう1枚増えた**（第111期）。トモは 振/T 0.00・干渉/T 0.00 だが
`ablate` は **−45.4pt**——出力が**強化（灯）と手番の譲渡**で、**どちらも `pulse` の列に出ない。**
新しい行は5枚とも `ablate` **−35.1 〜 −66.5pt** で、**入れ得の駒は1枚も無い。**

**`cross quality` の理由の文字列が条件と合っていない**（第111期に判明・**直していない**）。
交差帯の席の判定は `curV.Info >= topV.Info`（1位が情報セルを**増やさない**なら据え置き）なのに、
表示は **「1位は情報セルが減る」**——今期の差し替えで両方 2 になり、**同数なのに「減る」と出た。**
**判定は規則どおりで正しく、間違っているのは文字列だけ。** 次に `cross` を触る期で直すこと。

**尾灯の譲渡条件を3版で測り、窓（V1）を採った —— 即時（V2）は測って採用しなかった**
（第110期。**新しい `TraitId` ゼロ・新しい駒ゼロ・engine の新しい窓口ゼロ**〈`HandleDeath` に
死亡通知の連鎖の印を1つ足しただけ。既定では走査そのものが走らない〉・
`Presets` / `Stages` / `UnitCatalog.All` の 52 枚は1行も触っていない・
`compare` 305 セル 0 件・`audit` ずれ 0 件・`derive scan` 欠落 0 / 過剰 0・`ctx.PickOne` 26 箇所で不変・
`docs/` 10ファイルの差分は `units.md` 1 行 ＋ `rules.md` の `TaillightRule` の行と `期` の列 ＋
`crossing.md` の所要時間 1 行だけで、**`balance.md` は 0 バイト**）。
`TaillightRule.Default` = `new(YieldMode.OwnTurnWindow)`。経緯は design/PHASE110_YIELD.md。

    Q1 V2 でハギ台の譲渡が増えるか   0.08 → **0.23** 回/戦（線 V0 + 0.5）           **×**
    Q2 V1 でドルガ台・第四波の条件成立  2.17 → **2.88** 回/戦（線 V0 + 0.5）           ○
    Q3 1つの撃破で追い打ちと譲渡が両方  **0.09** 回/戦（線 0.5）                      **×**
    Q4 V2 の第二波の譲渡が 0          4台合計 **1.47**（**粛は伝令を割ると解ける**）  **×**
    Q5 帯に入った台の帰属             **帯 0 台 / 220 組**                          測れていない

### 第113期 — `lit`

**(G14) の波及を数えたら、席で直せる行は 61 行中 2 行しか無かった**（第113期）。
情報セル（帯A ＝ seed 0..199・第2〜5波）が **1 以下の行は 6 行**あるが、
120 通りの席を帯A で測り直して情報セルが増えたのは **`惨禍×被弾強化`（1 → 2）と
`澱み喰い (グザ×ヴィオ)`（1 → 3）の 2 行だけ**。**代金は −1.9pt / −2.4pt**（採否閾値 5.0pt の内側・帯B でも同符号）。
**据え置いた 4 行のうち 3 行は、候補の全席で第2〜4波のうち 2〜3 セルが動かない**
——`反撃 (ヒサ×カド)` と `反撃改 (ドハ×カド)` は 21 / 20 通りすべてで第1〜4波が
`100.0 / 0.0 / 100.0 / 100.0` に固定、`逆しま改 (クビ×ウツ)` は第1〜3波が一定で第5波が全席 100.0%。
**この 3 行は席では直せない。直すなら5枚組の側**（第111期の差し替えの形）。

**採ったのは追順 9 位と 17 位**で、**どちらも勝率の1位ではない。**
勝率の上位は 6 行とも情報セル 1 のまま——**天井（100.0%）に張り付く席がいちばん強い**ので、
**勝率で席を選ぶ限り情報セルは増えない**（第50期「`reseat` は勝つ席を探す道具で、
機構を活かす席を探す道具ではない」の情報セル版）。
`澱み喰い` でいちばん効いたのは**第五波が 0.0% → 3.0% に床から離れたこと**
——床のセルの「差 0.0」は「差が無い」ではなく**「測っていない」**（第61期）。

**灯の対象の濾し（`LitFilter`・W0/W1/W2）は実装したが採らなかった**（第113期。
**採否は第114期へ送る**——Phase 0 の (P-4) が「席の期へ切り替える」線を満たしたため。
既定は `SupportOnly` ＝ W0 で、`compare` 305 セル 0 件・トモを含まない 60 行が 600 セル 0 件）。
**engine に規則はゼロ**（足したのは観測専用の `BattleContext.CanActNow` と静的な札 `Trait.NeverAttacksOwnTurn` だけ）。
3版の実測:

    Q1 潰れ/譲渡が W0 の 1/2 以下   0.402 → W1 **0.402** / W2 **0.000**    W1 ×  / W2 ○
    Q2 帰属が W0 ±0 以上            +46.0 → W1 +46.0 / W2 **+54.1**        両方 ○
    Q3 W2 の替が増えるか            0.34 → **2.84** 回/戦                  増
    Q4 ハギ台の自己点灯             78.8% → **0.0%**（W1）                 ○
    Q5 トモを含まない 60 行         **0 件 / 600 セル**（60行 × 5波 × 2版）  ○
    Q6 対象の分布                   W1 3 枚 / W2 3 枚                      両方 ○

**W1（静的な濾し）は主判定行で 1 ビットも動かない。** ドルガは (A)(B) のどちらでもないので濾されず、
**効いたのはハギ台だけ**（灯の対象がハギ自身 78.8% → 0%・帰属 +0.6 → +4.9pt・味方の総与ダメ 194 → 240）。
**指示書の予測「帰属はほとんど動かない」を外した理由は、「量が無い」を譲渡の回数だけで数えていたこと**
——濾しは灯（攻撃力 +5・累積）の行き先も変えるので、**譲渡が増えなくても打点が動く。**

**W2（動的な濾し）は灯の到達点を 18.1 → 6.8 と 62% 落としながら勝率を上げる**（帰属 +46.0 → +54.1pt）。
**「累積を積むこと」より「譲った手番が潰れないこと」のほうが高い**
——第109期以来の「灯は伸びる＝資産」の逆側で、**指示書の予測3（W2 < W1）は外れた。**
ただし **W2 は主判定行の情報セルを 4 → 3 に減らす**（第四波 74.0% → 98.0%）ので、
採るなら席の再判定と対にすること（第107期）。

**「灯が無駄になる駒」はロスターの 11.8%**（灯の候補になりうる 51 枚のうち 6 枚）。
内訳は (A) 恒久的に自分の手番で攻撃しない型 2 枚（不動のカド・追い打ちのハギ）と
(B) 周期に `Attack` が1つも無い駒 5 枚（トモ・カド・ヒヨ・ノノ・ミオ。カドは両方）、
(C) 休み番のドルガ 1 枚（W2 だけ）。**ロスター 52 枚のうち速6 以下は 25 枚**なので、
**「遅い」と「灯が無駄になる」は同じではない**——効くのは枚数ではなく**その 1 枚が最遅かどうか。**

### 第114期 — `lit`（採否）

**灯の濾し W2（`LitFilter.ActingNow`）を `TaillightRule` の既定に採った**（W0 / W1 はノブで残置）。
**新しい機構は1つも作っていない**——動いたのは既定値の1行と、`灯×薙ぎ (トモ×ドルガ)` の席だけ。

**強化の価値は積んだ量ではなく、受け手がそれを振った回数で決まる**（第105期「手番は与ダメージの通貨」の強化側の系。
**一般則そのものの検証は第115期**）。W2 は**灯の到達点を 18.1 → 6.8（−62%）まで落とすのに帰属が +46.0 → +54.1pt** で、
盤面では**総与ダメが 369 → 364 と減っているのに撃破が 3.97 → 4.06 に増える**
——休み番のドルガに積んでいた +5 が、毎ターン振るボルグ（と偶数ターンのソラ）へ回ったぶん。
**第56期に数えた「配る側は配る先が振るかを1ビットも見ていない」（死蔵率 9.2%）の、見るようにした版が初めて盤面に載った。**

**採用の代金は「勝率」ではなく「測定器の情報」で払った。** W2 はこの行の情報セルを **4 → 3** に減らす
（第四波 74.0% → 98.0%）。(G9) の拒否権には当たらないが、**情報を減らす変更は席の再判定と対にする**（第107期）ので、
同じ期に 120 通りを W2 で測り直し、**狙 ○ かつ情報セル 2 以上の 17 通りのうち情報セルが最大（4）の段の最上位**を採った:

    版 × 席 の 2×2（第2〜5波平均・情報セル）
                旧席（後1 トモ / 後3 ボルグ）      新席（後1 ボルグ / 後3 トモ）
      W0        83.1% ・ 4                        82.2% ・ 4
      W2        91.2% ・ **3**                    **87.6% ・ 4**  ← 採った
    版の効き  旧席 +8.1pt / 新席 +5.4pt      席の効き  W0 −0.9pt / W2 −3.6pt

**情報セルを 4 に戻す代金は −3.6pt**（W2 の効きの 44%）で、それでも採用前より +4.5pt 高い。
**`docs/layout.md` で現行が 1位 → 7位 に落ちる**のがその買い物の領収書——
**「勝つ席」と「測れる席」は別物**（第50・64期）で、この期は測れる側を買った。
**61 行の情報セルの合計は 164 → 164 で不変**（分布も 0/4/22/24/11 のまま）。

**規約 (G16) を初めて自分に当てた期。** 席の線（狙 ○ かつ情報セル 2 以上）と採る条件の両方に狙を掛け、
**線を満たすが狙で落ちた席の数（20 通り中 3 通り）を出力に書いた。**
**この期は狙が結論を変えていない**——落ちた 3 席はすべて情報セル 4 だが最上位が 85.0% で、採った席（87.6%）より低い。
**「掛かっているが効かなかった」ことも記録する。**

**指示書の予測5（全61行の第五波が上がり、歯止めとの余裕が +8.1pt から増える）は外れた。**
**歯止めの分母は第60期に主判定19行の固定集合へ移してあり、`灯×薙ぎ` はそこに入っていない**ので、
**この行が何 pt 上がっても余裕は原理的に1ビットも動かない**（全61行のほうも 1 行 +0.5pt ÷ 61 = +0.008pt で桁に出ない）。
**同じ指示書の予測4（主判定は不動）と予測5は同時に成り立たない**
——第107期・第113期の「線と条件の食い違い」の親戚で、今回は**予測どうしが食い違っていた。**
**規約 (G4)（その条件の分母を必ず書く）を、予測の側にも当てること。**

**器具の注記**: `lit` の副判定4台は**診断のローカルで、席は第111〜113期のまま凍結してある。**
第114期に動かしたのは `Presets` 側の席だけなので、**副判定のドルガ台と主判定行はもう同じ台ではない**
（第113期の帰属 +46.0 / +54.1pt は旧席の値）。**台を追随させると「濾しの効き」と「席の効き」が混ざる。**


### 第115期 — `reader`

**強化の2枚目の読み手（積み過ぎのバン）を採る判定を出した。盤面は1ビットも動かしていない**
——`TraitId.Overload` / `OverloadTrait` / `ReaderRule` は**既定が不活性のまま残置**で、
搭載（`UnitCatalog` への札の追加と既定の切り替え）は**席の再判定と対にするので次の期**。
機構は `AtkBonus >= 5` のあいだ単体の一撃が薙ぎになるだけ（`ModifyPattern`）で、
**engine に足したのは規則の受け渡しと計数のみ。**

**二値の鍵を持つ読み手では、閾値は「量」ではなく「席」で決まる。**
号令の開戦時の配りは **+4** なのに、中央にガルド（`Stoic`）を置いた台では読み手の `AtkBonus` が
**常に 8.00**（最大も 8.0）になり、到達率が **1.000** に張り付いた——`Stoic` は
**味方全体に配られる強化を自分に乗せず隣接する味方へそのまま流す**ので、
中央（隣接次数4）に立つと全体供給が**二重に届く**。同じ供給者・同じ席のまま中央だけをウケに替えると:

    台1 （中央 ガルド）  `AtkBonus` = 8.00 一定 → 到達率 1.000 → 帰属 **+44.1pt**
    台1'（中央 ウケ）    `AtkBonus` = 4.00 一定 → 到達率 **0.000** → 帰属 **0.0pt**

**指示書の予測「+4 の1回きりでは閾値 5 に届かない」は正しかったが、台1 では観測できなかった。**
**数え落としていたのは供給者の側ではなく、読み手の隣に誰が立っているか。**
この対照（台1'）は指示書に無く、**予測が外れた原因を切り分けるために測りながら足した**もので、
これが無ければ帰属 +44.1pt のうちどれだけが台の作りだったか読めなかった。

**供給量の順位と帰属の順位は一致しない。** 強化/戦 は 台2 36.15 ＞ 台1 30.08 ＞ 台1' 17.67 ＞ 台3 11.09 だが、
帰属は 台1 +44.1 ＞ 台2 +14.0 ＞ 台3 +8.9 ＞ 台1' 0.0。順位を説明するのは供給量ではなく**到達率**
（1.000 / 0.637 / 0.824 / 0.000）——第65期「帰属を決めているのは供給量 × 使用率」の**読み手側での再現**。
**ただし第114期の一般則（強化の価値は積んだ量ではなく振った回数）の検証はこの器具ではできない**
——**台3 は V0 が第2〜5波とも 0.0% の床**で、床のセルでは誰に注入しても差が潰れる（第24・61期）。

**同じ形の機構が、載せる駒を替えると台ごとに割れた。** `AtkBonus >= 閾値` で単体を薙ぎにする形は
**第66期（軋みのヨミ）とまったく同じ**で、あちらは3行とも帰属 +17.1 / +10.8 / +13.5pt を出しながら
**拒否権2（第五波 96.5〜99.2% → 100.0%）で落ちた。** 違いは2つ:
**(1) ヨミは自己強化を持つ**ので閾値が「一度でも動かされたか」の起動スイッチに潰れ、
薙ぎ化率 85.9% で**台に依らなかった**（＝「供給者で効き方が変わるか」はヨミでは原理的に立たない判定）。
**(2) 拒否権2 は第100期に (G9) で「注意」へ降りている。**
**第115期が通ったのは駒の選び方と規約の版の両方による。この記述を消さない。**

**選定規則に実測を1本入れると、説明文で選んだ候補が実際に落ちる。**
候補3枚のうち引き受けのウケは「隣の味方が受ける攻撃力低下を代わりに背負う」駒だが、
`compare` 61 行 × 5 波 × seed 0..49（15,250 戦）で**1 行も 1 点も強化を受けていなかった**
（バンは 8,525 点・4 行で 41 枚中 9 位）。第39期「駒の説明文から数えると必ず抜ける」の**受け手側**。

**器具の反省——同じ比を作る2つの計数は、同じ瞬間に取ること。**
空振りの分母を**ターン頭**で、分子を**振った瞬間**で数えたら、ターンの途中で届いた強化のぶんだけ
**分子が分母を超えて空振りが負になった**（−0.24 / −0.30）。ターン頭の印を立てて振りの側もその印を見る形に直した。
直した後の空振りは 0.63〜1.00 回/戦 ＝ 門2 の **10〜16%** で、**予測（2割以上）は外れ**
——**痺れ・のろま・休み番の供給源が台に1枚もいない**（バンは毎ターン動ける）ためで、
残っていたのは決着ターンと読み手が倒れたターンだけだった。

**(B) 索引の穴**: `CLAUDE.md` の `→ LESSONS_*.md` の参照 **113 件**のうち、
**参照先にその期の節が実在しないものが 16 件**あった。**第65期の1件を `LESSONS_061_080.md` に
逐語で起こして 15 件**（規約 (G15) の機械照合つき）。**原因は第112期の分割の設計そのもの**
——則は `CLAUDE.md` に残して実測だけを移したので、**「則しか書かれていなかった期」は移す本文を持たず節が生まれなかった。**
**残り 15 件は「節を起こす」か「索引の側を直す」かの二択で、決めるのも次の期**（B-4）。

経緯は design/PHASE115_READER.md。

### 第116期 — `reader load`

**積み過ぎ（`Overload`）を据えのバンに載せた**（`UnitCatalog.Ban.Traits` に2本目の札・
`ReaderRule.Default` を 0 → **5**）。**engine に足したのは巻き込みの計数1行だけ**（`ReaderSplash`）。
動いた行は **2 行 / 6 セル**——`縛め収入型` が +7.5 / +7.0 / **+43.0** / +4.5（第2〜5波平均 **+15.5pt**）、
`溜め改` が第五波 +0.5。**バンを含まない 68 行 / 340 セルは 1 セルも動かない。**
主判定19行は **100 / 72.9 / 72.4 / 70.8 / 41.5**（歯止め 33.2% との余裕 **+8.3pt**）で、
**第107期以来 6 期ぶりに動いた**——`Presets` のバンの 4 行が**全部 `Baseline.PrimaryRows` に入っていた**。

**閾値の当落は「隣接数」ではなく「読み手に隣接しているか」で決まる**（第115期の系。**駒を1枚も替えずに再現した**）。
`Presets` のガルド（`Stoic`）は該当 2 行とも **前1**（隣接する占有枠はどちらも **2**）なのに、
**バンに隣接する `縛め収入型` だけが号令の開戦時 +4 を 8.00 で受け取る**（`強化×手番` は 4.00）。
席の表がこれを同じ行の中でもう一度示した:

    現行席      （ガルド 前1・バン 後1・隣接する）  到達率 **100.0%**
    追順18位の席（ガルド 前1・バン 後3・隣接しない）到達率 **32.3%**（平均 −10.3pt）

**ガルドの隣接数は 2 のまま同じ。** 第115期は中央（隣接次数4）を疑ったが、
**効いていたのは次数ではなく「その隣が読み手か」だった。**

**強化は連続量ではなく段で来る。** バンの `AtkBonus` の格子は
`≥5` と `≥10` と `≥20` が**同じ値になる行が2つ**（溜め改 6.9%・縛め収入型 30.8%）
——号令が +4、縛めが +16 なので **5 と 20 のあいだに標本がほとんど無い。**
だから薙ぎ率は **0% か 100% しか出ない**（届いたら初到達 T1.00・到達率 100% のまま最後まで）。
**「量ではなく二値で読む」（第115期）という設計判断が、そのまま観測の形として出た。**

**与ダメ +47% に対して撃破は +11% にしかならない。** 84.5 → 124.5 / 1.32 → 1.46。
薙ぎの副次目標は 60% に割り引かれ、**巻き込みで削った相手を倒しきる保証が無い**ので、
増えた出力の多くは「削り」に落ちる。**第四波が +43.0pt 動いた本体は打点ではなく
決着の短縮**（7.39 → 6.92 ターン ＝ 敵に振らせる回数が減った）。

**情報セルの心配をする前に、その行がどちら側の壁にいるかを見ること。**
「勝率が上がる行は飽和して情報セルが減るかもしれない」という予測は外れた
——`縛め収入型` の第五波は **1.0% → 5.5%** で、**天井ではなく床から離れた**。
61 行の情報セル合計は **164 → 164** で不変（分布も 0/4/22/24/11 のまま）。

経緯は design/PHASE116_LOAD.md。

### 第117期 — `boss`

**「被害で育つ駒」×「死なない駒」の 4×4 を測り、ボスの土台は立たなかった**（Q1 × / Q2 × / Q3 × / Q5 ×。
Q4 は形の上では通ったが (G13) の同値塊 42% で使えない）。
**新しい機構は1つも作っていない**——`BattleCore` に足したのは計数（`BossRule`）だけで、
`Presets` も `EnemyCatalog.Stages` も1文字も触っていない（`compare` 305 セル 0 件）。

**出力はターンに対して伸びない。落ちる。** 傾き（後半3T の与ダメ/T ÷ 前半3T）は
16 組すべてで **0.06〜0.30**。最上位が `ムド × ゴルム` の **0.30** で、1.0 を超える組は 1 つも無い。

**標的が尽きたからではない。** 既存5波は敵が 3〜5 体なので後半に殴る相手が減る——
その交絡を消すために**敵の最大HPを一律 ×4 にした台**（`boss long`。特性・攻撃力・速さ・攻撃型・体数は不変）で
測り直すと、傾きは **0.03〜0.18** で**むしろ下がる**。**落ちているのは味方の側である。**

**育ちそのものは本物。** ムドの `CurrentAttack` は **3.0 → 36.1（12.0 倍）**、カドは 11.0 → 30.3。
**「時間に比例する項」は実在する**——足りないのは項ではなく**ターン数**で、
育つ駒の生存Tは **3.3〜7.5T** しかなく、後半3T には盤上にいない
（`boss long` では**育つ側だけの傾きが 16 組中 4 組で 0.00** ＝ 最後の3ターンの与ダメが 0）。
**第114期「強化の価値は積んだ量ではなく受け手が振った回数で決まる」の、回数が生存で切られる版。**

**「死なない」で括った4枚のうち、時間を買っているのは1枚だけ**（単騎対照との生存Tの差:
**ゴルム +1.30T** / ガルド +0.79T / ムグ +0.15T / **ヴェル −0.14T**）。
継ぎ接ぎは自分の最大HPを半分にして蘇生するので、**蘇生した回数だけ自分が脆くなる**
——ボスの前提（時間を買う手段が4枚ある）が実測で崩れた。

**土台の選定規則が、測る対象の入力を 0 にすることがある**（この期のいちばん大きい器具の穴）。
指示書の規則 (c)「被弾・移動・弱体の供給者を土台に入れない」は組の差を守るための規則だが、
**移動の供給者と弱体の供給者を1枚残らず除いたので、ヨミ（`Displaced`）とウツ（`Perverse`）の入力が 0 になった。**
ヨミの帰属は 4 組すべて **0.0pt**・到達点は素体と小数第1位まで一致、
ウツは吐き戻しを受けるたびに攻撃力が半減して**符号が負**（−24.6pt）。
**「弱かった」のではなく「測っていない」**——第56期「過去の期の順位はその期の台の性質であって駒の性質ではない」。
**この期の結果からヨミ・ウツについては1文字も言えない。**
第59・60・61・107・110期に続く**6例目**で、**今回は判定式ではなく「台の作り方」の側で同じ形が出た。**

**決着が速い組は、傾きの分母が桁で違う。** カドの4組は `L ≥ 6` の戦が **1.1〜13.4%**
（カドの台は 70% 勝つので 4.3T で終わる）、他の 12 組は 57.5〜99.8%。
`run` を回す前に **`L ≥ 6` が 20% 未満の組を判定から外す**線を固定した（規約 (G12)）ので、
**カドは「傾きが低い」のではなく「傾きを測っていない」。**
**(G12) は版だけでなく<u>行</u>をまたぐときにも要る。**

**実装から引く分類は、引けなかったときに「該当なし」と区別がつかない**（実際に踏んだ）。
`boss` の分類は `BattleCore/Traits.cs` の走査から引いているが、リポジトリの外から叩くと走査が空になり、
分類がすべて偽を返して**土台が静かに化けた**（ドルガ・ナタ・エグ → ドルガ・**ボルグ**・**ハギ**＝どちらも味方を巻き込む駒）。
**走査が空なら止めること。**

**指示書の予測1の理由付けは実装と合っていなかった。** 「巨躯が後列への攻撃を引き受けるので `Rage` の
入力が最大化される」と書かれていたが、**巨躯は被弾を肩代わりするので `Rage` の入力を奪う。**
実際に効いているのは**吐き戻し**（`ctx.Whet`）で、経路は「被弾で育つ」ではなく「**庇われて育つ**」
——素体でも到達点が 3.0 → 10.2 まで伸びることがその証拠。

経緯は design/PHASE117_BOSS.md。

### 第118期 — `tank`

**自己回復は時間を買う。糧（殴った者が強くなる）がその 66〜87% を食う**（第118期・**採用しなかった**）。
台は第117期の 16 組と同一の土台3枚（ドルガ・ナタ・エグ）で、前1 の「死なない」側だけを
**HP 160 / 攻 3 / 速 3 / 回復 15/T / 糧 +2/hit** のタンクに差し替えた。

`Gain = 0`（回復だけ）の版は、味方全体の生存Tを素体比で **+1.55 / +0.25 / +1.71 / +1.62T**、
タンク自身の生存Tを **+2.82 / +0.32 / +2.18 / +2.37T** 伸ばす。
**現行版（`Gain = 2`）の味方生存Tは素体比 −0.41 / −0.02 / −0.79 / −0.71T**
——**買った時間が1つも残らない。育った敵が味方を早く割る。**

**代金は係数で薄まらない。この機構は自分の分母を増やす。**
回復を厚くすると生存が伸び、生存が伸びると殴られる回数が増え、殴られる回数がそのまま供給量になる
——`Gain = 0` の版の**発火回数は現行より 28〜32% 多い**（13.4 → 17.3 回/戦）。
**規約 (G7)「自分の分母を削る機構では紙は下限ではなく上限になる」の裏返し**で、
**分母が自分で増える機構では、係数を上げても比が動かない。**
指示書 §4 の「HP と回復量だけを触り +2/hit は動かさない」という枝は、**この理由で効かない。**

**渇き（第三波）は本物。軛（第四波）は測れていない。**
第三波の帰属は測れている 3 台で **−58.0 / −20.0 / −10.5pt**（回復が止まっても糧は止まらないので代金だけが残る）。
一方**第四波は 4 台すべてで床か天井**——ムドは現行が床・カドは両方天井・ヨミとウツは両方床。
**Q3（第三波と第四波で符号が逆）の × は「同じ顔だった」ではなく「片方の顔が見えなかった」。**

**第117期の「ヨミ・ウツの 0.0pt」は駒の性質ではなかった**（§1-B の再測定・Q5 ○）。
供給者を1枚入れるだけで**ヨミの到達点が 13.2 → 20.3（差 ±0.0 → +10.0）**、
**ウツが 12.7 → 24.0（差 −7.1 → +11.3）**。第56期がそのまま当たった。
**ただしヴェルは測れていない**——組んだ台は現行版も素体版も勝率 0.0%（床）で、
その「差 +0.2T」は「差が無い」ではなく「測っていない」（第61期）。**判定は 3/3 だが実体は 2/3。**

**器具**: `NourishRule`（`WhetRoute.Nourish`）は**陣営をまたぐ初めての強化経路**で、
**`AcceptsSupport` を見ない初めての強化経路**でもある（第56期の「強化側は無検査 0」が破れる）。
`ApplyDamage` の `levy`（徴収の札・3箇所）と `BattleContext.Hit` を足したが、
**読むのは糧の1箇所だけ**で、残りは経路表を実測で検証するための計数専用。
`compare` 305 セル 0 件・`PickOne` 26 箇所・ノブの既定は1つも動いていない。

**自分で第110期の穴に落ちた**（自己検査 (a)）。最初に書いた線は
「渇きの第三波で回復量が 0 になること」で、**実測 1.8/戦 で × が出た**——
**渇きは保持者が生きている間だけ効く**ので、祭司を割った後は回復が戻る。**原理的に到達不能な線だった。**
正しい形は**同数値・特性なしの祭司に差し替えた対照との比**（実測 2.6%）。

経緯は design/PHASE118_TANK.md。

### 第119期 — `checkup`（52 枚で回し直し ＋ 「マイナスを外した版」の軸）

**「マイナスを外す」は 52 枚のうち 24 枚でしか実行できない。**
(i) 独立した札 13 体 ／ (iii) 数値 11 体 ／ **(ii) 1つの札が両義 28 体**（(i) と (iii) の重複は 0 なのでちょうど分割）。
**(ii) では `yP` を作ろうとしても本物と同じものしか作れない**ので、**プラス値は定義上 単独 と一致する**
——新しい軸は**この 28 体について何も言わない。**

**「思想に合わない」象限（現行が弱い × マイナス抜きも弱い）は 12 体だが、9 体は (ii) である。**
**実際にマイナスを外して測れたのに弱いままだったのは 3 体**——
**継ぎ当てのノノ（プラス値 +0.28）・断ちのナタ（+0.59）・抉りのエグ（+0.66）**。
**3 体とも傷軸**で、**代金を外しても線（+1.5）に届かない**＝「マイナスのせいで組めない」のではなく**プラスの側が薄い。**

**「マイナスが実質プラス」は 逃亡兵セロ 1 体**（代金 −1.30）——臆病（`Coward`）を外すと
**後衛特化（`Sniper`）の起動条件である「一度後退したこと」を供給する者が居なくなる。**
**代金が負の駒はもう1体いる**（刻みのノミ −1.13・執着）が、こちらは**象限では「素で有能」に入る**
——**象限は線の左右でしか切らないので、両方とも強い駒の中にある「外すと弱くなる」を拾わない。**

**プラスが特性ではなく体でできている駒が1体だけある**——**のろまの巨兵ドルガのプラス値はちょうど +0.00。**
持っている札が `Sluggish`（純粋なマイナス）1 枚なので、**外すと素体そのものになる。**

**マイナス代の最大は 裂きのキリ +29.21**（薄刃）で、2 位（ドルガ +13.65）の 2.1 倍。
以下 ボルグ +7.65（巻き込み）・リィカ +5.82（生贄）・カド +5.69（不動＋惨禍）。

**器具の再現（Q1）は 46/51 で落ちたが、器具は壊れていない。**
**群の下の連続量は 単独 r = 0.987・最良の相乗 r = 0.978 で再現している**のに、
**3分の群だけが 5 体ずれた**——**5 体とも線から 1.0pt（相乗は 1.5pt）以内**にいて、**5 体とも上へ動いた。**
ずれた 5 体（ノノ・ラウ・エグ・ナタ・ハリ）は**第85〜108期に触られ続けた傷軸と毒軸の駒**である。

**切れる駒は 52 枚でも 0 体。** 差し替え候補は 2 体（突き返しのハネ・継ぎ当てのノノ）で、**2 体とも拒否権1。**
**「ドラフト台を正とする」と決めても差し替えは1体も解禁されない**
——**拒否権1 は「もう一方の台で残す側なら切らない」なので、正をどちらに決めても両方の台を見ている。**

**`Traits.cs` の enum のブロックは分類として使えなかった**——**85 件すべて走査できたのに、68 枚中 31 枚が食い違う。**
**プラス側ブロックに並んでいる 18 枚が実は両義**（`Curse` は味方に漏れ、`Mender` は半分自分が減り、`Bind` は味方を縛る）で、
`Immobile` と `Havoc` は**純粋なマイナスなのにプラス側ブロック**にいる。
第74期に代金を切り出した4組（`Rend`/`ThinBlade` ほか）は**コメントが切り出す前のまま**。

経緯は design/PHASE119_CHECKUP2.md。

### 第120期 — `wound2`

**傷の供給の 89.4% は駒ではなく engine の巻き込み則。** 経路別は
**巻き込み則 8.211 / 刻み 0.570 / 引き取り（中継）0.335 / 裂き 0.068**（合計 9.184 個/戦）。
**駒が書く2経路は敵にしか書かず、engine の2経路は味方にしか書かない**ので、
**味方比 93.1% は「傷がどちらに溜まるか」ではなく「供給の 93.1% が engine 側だ」と言っているだけ**である。
第107期の 90.5% から**下がるという予測は外れた**——**第111期に刻みを1本足しても、
駒の経路の総量が engine の 7% しか無いので分母は動かない。**

**書かれた傷の 94.7% は一度も読まれずに消える**（読まれず駒が死亡 59.7% ＋ 読まれず決着 34.9%）。
**消費経路の合計は 5.3% しか無い**——断ちが 0.6%・繕いの塞ぎが 1.1%・引き取りの donor が 3.6%。
**「読まれない」の原因は在庫の取り合いではなく、読み手が触れない側にあること。**
**敵に傷持ちがいるターンは 9.1%** で、攻めの読み手は残り 90.9% のターン読む相手が盤上に1体もいない。

**深い在庫は全部、読み手のいない側にある**——**味方側は 40.5% が深さ 3 以上**まで積むのに、
**敵側は 73.1% が深さ 1** で断ちの閾値 2 に届かない。

**単価は「読まれた傷あたり」と「書かれた傷あたり」で 18 倍違う**（**2.72 点 対 0.15 点**）。
**読み手は空振りしていない**——発火したときの単価は定数どおり（抉り 2.73 / なぞり 2.60 / 断ち 3.23 / 繕い 2.58）。
**割っているのは単価ではなく到達率。** 傷由来の与ダメは盤面から減った HP 全体の **0.20%**。

**鎖が繋がっている行は在席で数えると 10 行、実測で数えると 18 行**（/ 73）。
**差の 8 行はすべて engine（巻き込み則）が書き手を務めている行**で、駒の顔ぶれからは引けない。

**傷を丸ごと外すと 55 / 73 行がちょうど ±0.00pt で動かない。** 動くのは 18 行だけで、
**落ちる 14 行のうち上位6行はすべてノミかソラを含む刻み系**（最大 `刻み×断ち` 第4波 **−91.5pt**）。
**逆に外したほうが強くなる行が3行あり、最大は `追撃×毒 (ハギ×グザ)` の第2波 +58.5pt**
——**規約 (G1) が第90期の事故として名指ししている行・波と同一で、傷を外すとそっくり戻る。**
**あの落差は滲み則そのものではなく、「巻き込み則が味方に書いた傷を、敵の毒が滲み則で読む」往復の値段だった。**

**案A（庇いが傷を読む）は測る前から効かない**——介入 4.786 回/戦の **78.7% が巨躯（ゴルム）1枚**で、
**巨躯が守る相手（自分より前の味方）と巻き込み則が刻む相手は同じ集合**なので、
**守った相手が傷持ちだった割合は既に 96.6%。** 動く余地は**ガルドの 14.0% だけで実効 0.36 回/戦。**
**後備え（ドハ）の 100.0% は発火 0.015 回/戦の上の割合**で、数として読めない（(G12)）。

**`WoundWriters`（第104期）は今も版に依らず取られている**ので、
落とし穴の対策 (2)「自分で書いた傷は読まない」は**実装可能**である。

経緯は design/PHASE120_WOUND_STOCK.md。

---

### 第121期 — `wound2 spill`

**供給の 9 割を書いている規則を止めても、その通貨を主題にした行は 1 ビットも動かないことがある。**
巻き込み則（`SpillWoundRule`）を切る S2 で、**刻み系 6 行の 24 セルはすべて厳密に 0.0pt**
（`刻み×断ち` / `刻み` / `刻み×抉り` / `止め` / `刻み×澱み` / `逸らし改`）——
**その 6 行には味方の刃も味方の傷の読み手も 1 枚もいないので、乱数列ごと同一になる。**
**同じ通貨の中に混ざらない 2 つの流れがある**（駒は敵にだけ書き、engine は味方にだけ書く）。

**在庫の値段は単価でも消費でもなく到達率で決まる。** 供給を 9.184 → 0.638 個/戦（**7%**）に絞ると、
**実効÷読まれた傷は 2.72 のまま 1 ビットも動かず**、
**到達率（読まれた ÷ 書かれた）が 11.2% → 87.8%・実効÷書かれた傷が 0.15 → 2.22 点**になる。
第120期の「割っているのは単価ではなく到達率」を、**到達率だけを動かして確かめた形。**

**中継は供給を止めると自動的に止まる。** 引き取り（ガルド・`GatherRule`）は独立したノブを持つのに、
**自分のノブに触れないまま 0.335 → 0.000 になる**——**中継の入力は別の規則の出力だから。**
**「規則をいくつ止めたか」で盤面の変化を数えると、連鎖して止まる規則を数え落とす。**

**`追撃×毒 (ハギ×グザ)` の第2波は 29.0 → 87.5（+58.5pt）**で、
**第120期の「傷を丸ごと外すと +58.5」と 0.1pt も違わない**
——**規約 (G1) が第90期の事故として名指ししている落差は、「傷」ではなく
「巻き込み則が味方に書いた傷」の値段だった。供給側のノブ1本で元に戻る。**

**代金は小さい。** −10.0pt 以上落ちる行は S1・S2 とも **0 行**。S2 で下がったのは **6 行**で
最大は `反撃改 (ドハ×カド)` の **−4.0pt（1波）**、**73 行の平均は +0.26pt・動いた行は 9 / 73**、
**主判定19行の第五波は 41.5% → 41.4%**（動いた 9 行に `Baseline.PrimaryRows` は 0 行）。
**下がった 6 行のうち 5 行はノノ（繕い）かガルド（引き取り）を含む**
——**味方の傷を読む駒は `Presets` にこの 2 枚しかいない**（ハリは第108期に外れて 0 行）。

**判定の線は、計数の<u>名前</u>ではなく実装が数えている出来事に当てる。**
Q2 の線「読まれずに消えた割合が 20pt 以上下がる」は ×（94.7 → 91.7）だったが、
**この計数は実装では「<u>消費されずに</u>残った割合」**で、抉り・なぞりのような
**消費しない読み手が読んだ傷もここに落ちる**（第120期の報告書自身がそう書いている）。
消費経路は S0 でも 5.3% しかないので、**供給を 93% 削っても構造的に 3pt しか動かない。**
**同じ表の到達率は 76.6pt 動いていた**——**線が読む量を間違えると、動いている物を「動かない」と記録する。**
第59・60・61・107・110・117・118期に続く 8 例目だが、**今回は「セルが見えるか」ではなく「量の定義」の食い違い。**

**実装から引く索引は、引けているのに結ばれないことがある。**
診断を別ファイルに切り出して `static partial class` にしたら、
**`derive rules` の索引から `wound2` が静かに消えた**——`derive` は
`(?:static|sealed) class (\w+)` でファイルとモードを結ぶので、**`partial` が入ると一致しない。**
**`docs/rules.md` の `利用者` 列から 5 本のノブの `wound2` が落ちるだけで、エラーも警告も出ない。**
第117期（走査が空なら止める）・第119期（引けたことと使えることは別）に続く 3 例目で、
**今回は引けているのに結ばれない形。同じファイルに畳んで直した。**

**既定は変えていない**（`SpillWoundRule.Default` は現行のまま・`compare` 305 セル 0 件）。
経緯と採否の材料は design/PHASE121_SPILL.md。

### 第122期 —— 巻き込み則を止め、対の読み手2枚を同じコミットで降ろした（**採用**）

**Q1〜Q8 の 8 つとも ○。3 本の既定を動かした**——`SpillWoundRule.Enabled` `true` → **`false`**（第88期の採用）／
`MendRule.Side` `Wound` → **`Plain`**（第92期）／`GatherRule.Enabled` `true` → **`false`**（第90期 (P1)）。
**3つとも残置**（ノブを戻せば全部そのまま動く）。**新機構ゼロで、`BattleCore` で触ったのは既定値 3 行と1文 3 行の計 6 行。**

**読み手2枚を降ろしたぶんは 73 行すべて厳密に 0.00pt。**
**供給を止めた時点で、繕いの傷読み（ノノ）と引き取り（ガルド）には読む在庫が1つも来ていない**
——これは「2枚を降ろしても何も起きない」の証拠であると同時に、
**「供給が止まった時点で2枚は既に死んでいた」の証拠**でもある。

**動いたのは 9 行 / 73。** 最大は `追撃×毒 (ハギ×グザ)` の第2波 **29.0 → 87.5（+58.5pt）**で、
**第120期の「傷を丸ごと外すと +58.5」と 0.1pt も違わない**——
**規約 (G1) が第90期の事故として名指ししている落差は、供給側のノブ1本で元に戻る。**
下がったのは 6 行・最大 **−4.0pt（1波）**で拒否権（−10.0pt）は **0 行**、
**主判定19行の第五波は 41.5% → 41.4%・歯止め 33.2% との余裕は +8.2pt**、
**情報セルは 164 → 164 で不変**（席は測り直したが 1 行も動かさなかった）。

**味方の傷はノノにとって<u>収入</u>だった。** 繕いは患者の傷 1 つにつき +3 癒すので、
**巻き込み則が味方に書いた傷はそのまま回復量の上乗せになっていた**——
下がった 6 行のうち 5 行がノノかガルドを含み、残る 3 行はどれも**棘の反撃（カド）が隣の味方を巻き込む刃**を持つ。
**代金は A1（供給を止めた段）で全部払い終わっている**ので、
**「読み手を降ろすと下がる」ではなく「供給を止めると読み手の収入が消える」**である。

**予告は分母の 96% を選んだ。** 味方に傷を書く刃6枚（ボルグ・リィカ・ゴルム・ゾト・カド・ナラ）と
読み手2枚を含む行は **70 / 73**、**主判定19行では 19 / 19（100%）**で、実測で動いたのは 9 行・主判定は 0 行。
**分母のほぼ全部を選ぶ条件は予告になっていない**（規約 (G4) を予告の側に当てた形）。

**旧文は逐語では戻せない欄がある。** ノノの `MinusText` は第92期が節を足したあと
**第106期（繕いの代金半額）が同じ行の別の箇所を変えている**ので、
`git log -S` の親コミットから引いた旧文をそのまま書き戻すと**第106期の採用を巻き戻す**。
戻したのは第92期が足した「繕うとその傷はひとつ塞がる。」の節だけ。

**器具の的も同じ期のうちに作り直した。** 第121期の自己検査 (a') は「S0（現行）が既定と一致する」で、
**既定が S2 へ動いた瞬間に、採用したこと自体が「×」として出た。**
数える量を「**どの版が既定と一致するか**」に変え、
**一致した版が `SpillWoundRule.Default` と等しいこと**を合格条件にした
（第60期「採用で既定が動いた診断は、検算の相手が V0 から V1 へ移る」）。

**副産物**: 味方の傷が構造的に 0 になったので、**スィド・グザ・ラウの1文
「傷を負った味方には深く入る」（滲み則）が到達不能**になった。**この期では直していない**
——採用の期に、採用と関係のない駒の説明文を同じコミットで動かさない。

**第88期の対の顛末**（第85期に作り、第88期に対で採り、第90・92期に3段へ育て、第108期に片側が消え、
第120期に在庫として見つかり、第122期に両側を降ろした）は design/PHASE122_ADOPT_SPILL.md §5。


### 第123期 — `watch`（見る期）

**見えない原因の過半は「窓口が無い」ではなく「窓口はあるのに誰がやったかが載らない」**（第123期）。
推奨12戦で特性由来イベントが1件も出ない駒は **12 / 21 体**あり、原因の内訳は
**(i) 窓口が無い 3 ／ (ii) 発火していない 1 ／ (iii) 発火したが痕跡を残さない 8**。
`BattleEventKind` **15 種のうち 9 種が `ActorId` を持たない**
（`Heal` / `Revive` / `Summon` / `Move` / `Highlight` / `Status` / `StatusSnapshot` / `StatSnapshot` / `TurnStart`）ので、
**蘇生（ヴェル）・逃亡（セロ）・散開（ササ）・呪詛（ネル）は、起きたことが画面に出ても線が引けない。**
第99期 §F が挙げた「痺・標・破片の `StatusGain` 3本」より、
**既にある出来事に書き手を1フィールド足すほうが効く駒が多い。**

**残量と「付いた瞬間」は別の窓口である**（第123期）。`StatusSnapshot` は `StatusLabels` が
`StatusKeys.All` から作られるので**9キー全部に出る**が、`StatusGain` は **4 キー（毒・燃・傷・深手）＋
`DullKey`** しか出ない。**「いま乗っている」は見えるが「いま書かれた」は見えない**が正確な言い方で、
**「窓口が無い＝画面に何も出ない」ではない。**

**`Attack` とその `Damage` は 99.2% しか連続しない**（第123期 Q0-6・266 件中 2 件が非連続）。
非連続は**再行動（`EncoreRule`）を持つ行にだけ**出る——一振りの巻き込みを解決している途中で撃破が起き、
その撃破が別の駒の `Attack` を割り込ませると、残りの巻き込みの直前が別の駒になる。
**紐づけは隣接ではなく `Pattern` が非 null・`Reaction` が偽・`Relayed` が偽の3条件で書くこと。**

**実装から引く走査の対象が自分自身のファイルだと、検索文字列そのものが最初に当たる**（第123期）。
`Baseline.PrimaryRows` の軸コメントを `BattleSim/Program.cs` から引く走査で、
検索リテラルを素直に書いたら**自分のコードに当たって別の場所を読んだ**（症状は「走査が空」ではなく
「静かに違う表を作る」）。**アンカーは連結で組む。**
第117期「実装から引く分類は、引けなかったときに『該当なし』と区別が付かない」の自己参照の版。

**`derive rules` の「第nn期〜（k 期）」は生成した時点の値で、現在の値ではない**（第123期）。
`design/PHASE*.md` の期番号の異なり数を数えるので、**報告書を書く前に生成すると必ず1期ぶん足りない**
——HEAD の `SoakRule` は 9 だったが、実際の異なり数は 10 だった（第122期の報告書ぶん）。
**既定の列は正しい。** (G8) の必須3（触っていないノブの既定が動いていない）を
`docs/rules.md` の差分で示すときは、**k の列を除いて比べること。**

**写しは第110期前後で止まっていた**（第123期・前半）。取り込む前のデモ同梱の `BattleCore` は
正規化差分で **1,223 行**古く、**第116期の `Overload`（据えのバン）が存在しない盤面**を映していた。
**画面上の1文が変わったのは バン・ノノ・ガルドの3枚**で、うち2枚（ノノ・ガルド）は
**第122期に降ろした規則の説明文が残っていた**形——**降ろした規則の説明文が残っていると、画面が嘘をつく。**
経緯は design/PHASE123_DEMO.md。

### 第124期 — 戦績パネル・書き手・演出（画面の期）

**盤面は1ビットも動いていない。** `compare` 305 セルは段1・段2・段3 の3回とも `docs/balance.md` と 0 件差分で、
推奨12戦の台本のイベント総数も **2,567 件で第123期と完全一致**（＝出来事は1件も増減していない）。
経緯は design/PHASE124_SCREEN.md。

**`UnitTally` の 168 本の `int` は 53 : 115 に割れる**（第124期 Q0-1）。
**駒ごとに1戦で埋まる汎用のものは 53 本**（`Attacks` / `Interventions` / `Damage*` / `Healed` /
`Kills` / `LastActiveTurn` ／ 第105・106期の帰属 8 本 ほか）で、
**残る 115 本は特定の特性・規則の保持者がいないと恒等的に 0**（`Taillight*` 32 / `Deep*` 15 /
`Reader*` 11 / `Soak*` 9 / `Suture*` 8 / `Mend*` 8 ほか）。
**画面に出せるのは前者だけ**——後者を出すと 52 枚ぶんの列が要る。
**`TallyByUnit` は `verbose` に依存しない**ので、**判定を1つも通さずに画面へ出せる。**

**庇った回数は `UnitTally` に1本も無い**（第124期 Q0-3）。庇い（ガルド）・後備え（セッキ）は
`SelectTargetChain` で**主目標を差し替えるだけ**、分かち（ドハ）は `ApplyDamage` の中で肩代わりするが
増えるのは**肩代わりした側の `DamageTaken`** なので、**「庇ったから増えた」と「殴られたから増えた」が
数字から分けられない。** `NoteGuardPick`（第120期）はあるが `WoundCensus` が真のときだけ・味方側だけ・
`BattleContext` に積む**診断専用**で、駒には載らない。

**標の付与回数は<u>ある</u>。トメは1件も書かない**（第124期 Q0-4・予測 P1 の半分外れ）。
第105期が `UnitState.SetCounter` → `NoteStatusGain` → `NoteCarry` の道に書き手への帰属を置いていて、
`CarryMark`（標）はその範囲（`CarryPoison`〜`CarryIdle` の7キー）に入っている
——`StatusOutInTurn + StatusOutOffTurn` に**含まれている**。ただし**キー別の内訳が無い**。
実測（`tempo tables`）で **逸らしのソラ 状態外% = 100.0%・囃し立てのヒサ 100.0%** に対し、
**止めのトメは `—`（1件も書いていない）**——止めは標を**読んで消費する**駒で、
`SetCounter` は `delta > 0` しか計数へ通さないので**減算は帳簿に残らない**（第99期 §F の「減算の窓口」）。

**移動は engine が1度も起こしていない**（第124期 Q0-7・予測 P3 の否定）。
`.Slot =` の代入はリポジトリ全体で `SwapSlots` の中の 2 行だけ、`BattleEventKind.Move` を出す箇所も 1 箇所だけで、
**`SwapSlots` を呼ぶのは `Traits.cs` の 6 箇所がすべて**（逃亡・突き出し・曝き・突き返し・喧噪・錯乱）。
指示書は「庇い・突き飛ばし・後退は engine が動かしている」と見立てたが、
**庇い・後備えは `SelectTarget` の差し替えで駒を1ミリも動かさない。**
実測でも推奨12戦の `Move` 54 件は**全件に書き手が載った**（null 率 0.0%）。

**書き手を足す手数は、窓口を作る手数より2桁小さい**（第124期 段2）。
第123期が数えた「窓口はあるのに誰がやったかが載らない」(iii) 8体 / 12体を直すのに要ったのは、
**新しい窓口3本ではなく `Emit` への1フィールドと、呼び出し元 53 箇所への引数1つ**だった:

    Heal 10 ／ Revive 1 ／ Summon 2 ／ SwapSlots 6 ／ Dull 9 ／ Highlight 25 ＝ 53 箇所・全件に原因の駒があった

**`ActorId` を持たない `BattleEventKind` は 9 → 4**（`TurnStart` / `Status` / `StatusSnapshot` /
`StatSnapshot`）。**残る4種はどれも「原因になった駒がそもそも居ない」**——盤面全体の区切り、
継続効果が働いた記録（`ApplyDamage(u, poison, null)` で source を持たない）、ターン頭の写し2種。
**第99期 §F の3本目（`ctx.Dull` の writer）はこれで埋まった。上2本（痺・標・破片の `StatusGain`）は未着手。**

**推奨12戦の残った null は 865 / 2,567（33.7%）で、その 534 件は `StatSnapshot`**（第124期）。
ターン頭に全駒ぶん写す種類が、件数では台本の最大勢力である。**「線が引けない」の数え方を件数でやると、
写しの多さに埋もれる**——種類ごとに割ること。

**`SubViewport.Size` は効かない**（第124期 3-c）。`SubViewportContainer.Stretch = true` が
SubViewport のサイズを枠のサイズで上書きするので、**上げても1ピクセルも増えない**（一度これで測って外した）。
密度を上げるノブは `Scaling3DScale`（3D のスーパーサンプリング）。

**寄せ幅は手前のレーンの角の名前札で決まる**（第124期 3-c）。駒は x ∈ [−5.2, 5.2] に並ぶが、
手前のレーン（z = +2.15）はカメラに 2.15 近いぶんだけ横へ広がるので、**原点で足りていても角が枠から出る**。
上限は距離 15.5（元 17.7）＝ **駒が 14.6% 大きくなるところまで**。
**注視点を奥へ振っても駒は大きくならない**——手前の地面が枠から出るぶん空が上から入るだけ。

**薙ぎだけが「面」を持っていた**（第124期 Q0-9・3-d）。第123期の「ボルグとドルガの薙ぎはなぜかよく見えた」の
正体は `MakeSweepFan` で、**貫きは線と丸い輪しか持たず、単体と同じ語彙だった。**
「なぜかよく見えた」ものを調べると、**見えなかった側に足すべき語彙がそのまま出てくる。**

**型を持たない `Damage` は「特性が直に削った」である**（第124期 3-g）。
抉り・断ち・なぞり・追い打ち・破裂は `PerformAttack` を通らないので `Pattern` が null で、
デモの札は `効果` に落ちて**毒・燃焼（`ActorId` も無い）と同じ見た目**になっていた。
**`ActorId` の有無と `Pattern` の有無は別の情報で、掛け合わせて初めて「誰が・どう削ったか」が割れる。**

### 第125期 — `offturn`（手番の外を見せる期）

**「情報が台本にあるか」と「いつ起きたかが読めるか」は別の問いである**（第125期）。
割り込み（`Reaction`・第97期）と肩代わり（`Relayed`・第85期）と溜めの予告（`Charge` の
`Amount` / `Pattern` / `Text`）は**何期も前から台本に載っていた**のに、第124期の観察では
ヨミ・ゴルム・ドルガが3枚とも「見えない」と名指しされた。**足りなかったのは窓口でも書き手でもなく、
時間軸の枠**である——再生が `_result.Events` を先頭から1件ずつ流すだけで、
**束ねる単位が `TurnStart` しか無かった**（Q0-8）ので、割り込みも肩代わりもターン頭の一括も
「そういう順番で起きた1件」に潰れていた。**第123期の「窓口が無い」「書き手が載らない」に続く3つ目の欠け方。**

**駒が1ミリも動かない出来事は、被弾と区別が付かない**（第125期）。介入の鎖の6箇所
（標的 / 後備え×2 / 庇う / 殉教 / 棘守り）は**主目標を差し替えるだけ**で、
`ApplyDamage` は最初から庇った駒へ1回だけ入る——**分割も中継も移動も起きない**ので、
画面では「ガルドが殴られた」としか見えない。**台本にも `Log` の文字列しか無かった唯一の出来事**で、
第123期 (iii) と第124期 Q0-3 / P5 が2期続けて「出せない」と記録していた。
**`BattleEventKind.Intercept`（表示専用・どの規則も読まない）を1種足して、6箇所から1件ずつ出すようにした。**

**「その通貨を持つ駒がいる行」と「その出来事が起きる seed」は別物である**（第125期）。
推奨6行（第123期から固定）の実測は **`Intercept` 合計 17 件だが2行は 0 件**、
**`Reaction` は6行合計で 1 件**（軋みが `隊列崩し` で1回発火するだけ）。
**観察の器には行ごとの実測を添えること**——添えないと、ポンは「出たが分からなかった」と
「そもそも出なかった」を区別できないまま × を付ける。
**第124期の「貫き:×」「全体:×」が同じ形で汚れていた**（味方に常時の貫き・全体は 0 体・Q0-7）ので、
攻撃型の記号に **`−`（その戦で1回も出なかった）**を足した。

**肩代わりは元の駒の HP を引く前に解決するので、台本では中継の段が先・本来の標的が後に来る**（第125期）。
`ApplyDamageBody` は巨躯・分かちの分岐で `ApplyDamage(wall, blocked, ..., relayed: true)` を
**先に**呼び、そのあとで `target.Hp -= amount` と `Emit(Damage)` を通る。
**「1発が分けられた」を絵にするには、中継の段から<u>前方</u>へ本来の標的を探すことになる。**

**強化は `StatusGain` を1件も通らない**（第125期・予測 P4 の実装側の確認）。
`EmitStatusGain` の呼び出し口は **5 箇所（深手・傷・毒・燃焼・なまり）だけ**で、
`BattleContext.Whet` はそのどれでもない——**強化は受け手の攻撃力の数字としてしか画面に出ない。**
供給 15 本に対して読み手 2 枚というロスターでいちばん偏った通貨（第56期・第115/116期）が、
**画面では「出来事として1件も残らない」側にいる。**
**第126期以降に窓口を足すなら優先順位は「強化 > 痺・標・破片」で確定してよい。**

**手番が潰れたターンは台本に1件も残らない**（第125期・持ち越し）。`TakeTurnCore` が
`Stalled` で返ると `Emit` を1つも通らないので、**痺れ・まどろみ・`CanAct` 偽で飛んだ手番が
画面から引けない。** 数字の側は第105期に全部ある（`StallStun` / `StallSlumber` /
`StallImmobile` / `StallCanAct`）ので、**出すなら表示専用のイベント1種で足りる。**

### 第126期 — `time`（軸が回る前に落ちる問題。**3案とも採用しなかった**）

起点は2つの期が別の道から同じ場所に着いたこと——第117期（測定）の
「育ちは実在するのに傾きが 16 組すべて 1.0 未満。足りないのはターン数」と、
第125期（観察）の「シナジーの無いボルグ 254・ドルガ 201 が軸の駒を倍近く上回る」。

**盤面は動かしていない**（`UnitCatalog.All` / `Presets` / `EnemyCatalog.Stages` は1文字も触っていない）。
足したのは診断 `time`、生成物 `docs/quality.md`、対照として残置する札2枚。

**生存はほぼ「HP ÷ 被弾/T」だけで決まっている。** 推奨6行 30 席で
最大HP ↔ 生存T は **r = 0.317** しかないのに、紙のT（`MaxHp` ÷ `被弾/T`）↔ 生存T は **r = 0.853**。
**`実測 ÷ 紙` は 30 席中 26 席で 0.9〜1.1** ——**時間を買っている特性がいま盤面にほとんど無い**、
というのがこの期でいちばん強い測定値である（第117期の「壁が1枚しかない」より強く言える）。
効いているのは HP ではなく**席**で、前列は平均HP が厚い（85 対 67）のに生存Tは短く（4.01 対 5.52）、
**被弾/T が 29.8 対 12.8 で 2.3 倍**ある。

**ポンの定性の評がそのまま数字で出た。** ソラ 2.32T・トメ 3.20T（どちらも落ち率 100.0%）、
ハギの与ダメ **0.8 / 戦**（30 席で最低）、ガルド 2.17T・落ち率 98.5%（被弾/T 49.5 で全席2位）。

**ガルドに `Regen` は載らない**（Phase 0 Q0-6）。`RegenTrait` は `ctx.Heal` を通り、
`Heal` は入口で `!AcceptsSupport` を返し、`Stoic` は `BlocksSupport`——4つとも ○ なので、
**自分で自分を回復する経路も窓口を通る以上は支援拒否に弾かれ、効果は厳密に 0 になる。**
指示書が載せ先の第一候補に挙げていた駒だった。**`Stoic` に届く耐久は破片だけ。**

**3案の結果**（第117期の台・16 組 × 第2〜5波 × seed 0..199）:
案A 自己回復 **+0.83T**（16 組中 6 組は +1.0T 超・最大 +1.82T・勝率 25.9% → **39.3%**）／
案B 猶予 **+0.21T** ／ 案C 育ち耐性 **+0.02T**。
**線（+1.0T）に 0.17T 足りなかった案A も × のまま記録した**（第64期・結果を見てから緩めない）。

**案C の 0.00 の半分は「測っていない」**（→ CLAUDE.md「台を引き継ぐと、台の選定規則も一緒に引き継ぐ」）。
**条件3 の `残存` は分母が動いていた**（→ CLAUDE.md「採否の条件に割合を使うときは…」）。
**「殺さない」制約は出口にしか置けない**・**割合の軽減は整数の切り捨てに負ける**も同節。

**器具として残したもの**:

- `docs/quality.md`（`compare quality`）——`balance.md` と同じ戦・同じ帯から
  残存・圧勝率・**完全勝利**・全滅勝ちを出す。**戦闘は増えない。**
  **`balance.md` に節を足すと自己検査が2種類とも壊れる**ので別ファイルにしてある（実測 61 行 / 293 件）。
  出発点は全61行の通算で **完全勝利 6.7%**・全滅勝ち 18.7%
  ——**勝った試行の「ぎりぎり」が「完全勝利」の 2.8 倍**で、
  ポンの「完全勝利がしたい」は現状ほぼ起きない。
- `TraitId.Reprieve` / `Tempered`（保持者 0 の対照。`compare` 305 セル 0 件差分で不活性を確認）。

**次に触るなら**（この期の結論から）: 時間は機構では買えなかったので、
**被弾/T の側**（席・狙われ方・敵の火力配分）に戻るのが筋。
`残存度` を `docs/quality.md` に足すこと、埋め草の規則を「入力を供給するか」から書き直すことも積み残し。

### 第127期 — `grade`

**盤面を動かしたのは1語だけ**（`UnitCatalog.Ban.Traits` の `Overload` → `GradeStep`）。
足したのは診断 `grade` と、器具の札3本（`GradePierce` / `GradeAll` / `GradeStep` ＝ **1クラス3インスタンス**）。
**engine には規則も窓口も1行も足していない**（読む値も閾値も窓口も積み過ぎ＝第116期と同じで、違うのは上がる先だけ）。

**段の値段は線形でも打点でもない。** 同じ読み手・同じ供給・同じ閾値（`ReaderRule` = 5）で
上がる先だけを振ると、読み手の与ダメは 単体→薙ぎ **+40.0** ／ 薙ぎ→貫き **+4.5** ／ 全体 **+80.7**。
**貫きだけが「与ダメが増えて勝率が下がる」**——薙ぎに 3.1pt 負け（台2 では 5.4pt）、
**撃破/戦は 1.46 → 1.26 と減る**（減衰 25%/体 で後ろへ薄く配るため）。

**介入の素通りは全体に固有ではなかった**（指示書 P2 の半分が外れ）。
`SelectTargetChain` の早期リターンは **`pattern != Single` の1本**なので、
**薙ぎに化けた時点で既に庇い・標的・殉教・棘守りは通らない**（後備えだけが範囲にも割り込む）。
実測でも第五波の `敵の介入/戦` は **V1 薙ぎ / V3 全体 / V4 段 がすべて 0.99**（対照 1.17）。
**介入を持つ敵は第五波の殉教者だけ**で、指示書が挙げた「第三波の祭司」は介入を持たない。

**格上げの一覧は手で並べると落ちる**（第98期の再発）。指示書の下読み5枚は
**軋みのヨミと鱗のウロを落とし、`ModifyPattern` を1行も持たない尾灯のトモを入れていた**（一致 4/7）。
**正本は `grade phase0` の Q0-1**（`Traits.cs` 90 本 → 実装 9 本）。
味方の常時の型も **単体 49 / 薙ぎ 3 / 貫き 0 / 全体 0**（指示書の 48/4 は誤り）で、
**味方が全体に到達する経路はこの期の段2 が初めて。**

**供給なしの台は5版とも対照と小数点以下まで完全一致**（発火率 0.0% ＝ **測定不能**・第61期）。
札を足しても**乱数列が1ビットも動かない**ことの検算でもある。

**段2 の `compare` は 4 セル・±0.5pt しか動かないのに、絵は変わっている**——
`縛め収入型` の最大同時撃破 1.98 → **2.01**、全滅勝ち 8.6% → **6.6%**、決着 5.9 → 5.8T で、
第五波の1戦ログではバンの**全体攻撃が 1〜4 回/戦**出る。
**上の段が立つのは縛めが乗った2行だけ**（第116期の格子は **5 と 20 の2点にしか標本が無い**）。

**この期の採否は数値では決まらない**（目的が爽快感なので主判定はポンの目視）。
**戻すのは1語 ＋ `docs/` の再生成で済む。**

**器具として残したもの**: 診断 `grade`（`phase0` が**格上げの一覧・状態キーの書き手と読み手・
`AtkBonus` の供給 10 経路**の正本）と、保持者 0 の札 `GradePierce` / `GradeAll`。

**次に触るなら**: §8-1 の支援・耐久の格上げ（「盤面全体の状態」を読む初めての形）。
Q0-5 の表がそのまま材料で、**駒の書き手が 0 の鍵は 手番 と 深手 の2本**、**呪の読み手は ムド 1 枚きり**。


### 第128期 — `grade2`（段の載せ替えと、強化の棚卸し）

**「どの駒に強化が積まれるか」は供給側の説明文ではなく、`Whet` の宛先の式にしか書いていない**（第128期）。
指示書は「号令も縛めも『動かない味方』に積む」を出発点にしたが、
**`Whet` の呼び出し口 9 本のうち `SurrenderedTurn` / `IdleTurn` を読むのは号令の溜め 1 本だけ**。
**縛めは休む駒を選ばない**——`Roll` で無作為に選んだ味方を**その場で縛って休ませる**ので、
**選択は無作為で、休みは結果**である。**選択と結果を取り違えると「ドルガに集中する」が導けてしまう。**
第39期「駒の説明文から数えると必ず抜ける」の**供給側の版。**

**`AtkBonus` の在庫は「到達点」と「外から届いた量」で別の順位になる**（第128期・`docs/stock.md`）。
`AtkPeak`（自己強化を含む）の首位は 墓守リィカ **85.39** / 澱み喰いのヴィオ 70.78 / 分かちのドハ 47.91 で、
**外から届いた量（`Whetted`）の首位は 仇討ちのザン 11.42**（休み 45.0%）。
**ドルガは `外` で 6 位・`到達` で 12 位**（予測は「52枚で1位」だった＝**外れ**）。
**「誰が強化を受け取っているか」は `到達` の列では読めない。**
「休むのに読み手を持たない」駒は **3 枚**（ドルガ・カド・ハギ）で、**これが次の載せ先の候補表。**

**二値の鍵の閾値は、格子の到達率を見てから置く**（第128期）。ドルガの到達率は
**≥5 27.3% / ≥10 10.4% / ≥20 2.1%** で、**第127期の閾値 20 では全体率が 2.8%**
（供給のある16行に絞っても 4.4%）＝**死に札**だった。10 に下げると **13.6% / 21.2%** で帯に入り、
**行ごとの最大でも 79.2%** なので「普段は薙ぎ」は残る。
第116期の「強化は連続量ではなく段で来る」が、**同じ格子の別の駒でそのまま出た。**

**門の分母（ターン頭）と分子（振る瞬間）は、振る頻度が 1/2 の駒では構造的にずれる**（第128期）。
`刻み (ノミ単騎)` 第2波は**到達率 0.0% なのに全体率 28.2%**——ドルガは `Sluggish` で2ターンに1回しか振らず、
**ターン頭には届いていなかったが振る時点では届いていた手番**がそのまま差になる。
第115期「同じ比を作る2つの計数は同じ瞬間に取ること」の、**分子と分母が逆向きにずれた版。**

**勝率が上がる変更で情報セルが増えることがある。ただし帯ごとに別々に数えること**（第128期）。
`compare` 61 行では **164 → 165**（`責め苦` 第4波 1.0 → 65.5・`仇討ち` 第4波 18.5 → 94.0 と
**床から離れた**行のほうが、`縛め収入型` 第3波 95.5 → 100.0 と**天井に着いた**行より多い）のに、
**主判定19行は 57 → 56**、**交差帯は `強化×手番` が第4波 0.5 → 97.0 で 3/5 → 2/5 に落ち、Q1 が 10/12 → 9/12 で ×。**
**「情報が減る」は分母を指定しないと真偽が決まらない**（規約 (G4) の情報セル版）。

**同じ札でも載せ先を替えると `compare` の動く量が2桁変わる**（第128期）。
第127期（バン）は **4 セル・最大 ±0.5pt**、第128期（ドルガ）は **21 セル・最大 +75.5pt。**
機構も閾値の読み方も同じで、違うのは**その駒に供給が届くか**と**素の攻撃型**だけである。

**器具として残したもの**: 診断 `grade2`（`phase0` が `Whet` の宛先・休む駒・ドルガの行と席の正本、
`stock` が `docs/stock.md` を吐く）と、`UnitTally.ReaderAlls`（全体で振った回数）。

**次に触るなら**: `docs/stock.md` の「積まれるのに読み手を持たない」表の上位
（**仇討ちのザン 11.42 / 萎縮のクビ 7.95 / 逸らしのソラ 7.04**）と、
交差帯の `強化×手番` が失った情報セル。


### 第129期 — `survive`（指標の直しと、「起動まで守れるか」）

**「指標が緩い」の出どころを、実装の形から推測しない**（第129期）。
`完全勝利` の判定式は `PlayerSurvivors >= 出撃数` で、`PlayerSurvivors` は
`ctx.LivingMembers(PlayerTeam).Count()`——**原理的には湧いた駒（胞子・餌）で埋められる。**
だが実測は **2,115 件中 0 件**だった。湧いた駒が決着時に残る戦は 348 件（0.7%）あるのに、
**「湧いた数 ≧ 欠けた数」には一度もならない**——胞子が湧くのはムグが落ちた戦で、
その戦は他の駒も一緒に落ちている。**緩さの実体は分母だけ**（勝った試行 6.7% 対 全試行 4.3%）。
**指示書 P1（通算 3% 未満）は 4.3% で外れ、外れ方の中身も予測とは別だった。**

**数え方を1つ間違えると、その実測すら出ない**（第129期）。最初 `PlayerSurvivors > 出撃数` で数えたら
**0 件**になり、「召喚体は数えられていない」という誤った結論が出た
——あの式は「**欠けた数 ≦ 湧いた数**」まで要求するので、**1枚欠けて1体湧いた戦を数え落とす。**
正しくは `PlayerSurvivors ≠ 出撃数 − 欠けた数` で、そちらなら 348 件が出る。
第121期「判定の線は計数の<u>名前</u>ではなく実装が数えている出来事に当てる」の**分子の側の版。**

**死軸は本当に「意図した犠牲」だった**（第129期・指示書 P2 は ○）。
`OnDeath` を上書きする札の保持者（ゾト・ムグ・ゴルム）を免除して数え直すと、
**無傷勝利 0.0% に潰れていた行が 25〜79% に立ち上がる**
——`鱗 (ウロ×死軸)` **0.0% → 79.4%** ／ `追撃×死` 0.0% → 31.2% ／
`駆り立て改` 0.0% → 29.5% ／ `死の連鎖 (リィカ軸)` 0.0% → 25.5%（**上がった行は 9 行**）。
**ただし予測の顔ぶれは半分違う**——`毒爆弾 (ラウ×ヴィオ)` は 0.2% → 0.2% で1ミリも動かない
（ラウは `OnAnyDeath` 側で、この行に `OnDeath` の保持者が 1 枚もいない）。
**免除の 25 行中 21 行はゴルム**（`ColossusTrait.OnDeath` ＝ 還し）で、
**設計上の「意図した犠牲」（ゾト・ムグ）と「壁が1枚死ぬのを許した」が同じ列に混ざっている。**

**延命は時間を買うが、発火は時間に比例しない。比例の崩れ方が「鍵の供給」の種類と一致する**（第129期）。
代金ゼロの器具（`TraitId.Undying`・保持者 0 枚）で在席行を測ると、5 駒とも
**「発火と与ダメの両方が増える」という事前に固定した線は通る**。
だが **`密度` ＝ 発火 ÷ 生存T** はきれいに並ぶ:

| 駒 | 鍵の供給 | 生存T | 発火 | 密度 | 与ダメ(敵) |
|---|---|--:|--:|--:|--:|
| 逸らしのソラ | 自前（毎ターン無条件） | 3.09 → 7.24 | ×2.23 | **×0.95** | ×2.07 |
| 逃亡兵セロ | 自分の被弾（HP の 1/3） | 4.77 → 9.74 | ×1.52 | ×0.74 | ×1.65 |
| 追い打ちのハギ | 撃破（希少） | 3.41 → 9.51 | ×2.01 | ×0.72 | ×1.78 |
| 廃棄聖騎士ガルド | 味方が単体攻撃を受ける | 3.48 → 7.60 | ×1.41 | ×0.65 | ×2.75 |
| 止めのトメ | 標（書き手1枚・殴ると外れる） | 4.46 → 8.52 | **×1.05** | **×0.55** | ×1.62 |

**トメは時間を 1.9 倍買っても発火が 5% しか増えない**——指示書 P3「延命しても起動しないのはトメ」は
**線の上では外れ、中身では当たっている。トメに要るのは HP ではなく標の供給である。**
P6「ガルドは鍵を必要としないので生きてさえいれば働く」も**半分外れ**
——庇いの条件は**味方が単体攻撃を受けること**で、**味方が先に落ちれば鍵も尽きる**（密度 ×0.65）。

**密度が上がる駒もある —— 鍵が戦闘後半に偏る機構**（第129期）。追い打ち（ハギ）だけが **×1.10** と逆を向く。
鍵が「味方の撃破」で、**撃破は敵が削れた戦闘後半に集まる**ので、
早く落ちる駒は**鍵が配られる時間帯に居合わせない**——買った時間が時間以上の仕事になる。

**「殺さない」器具は、出口に置くだけでは足りない**（第129期。第126期の続き）。
`Undying` を `ApplyDamage` の出口（`target.Hp -= amount` の直前・猶予の直後）に置くと
**HP が 1 に張り付き、以降の被弾が `amount <= 0` で早期に返って `OnDamaged` が呼ばれなくなる**
——**庇い（ガルド）のように被弾そのものが起動条件の駒では、延命が機構を止める**（測りたいものの逆を測る）。
**ターン頭に満タンへ戻す**のを対にして解いた。**戻しは `ctx.Heal` を通さない**
——窓口を通すと渇き（第三波）で止まり `Stoic`（ガルド）に届かず、
**いちばん測りたい2枚にだけ効かない器具**になる（回復ではなく台の初期化。`UnitTally.Healed` にも乗らない）。

**`MaxHp` を膨らませる延命台は採れない**（第129期）。`Traits.cs` の `MaxHp` の読みは 10 件で、
**他人の `MaxHp` を読むのは囃し立て（ヒサ）の1箇所だけ**だが、自己参照の中に
**逃亡兵セロの後退（`Hp * 3 > MaxHp * 2`）**がいる——HP を上げると**起動条件も一緒に上がる。**
実測でセロは `MaxHp` 62 → 72 のとき**発火が 2.08 → 1.99 と下がった**（密度も 0.365 → 0.337）。
**HP は素直な耐久のノブではない。**

**「買った時間」を量の逆算に使えないことがある**（第129期・規約 (G11) の系）。
指示書 §3-3 は第126期の線（**+1.0T**）を段3 の目安にしたが、
**ハギは `MaxHp` を +30 しても +0.52T で、HP では到達できない線**だった。
だから量は時間ではなく**密度と代金**（拒否権・情報セル・跳ねる行）で選んだ。
第118期 Q1（他の期の数値を線にしたら器具が違った）と同じ形。

**標の書き手は3枚あるが、敵に立てるのは1枚**（第129期・指示書 Q0-7 の訂正）。
囃し立てのヒサ（開戦時1回・**隣接する最大HPの味方**）／逸らしのソラ（毎ターン・**敵の現在HP最大 1 体**＋自分）／
駆り立てのカリ（毎ターン・隣接する `CurrentAttack` 最大の味方）で、
**トメ（敵の標を読む）の鍵になるのはソラだけ**。`compare` でトメを含む 2 行はどちらもソラを含むので、
**鍵は配られている。足りないのは量である**（1枚・`TargetCount = 1`・トメが殴ると外れる）。

**決着ターンは平均も σ も何も言わないことがある**（第129期・第128期の宿題 P5 は ×）。
`責め苦` 第四波の σ 0.86 は **61 行中 39 位**で、第4波の中央値 0.77 とほとんど変わらない。
**中身は分散ではなく<u>勝敗で山が2つに割れている</u>こと**だった
——勝ちは **T10 に 112 / 131 が集中**する一山、負けは T7〜11 に広がる。
**「8ターン決着と9ターン全滅が同居している」という観察は正しいが、決着Tの分散には出ない。**

**`derive rules` の索引は、入れ子のクラス名まで拾って本文ごと結ぶ**（第129期・第121期の裏返し）。
`(?:static|sealed) class (\w+)` はメソッドの中の `sealed class Acc` も拾い、
`"Acc."` が現れたモードに**そのファイル全体**を結び付ける——実測で `docs/rules.md` の `利用者` 列に
**無関係な `pairs` が付いた**（クラス名に接頭辞を付けたら消えた）。
第121期は「引けているのに結ばれない」だったが、**今度は結ばれ過ぎる。**
なお `利用者` 列は「その診断がその規則を使っている」を意味しない
——`DivertFires` を読むだけの診断が `DivertRule` の利用者として出る。**(G8) の必須3 は既定の列だけ。**

**器具として残したもの**: 診断 `survive`（`phase0` が**現行「完全勝利」の実証・`OnDeath` と `OnAnyDeath` の
全数・標の書き手・生存Tの取り直し**の正本、`run` が延命台、`hp` が `MaxHp` の掃引）と、
**保持者 0 の札 `TraitId.Undying`**、計数 `BattleResult.PlayerStarterFallen` / `UnitTally.SniperSwings`。

**次に触るなら**: **トメの標の供給**（(a) `DivertRule.TargetCount` を上げる ／
(b) 敵に標を立てる2枚目 ／ (c) `FinisherRule.Consume` を落とす。**(c) は既に対照として実装済み**）。
次点は**「実質無傷勝利」の免除の集合をゴルムと死軸で割るか**の判断と、
**ガルドを耐久ではなく「庇う対象が先に落ちない」側から直す**案。

### 第130期 — `ember`（火を配る・**段0 でハギを戻し、段1 は採らなかった**）

**「HP を +20 しても働かなかった」を、目視が数字より先に言い当てた**（第130期 段0）。
第129期 段3 はハギの与ダメを 0.8 → 6.9（**×8.6**）に伸ばしたが、
**その 6.9 は 200 試行の平均**で、実態は「2回に1回、1発だけ」——
目視（`止め改` 第四波・seed 7 / 0）では**振 0 回・与ダメ 0** の戦が出る。
**平均が上がっただけで分散が広がっている**（第128期の責め苦と同じ形）。
**採否条件（発火と与ダメの両方が増える）は通っていたが、条件が緩かった**
——線にすべきだったのは「増えた」ではなく**「その駒が仕事をしたと言える量になったか」**。
**「測って採らなかった機構は残置」は機構の話で、能力値には当たらない**ので `MaxHp` は戻した
（戻さないと、鍵を緩める次の実験で「鍵が効いたのか HP が効いたのか」が混ざる）。

**上限を持つ波に固定量の継続ダメージを持ち込むと、上限の外側だけが素通りで効く**（第130期）。
ホタの配布で落ちたのは**第四波（軛・1発 25 上限）で −79.0pt**、次が第三波（渇き）で −56.0pt。
燃焼は `BurnRules.Damage` = 6 の**固定量**で `burnTick` として入るので**軛に一度も触れず**、
**敵の一撃が 25 に切られる波では、味方が自分に足した 6/T だけが上限の外で効く。**
第三波は渇きなので**回復で買い戻せない。**
**第25期「1発の重さに課金すると、課金されるのは1発を育てる機構」の裏返し**で、
破片（`Armor`）が上限の外側で効くのと**同じ位置**にいる——ただし符号が逆で、こちらは自分に落ちる。
**味方に継続ダメージを配る機構は、第四波で必ず重くなる。**

**主判定に使う量は「誰の量か」まで決める**（第130期・第121期の主判定側の版）。
P1 は「燃焼していたターンの割合が 90% を超えたら点きっぱなし」だったが、
**ホタは自分に配らない**（`ally == self` の除外）ので**ホタの燃焼率は機構に対して定数**
——実測で **69.3% → 69.3%** と小数第1位まで動かない。
動いたのは配られた側（ボルグ **0.0% → 11.2%**）だけで、**実質の主判定はボルグ1枚**だった。
**量の名前は正しいのに、誰の量かを決めていなかった。**

**穴が空いていることと、埋めて得になることは別**（第130期）。
`CinderTrait` は `ally == self` で自分を除外するので**供給源のボルグだけが燃えず、
ヒヨ（火選り）の強化対象になれない**——この穴は確かに埋まった（**0 → 10,124**）。
**が、埋めた先が悪かった。** ボルグは**巻き込みが本体で攻撃力への依存が薄い**（第58/60期）うえ、
**乗算を持つホタ（燃焼中ちょうど4倍）への配分が 8,484 → 7,056（−17%）と減る**
——実効では**「4倍の受け手から、倍率の無い受け手へ 1,428 点を移した」**ことになる。
**通貨の穴を埋める案は、埋めた先の倍率を先に見ること。**

**走査は `class` だけを見てはいけない**（第130期・第121/129期に続く3例目）。
直前の `class` 宣言だけで帰属を引くと、**`record struct` である `SoakRule` の中の `Kinds` が
手前の `ImmobileTrait` に結び付き、「棘鎧のカドが燃焼を読む」という嘘の行が出た。**
**症状は「走査が空」ではなく「静かに違う表を作る」**（第123期）ので、
第117期の「引けなかったときの分岐」では捕まらない。
**`record struct` / `struct` / `interface` も宣言として数えること。**
**他の診断が同じ穴を踏んでいないかは数えていない。**

**「その行で確かめる」と書く前に、その行が存在するかを数える**（第130期 Q0-6・規約 (G4)）。
P5（ボルグを含まない行では何も変わらない）は、**ホタを含む 2 行がどちらもボルグと同席している**ので
`compare` 61 行では原理的に確かめられない——確かめられるのは
「**ホタを含まない行が ±0.0**」のほうだった（そちらは 295 セル 0 件で ○）。
第59・60・61・107・110・117・118・121・126期に続く。

**燃焼の起点は「1枚」ではなく「毎ターン供給する起点が1枚」**（第130期 Q0-1・指示書の前提の訂正）。
`BlazeRule` の既定は `Both`（第59期に採用済み）なので**破裂のゾトも着火する＝起点は2枚**。
ただしゾトは `OnDeath` なので**蘇生が無ければ 1.00 回/戦**。
**「起点が細い」を数えるときは、枚数ではなく供給の周期で数えること。**

**器具として残したもの**: 診断 `ember`（`phase0` が**燃焼の書き手・読み手・`Ignite` の再付与・
隣接・燃焼を含む行**の正本、`run` が V0/V1 の対照、`check` が自己検査）と、
**既定 `Off` の `EmberRule`**（`EmberRule.On` で版を振れば全部そのまま回る）。
`PyreTrait.OnAfterAttack` は実装のまま残してある。

**次に触るなら**: 逃げ道の (a) カウンタを +1 だけ動かす（`ScapegoatTrait` の形）／
(b) 1戦にN回まで——**ただしこの期の実測を踏まえると、先に「配る先」を直すべき**である
（上の「埋めた先が悪かった」）。ハギの処遇（転生 / 差し替え）とトメの標の供給は未決のまま。


### 第131期 —— 踏み込みの札（`UnitDef.Advances`）と、第128〜130期の則の取り込み

**この期は盤面を1ビットも動かしていない。** `compare` 305 セルは `docs/balance.md` と 0 件差分で、
**札を読んで分岐する規則は 0 件**（走査で定義 31 ／ 出力 1 ／ doc 参照 1。
`BattleEngine.cs` / `Traits.cs` / `DemoApp` に 0 件）。

**engine に射程という軸は一度も存在しなかった**（Q0-8）。攻撃の到達は**席**と `AttackPattern`
（前列規則・薙ぎの列・貫きの経路）だけで完全に決まる。`design/` を「射程 / 間合い / 近接 / 遠距離」で
走査すると **5 ファイル**が当たるが、**機構として入れた期は 0 期**——内訳は
第124/125期の観察ログ（ポンの言葉）と第131期の指示書自身、そして第37/38期の「最**近接**行」
（`max|Δ|` の相手を指す語で、距離とは無関係）である。
**だから札の名前に距離・射程・間合いを入れなかった**——`IsMelee` は「射程が短い」と読める。

**表示専用の札を割り当てる基準は、機構の向きではなく手番の絵に当てる**（§0-2 の基準の作り直し）。
指示書の基準は「その駒は**行く**のか、**来られる**のか」で、
例（ハギ「続けて**踏み込む**」／ザン「**割り込んで**刃を返す」＝行く、
カド「**自分からは決して攻撃しない**」／ヨミ「**その場で**割り込んで攻撃する」＝来られる）は
どれも**機構の起動条件**を読んでいる。**この読み方を全 52 枚に当てると肩代わり3枚が割れる**
——ガルド（「味方への攻撃を肩代わりし」）・ドハ（「他人の痛みを勝手に引き受ける」）・
ウケ（「誰の痛みでも代わりに背負う」）は機構としては完全に「来られる」側だが、
**札が支配するのは通常攻撃の絵**であり、**肩代わりは §0-4 の制約により席の位置で描かれる**
（＝札を1ミリも通らない）。実際の割り当てはこう組み直した:

    (a) 一度も攻撃しない                                    → 据置
    (b) 1文が「撒く・飛ばす・投げる・吐く・縛る・喚ぶ」     → 据置
    (c) 1文が「自分では動かない・その場で・決して出ない」   → 据置
    それ以外                                                → 踏込（既定）

**基準の文が読む対象（機構か、手番か）を決めずに割り当てると、同じ1文で逆の答えが出る。**

**攻撃型と踏み込みは対応しない**（予測 P3 の否定）。指示書は
「貫き（狙撃手）・全体（詠唱兵・審問官）を持つのは敵の据置側だけになるはず」と書いたが、
**第五波の槍騎兵（`Lancer`・貫き・攻17・速12）は踏込**である——指示書自身が §0-3 で
「敵の近接（城塞の重装兵・戦斧兵・**槍騎兵**ほか）」と挙げていた駒で、
**同じ文書の中で予測と前提が食い違っていた。**
**「遠くから広く届く」は貫き・全体の<u>一部</u>の読み方でしかなく、突撃もまた列を貫く。**

**`docs/rules.md` の「初出 第nn期〜」の「〜」に、第124期 段0 が外し切れなかった自己参照が残っていた。**
`EmberRule` は第130期の初出なのに、**`design/PHASE131_ADVANCE_SPEC.md` が型名を挙げただけで
「第130期」→「第130期〜」に変わる**（指示書を `design/` から外して再生成すると戻ることを実測した）。
第124期は「（k 期）」という**数**を落としたが、**「〜」は同じ自己参照を1ビットに畳んだもの**で残っていた。
**規約 (G8) の必須3 を `docs/rules.md` 丸ごとの差分で示す、は
指示書が既存のノブに言及した期では必ず偽になる。**

**生成物は、その期の `CLAUDE.md` を書き終えてから作り直す。**
`docs/rules.md` の最終列（`CLAUDE.md` / `LESSONS_*.md` に型名が出るか）は**その2つを走査する**ので、
**`docs/` を先に再生成して後から `CLAUDE.md` を書くと、その列だけが1期ぶん古いまま commit される**
——第130期の `EmberRule` の行がそうなっていた（`docs/rules.md` は 5b41c87、`CLAUDE.md` は 0b2daa9）。

**同じ名前の比が、器具を変えると符号を変えていた**（第129期の積み残し・この期に判明）。
`密度`（発火 ÷ 生存T）は追い打ちのハギについて**2つの値**を持つ:
**代金ゼロの延命（`Undying`・生存T ×2.8）で 0.463 → 0.335（×0.72）**、
**`MaxHp` の掃引（62 → 92 ＝ +0.52T）で 0.463 → 0.508（×1.10）。**
第129期の報告書は §7-2 に前者、§7-3 と §5-1 に後者を載せ、
`CLAUDE.md` は**後者を「延命台の値」として写していた**（この期に直した）。
**符号を割っているのは延命の量**で、鍵（味方の撃破）が戦闘後半に集まるので
**少し延ばすと鍵の時間帯に居合わせるようになり、延ばし過ぎると鍵の尽きた尾を買う。**
**「延命と相性が良い」は量を言わないと真偽が決まらない。**

**割り当ての件数**: 味方 52 枚のうち**据置 25 / 踏込 27**。敵 40 件のうち**据置 11 / 踏込 29**。
`UnitCatalog.All` の外の 7 件（召喚2・残置5）にも付けた（据置は `fodder`・`hari` の 2 件）。
**迷った駒は味方 5 枚（セロ・バン・ヒビ・ウケ・ワタ）と敵 3 件（告発人・粛の伝令・誹りの巡礼騎士）**
——**ポンが 3 枚（セロ・バン・ヒビ）を据置へ倒し、5 件は現状維持。**
**セロはポンの判断で「弓兵なので遠距離から攻撃するのが自然」**——1文の「敵の後列を**狙い撃つ**貫きに
変わる」に合わせて**覚醒前も据置で通す**ので、**前列にいるあいだの一撃だけが絵と合わない**
（札が静的であることの唯一の代金。動的にすると `UnitState` 側の量になり「表示専用」でなくなる）。
**予測 P2 の見積り「15〜20」は下振れ、拒否線「25 を超えたら既定の向きが逆」には触れていない**
——**ちょうど 25 なので、次にもう1枚でも据置へ倒れると線を割る。**

**`checkup check` が第128期から1行も測っていないことが分かった**（A7 を取ろうとして判明）。
出力は「**ロスターの札に分類の無いものがある: `GradeStep`。止める**（第117期）」の1行だけで、
**(a)〜(d) と必須4項目に1つも到達しない。HEAD でも同じ**なので、この期の変更とは無関係。
第128期が `UnitCatalog.Dolga` に `GradeStep` を足したとき、**`checkup` の分類表を足さなかった。**
**門は設計どおり正しく止めている**（第117期）——**止まったまま3期が過ぎたことだけが問題で、
52 枚の健康診断（切る駒を決める唯一の器具）はいま走らない。**
A7 は **(a) のパーサを逐語で再現して取った**（52 行走査・食い違い 0 件）。

**器具として残したもの**: `UnitDef.Advances`（既定 `true`）、`EnemyCatalog.MakeStill`
（`Make` の据置版。差分は札1つだけ）、`docs/units.md` の `踏込` 列とステージ行の `/踏込` `/据置`。

**次に触るなら**: `DemoApp` の演出（Codex の担当）。**申し送りは1つ**——
**踏み込みは主目標への一撃だけの演出にし、席で決まる効果（巻き込み・隣接・伝播）は席の位置で描く。**

### 第132期 — `yoke map`（上限の地図）と器具2本の修理

**`Cap` は第25期の導入以来、履歴上1度も動いていない**（`git log -S "public const int Cap"` で 1 本）。
軛の保持者は **第四波の中央に 1 体だけ**（`UnitCatalog.Yoker`・城塞の重装兵と数値・型・速さが同一）。

**上限を回避する経路の全件**（`ApplyDamage` の呼び出し口 27 ／ `.Hp` への直接書き 10 を走査）:

    (A) 上限の手前で引かれる       1 本  破片（`Armor`）だけ
    (B) 別の `ApplyDamage` になる  6 本  棘守りの超過 / 巨躯 / 分かち / 呪いの共有 / 薙ぎ・全体の副次 / 貫きの各段
    (C) そもそも通らない          10 箇所 繕いの代金・縫いの切り詰め・不死・回復・蘇生・死亡確定・会戦の境界4
    (D) 通るが構造的に届かない      —    継続ダメージ（毒・燃焼は固定量なので `amount > Cap` が立たない）

**惨禍（+50%）・据え・散開・萎縮・`lethal:false` のクランプ・猶予・不死は上限より前にあるが、
上限に押し戻されるので回避経路ではない**（第25期の「入口ではなく出口で切る」）。
**繕い（ノノ）の代金 `self.Hp -= paid` は、削りなのに上限も破片も肩代わりも通らない唯一の経路**
（4.01 点/戦）——**「代金は誰にも肩代わりできない」が規則ではなく実装の場所として決まっている。**

**実測（`CompareBuilds()` 61 行 × 第四波 × seed 0..199 × 3 版）**:

| 型 | 切られた量/戦 | 切られた率 | 撃破/一撃 (上限あり) | 撃破/戦 あり | 撃破/戦 なし | 過剰率 あり | 過剰率 なし |
|---|--:|--:|--:|--:|--:|--:|--:|
| 単体 | 47.8 | 11.8% | 0.061 | 0.923 | 1.014 | 4.8% | 9.6% |
| 薙ぎ | **57.6** | 33.6% | 0.107 | 0.729 | 0.854 | 5.9% | 15.9% |
| 貫き | 4.8 | 21.4% | 0.052 | 0.092 | 0.105 | 3.6% | 8.1% |
| **全体** | 10.9 | **100.0%** | **0.259** | **0.171** | **0.146** | 11.4% | 22.5% |
| 型なし | 53.3 | 24.6% | 0.087 | 0.904 | 0.950 | 6.5% | 11.4% |

**全 61 行: 上限あり 67.2% / 上限なし 84.1%（Δ −16.9pt）。上限があるほうが強い行は 1 行だけ**
（`刻み×澱み (ノミ×ミオ)` +5.0pt。理由は測っていない）。
**切られた側は 敵 7.68 回 / 174.3 点 対 味方 0.07 回 / 0.4 点**——
**第四波の敵の打点は全部 25 以下**（重装 12・詠唱兵の溜め 16・司祭 9）なので、
**「両陣営に等しくかかる」と書いてあるルールが、数値のせいで実質プレイヤー専用の税になっている。**

**1回あたりの切られ幅は 墓守リィカ 38.6・澱み喰いヴィオ 55.2 が最大で、
ロスター最高攻の ドルガ（38）は 14.4**——第25期の「課税されるのは大打点ではなく積み上げ」が
107 期ぶりに同じ形で再現した。

**回避経路の量（1戦あたり）**: 徴収 47.54（通る）／ 肩代わりの中継 36.03 ／ 継続ダメージ 27.12 ／
繕いの代金 4.01 ／ **破片 2.19**。
**予測「破片は上限の下で価値が跳ね上がる」は ×**——機構（上限の手前で引かれる）は確認したが、
量が肩代わり・継続の 1/10 以下しかない。ヒビを含む3行はどれも上限があるほうが弱い
（−22.0 / −19.5 / −16.0pt）。**行ごとの破片量は測っていない**ので、
「ヒビの破片は第四波で他の波より効率が良いか」は未回答。

**§0-3 の矛盾の解（段 × 上限の 2×2・ドルガを含む 24 行）**:

| 編成 | 主型 | 段の効き（上限あり） | 段の効き（上限なし） | 相乗 |
|---|---|--:|--:|--:|
| 責め苦 (トウ×シガ) | 全体 | **+64.5** | +33.0 | +31.5 |
| 仇討ち (ヒサ×ザン) | 全体 | **+75.5** | **+0.5** | +75.0 |
| 縛め収入型 | 薙ぎ | +9.5 | 0.0 | +9.5 |
| 刻み (ノミ単騎) | 単体 | +5.0 | 0.0 | +5.0 |
| 駆り立て (カリ×ドルガ) | 単体 | +3.5 | 0.0 | +3.5 |
| 残り 19 行 | — | 0.0 | 0.0 | 0.0 |
| **平均（24 行）** | — | **+6.6** | +1.4 | **+5.2** |

**仇討ちでは、上限が無ければ段の値打ちはほぼゼロ（+0.5pt）で、上限の下でだけ +75.5pt になる。**

**器具の修理2本**:

- **段0-a** `checkup` の分類表（`hcLabel`）に `GradeStep` を1行足した。
  **分類の分布は第119期と1つも動かない**（(i) 13 / (iii) 11 / (ii) 28）ので、
  **3期ぶん止まっていたあいだに失ったのは判定ではなく「判定できる状態」だけだった。**
  動いたのは ドルガ の `yM`（`—` → `GradeStep`）と、上書き表の1行。
- **段0-b** `derive rules` の「〜」を落とした。**動いた行は 91**（88 が「〜」の1文字・
  2 が列名 `期（design/）` → `初出（design/）`・1 が前書き）で、**既定値の列は1つも動いていない。**
  **註の中で `EmberRule` の型名に触れたら、そのノブの `測った診断` 列に `derive` が付いた**
  ——`測った診断` は診断の区間の本文まるごとで型名を結ぶので、
  **自己参照を消すコードのコメントが、消そうとしている表の入力になっていた**（第129期「結ばれ過ぎる」の続き）。

**器具として残したもの**: `BattleResult.Yoke`（`YokeLedger`・計数専用）、
`BattleContext.YokeBinding`（保持者のキャッシュ。判定は第25期から同値）、
`BattleContext.DirectHpLoss`（`ApplyDamage` を通らない削り）、`yoke map`。

**次に触るなら**: ボルグの倍率案（次期の第一候補）。**この期の則から、第四波では伸びないと予測できる**
——倍率は1発を大きくする方向で、ボルグの薙ぎは既に切られた率 33.6%・`惜しい` 1.68 回/戦（全型で最多）。
**ただし「燃えている敵の数」に比例する案は当たる体数の側にも効く**ので、
**Phase 0 で「1発の大きさを動かす分」と「当たる体数を動かす分」を分けて数えること。**

### 第133期 — `wildfire`（撒いた火を読む・**段1 で落ちた**）

`StatusKeys.Burn` を読む駒は第132期まで **2 枚しか無かった**——熾のホタ（**自分が**燃えている）と
火選りのヒヨ（**味方が**燃えている）。**敵が燃えていることを読む駒が 1 枚も無く**、一方で
**唯一の常時の着火役（火の粉・ボルグ）が、撒いた火に一切の価値を持っていなかった。**
この穴に**火勢**（`TraitId.Wildfire` ＝ 燃えている**敵**の数だけ一撃が重くなる）を当てて測った。

**盤面は1ビットも動いていない**——`WildfireRule` の既定は `Off`、
**保持者は `UnitCatalog.All` に 0 枚**（診断のローカルの `UnitDef` だけが持つ）。

**形は実装が1つに決めた。** `Trait.ModifyAttack` の署名は `(UnitState self, int atk)` で
**対象を受け取らない**ので、「<u>標的が</u>燃えていれば重い」は書けない
——止め（第53期）と薄刃（第75期）が2枚とも engine 側（`PerformAttack` が `atk` を作った直後）に
あるのはこれが理由である。**書けるのは「盤面の燃えている敵の数を読む」形だけ**で、
窓口は `UnitState.Board`（第46期の驕りが足した参照）。**engine に規則も窓口も1本も足していない**
（足したのは計数1行＝`NoteWildfireSwing`。**`Wildfire.Mode` / `.Amount` / `.Active` で分岐している箇所は 0 件**）。

**供給は完全に編成の中にある**——`ctx.Ignite` の呼び出し 5 件のうち敵に向くのは 2 件（ボルグとゾト）で、
**`EnemyCatalog` に `Cinder` / `Bomber` は 0 件**。**敵の燃焼はすべてプレイヤー側が撒いたもの**である。
しかも **`compare` 61 行・交差帯 12 行を通じてボルグとゾトが同席する行は 0 行**なので、
**ゾトの破裂はこの機構に 1 点も供給しない。**

**段1（2台 × 4版 × 第2〜5波 × seed 0..199 ＝ 6,400 戦）:**

| 台 | 版 | 発火率 | **燃敵 平均** | 燃敵 最大 | 上乗せ分散 | 与ダメΔ | 撃破Δ | **勝率Δ** |
|---|---|---:|---:|---:|---:|---:|---:|---:|
| 台1 | V0 | 68.9% | **0.87** | 2 | 0.00 | — | — | — |
| 台1 | V1 加算 +4/体 | 64.9% | **0.80** | 2 | 7.34 | +29.6 | +0.64 | +0.6pt |
| 台1 | V2 乗算 ×1.25/体 | 64.5% | **0.79** | 2 | 8.88 | +31.1 | +0.67 | +0.6pt |
| 台1 | V3 二値 ×1.40 | 61.7% | **0.75** | 2 | 11.58 | +41.6 | +0.77 | +0.8pt |
| 台2 | V0 | 42.1% | **0.46** | 2 | 0.00 | — | — | — |
| 台2 | V1 | 41.0% | **0.44** | 2 | 5.04 | +15.1 | +0.32 | **+18.5pt** |
| 台2 | V2 | 40.8% | **0.44** | 2 | 5.53 | +15.5 | +0.34 | **+19.9pt** |
| 台2 | V3 | 40.5% | **0.43** | 2 | 11.80 | +23.2 | +0.50 | **+26.8pt** |

**段2 に進む条件（指示書 §5-1）は 4 本のうち 3 本が 3 版とも通り、条件2 だけで落ちた**
——「同時に燃えている敵が **1.0 以上**」に対し実測 **0.43〜0.80**。
**線は予測 P1（1.5〜2.5 体）から引いたもので、対照 V0 を1回回せば実装前に出せた量**だった（規約 (G11)）。
**線は結果の後で緩めていない**（第64期）。

**P1 が外れた理由**: Q0-4 の紙の上限（主目標1体/振り × 持続3ターン ＝ **最大3体**）が置いた前提
「**毎ターン別の敵を主目標にする**」が盤面で成り立たない。**標的選択は前列が生きている限り前列しか含まない**
ので**ボルグは同じ敵を殴り続け、再着火は残ターンを 3 に戻すだけで体数を増やさない**。
しかもその敵が落ちれば**燃えている体が1つ消える**——
**「火を増やす動作」と「火を消す動作」が同じ一振りの中にある。**

**P2 も外れた**（80% 超の予測に対し 68.9% / 42.1%）。差の 27pt は**前3 の火力**（キリ攻1 → ドルガ攻38）
だけで生まれている——**味方が強いほど自分の条件が消える**ので、
**発火率は保持者の性質ではなく同席する火力の性質である。**

**P6 は当たった**（燃敵の分散 0.30〜0.48・上乗せの分散 5.04〜11.80）。**盤面を読む意味はあった。**

**第132期の則の検証**（指示書 §7 の 6）: 薙ぎに倍率を足すと第四波（軛）で切られるようになる
（V0 は **0.00 回/戦**、V1 / V2 は **1.40 / 1.39 回/戦**）。**回数は同じなのに乗算は加算の 2 倍を捨てる**
（1回あたり 2.0 点 対 1.0 点）。**V3 の二値は上乗せ平均が最大（4.32）なのに 0.00 回**
——**上限に課金されるのは「平均の大きさ」ではなく「分布の上側の厚み」。**

**次に触るなら**: **順序が反転した。倍率ではなく「火を広げる口」が先である**
——同時に燃える敵が1体を切っているので、**係数をいくら上げても読む対象が増えない**。
候補は**薙ぎの巻き込み先にも着火する**案（engine の規則「攻撃1回につき特性は1度」に触るので独立した判断が要る）で、
**着火が3倍になれば燃敵が跳ねる。**
なお **`EmberRule.On`（ホタの火配り）の測り直しは動機が増えていない**
——指示書 §0-4 の根拠は「ボルグが倍率を持てば前提が変わる」だったが、**ボルグは倍率を持たなかった。**

---

### 第134期 —— 重ね掛けの実測と、盤面ルールの対称性（`stacks`）

**測定だけの期。機構も採否も 0 件で、`compare` 305 セルは `docs/balance.md` と 0 件差分。**
`docs/` で動いたのは **`units.md` の1行だけ**で、**それも第133期の積み残し**（段0）。

**段0 —— `docs/units.md` の再生成。** 第133期が `TraitId.Wildfire` を足したのに
`dump` を回し直していなかったので、特性一覧から `| \`Wildfire\` | - |` が1本落ちていた。
**`audit` は編成数依存の整合しか見ない**ので、編成数に依存しない `units.md` は「OK」としか出ない。

**段1 —— 燃焼の重ね掛け。** `Ignite` は残ターンを 3 に**上書きする（加算しない）**ので
**同じ駒に何度点けても燃焼は濃くならない**が、**その捨てられている供給が何回起きているか**を
誰も数えていなかった（第57期は `BurnLit` / `BurnRelit` の**総数**だけ）。

> **区間の定義**: 火が点いていない駒に点いた瞬間に開き、**燃え尽き／燃えたまま死／燃えたまま決着**の
> どれかで閉じる。**1区間の点け直し回数は、その区間が開いてから閉じるまでの再付与の回数。**
> **戦闘単位ではなく区間単位。** 検算は「点いた回数 20,665 ＝ 閉じた区間の数 20,665」。

**実測**（燃焼を含む 20 行 × 第2〜5波 × seed 0..199 ＝ 103,216 区間）:

    1区間あたりの点け直し   平均 0.57（敵 0.52 / 味方 0.62）・最大 5
    分布                    0回 49.5% / 1回 45.4% / 2回 3.8% / 3回以上 1.4%
    区間の閉じ方            燃え尽き 9.4% / **燃えたまま死 77.5%** / 燃えたまま決着 13.1%
    重ね掛け率              36.4%（第四波 44.1% で最大・第五波 28.0% で最小）

**P1（平均2回以上）は ×。棄却線 1.5 も下回る。層にしても積むものが無い。**
**P2（味方 > 敵）は ○**（0.62 対 0.52）。

**外れた原因は係数でも供給の周期でもない**——**燃えている駒の 77.5% が火より先に倒れる**ので、
**煽り直す機会がそもそも1回ぶんしか回ってこない。**
裏返しに、**上位10枚のうち煽られる率が高い2枚は、いちばん死なない2枚**
（被弾強化の泥人形ムドが **51.7%**・城塞の重装兵 HP145 が 43.2%）。
**表は総数で並べてあるので「全駒の最大」ではない。**

**「同じ総数」でも区間の最大値を見ないと機構が違う**——煽り 8.3 回/戦 の行が2種類ある:

    速攻 (ボルグ×ムド)      煽り 7.15  平均 1.23  **最大 5**  ← 同じ駒に積み直している（毎ターン供給）
    追撃×死 (ハギ×リィカ)   煽り 8.29  平均 0.92  **最大 1**  ← 9体への一斉着火が蘇生で2巡目に重なっただけ
    鱗 (ウロ×死軸)          煽り 0.00  平均 0.00  **最大 0**  ← 蘇生役がいないので破裂は1回きり

**段2 —— 盤面ルールの対称性。** 第132期の `YokeLedger` と同じ形を渇き・粛に当てた
（**逆位は `Stages` に保持者 0 体**なので対象外。予測 P5 ○）。
**該当の波だけを分母にした 1戦あたりの実測**:

| ルール | 非対称の種類 | 敵 | 味方 | 味方の取り分 |
|---|---|---|---|---|
| **軛**（第四波） | **数値**（敵の打点が全部 25 以下） | 7.68 回 / 174.33 点 | 0.07 回 / 0.35 点 | **0.2%** |
| **渇き**（第三波） | **配置**（敵の回復役は第二波にいる） | 0.00 回 / 0.00 点 | 3.85 回 / 38.69 点 | **100.0%** |
| **粛**（第二波） | **在庫**（5本を持つ敵が 0 枚） | 0.00 回 | 1.76 回 | **100.0%** |

**軛の数字は第132期の報告値（7.68 / 174.3 対 0.07 / 0.4）と小数第1位まで一致した**
——別の期の器具で取り直して同じ値が出たので、読み方は再現している。

**3つとも実質プレイヤー専用の税だが、非対称の理由が3つとも違う。**
とくに**渇きが「配置」だったのは予測の外**——指示書は「敵に回復役がいるので敵のほうが得かもしれない」と
書いていたが、**回復役（施しの司祭長・`Alms`）はいる。ただし第二波にいて、第三波の渇きの祭司と一度も同席しない。**
**`board-rule-units.md` の合否テスト1 は、波ごとに数えないと通ってしまう。**

**課税の総量と解けやすさは別の軸**（保持者が落ちるターン・解けた戦の割合）:

    軛    7.02T / 76.2%   ← いちばん遅く解けるのに、敵の取り分 99.8%
    渇き  4.20T / 87.8%   ← いちばん早く解けるのに、味方の取り分 100%
    粛    4.17T / 80.6%

**粛の内訳**: 止まった 0.44 回/戦 のうち **70% が棘（カド）1枚**。
**譲渡（トモ・第110期に足された5本目）は 0.00 回**で、`compare` 61 行では一度も止められていない。
**敵は `CanActOutOfTurn` を1度も問い合わせない**（2.05 対 0.00）。

**渇きの内訳**: 封じられた量の 72% が **ゴルム（4.84）とリィカ（2.15）**で、
**どちらも説明文のどこにも回復と書いていないのに回復する駒**——第39期の則が課税される側でも出た。
**要求量 12.94 に対し実効量 9.67**（満タンで元から入らなかったぶんが 25.2%）。

**走査で見つけた取り残し2件**（どちらも**規則を足したのに説明文が増えていない**・第122期の逆側）:
`CLAUDE.md` の「ターン外の窓口は**4本だけ**」は**第110期の譲渡で5本**、
「`ctx.Heal` は**9経路**」は**第118期の `Regen` で10経路**。**この期に本文を直した。**

**器具の反省**: Q0-1 で `BattleEngine.cs` 全体を `relit` で走査したら
**第134期に足した帳簿のコード自身が 9 件当たって表が汚れた**
——**第123期「走査対象が走査する側のコード自身なら、当たるのは自分」をそのまま踏んだ。**
`Ignite` の本体だけに切って直した。自己検査でも同じ形を2件踏んでいる
（`PickOne` の走査が診断自身の文字列に当たる／`RuleFallTurn` の分岐走査が計数の内輪の読みに当たる）。

**第135期の候補**: **層は落ちた**（積むものが無い）。**残るのは「区間の寿命を伸ばす」**
——77.5% が「燃えたまま死」で閉じるので、**濃さより先に寿命が効いている**。
盤面ルールの対称性は**直し方が3通り要る**（数値・配置・在庫）。

---

### 第135期 —— `parry`（ガルドは何で死んでいるか、と受け流し）

**盤面は1ビットも動かない。** 段1 は計数のノブ（`HarmRule.Census`・既定 `false`）だけ、
段2 の受け流し（`ParryRule.Uses`）は既定 0 で残置。`compare` 305 セルは 0 件差分。
本文は design/PHASE135_PARRY.md。

**ガルドは範囲で死んでいない。** `compare` のガルドを含む 35 行 × 第2〜5波 × seed 0..199 ＝
28,000 戦で、受けた総量の **63.0% が単体**・**致命打も 57.1% が単体**（範囲は 29.6% → 32.9%）。
**ずれは −5.9pt しかない**——第134期「同じ総数が3つの違う構造から出ていた」の逆側。
**範囲の取り分の差は 4 枚が ±5.5pt 以内**（ガルド +3.3 / ゴルム −4.4 / カド −5.0 / ドハ +5.3）で、
**ワタ（範囲 62.8% → 91.9%）とセッキ（単体 12.6% → 28.6%）だけが別物**になる
——**セッキのずれの正体は `lethal: false`**（総量の 49.1% は同席するゴルムの吸いで、原理的に殺せない）。

**ガルドの生存も「HP ÷ 被弾/T」でほぼ説明される**（第126期の則）——
被弾/T 29.0・紙のT 3.45・実測 3.48（比 1.01）。**特性はここに1ビットも効いていない。**

**一文「その傷のぶん強くなる」は死文ではなかった。** 指示書 §0-1 の前提が誤っていた
——あの記述は `GatherRule`（傷の引き取り）のもので、一文の実装は
`RedirectGainTrait.OnDamaged` の `self.AtkBonus += dmg / 2`。
実測で**庇い 1.36 回/戦・攻撃力 +7.98/戦**。死文なのは引き取りのほう（**0.00 回/戦**）で、
**しかも 0 なのは規則のせいではない**——巻き込み則も同時に降りたので
**隣接する味方に傷が載る経路そのものが消えている**（donor あり 0.00 回/戦）。
**規則を戻しても 0 のままである。**

**庇いの機会は3通りに読める**——成立 **1.47** / 判定を振られた **2.92** / 判定＋範囲で消えた **5.71** 回/戦。
**機会の半分は範囲で消えている**（`範囲で機会なし` 2.79 が `庇いの判定` 2.92 とほぼ同じ）。
成立率は **50.2%** で `GuardianTrait.RedirectPercent` = 50 とぴたり合う。

**`Stoic` が弾いた回復は 2.92 量/戦・0.15 回/戦、隣へ流したのは 0.56 回/戦（宛先 1.26 体）。**
素体対照（`Stoic` を外したガルド）では **勝率が 2.0pt 下がる**
——ただし対照の「他の味方に届いた強化+弱体」は**足した量**なので、符号の断定はしない（規約 (G12)）。

**「1発を丸ごと無効化する」機構はロスターに1枚も無かった**（Q0-8・14 件を全数）。
**受け流しはロスター初の無効化**で、`ApplyDamage` の出口（`lethal: false` のクランプの直後・
猶予の直前）に置いた——**破片・肩代わり・据え・散開・萎縮より後ろ、軛より前。**
**確率ではなく回数**にしたのは、確率だと弾いた量が敵の火力に比例して難易度カーブを平坦化するため
（真の反射を却下したのと同じ理由。**この判断はリポジトリ内に1件も記録が無かった**ので Q0-9 に残した）。

**測って採用しなかった。** N は §3-2 の決め方どおり実測の中央値（**1**）から引いて **N = 1 / 2**。
台は `parry scan` の 11 候補から**素体の勝率 40〜95% の帯**に入った4台のうち、
**庇いの成立回数がいちばん離れている2台**（A 1.61 / H 2.93 回/戦）。

    条件1 生存T +1.0T 以上   ×  8 版すべて。最大 +0.67T
    条件2 経路が致命打と重なる ○  V1 は 100% 単体 / V2 は 77〜86% 単体 ＋ 13〜16% 貫き
    条件3 味方の生存Tも上がる  ○  +0.05〜+0.34T

**§5-2 は「1 と 2 の両方」を要求するので段3 へは進まない。**
**機構が弱かったのではない**——台A の V2 N=2 は**勝率 67.6% → 82.2%（+14.6pt）**・残存 1.62 → 2.28 で、
**代金を1つも付けていない版でこれ**である。
**届かなかった理由は器具の側**——紙で引くと +1.0T に要るのは **N ≈ 2.2**（台A）で、
**N の決め方が許す上限 2 のすぐ外**にあった。実測 ÷ 紙 は 0.69（紙が上限として働いた）。

**踏んだ穴が2つ**（どちらも `CLAUDE.md` の既存の則）:
**(1)** `範囲で機会なし` を `pattern != Single` の分岐の中に置いたら**貫きが丸ごと落ちて 0.63 になった**
（正しい位置は攻撃型が決まった直後・貫きの早期リターンより前。実測 2.79）。
**(2)** 段2 の台を最初「B 前が硬い」にしたら**勝率 0.0% の床**で、版の差が定義上 0 にしかならなかった
（第61期。`parry scan` を足して 11 候補から帯の内側を選び直した）。

**作業中、ローカルの `master` は第131期のままだった**（`origin/master` から 15 コミット behind）。
指示書が引いている第132〜134期は `design/` に無く、**測定と判定は指示書の本文だけを根拠にしている。**
**後からマージして Q0-9 / Q0-10 を測り直した**——`docs/harm.md` は 1 バイトも変わらず
（第132〜134期は3期とも測定だけ）、**変わったのは Q0-9 のファイル数（233 → 239）と
Q0-10（分類の無い札 1 → 0。`GradeStep` の穴は第132期 段0-a に既に直っていた）だけ。**
**指示書を読んだ直後に `git fetch` してローカルが behind でないかを見ること。**

**気づいたが手を付けなかったこと**: **ガルドの肩代わりの見返り（+7.98 攻撃力/戦）が画面に1件も出ない**
——第125期「強化は `StatusGain` を1件も通らない」に、別の経路で同じ結論に着いた。

### 第136期 —— `wall`（ガルドを壁にする: 確実な庇い・受け流し・中継）

**この期は盤面を3段で動かした**（段0 は doc とコードの残置だけで 0 件差分）。
段1 `GuardianTrait.RedirectPercent` 50 → 100（`Roll` は残す。殉教者は別勘定）／
段2 受け流しの本採用（`ParryRule.Default` = Uses 4 / Any。構え＝`CanAct(Attack)` 偽で毎ターン頭に在庫を N へ戻す。
庇って身に受けるたび在庫 +1 ＝ `RedirectGainTrait.Reward` を `GuardianTrait` だけが上書き。攻撃力は上がらない）／
段3 中継（Relay true。`Trait.SurrendersTurnIn` で規則を読む）。**engine に規則は 1 本も足していない。**

**Phase 0 で決まったこと**: 「構える」を `Charge` / `Skill` で書くと `IdleTurn` が立たない（engine のコメントに明記）ので、
段3 が書けるのは `CanAct` 偽の形だけ。その経路に特性側のフックは無いので補充は `OnTurnStart`。
ガルド（速4）より遅い敵は 5 波の延べ 23 体のうち 3 体（第一波の速 3）なので、
「その手番で満タン」と「次のターン頭で満タン」は供給としてほぼ同値。
`Stoic` は号令を隣へ流す（`SupportTargets`）ので**ガルド自身は号令から 1 も得ない**。据えは `IdleTurn >= Turn` を読むが、
`IdleTurn` はガルドの手番（速4）に立つので**そのターンの被弾にはほとんど乗らない**。
群は行の中身から機械で引いた: ガルドを含む 35 行 ＝ 群A 28 ／ 群B（ガンのみ）6 ／ 群C（ガン＋バン）1、交差帯にもう 1 行。
指示書の 34 / 5 / 2 とは一致しない。

**実測**（`compare` 61 行）: 段0 100 / 71.1 / 76.4 / 67.2 / 43.8 → 段1 100 / 72.4 / 77.3 / 67.5 / 45.0
→ 段2 100 / 78.9 / 88.3 / 80.2 / 55.3 → 段3 **100 / 79.8 / 88.4 / 80.7 / 56.8**（主判定19行 第5波 42.3 → 50.7）。
ガルドを含まない 26 行は 3 段とも 0 セル。情報セル 165 → 122（主判定 56 → 39）・第5波 95% 超 7 → 16。
ガルド: 生存 3.48 → 3.08（段1）→ 11.36（段2）→ 10.97T（段3）、死亡率 84.3 → 9.2%、被弾 100.8 → 31.6/戦、
受け流し 25.2 回/戦（被弾の 91.0% を無効化）、構えで戻した 24.0 対 庇いで戻した 0.32（振り替えはほぼ働かない）。
ゴルム 4.94 → 5.73T。ガルドを含む行の決着T 5.72 → 6.78・全滅勝ち 13.1 → 3.7%。
群A は段2 で +20.9pt・中継で 0 セル。群B は段2 +3.6 → 中継 +10.7pt（ガルドの攻撃力の到達点 0.00）。
群C は +8.1 → +12.3pt（`縛め収入型` 第5波 54.0 → 70.5、`強化×手番` 48.0 → 90.0）。

**N の決め方（§5-2）は「1戦の総量」から引いた**（33.4 点/T × 1.84T ÷ 12.6 点 ＝ 4.90 → 4/5/6）が、
実装は毎ターン満タンに戻るので **N=1 で既に +1.89T、N=2 で +5.03T、4/5/6 は飽和**（5 と 6 は 1 セルも違わない）。
採ったのは指示書どおり最小介入の 4。**N=2 でも §6-1 の 3 条件は通る**——`ParryRule.AdoptedUses` を 2 に戻すかはポンの判断。

**予測の当否**: P0 ○／P1 向き ○・量 ×（成立 2.64、被弾 102.9）／P2 半分（平均は上がったが −10pt が 3 セル）／
P3 ×（N は 5。ゴルム越えは N=1）／P4 ○（符号ではなく動く群が分かれた）／P5 ○（回復 0.0 のまま・ゴルムの倍）。

**気づいたこと**: 受け流しは `OnDamaged` を走らせないので**棘の反撃・軋み・被弾強化がガルドの隣で起きない**。
`反撃 (ヒサ×カド)` 第3波は 100 → 0——殴れる駒が盤上に無くなり、残った戦斧兵の薙ぎを毎ターン受け流して 30 ターンの膠着で負ける。
61 行で −10pt 以上が 8 行（主判定は `逆しま` 第5波 −31.0 の 1 行）。規約 (G2) では 7 行が制約・1 行（`逆しま+後備え`）が
「壊れ」判定だが、その駒（クビ）の「他の行」は 1 行しかなく、それもガルドを含むので分解が働かない。
`docs/crossing.md` の Q1 は 9/12 → 5/12（`強化×手番` は段2 で 1/5 に落ちた。中継は動かしていない）。
`docs/rules.md` の既定値の列で動いたのは `ParryRule` だけ。`checkup check` は最後まで走る（`Parry` を分類表に足した）。
`MinusText` に「自分からは決して攻撃しない」を足した（ポンに要確認）。`DemoApp` は触っていない（受け流しの残り回数は画面に出ない）。

**追加（N=2 へ）**: N=4 は効き過ぎ（死亡率 9.2%・無効化 91%）だったのでポンの判断で `AdoptedUses` を 2 へ戻し、`docs/` を回し直した。
61 行 100 / 76.8 / 84.5 / 72.0 / 50.6（主判定 第5波 48.5・余裕 +15.3pt）、情報セル 139 / 45、第5波 95% 超 9 行。
ガルド 生存 7.96T・死亡率 42.0%・無効化 68.7%（13.48 回/戦）、構えで戻した 10.66 対 庇いで戻した 1.71（N=4 の 0.32 から働くようになった）。
群A +9.4pt・群B +9.5・群C +9.1。−10pt 以上は 8 行のまま（`逆しま+後備え` が抜け `突き返し` が入る）。
**`反撃 (ヒサ×カド)` 第3波の膠着は消えない**（100 → 0）——敵 1 体・1 発/ターンの薙ぎは毎ターン満タンに戻る在庫に N=1 でも届かない。
被弾強化の駒の燃料はほとんど減っていない（ムド 68.0 → 64.5、ウツ 45.0 → 44.0、カド 62.7 → 60.0 被ダメ/戦・全5波・ガルド同席行）。

### 第137期 —— `shard`（砕けの鍵を自前にする・ヒビ）

**反射を却下した判断**（第135期 Q0-9 で `design/` 233 ファイルを走査して 0 件だった件。
Web 側の設計メモにしか残っていなかったので、第137期 Phase 0 Q0-7 でここへ書き戻した）:

> **反射（被ダメージ無しで返す）は採用しない。** 棘鎧のカドが既に「正しく設計されたカウンター」で、
> 反射はその劣化ではなく暴走版になる。決定的な違いはスケール元——カドの反撃は**自分の攻撃力**の2倍で
> 自己完結するが、真の反射は**被ダメージの一定%**なので敵の火力に比例し、難易度カーブを平坦化する。
> さらに反射は被弾しないのでカドのHPコストを踏み倒す。
> **第136期の受け流しを「無効化 × 回数制」にした根拠でもある**（被ダメージ比例にしなかった理由）。

**測って採用しなかった**（`ShatterRule` は `Passive` 既定で残置・**盤面は1ビットも動かない**）。
`docs/balance.md` は 305 セル 0 件差分、`docs/` で動いたのは `rules.md`（ノブ2本）と
`harm.md`（**ヒビの行を足した**）だけ。器具は `shard`（`BattleSim/Shard.cs`）。

**採否**: 条件1（物差し）× ／ 条件2（特異性）○ ／ 条件3（鍵の入れ替え）×。
掃引点（cost 1/2/3）では **9 台・行のうち 7 つで下がる**（最大 `台2 素通り` −32.4pt）。
V1 が勝ったのは `台4 肩代わり` cost=2（+1.3）と `弱体×被弾` cost=2,3（+1.5 / +4.1）の 2 つだけ。

**Q0-3 の数え直し**: 指示書の「ヒビを含むのは3行」は `compare` 61行に限れば正しいが、
**交差帯 12行の 2 行（`弱体×被弾 (ドハ×ワタ×ヒビ)` / `破片×被弾 (ヒビ×ウロ×セロ)`）を数え落としている。**
測れる既存行は **3 ではなく 5**。

**Q0-1 の実測**（ヒビを含む 5 行 × 第2〜5波 × seed 0..199）:
発火 2.71 回/戦 ／ 配布 33.5（1体あたり 8.4）／ 吸った 33.9 ／ **ヒビ被弾 61.8・生存 3.24T・死亡率 94.2%**。
**`ShatterTrait` のコメントの「1戦の被弾110 / 1体あたり30弱」は現行では成り立たない**（3分の1以下）ので書き換えた。

**Q0-5 の答え**: HP1 では `lethal: false` のクランプが `amount` を 0 にして即 return するので、
**「払えなくなったら供給が止まる」が規則を1本も足さずに出てくる。**

---

**鍵を「敵依存」から「自前」に変えると、供給の分母が「敵が振る回数」から「自分が生きたターン数」に変わる**（第137期）。
**敵の範囲は薙ぎ・貫き・全体で1ターンに複数回飛ぶので、"遠い鍵" のほうが発火が多かった**
——範囲の厚い台では **V0 の 発火 ÷ ヒビ生存T が 1.43〜1.51 回/ターン**（台1 3.25/2.25・台2 4.54/3.00・台3 2.50/1.75）。
一方 `OnTurnStart` の自前の鍵は**構造的に 1 回/ターンが上限**で、しかも分母が
**ヒビの生存T（平均 3.24T・死亡率 94.2%）に固定される。**
実測で 発火は 3.25 → 1.97 / 4.54 → 2.25 / 2.50 → 1.25 と落ち、配布もそのぶん落ちた。
**第129期の密度表（自前 ×0.95 が最良・標 ×0.55 が最悪）は「生存Tあたりの発火」の話で、
そこには "1ターンに複数回来る鍵" という列が無かった。**

**「1戦の総量 ÷ 何か」で強度を引く式は、実装の発火周期を勘定に入れていないと桁を外す**（第137期・第136期の裏返し）。
掃引の中心は「現行の1体あたり配布 8.4 ÷ ヒビ生存T 3.24 ＝ 2.58」から `SelfCostPerTurn` **2** と引いたが、
**物差し 1.0 に要るのは cost 6〜10 で 3〜5 倍**（台1 は cost=10 で 1.28・cost=16 で 1.70、Δ +16.6 / +27.3）。

    第136期  在庫が毎ターン戻る機構   → 1戦の総量から引いた N は **過剰**（N=5 の導出に対し N=1 で目標達成）
    第137期  発火が1ターン1回に落ちる → 1戦の総量から引いた cost は **過小**（中心 2 に対し 6〜10 が必要）

**どちらも同じ穴**——式が「新しい版も現行と同じ回数だけ発火する」を暗黙に仮定していた。
第135期は「線が器具の上限の**すぐ外**」で偶然に見えたが、**第137期は桁が1つ違うので構造的である。**
**cost 6〜10 は採らなかった**（§5・規約 (G16)。結果を見てから掃引点を広げるのは第64期が禁じた緩めに当たる）。

**採否の物差しを立てるときは、その物差しを対照（V0）に当ててから線を引く**（第137期・規約 (G11) の物差し版）。
この期は「ある駒が編成に加わる価値は、少なくともその駒が受ける被弾を上回らなければならない」を立てたが、
**現行の砕けも 1.0 を1つも超えていない**（V0 の物差しは 0.23 / 0.49 / 0.80 / 0.83、5行合計 **0.55**）
——条件1 は「**現行が一度も超えたことのない線を新版に超えさせる**」要求だった。
**判定は事前の線どおり × のまま記録した**（第64期）。
**原因は分子と分母の枚数が違うこと**——分子は味方4枚に配った破片の合計、分母はヒビ1枚の被弾で、
**「1枚の代金 対 4枚の利益」を 1.0 で比べている。** 次に使うなら枚数で割るか、分母を編成の総損失にする。

**二値で読む機構の「稼働率」と、その通貨の「総量」は逆を向きうる**（第137期）。
自前の鍵はウロが砕けから受け取る量を **11.2 → 2.0（−82%）**に落とすのに、
**纏い率は 47.4% → 62.9% へ上がる**——「毎ターン少量」なので在庫が切れている時間が短くなり、
ウロの読みは `Armor > 0` の**二値**（`ModifyPattern`）なので**稼働率だけ見れば改善している。**
それでも勝率が落ちるのは、破片のもう半分の値打ち（**ダメージを吸う量**）が一緒に落ちるから。
**第114期「強化の価値は積んだ量ではなく受け手が振った回数で決まる」の逆側**で、
こちらは**回数が増えて量が落ちた。稼働率の改善を機構の改善と読んではいけない。**

**`LastActiveTurn` を版どうしで比べるときは決着Tで割る**（第137期・規約 (G6) がそのまま効いた例）。
予測5（破片が先に吸えば受け流しの在庫が減らずガルドが長持ちする）は **×** だったが、
**決着Tを併記するまでは ○ に見えていた**——ガルド生存T は V0 6.98 → V1 7.70 と伸びるのに、
**`生存T ÷ 決着T` は全版 0.72〜0.74 で一定**（決着T が 9.46 → 10.36 と伸びただけ）。
**ガルド被弾/戦 は 101.1 → 100.5 でほぼ不動**で、破片と受け流しは**食い合っていない。**

**符号の違う2つの効果を1つの比に入れると、期待値を隣に並べるまで読めない**（第137期）。
`自弁 ÷ 請求` は **脆弱（+50%）× HP1 のクランプ（−）× 肩代わり（−）**の積なので、
**脆弱のみの期待値を隣の列に出して初めて分解できる**
——台4（ゴルム）74.0% 対 期待 150% ＝ 肩代わりが半分持っていく／
`弱体×被弾`（ドハ）98.5% 対 150%／`仇討ち×砕け` 123.7% 対 133.3% ＝ **ゴルムがいてもほとんど肩代わりしない**。
**cost=1 では脆弱の期待値そのものが 100%**（`dmg + dmg / 2` の**整数**切り捨て。第126期）。

**段1 のローカル台は、指示書の構成のままでは床に落ちることがある**（第137期・第133期の則の適用）。
指示書 §4-3 の台1（ヒビ ＋ ウロ ＋ **死軸**）は V0 の第2〜5波平均が **33.0% ＝ 床**だった。
差し替え候補を 5 通り測って **ムド前1 の版（65.0%）**を採った
——**副産物として破片の出どころが砕けだけになり、`ShatterSoaked` の帰属が濁らなくなった**
（死軸を入れると鱗の供給が砕けと味方の死の2本になる）。
**「台を作る条件に V0 の勝率 40〜95% を入れる」を、指示書に書いてある台にも当てること。**

**採らなかったが、いちばん効いたのは「差し替え」ではなく「足す」**（第137期の持ち越し）。
`V2 両方`（受動 ＋ 自前）は掃引点の中で **9 台・行すべて非負**（`台2 素通り` +24.1・`台1 変換` +13.4・`台4` +12.4）。
指示書は `V2` を「対照。採用候補ではない（どちらが効いたか分離できない）」としたが、
**V0 と V1 を両方測った後では `V2 − V0` が「受動を残したまま自前を足したぶん」として分離できている。**
**この期の問いは「差し替え」だったので採らなかった。** 測り直すなら**物差しの分母を先に直すこと。**
