# 第114期 —— 灯の濾し（W2）を採り、席を情報セルで取り戻した

> **採った版**: `LitFilter.ActingNow`（W2・動的な濾し）を `TaillightRule` の既定にした（W0 / W1 はノブで残置）。
> **採った席**: `灯×薙ぎ (トモ×ドルガ)` の **後1 と 後3 を入れ替えた**（旧 後1 トモ / 後3 ボルグ → 新 **後1 ボルグ / 後3 トモ**。5枚組は据え置き）。
> **5セル**: `100.0 / 99.5 / 98.5 / 74.0 / 60.5` → **`100.0 / 99.5 / 93.5 / 96.5 / 61.0`**（第2〜5波平均 83.1% → **87.6%**）。
> **情報セル**: この行は **4 → 4**（W2 で一度 3 に落ちたぶんを席で取り戻した）。**61 行の合計は 164 → 164 で不変。**
> **`Baseline`**: 主判定19行 **100.0 / 72.5 / 72.1 / 68.6 / 41.3**（**5期続けて1ビットも動かない**）／全61行 100.0 / 69.5 / **75.7** / **63.6** / 42.9。歯止め 33.2% との余裕は **+8.1pt**。

---

## 0. この期でやったこと

| | 内容 | 結果 |
|---|---|---|
| (T1) | `TaillightRule.Filter` の既定を `SupportOnly` → `ActingNow` | **1行**。`compare` は `灯×薙ぎ` の3セルだけが動く |
| (T2) | **本体**: W2 での席の再判定（`lit seat w2`・120通り → 20候補） | **差し替え**（情報セル 3 → **4**・平均 −3.6pt） |
| (T3) | `docs/` 10ファイル再生成・`Baseline`・情報セル合計 | 差分は **9 ファイル / すべて `灯×薙ぎ` の行だけ**（`units.md` は 0） |
| (T4) | 規約 **(G15) / (G16)** を起こす | **別コミット**。規約は 14 本 → **16 本** |

**新しい機構は1つも作っていない。** `BattleCore` で動いたのは
**既定値の1行**（`Traits.cs`）と**席の定義**（`Presets.cs`）だけで、**機構のコードは1文字も動いていない**
（残りは既定が動いたことに合わせた doc コメント3箇所。自己検査 (a) を参照）。

---

## 1. (T2) 席の再判定 —— 表A

### 1-1. 線と採る条件（**測る前に固定した。指示書 §1 (T2)**）

    線   : 狙（ガルドが前列 / セッキが後列）○ **かつ** 情報セル 2 以上
    採る : 情報セル **4 → 3** の順に、その段の中で平均が最上位。
           出発点との差が 5.0pt 未満なら**情報セルの多い側**
    帯   : 粗探索 seed 0..49 → 追試 **seed 0..199（帯A）**。情報セルは帯A で数える（規約 (G14)）。
           帯B（200..599）は安定の確認として併記するだけ

**狙を線にも採る条件にも掛けた**（規約 (G16)。第113期に食い違って `後衛特化+後備え` の 11 通りが全部落ちた反省）。
実測では **候補 20 通りのうち情報セル 2 以上が 20 通り、うち狙で落ちたのは 3 通り**（狙 ○ は 17 通り）。
**この期は狙が結論を変えていない**——狙 × の 3 席（追順 7 / 8 / 19 位）はすべて情報セル 4 だが、
その最上位（85.0%）は採った席（87.6%）より低い。**「掛かっているが効かなかった」ことも記録する。**

### 1-2. 候補（W2・帯A）

| 情報セル | 狙 ○ の席数 | その段の最上位 |
|--:|--:|---|
| **4** | **4** | **追順 3 位・87.6%・−3.6pt** ← 採った |
| 3 | 4 | 追順 1 位・91.2%・±0.0（＝出発点） |
| 2 | 9 | 追順 2 位・91.1%・−0.1pt |

**採った席**: `front1: のろまの巨兵ドルガ, front3: 廃棄聖騎士ガルド, center: 逸らしのソラ, back1: 焼け残りのボルグ, back3: 尾灯のトモ`
（帯A 87.6% / 情報セル **4**、帯B 88.8% / 情報セル 3。出発点との差は帯A −3.6pt・帯B −2.4pt）。

**落とした上位3席**:

| 追順 | 狙 | 情報セル | 平均(2〜5波) | Δ | 落とした理由 |
|--:|:-:|--:|--:|--:|---|
| 1（出発点） | ○ | 3 | 91.2% | ±0.0 | 情報セル 3 < 4 |
| 2 | ○ | 2 | 91.1% | −0.1 | 情報セル 2 < 4 |
| 4 | ○ | 2 | 87.4% | −3.9 | 情報セル 2 < 4 |

> **第113期の `lit seat` が出す判定（「据え置き」）と結論が違うのは、規則が違うからである。**
> あちらは**平均が最上位で、差が 5.0pt 以上あれば差し替え**——情報セルは「2 以上」という門にしか使わない。
> 第114期は**情報を減らす変更の埋め合わせ**が目的なので、**情報セルを先に見る。**
> 診断には第113期の判定を残したまま、**第114期の段を下に足した**（`lit seat` の出力の最後）。

---

## 2. 表B —— 採用前後の5セル（版 × 席の 2×2）

| | 旧席（後1 トモ / 後3 ボルグ） | 新席（後1 ボルグ / 後3 トモ） |
|---|---|---|
| **W0**（第108〜113期の既定） | 100.0 / 99.5 / 98.5 / 74.0 / 60.5 —— 平均 **83.1%**・情報セル 4 | 100.0 / 99.5 / 93.5 / 81.5 / 54.5 —— 平均 82.2%・情報セル 4 |
| **W2**（第114期に採用） | 100.0 / 100.0 / 99.5 / 98.0 / 67.5 —— 平均 91.2%・情報セル **3** | **100.0 / 99.5 / 93.5 / 96.5 / 61.0 —— 平均 87.6%・情報セル 4** ← 現行 |

    版の効き（席を固定）  旧席 +8.1pt ／ 新席 +5.4pt
    席の効き（版を固定）  W0 −0.9pt  ／ W2 −3.6pt

**情報セルを 4 に戻す代金は −3.6pt**（W2 の効き +8.1pt のうち 44%）。
**それでも採用前（W0・旧席）より +4.5pt 高い。**

**`docs/balance.md` で実際に動いたセルは 3 つ**（第3波 −5.0 / 第4波 +22.5 / 第5波 +0.5）。
**第1・2波は 100.0 / 99.5 のまま同じ値**なので、**305 セル中 302 セルが 0 件。**

**(G9) の注意（拒否ではない）**: 第五波が 95% を超える行は **7 行 → 7 行**で増えていない。
拒否権 (1) 主判定19行の第五波 41.3%（歯止め 33.2 に対し +8.1pt）・
拒否権 (3) 主判定19行のセルは **1 つも動いていない**（この行は `Baseline.PrimaryRows` に無い）。
**(G1)(G2) の壊れ**——`compare` 61 行で −10.0pt 以上落ちた行は **0 行**（最大の低下はこの行の第3波 −5.0pt）。

### 2-1. 盤面で何が起きたか（`docs/pulse.md` / `docs/ablation.md`）

| | 旧（W0・旧席） | 新（W2・新席） |
|---|---|---|
| ドルガ 振/T ・ 与ダメ | 0.49 ・ 195 | **0.40 ・ 153** |
| ボルグ 振/T ・ 与ダメ | 0.83 ・ 142 | **0.93 ・ 166** |
| 味方の総与ダメ（敵） | 369 | 364 |
| 撃破/戦 | 3.97 | **4.06** |
| フル編成（全波通算） | 86.5% | **90.1%** |
| トモを抜いたときの差 | −45.4pt | **−60.8pt** |
| ソラを抜いたときの差 | −35.1pt | **−15.1pt** |

**総与ダメはむしろ 5 点減っているのに、撃破は増え勝率は上がる。**
灯が**休み番のドルガ**から**毎ターン振るボルグ**（と偶数ターンのソラ）へ回った結果で、
**「同じ量をどこへ積むか」だけが変わっている**——冒頭の一般則の、この期でいちばん直接の証拠。
**トモを抜いたときの落差が 15pt 増え、ソラの落差が 20pt 減った**のも同じ現象の裏側
（ソラが灯の受け手に加わったので、ソラ自身の素の出力よりトモの配り方のほうが効くようになった）。

---

## 3. 表C —— `docs/` の差分（必須2）

| ファイル | `git diff --numstat` | 中身 |
|---|---|---|
| `balance.md` | 1 / 1 | **`灯×薙ぎ` の行だけ**（3 セル） |
| `units.md` | **0 / 0** | 駒の定義を1つも変えていない |
| `chain.md` | 1 / 1 | 同じ行（連鎖 2.58 → 2.43・決着 4.8 → 4.9） |
| `ablation.md` | 6 / 6 | 同じ行（見出し ＋ 5 枚。席順が入れ替わる） |
| `pulse.md` | 6 / 6 | 同じ行 |
| `engage.md` | 5 / 5 | 同じ行（突破率(2部隊) 95.5 → 99.5%・3部隊の期待突破数 2.7 → 2.8 体） |
| `layout.md` | 12 / 12 | 同じ行（`現行` が 1位 → **7位**。**勝つ席ではなく測れる席を採ったので当然そうなる**） |
| `reseat.md` | 20 / 20 | 同じ行（★現行 が追順 1位 → 7位） |
| `crossing.md` | 1 / 1 | **所要時間の行だけ**（交差帯 12 行は 1 セルも動かない） |
| `rules.md` | 2 / 2 | **`TaillightRule` の既定値の行が2つ**（ノブ一覧と型一覧）。他のノブの既定は1つも動いていない |

`audit`（生成物と現行の編成数の整合）は **ずれ 0 件 / 8 ファイル**。

---

## 4. 表D —— `Baseline` と情報セル

| 分母 | 第1波 | 第2波 | 第3波 | 第4波 | 第5波 |
|---|--:|--:|--:|--:|--:|
| **主判定19行** | 100.0 | 72.5 | 72.1 | 68.6 | **41.3** |
| 全61行 | 100.0 | 69.5 | 75.7 | 63.6 | 42.9 |

**主判定19行は第107期から5期続けて1ビットも動いていない**（第107・108・111・113・114期に動かした行は
どれも `Baseline.PrimaryRows` に入っていない）。**歯止め 33.2% との余裕は +8.1pt。**

| 情報セル | 0 | 1 | 2 | 3 | 4 | 合計 |
|---|--:|--:|--:|--:|--:|--:|
| 第113期 | 0 | 4 | 22 | 24 | 11 | **164**（平均 2.69） |
| **第114期** | 0 | 4 | 22 | 24 | 11 | **164**（平均 2.69） |

**分布ごと1行も動いていない**——W2 が奪った1セルを、席がそのまま返した。

---

## 5. 予測の答え合わせ（指示書 §2）

| | 予測 | 実測 | 判定 |
|---|---|---|:-:|
| 1 | 情報セル 4 の席が存在する | 狙 ○ で **4 通り**（最上位 87.6%） | ○ |
| 2 | 平均は下がるが差 5.0pt 未満 | **−3.6pt**（帯B −2.4pt） | ○ |
| 3 | 61行の情報セル合計は 164 のまま | **164**（分布も同一） | ○ |
| 4 | `Baseline` の主判定19行は不動 | **0 セル** | ○ |
| 5 | 全61行の第五波は上がり、**余裕が +8.1pt から増える** | 第五波 42.9 → **42.9**（+0.008pt）・**余裕は +8.1pt のまま** | **×** |

> **予測5が数えていなかったもの: 歯止めの分母。**
> 歯止めは第60期に**主判定19行の固定集合**へ移してある——`灯×薙ぎ` はそこに入っていないので、
> **この行が何 pt 上がっても余裕は原理的に1ビットも動かない。**
> 全61行のほうも、1 行の第五波 +0.5pt は 61 で割ると **+0.008pt** で表示の桁に出ない。
> **予測4（主判定は不動）と予測5（余裕が増える）は同時に成り立たない**——
> **同じ指示書の2つの予測が互いを否定していた**（第107期・第113期に続く「線と条件の食い違い」の親戚で、
> 今回は**線ではなく予測どうし**。規約 (G4) の「分母を必ず書く」を予測の側にも当てること）。

**外れたのは1つだけで、外れ方は「機構ではなく分母」だった。**

---

## 6. 自己検査

### 6-1. 必須4項目

| | 項目 | 実測 | 判定 |
|---|---|---|:-:|
| 必須1 | `compare` 305 セルと `docs/balance.md` | **既定（W2）の経路 0 件**。W0 を明示した経路は 2 件で、**動く行は `灯×薙ぎ` の 1 行だけ** | ○ |
| 必須2 | `docs/` 10ファイル再生成 | §3 の差分表（`audit` ずれ 0 件） | ○ |
| 必須3 | 触っていないノブの既定 | `docs/rules.md` の差分が **`TaillightRule` の2行だけ** | ○ |
| 必須4 | `ctx.PickOne` | **26 箇所**（第94期・第113期と同数。1つも足していない） | ○ |

### 6-2. この期固有

| | 項目 | 実測 | 判定 |
|---|---|---|:-:|
| (a) | `BattleCore` の差分が既定値1行と席の定義のみ | **機構のコードは1文字も動いていない。** ただし**既定が動いたことを書いた doc コメントが3箇所**（`LitFilter` の要約・`SupportOnly` の説明・`TaillightSkipStatic` の説明）——**「既定では走らない」と書いてあったので放置できない** | ○（注記つき） |
| (b) | W0 / W1 を明示した経路が第113期の実測と一致 | `lit run` の**副判定4台・門・帰属・対象の分布・Q1〜Q6 がすべて一致**（差分は主判定行のセルと自己検査 (a) の件数だけ） | ○ |
| (c) | 情報セルを数えた帯 | **seed 0..199（帯A）**。帯B は席の表の右端の列にだけ使った | ○ |
| (d) | 狙が線にも採る条件にも掛かっている | 出力に明記（線 20 通り → 狙で 3 通り落ちて 17 通り）。**掛かっているが結論は変えていない** | ○ |
| (e) | 規約節の本数が +2・既存の本文が逐語で不変 | 14 本 → **16 本**。既存 (G1)〜(G14) は**1文字も触っていない** | ○ |

> **(b) の器具の注記**: `lit` の副判定4台は**診断のローカル**で、**席は第111〜113期のまま凍結してある。**
> 第114期に動かしたのは `Presets` 側の席だけなので、**副判定のドルガ台と主判定行はもう同じ台ではない**
> （第111期のコメント「同じ5枚・同じ席」はこの期に書き換えた）。
> **凍結は意図的**——台を追随させると第113期に取った3版の実測が再現できなくなり、
> **「濾しの効き」と「席の効き」が混ざる。**
> **したがって第113期の帰属（W0 +46.0 / W2 +54.1pt）は旧席の値である。**

---

## 7. 結論

**W2 を採った。** 決め手は第113期の実測（潰れ/譲渡 0.402 → 0.000・帰属 +46.0 → +54.1pt）で、
**この期に測ったのは「その採用が測定器から奪う情報を、席で返せるか」だけ**である。返せた（情報セル 4 → 3 → 4）。

**代金は勝率 −3.6pt。** 情報セルを 4 に戻す席は、W2 の最強席より 3.6pt 弱い
——**「勝つ席」と「測れる席」は別物**（第50・64期）で、**この期は測れる側を買った。**
`docs/layout.md` で現行が 1位 → 7位 に落ちるのは、その買い物の領収書である。

---

## 8. 申し送り（第115期以降）

- **第115期の本命 (b)**: **「積んだ量 vs 振った回数」を強化軸の全書き手で測る**（ガン・クグ・ドハ・トモ）。
  この期の §2-1 が**同じ台の中でその対比を1つ出した**——総与ダメ 369 → 364（−5）で撃破 3.97 → 4.06（+0.09）。
  **器具は「書いた総量」と「受け手がその強化を乗せて振った回数」を並べる1つで足りる**（新しい機構は要らない）。
- **`lit` の副判定4台の席**（§6-2 の注記）。第115期に灯を測り直すなら、
  **凍結を解くか、解かずに新席の台を1つ足すかを先に決めること**——今は旧席で凍っている。
- **`反撃` 系 3 行（反撃・反撃改・逆しま改）は席では直せない**（第113期）。5枚組の差し替えでしか動かない。
- **`後衛特化+後備え`**（主判定19行・情報セル 1）は**狙を守る限り席では動かない。**
- **`README.md`（617,950 字）の分割**（第112期の形）／**`cross quality` の表示文字列**（判定は正しく表示だけ古い）。
