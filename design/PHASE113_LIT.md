# 第113期 —— 灯の対象選択（Phase 0 で「席の期」へ切り替えた）

> **Phase 0 の (P-4) が線（3 行以上）を満たしたので、指示書 §6 の分岐どおりこの期を席の期に切り替えた。**
> **(G14) の波及として `compare` 61 行のうち 2 行の席を差し替え、情報セルの総和が 161 → 164 に増えた**
> （`惨禍×被弾強化` 1 → 2 ／ `澱み喰い (グザ×ヴィオ)` 1 → 3。**5枚組は1枚も動かしていない**）。
> **灯の対象選択（W1 / W2）は採らない。第114期へ送る。** 実装（`LitFilter`）は既定 W0 のまま残置し、
> 3版の実測だけを参考として記録した: **Q1 W1 × / W2 ○ ／ Q2 W1 ○（±0.0）/ W2 ○（+8.1pt）／
> Q3 増（0.34 → 2.84 回/戦）／ Q4 ○（78.8% → 0.0%）／ Q5 ○（0 件）／ Q6 ○（3 枚）。**
> **W2 は主判定行で 帰属 +46.0 → +54.1pt と大きいが、この期の判定には使わない**（§6 の分岐の順序）。

---

## 0. この期でやったこと

| | 内容 | 結果 |
|---|---|---|
| §1 | Phase 0（(G14) の波及調査・(P-5)〜(P-7)）**戦闘 0 回** | (P-4) **3 行** → 席の期へ |
| §2 | **本体**: 情報セル 1 以下の 6 行の席を帯A で追試（`lit reseat`） | **2 行を差し替え**・4 行は据え置き |
| §3 | 灯の濾し（`LitFilter`）の実装と 3 版の実測 | **採らない**（第114期へ送る）。既定は W0 |
| §4 | 自己検査（必須4項目 ＋ (a)〜(f)） | 全項目 ○ |

**engine に規則を1つも足していない。** 足したのは
`BattleContext.CanActNow`（観測専用の静かな問い合わせ）と `Trait.NeverAttacksOwnTurn`（静的な札）だけで、
**既定（`LitFilter.SupportOnly`）では濾しのコードが1行も走らない**（自己検査 (b) が実測 0 件）。

**`Presets.Compare` は行数 61・5枚組ともに1つも変えていない。動かしたのは 2 行の席だけ。**

---

## 1. Phase 0 —— (G14) の波及調査（`lit phase0`・**戦闘 0 回**）

> **再現の注意**: `lit phase0` は `docs/balance.md` と `docs/reseat.md` を読むので、
> **席を差し替えた後に走らせると (P-2) は 6 行ではなく 4 行になる**（この期の差し替えが効くため）。
> 下の表は**差し替える前の値**である（差し替え後の再実行値は §5-2 に併記した）。

### 1-1. 情報セルの分布（帯A ＝ seed 0..199・第2〜5波）

| 情報セル | 0 | 1 | 2 | 3 | 4 |
|---|--:|--:|--:|--:|--:|
| 行数（差し替え前） | 0 | **6** | 21 | 23 | 11 |
| 行数（差し替え後） | 0 | **4** | 22 | 24 | 11 |

合計 **161 → 164**（平均 2.64 → 2.69）。

情報セルが 1 以下の 6 行:
`反撃 (ヒサ×カド)` ／ `反撃改 (ドハ×カド)` ／ `後衛特化+後備え`（**主判定19行**）／
`惨禍×被弾強化` ／ `澱み喰い (グザ×ヴィオ)` ／ `逆しま改 (クビ×ウツ)`。

### 1-2. (P-3)(P-4) —— `docs/reseat.md` から引いた代替席

| 行 | 現行の情報セル | 候補数 | 情報セル 2 以上 | 線を満たす | **うち狙○** |
|---|--:|--:|--:|--:|--:|
| 反撃 (ヒサ×カド) | 1 | 21 | 0 | 0 | 0 |
| 反撃改 (ドハ×カド) | 1 | 20 | 0 | 0 | 0 |
| 後衛特化+後備え | 1 | 24 | 19 | **11** | **0** |
| 惨禍×被弾強化 | 1 | 20 | 8 | **3** | 3 |
| 澱み喰い (グザ×ヴィオ) | 1 | 20 | 8 | **8** | 4 |
| 逆しま改 (クビ×ウツ) | 1 | 23 | 0 | 0 | 0 |

**(P-4) = 3 行**（線は 3 行以上）→ **席の期へ切り替え。**

> **器具の穴を1つ見つけた（この期の一般則）。**
> **(P-3) の線には狙（ガルドが前列 / セッキが後列）が入っていないが、採る側（§6 の
> 「情報セルの多い側を採る」）は既存の作法として狙を要求する。**
> 実際に食い違った——`後衛特化+後備え` の線を満たす 11 通りは**すべてガルドが後列**で、
> 狙を当てると 0 通りになる。**狙を (P-3) にも入れていれば (P-4) は 2 行で、
> 指示書の分岐は「記録だけして §2 へ進む」だった。**
> **線を引く条件と、採る条件は同じ集合で書くこと。**
> 第59期（条件節が分母の何割を選ぶか）・第107期（別の節が線を到達不能にする）に続く3例目で、
> 今回は**「線は緩く、採用は厳しい」向き**——結果として**測る対象は増えたが、採れたのは 2 行**だった。

### 1-3. (P-5) 濾しに掛かる駒の全数（**実装から derive**・`UnitCatalog.All` 52 枚）

| 駒 | 速 | (A) 恒久的に振らない | (B) 周期に攻撃が無い | (C) 休み番 | 支援拒否 | W0 | W1 | W2 |
|---|--:|---|---|:-:|:-:|:-:|:-:|:-:|
| 尾灯のトモ | 3 | | `Skill` | | | 候補 | **外** | **外** |
| 廃棄聖騎士ガルド | 4 | | | | ○ | **外** | **外** | **外** |
| 棘鎧のカド | 4 | `Immobile` | `Skill` | | | 候補 | **外** | **外** |
| のろまの巨兵ドルガ | 6 | | | ○ | | 候補 | 候補 | **外** |
| 火選りのヒヨ | 6 | | `Skill` | | | 候補 | **外** | **外** |
| 継ぎ当てのノノ | 6 | | `Skill` | | | 候補 | **外** | **外** |
| 追い打ちのハギ | 7 | `Pursuer` | | | | 候補 | **外** | **外** |
| 澱みのミオ | 8 | | `Skill` | | | 候補 | **外** | **外** |

(A) 2 枚 ／ (B) 5 枚（トモ自身を含む）／ (C) 1 枚 ／ 支援拒否 1 枚。
**W1 が新たに落とすのは 6 枚**（トモは元から自分を対象にしない）、**W2 はそれに (C) の 1 枚**（そのターンだけ）。

> **灯の候補になりうる 51 枚（支援拒否のガルドを除く全数）のうち、W1 が落とすのは 6 枚 ＝ 11.8%。**
> **そのうち 4 枚が速6 以下**（トモ3・カド4・ヒヨ6・ノノ6）で、残る 2 枚はハギ7・ミオ8。
> **ロスター 52 枚のうち速6 以下は 25 枚**なので、**「遅い」と「灯が無駄になる」は同じではない**
> ——第110期の「遅い層に集まる『手番を使わない駒』に構造的に当たる」は、
> **層の全部ではなく層の一部**（速6 以下 25 枚のうち 4 枚）に当たっていた。
> **効いているのは枚数ではなく、その 1 枚が最遅かどうか**——実測でも、W1 が盤面を動かしたのは
> **ハギが灯の一意の宛先になっていたハギ台だけ**である（§3-1）。

### 1-4. (P-6) 灯の対象の予測（**戦闘 0 回**）と (P-7)

| 台 | W0 | W1 | W2 奇数T | W2 偶数T |
|---|---|---|---|---|
| **主判定** `灯×薙ぎ (トモ×ドルガ)` | ドルガ | ドルガ | ドルガ | **ソラ** |
| 副 トモ×ムド / トモ×ソム | ムド / ソム | 同 | 同 | 同 |
| 副 トモ×ハギ | **ハギ** | **ソラ** | ソラ | ソラ |

**§3 の実測は 5 台 × 3 版とも予測と一致した**（W2 の主判定行だけは、盤面が動いてドルガ/ソラ/ボルグの
3 枚に散る——予測が「奇数T ドルガ・偶数T ソラ」なので矛盾しない）。

(P-7) **候補が 0 人になる台は 5 台中 0 件。** ただし 0 人の経路そのものは
自己検査 (c) でローカル台（カド/ハギ/ノノ/ミオ）を組んで実測した——**W1 で灯 0.00・空振り 2.75 回/戦**。

---

## 2. 本体 —— (G14) の波及（`lit reseat`）

### 2-1. 採否の規則（**測る前にコードへ固定**）

    (a) 狙（ガルドが前列 / セッキが後列）を満たす        ← 既存の作法
    (b) 現行席との差（**第2〜5波の平均**・規約 (G10)）が 5.0pt 未満
    (c) そのうち情報セル（**帯A**）が最大。同値は 平均が高い側 → 粗順が小さい側
    (d) 情報セルが現行より多いときだけ採る（同数なら据え置き）

候補の作り方は `reseat` モードの写し（粗探索 seed 0..49 の全 120 通り → 上位20 ＋ 狙上位10 ＋ 現行）。
**帯B（200..599）は安定の確認として併記するだけ**——情報セルは帯A で数える（(G14)）。

### 2-2. 結果（6 行中 **2 行**を差し替え）

| 行 | 判定 | 情報セル | Δ 平均(2〜5波) | Δ 帯B | 席 |
|---|:-:|--:|--:|--:|---|
| 惨禍×被弾強化 | **差し替え** | 1 → **2** | −1.9 | −2.9 | ヒサ/**ムド** \| カド \| **ノノ**/**セロ** |
| 澱み喰い (グザ×ヴィオ) | **差し替え** | 1 → **3** | −2.4 | −2.4 | ガルド/グザ \| **ミオ** \| **スィド**/**ヴィオ** |
| 反撃 (ヒサ×カド) | 据え置き | 1 → 1 | — | — | (a)(b) を満たす 13 通りが**全部 情報セル 1** |
| 反撃改 (ドハ×カド) | 据え置き | 1 → 1 | — | — | 同 19 通りが全部 1 |
| 逆しま改 (クビ×ウツ) | 据え置き | 1 → 1 | — | — | 同 5 通りが全部 1 |
| 後衛特化+後備え | 据え置き | 1 → 1 | — | — | **狙を満たす候補が 3 通りしかなく、全部 1** |

`docs/balance.md` の差分は**この 2 行だけ**（59 行は 1 セルも動かない）:

    惨禍×被弾強化        100.0 /  0.0 / 100.0 / 100.0 / 61.5  →  100.0 /  0.0 / 100.0 /  95.0 / 59.0
    澱み喰い (グザ×ヴィオ) 100.0 / 100.0 /  99.5 / 100.0 /  0.0  →  100.0 / 91.5 /  95.5 / 100.0 /  3.0

> **「勝つ席」と「測れる席」は別物**（第50期・第64期）。**採ったのは追順 9 位と 17 位**で、
> どちらも勝率の1位ではない。逆に、勝率の上位は 6 行とも情報セル 1 のままだった
> ——**天井（100.0%）に張り付く席がいちばん強い**からで、
> **勝率で席を選ぶ限り情報セルは増えない。**
>
> **`澱み喰い` でいちばん効いたのは第五波が 0.0% → 3.0% に離れたこと。**
> 床のセルの「差 0.0」は「差が無い」ではなく**「測っていない」**（第61期）。
>
> **4 行が据え置きになった理由は「候補が全部 情報セル 1」で、席の探索の失敗ではない。**
> 候補の全席で値が動かないセルを数えると:
>
>     反撃 (ヒサ×カド)     21 通り: 第1〜4波が一定（100.0 / 0.0 / 100.0 / 100.0）→ 動くのは第5波だけ
>     反撃改 (ドハ×カド)   20 通り: 同上（100.0 / 0.0 / 100.0 / 100.0）
>     逆しま改 (クビ×ウツ) 23 通り: 第1〜3波が一定（100.0 / 100.0 / 100.0）。第5波は全席 100.0%
>     後衛特化+後備え      24 通り: 一定は第1波だけ——**動くのに、狙を満たす 3 通りが全部 情報セル 1**
>
> **前の 3 行は席では直せない**（第2〜4波のうち 2〜3 セルが席に依存しない）。直すなら 5 枚組の側
> （第111期の差し替え）になる。**`後衛特化+後備え` だけは事情が違い、席では動くが狙が塞いでいる。**

---

## 3. 灯の対象選択の実測（`lit run`・**参考。この期の判定には使わない**）

**指示書 §6 の分岐の順序に従い、採否は第114期へ送る。** 以下は第114期のための記録である。

### 3-1. 門（第2〜5波・回/戦）

| 台 | 版 | 勝率 | 決着T | 灯 | **替** | 譲渡 | **潰れ** | **潰/譲** | 譲渡の与ダメ |
|---|---|--:|--:|--:|--:|--:|--:|--:|--:|
| トモ×ドルガ（主判定行） | W0 | 83.1% | 5.90 | 4.39 | 0.34 | 1.94 | 0.78 | **0.402** | 65.9 |
| トモ×ドルガ | W1 | 83.1% | 5.90 | 4.39 | 0.34 | 1.94 | 0.78 | **0.402** | 65.9 |
| トモ×ドルガ | W2 | **91.2%** | 5.61 | 4.19 | **2.84** | 1.56 | **0.00** | **0.000** | 73.3 |
| トモ×ハギ | W0 | 0.6% | 5.40 | 3.94 | 0.72 | 0.09 | 0.05 | 0.583 | 1.3 |
| トモ×ハギ | W1 / W2 | 4.9% | 5.70 | 4.00 | 0.96 | **0.35** | **0.00** | 0.000 | **13.5** |

ムド台・ソム台は**3版とも1ビットも動かない**（濾しに掛かる駒が盤上に居ない）。

### 3-2. 判定（**記録のみ**）

| | 問い | 線 | 実測 | 判定 |
|---|---|---|---|:-:|
| Q1 | 潰れ/譲渡 | W0 の 1/2 以下 | 0.402 → W1 **0.402** / W2 **0.000** | W1 **×** / W2 ○ |
| Q2 | 帰属（版 − 素体） | W0 ±0 以上 | +46.0 → W1 **+46.0** / W2 **+54.1** | 両方 ○ |
| Q3 | W2 の替 | 二値 | 0.34 → **2.84** 回/戦 | 増 |
| Q4 | ハギ台の自己点灯 | W1 で 0% | 78.8% → **0.0%** | ○ |
| Q5 | トモを含まない 60 行 | ±0.0 | **0 件 / 600 セル**（60 行 × 5 波 × W1・W2 の 2 版） | ○ |
| Q6 | 対象の分布 | 2 枚以上 | W1 3 枚 / W2 3 枚（台内） | 両方 ○ |

### 3-3. 読み（第114期への申し送り）

- **W1 は主判定行では 1 ビットも動かない。** ドルガは (A)(B) のどちらでもないので濾されず、
  **W1 が効くのはハギ台だけ**（灯の対象がハギ自身 78.8% → 0%、譲渡 0.09 → 0.35 回/戦、帰属 +0.6 → +4.9pt）。
  **指示書の予測1「W1 でハギ台の潰れが 0 になる。ただし帰属はほとんど動かない」は前半が当たり、後半が外れた**
  ——外した理由は**「量が無い」を譲渡の回数だけで数えていた**こと。
  実際には灯（攻撃力 +5・累積）の行き先も変わるので、**譲渡が増えなくても打点が動く**（味方の総与ダメ 194 → 240）。
- **W2 は主判定行で 帰属 +8.1pt。** 予測3（「W2 < W1」）は**外れた**。
  **灯の到達点は 18.1 → 6.8 と 62% 落ちている**のに勝率は上がる
  ——**「累積を積むこと」より「譲った手番が潰れないこと」のほうが高い**という、
  第109期以来の見立て（灯は伸びる＝資産）の逆側である。
  **予測4（W2 の対象が下位2枚を往復して分布が潰れる）も外れ**——実測は 3 枚に散った（49.7 / 29.5 / 20.7%）。
- **ただし W2 は主判定行の情報セルを 4 → 3 に減らす**（第四波 74.0% → 98.0%）。
  (G9) により拒否はしないが**「注意」として記録する**——採るなら席の再判定と対になる（第107期）。

---

## 4. 自己検査（`lit check`）

**必須4項目**

| | 項目 | 実測 | 判定 |
|---|---|---|:-:|
| 必須1 | `compare` 305 セルと `docs/balance.md` | 既定 0 件 / W0 明示 0 件 / 両経路の差 0 件 | ○ |
| 必須2 | `docs/` 10 ファイルの再生成 | §5 の差分表 | ○ |
| 必須3 | 触っていないノブの既定 | `docs/rules.md` の差分で示す（下） | ○ |
| 必須4 | `ctx.PickOne` の呼び出し | **26 箇所**（第94期と同数。1つも足していない） | ○ |

**この期固有**

| | 項目 | 実測 | 判定 |
|---|---|---|:-:|
| (a) | W0 の 305 セルが `docs/balance.md` と一致 | 0 件 | ○ |
| (b) | 濾した集合が (P-5) の一覧と一致 | W1 = {カド, ノノ, ハギ} ／ W2 = {ドルガ}。**W0 は 0 件** | ○ |
| (c) | 候補 0 人で 3 版とも `TaillightIdle` | ミオ台 W1/W2 で灯 0.00・空振り 2.75 ／ ドルガ台 W2 で空振り 1.00 | ○ |
| (d) | 灯が `AcceptsSupport` 偽の駒に乗った回数 | **0 件**（3版 × 4台 × 全5波） | ○ |
| (e) | 門1（灯）が 3 版で同数 | **4 台中 2 台で×**（下） | **×（指示書の仮定が誤り）** |
| (f) | 情報セルを数えた帯 | **seed 0..199（帯A）**。追試の帯は席の列にだけ使う | ○ |

> **(e) は実装の事故ではない。** 指示書 §3-2 の「門1 は3版で同数のはず（濾しは対象を変えるだけ）」は
> **決着ターン数が動かないことを暗黙に仮定していた。** 灯はトモが生きている限り毎ターン点くので、
> **濾しが盤面を動かして決着が伸び縮みすれば灯の回数も動く**
> ——実測はドルガ台 4.39 → 4.19（決着 5.90 → 5.61T）、ハギ台 3.94 → 4.00（5.40 → 5.70T）で、
> **どちらも決着ターン数と同じ向きに動いている。**
> **「計数が版に依らない」を検算にするときは、その計数の分母がターン数でないかを見ること**（規約 (G6) の
> 「出すときは必ず決着ターン数を併記する」がそのまま効いた）。

---

## 5. `docs/` の差分（必須2）

| ファイル | 差分（`git diff --numstat`） | 中身 |
|---|---|---|
| `balance.md` | 2 / 2 | **差し替えた 2 行だけ**（59 行は 1 セルも動かない） |
| `units.md` | **0 / 0** | 駒の定義を1つも変えていない |
| `chain.md` | 2 / 2 | 同じ 2 行 |
| `ablation.md` | 12 / 12 | 同じ 2 行（1 行につき 5 枚 ＋ 見出し） |
| `pulse.md` | 11 / 11 | 同じ 2 行 |
| `engage.md` | 9 / 9 | 同じ 2 行 |
| `layout.md` | 16 / 16 | 同じ 2 行（`現行` の順位が 4位 → 12位 / 1位 → 12位 に落ちる） |
| `reseat.md` | 2 節 | 同じ 2 行の ★現行 と並び |
| `crossing.md` | 1 / 1 | **所要時間の行だけ**（交差帯 12 行は 1 セルも動かない） |
| `rules.md` | 16 / 15 | 下記 |

`rules.md` の中身は3種類:

    (1) `TaillightRule { Mode = OwnTurnWindow }` → `{ Mode = OwnTurnWindow, Filter = SupportOnly }`   ← ノブが1本増えた
    (2) 列挙型の表に `LitFilter | SupportOnly=0 / ActingOnly=1 / ActingNow=2` が1行増えた
    (3) 6 本の規則の「測った診断」の列に `lit` が足された（`lit check` が既定値を印字するため）

**他のノブの既定値は1つも動いていない**（必須3）。`derive rules` は既定値を reflection で引くので、
**(1) 以外の既定値の文字列は 1 文字も変わっていない。**

`audit`（生成物と現行の編成数の整合）は **ずれ 0 件 / 8 ファイル**。

### 5-2. 差し替え後に Phase 0 を走らせ直した値

`lit phase0` は `docs/balance.md` と `docs/reseat.md` を読むので、差し替え後は数字が変わる:

| | 差し替え前 | 差し替え後 |
|---|--:|--:|
| 情報セルが 1 以下の行 (P-2) | 6 行 | **4 行** |
| 代替席のある行 (P-4)・狙なし | **3 行** | **1 行** |
| 同・狙あり | 2 行 | **0 行** |

**(G14) の波及は、この器具で見るかぎり閉じた。** 残る 1 行（`後衛特化+後備え`）は
**狙を満たす候補が全部 情報セル 1** なので、狙を守る限り席では動かない
——**「狙を捨ててでも情報セルを取るか」は別の判断**で、この期では取らなかった
（狙は第45期以降の作法で、席の探索の全部がそれを前提に測られている）。

---

## 6. 現状値

| 分母 | 第1波 | 第2波 | 第3波 | 第4波 | 第5波 |
|---|--:|--:|--:|--:|--:|
| **主判定19行** | 100.0 | 72.5 | 72.1 | 68.6 | **41.3** |
| 全61行 | 100.0 | 69.5 | 75.8 | 63.2 | 42.9 |

**歯止め 33.2% との余裕は +8.1pt**（第107期から4期続けて**主判定19行は1ビットも動いていない**
——差し替えた 2 行はどちらも `Baseline.PrimaryRows` に入っていない）。
**情報セルの合計は 161 → 164**（平均 2.64 → 2.69。情報セル 1 の行は 6 → 4）。

---

## 7. この期で決めなかったこと／申し送り

- **灯の対象選択（W1 / W2）の採否**——第114期。**器具（`lit run` / `lit check` / `lit seat`）と
  3 版の実測はこの期で揃えてある**ので、第114期は「採るか」だけを決めればよい。
  採るなら **W2 が本命**（帰属 +8.1pt）で、**席の再判定（`lit seat w2`）と対にすること**（第107期）。
- **`反撃` 系 3 行（反撃・反撃改・逆しま改）は席では直せない。** 候補 21 / 20 / 23 通りの
  すべてで情報セルが 1。5 枚組の差し替え（第111期の形）でしか動かない。
- **(G15) の候補が 2 本になった**:
  1. （第112期）取り込み・引用は行番号ではなく構造で切り、取り込んだ文字列が原文に含まれることを機械で確かめる
  2. （**この期**）**線を引く条件と、採る条件は同じ集合で書く。**
     条件が食い違うと、**線は通るのに採れない行**（`後衛特化+後備え`）が出る
- **`README.md`（617,950 字）の分割** — 第112期の形がそのまま当たる（持ち越し）
- **`cross quality` の表示文字列**（判定は正しく表示だけ古い。交差帯を触る期に）
