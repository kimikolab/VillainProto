# 第106期 —— 保留の4枚を決める（ムド・ノノ・ササ・ハリ）

> **ノノは採った**（繕いの代金を半額に。9行すべてが上がって平均 +3.67pt・拒否権 0 行）。
> **ササは採った**（被弾で隣を弾く。盤面に何も書かない駒から外れ、軋みのヨミと組ませると 13.5 → 92.4%）。
> **ムドは採らなかった**——**保留の理由そのものが実測で消えた。**
> 「育つのに42点必要・到達点は攻24・育つ前に落ちる」は3つとも誤りで、
> 実測は**発火 7.03 回/戦・1発あたり 4.52・到達点 攻36・到達ターン 3.31**。
> 指示書の +3 はその誤った前提から引かれた較正なので、**天井を 33 → 22.4 に切って平均 −1.52pt**、
> 拒否権も 3 行立った。**ムドは現行のまま置く。**
> **ハリは触っていない**（材料のみ）——手番 4.03 回/戦・稼働率 65.7%・完全な手番型で、
> **第104期の再行動は 0.00 回/戦 しか届いていない。**

触ったもの: ノブ3本（`RageRule` / `MenderCostRule` / `LooseRule`）と、
4つ目の通貨（強化・弱体）の観測、診断 `hold` 1本、`TraitKeyMap` / `TraitEntryMap` へ散開の移動 1 行。
**engine の判定・数値・`CompareBuilds()` / `CrossBuilds()` / `Stages` / `UnitCatalog.All` の 52 体は 1 行も触っていない。**

    dotnet run --project BattleSim -c Release 0 hold phase0    # §4 と (T1)
    dotnet run --project BattleSim -c Release 0 hold run <p>   # p = 0..5
    dotnet run --project BattleSim -c Release 0 hold hari      # (T3)
    dotnet run --project BattleSim -c Release 0 hold tables    # 表A〜F（438,000 戦・64 秒）
    dotnet run --project BattleSim -c Release 0 hold check     # 自己検査 (a)〜(f)

---

## 0. 版

| 版 | 内容 | 採否 |
|---|---|---|
| P0 | 現行（対照） | — |
| P1 | ムドのみ `RageMode.Count`・`Gain = 3`（**指示書 §2-1**） | **×**（Q1 未達・拒否権 3 行） |
| P2 | ノノのみ `MenderCostRule(50)` | **○ 採用** |
| P3 | ササのみ `LooseRule(true)` | **○ 採用** |
| P4 | 3つとも | — |
| P5 | ムドのみ `Count`・**`Gain = 5`**（Phase 0 から引き直した対照） | 保留（→ §8） |

**P5 は Phase 0 の実測から引き直した対照であって、結果を見てから緩めた版ではない。**
§7「数値の掃引はしない」に従い、**P5 を採用の候補にはしていない。**

---

## 1. (T1) 4つ目の通貨 —— 足したが、Q0 は落ちた

### 1-1. 観測点は `AtkBonus` の setter 1 箇所

第105期の3通貨（与ダメージ・回復・状態異常）に**強化・弱体**を足した。
帰属は第94期 (T2) の印（`Mark.Owner`）で、**符号を問わず絶対量**を数える。

> **窓口（`Whet` / `Dull`）ではなく `UnitState.AtkBonus` の setter に置いた。**
> 窓口だけを見ると**自己強化の9本**（怒り・庇う／殉教・墓守2本・処刑・棘・澱み喰い・軋み・分かち）が
> まるごと落ちる。setter なら経路を追加しても数え漏らさない。

**境界と蘇生の一括消去は `ResetAtkBonus` を通るので setter に来ない**（第68期の判断を引き継いだ）。
**逆しま（`Perverse`）は `ModifyAttack` で `AtkBonus` を1点も動かさないので、この通貨には現れない。**

### 1-2. `AtkBonus` を動かす経路の全数（**観測。手で書いていない**）

73行 × 5波 × seed 0..199 = 73,000 戦。**19 本**（印なしを除く）。

| 特性（印） | 上げた量 | 下げた量 | | 特性（印） | 上げた量 | 下げた量 |
|---|--:|--:|---|---|--:|--:|
| `Necro` | 977,462 | 67,260 | | `Cower` | 0 | 90,000 |
| `Colossus` | 402,517 | 0 | | `Bind` | 65,600 | 0 |
| `Curse` | 0 | 397,800 | | `Martyr` | 64,937 | 0 |
| `Sharer` | 316,376 | 0 | | `Executioner` | 49,168 | 0 |
| `Rage` | 305,503 | 0 | | `Drifter` | 38,420 | 0 |
| `Guardian` | 297,321 | 0 | | `Relay` | 0 | 25,328 |
| `Rally` | 290,480 | 0 | | `Goad` | 24,476 | 0 |
| `Thorns` | 253,899 | 0 | | `Shove` | 0 | 14,318 |
| `Blightfed` | 163,628 | 0 | | **（印なし）** | 0 | 42,194 |
| `Displaced` | 143,182 | 0 | | | | |
| `Favor` | 65,388 | 25,326 | | | | |

**墓守（`Necro`）1枚で全体の3分の1**を動かしている。**両方向に動くのは `Necro` と `Favor` の 2 本だけ。**

### 1-3. 4通貨の3分割（第2〜5波・73行）

| 通貨 | 手番の中 | 手番の外 | 誰のものでもない |
|---|--:|--:|--:|
| 与ダメージ | 75.3% | 18.5% | 6.2% |
| 回復 | 9.7% | 90.3% | 0.0% |
| 状態異常（回数） | 17.6% | 76.0% | 6.4% |
| **強化・弱体（量）** | **5.4%** | **93.6%** | **1.0%** |

**4通貨のうち3つが「手番の外」に寄っている。**
第105期の「手番は与ダメージの通貨であって、盤面を書き換える通貨ではない」は、
4本目を足すと**さらに強くなる**——強化・弱体は回復（90.3%）よりも手番から遠い。

### 1-4. Q0 —— 4通貨でも 4/5 のまま。**(G14) は書かない**

| 駒 | 第103期の Δ相乗の順位 | 3通貨（第105期） | 4通貨（この期） | 期待 | 判定 |
|---|--:|---|---|---|:-:|
| 追い打ちのハギ | 1 / 51 | 手番外型 | 手番外型 | 手番外型 | ○ |
| 棘鎧のカド | 2 / 51 | 手番外型 | 手番外型 | 手番外型 | ○ |
| **火選りのヒヨ** | 3 / 51 | **出力なし** | **手番型**（強化弱体 手番外 0.0%） | 手番外型 | **×** |
| 焼け残りのボルグ | 32 / 51 | 手番型 | 手番型 | 手番型 | ○ |
| のろまの巨兵ドルガ | 46 / 51 | 手番型 | 手番型 | 手番型 | ○ |

> **器具の欠落ではなく、期待のほうが間違っていた。**
> 第105期は「ヒヨの通貨が無いから分類できない」と読んだが、
> **通貨を足すとヒヨは手番型に落ちる**——第60期に火選りを `OnTurnStart` から
> `OnAction`（手番）へ降ろしたので、ヒヨの出力は**全部が自分の手番の中**にある。
> **ヒヨが第103期に 3 位だったのは「手番で殴らない」からであって「手番を使わない」からではない。**
> `Actions = [Skill]` の駒は手番を持ち、手番を使い、ただし `PerformAttack` を通らない。

**Q0 は 4/5 で落ちた。指示書 §1 のとおり (G14) は `CLAUDE.md` に書かない。**

**分類の分布（4通貨・73行に在席した 51 枚）**: 手番外型 4 / 混合 26 / 手番型 21 / **出力なし 0**。
第105期の「出力なし 1 枚」は消えた（**分母の穴は塞がった。判定の当たり外れは別の話**）。

---

## 2. ムド —— 保留の理由が実測で消えた（Q1 ×）

### 2-1. Phase 0 で前提が3つとも否定された

| 指示書 §0-1 の記述 | 実測（第2〜5波・9行 × 200 seed） |
|---|---|
| 育つのに **42 点**必要（`dmg / 2`） | 1発あたり **4.52**（＝1発が平均 9 点）・被弾 **7.03 回/戦** |
| 到達点は **攻24** | `AtkBonus` **33.0** ＝ **攻36**（憤怒だけで 31.8 積む） |
| **育つ前に落ちる** | **+24 に届く試行が 84.8%・到達は 2.69T**（決着は 6〜7T） |

**ムドは育つ。攻36 まで、3ターン以内に。**

### 2-2. だから +3 は「速度だけを変える」にならなかった

| 版 | 発火/戦 | 1発あたり | 到達点 | 到達T | +12到達% (T) | +18到達% (T) | +24到達% (T) |
|---|--:|--:|--:|--:|--:|--:|--:|
| P0 | 7.03 | 4.52 | **33.0** | 3.31 | 95.2% (2.08) | 89.9% (2.42) | 84.8% (2.69) |
| P1（+3） | 7.06 | 3.00 | **22.4** | 3.30 | 87.6% (2.30) | 66.7% (2.53) | **44.2%** (2.75) |
| P5（+5） | 7.01 | 5.00 | **36.3** | 3.31 | 94.8% (2.00) | 90.9% (2.27) | 81.6% (2.46) |

**+3 は天井を3分の1切っている。しかも到達ターンは1つも早くなっていない**（3.31 → 3.30）
——**「量で育つ」と「回数で育つ」の速さの差は、1発の平均が 9 点もあると出ない。**
`Amount` でも1発ごとに `max(1, dmg/2)` が入るので、**もともと1発目から育っている。**

### 2-3. 結果

| 版 | 憤怒を含む 12 行の平均Δ | 拒否権1（−10.0pt 以上落ちた行） |
|---|--:|--:|
| P1 | **−1.52pt** | **3 行**（毒→被弾強化 / 燃焼 / 火選り。うち ムド・ホタ が (G2) で「壊れ」） |
| P5 | +2.53pt | **0 行** |

**Q1 は ×。ムドの既定は現行（`RageMode.Amount`）のまま。**

**`RageRule` は後備えのセッキにも効く**（憤怒の保持者は 2 枚）。
セッキの行は P1 でも上がっている（`逆しま+後備え` +16.0pt）——**上がったのはセッキ側だけ**で、
ムドの 9 行は 7 行が下がった。**予測1（第2波でいちばん伸びる）も外れ**、
下がり方がいちばん大きいのは第3〜4波だった。

---

## 3. ノノ —— 採用（Q2 ○）

`MenderCostRule.Default = new(50)`。**癒す量は1点も変えず、引く量だけを半分にする。**

| 行 | ボルグ同席 | P0 | P2 | Δ |
|---|:-:|--:|--:|--:|
| 反撃 (ヒサ×カド) | | 69.4 | 70.9 | +1.5 |
| 惨禍×被弾強化 | | 64.9 | 65.4 | +0.5 |
| 耐久 (ガルド×ノノ) | | 91.6 | 95.1 | +3.5 |
| 反撃改 (ドハ×カド) | | 69.4 | 71.6 | +2.3 |
| 燃焼 (ボルグ×ホタ) | ○ | 64.6 | 71.4 | **+6.8** |
| 分かち×逆しま (ドハ×ウツ) | | 66.4 | 74.1 | **+7.8** |
| 引き受け (ウケ×ドハ) | | 45.0 | 51.1 | **+6.1** |
| 渡し (ワタ×ドハ) | | 72.0 | 75.5 | +3.5 |
| 傷×被弾 (カド×ハリ×ノノ) | | 57.9 | 59.0 | +1.1 |

**9 行すべてが正・平均 +3.67pt・拒否権1 は 0 行。**

### 3-1. 増えたのは「1回の癒し」ではなく「癒せる回数」

| 版 | 繕い/戦 | 癒した量/戦 | 引いた量/戦 | 生存T | 決着T | 稼働率 | 落% |
|---|--:|--:|--:|--:|--:|--:|--:|
| P0 | 2.72 | 29.3 | **38.9** | 3.62 | 6.47 | 44.5% | 85.3% |
| P2 | **3.28** | **37.9** | **23.7** | **4.33** | 6.45 | 52.7% | **68.3%** |

**1回の癒しは +14 のまま**（自己検査 (c) の1戦監査で両版とも同じ行）。
**代金を半分にすると 生存Tが 0.71 伸び、その伸びぶんだけ手番が増え、繕いの回数が 20% 増える。**

> **等価交換の代金は「HP」ではなく「自分の寿命」だった。**
> ノノは自分を削って癒すので、**削るほど次の癒しの原資が減り、落ちるまでの手番も減る**
> ——第104期の「損の側を名指しするときは、消す動作が撃破を伴うかを先に見る」の回復版で、
> ここでは**自分の HP が自分の手番数の関数**になっている。

### 3-2. 予測2（ボルグ同席でだけ大きい）は**向きは当たったが n = 1**

ボルグ同席 1 行 +6.75pt 対 非同席 8 行 +3.28pt。**同席行は 61 行に 1 行しかない**ので、
これを根拠に「巻き込みの後始末が緩んだ」とは書けない。
非同席でも +7.8（分かち×逆しま）・+6.1（引き受け）が出ている。

### 3-3. 敵側の従軍司祭長は動かない

`MenderCostRule` は陣営を問わない規則だが、`Chaplain`（同じ `MenderTrait` を持つ敵）は
**`Stages` に1体も出ていない**（第二波は施しの司祭長に差し替え済み）。
**単発戦では敵側に1点も効かない**ことを Phase 0 で確認してから採った。

---

## 4. ササ —— 採用（Q3 ○）。**「盤面に何も書かない駒」から外れた**

`LooseRule.Default = new(true)`。**被弾すると隣接する味方1体を別の席へ弾く（1ターン1回）。
被ダメ −35% はそのまま残す。**

| 版 | 弾き/戦 | 上限で弾かれ/戦 | 隣に味方なし/戦 |
|---|--:|--:|--:|
| P0 | **0.00** | 0.00 | 0.00 |
| P3 | **2.23** | 1.94 | 0.12 |

**弾いた回数 > 0 ○。** 上限が 1.94 回/戦 弾いているので、**被弾クロックは1ターン1回で正しく切れている**
（自己検査 (d): 弾きが決着ターンを超えた試行 0 件）。

### 4-1. Q3 の後半は 73 行では**原理的に測れない**

**ササと 移動軸の読み手（ヨミ・バサ・ハネ・シオ）が同席する行は 0 / 73。**
ローカル台（`CompareBuilds()` は触らない・出力役にドルガを必ず入れた5行）で測った:

| 行 | P0 | P3 | Δ | 弾き/戦 | ヨミの `AtkBonus` 到達点 | 味方の延べ移動/戦 |
|---|--:|--:|--:|--:|--:|--:|
| ササ単騎（対照） | 31.0 | 36.0 | +5.0 | 3.82 | — | 11.37 |
| **ササ×軋み (ヨミ)** | 13.5 | **92.4** | **+78.9** | 2.18 | 35.2 | 5.61 |
| ササ×喧噪 (バサ) | 80.1 | 78.8 | −1.4 | 2.05 | 39.6 | 11.25 |
| ササ×突き返し (ハネ) | 12.0 | 11.3 | −0.8 | 2.04 | — | 4.87 |
| ササ×怯み (シオ) | 28.5 | 33.0 | +4.5 | 2.13 | 12.6 | 5.00 |

> **移動軸の読み手なら誰でもいいわけではない。** 4枚のうち大きく動いたのは軋みのヨミ 1 枚。
> バサは**自分で毎ターン席を混ぜる**ので供給が既に飽和していて（移動 11.25 回/戦）ササの弾きが上乗せにならず、
> ハネは**敵陣しか動かさない**ので味方側の移動を読む口を持たない。
> シオ（動かされた味方を回復＋強化）は +4.5 と小さい。
> **「移動を読む」の中身は、量を読むのか出来事を読むのかで値段が変わる。**
> ヨミの軋みは**動かされるたびに攻撃力が上がり、その場で割り込む**——
> 出来事1つが出力1回に直結する唯一の口だった。**予測4 は当たった。**

### 4-2. 代金は実在する

`CompareBuilds()` でササを含む唯一の行 `置き去り×分散回復` は **27.3 → 12.8**（第2波 −54.0 / 第3波 −14.0）。
第91期 (G2) の分解では他の4枚（ガルド・セロ・ナラ・ドルガ）の「他の行」の平均変化が
**全部 ±0.00pt** なので**組み合わせ固有＝制約**だが、
**ササ自身は「他の行」が 0 行なので、この分解はササについては成立しない**
（`CLAUDE.md` の但し書きどおり。そう書く）。

**この行の席は測り直していない**（第46期の作法どおり `reseat` の上位5位を見る作業は別）。→ §8

### 4-3. 「盤面に何も書かない」から外れた（**指示書 §0-1 の分類は誤り**）

指示書は散開を「第96期の分類 A」と書いているが、**第96期は `Loose` を A に入れていない**
——A（条件が編成時に確定する完全な係数）は **5 種 / 4 駒**（`Stoic` / `Frail` / `Immobile` / `Havoc` / `ThinBlade`）で、
散開は「隣に味方がいない」が**味方が死ぬと戦闘の途中で成立する**ので明示的に外されている。
散開が入っていたのは**その上位集合**——「1戦のあいだ `Counters` に何も書かない **21 種**」のほう。

**この期でそこから外れて 20 種になった。** ササは `looseTurn` を書き、`SwapSlots` 経由で移動を供給する。
`derive scan` の観測で `Loose` が**移動の供給者**として出たので、
`TraitKeyMap` と `TraitEntryMap.Supplies` に 1 行ずつ足した（欠落 0 件・過剰 0 件に戻した）。

> **第78期の入口・発火口と、第80〜83期の「独立の広さ」はこの1行で動く。数え直していない。**
> ササはこれまで**キーを1つも持たない 8 枚**（リィカ・ドルガ・ムグ・ヴェル・ササ・ハギ・ナラ・ソム）の
> 1枚だったので、そこは **7 / 52** になる。

---

## 5. (T3) ハリ —— 材料だけ（去就はこの期で決めない）

| 量 | 実測（第2〜5波・1,600 戦） |
|---|--:|
| 決着T | 6.14 |
| **手番/戦**（再行動を含む） | **4.03** |
| 使えた手番/戦 | 4.03（**潰れる手番が 1 回も無い**） |
| **稼働率**（使えた手番 ÷ 決着T） | **65.7%** |
| 振/戦（`Attacks`） | 4.03 |
| **再行動/戦**（第104期） | **0.00** |
| 生存T | 5.15 |

| 通貨 | 手番の中 | 手番の外 |
|---|--:|--:|
| 与ダメージ | 46.0 | **0.0** |
| 回復 | 23.9 | **0.0** |
| 状態異常 | 0.00 | 0.00 |
| 強化・弱体 | 0.0 | 0.0 |

**分類は完全な手番型**（4通貨とも手番外 0.0%）。**ロスターで最も「手番だけで出来ている」駒の1枚。**

| 行 | 第2波 | 第3波 | 第4波 | 第5波 | 平均 |
|---|--:|--:|--:|--:|--:|
| 刻み×縫い (ノミ×ハリ) | 100.0 | 84.0 | 89.0 | 26.5 | 74.9 |
| 傷×被弾 (カド×ハリ×ノノ) | 0.0 | 99.0 | 98.5 | 34.0 | 57.9 |

### 5-1. 第85期の律速は動いていた。それでも足りない

第85期は「律速は振り回数 **2.15 回/戦**」だった。いまは **4.03 回/戦（1.87 倍）**
——第104期の再行動ではなく（**ハリの再行動は 0.00 回/戦**）、第88期以降の盤面の変化で伸びている。

> **第104期の再行動はハリに1回も届いていない。**
> 再行動は「傷を刻んだ駒に、刻んだ相手が倒れたときもう一度手番を渡す」機構だが、
> **ハリは傷を書かない**（縫いは傷を**読んで**塞ぐ側）。**傷軸の払い出しは書き手にしか届かない。**

### 5-2. 稼働率 65.7% を第86期の 35〜40% と比べてはいけない

第86期の帯は「**特性の発火** ÷ 決着T」、ここは「**手番** ÷ 決着T」（第105期の訂正）。
**同じ言葉が別の量を指す。**

---

## 6. Q4・拒否権・自己検査

### 6-1. Q4 —— 3つは**完全に加法**

73行すべてで `ΔP4 = ΔP1 + ΔP2 + ΔP3`。**|食い違い| の平均 0.00pt・最大 0.0pt。**
3つのノブが同じ行の上で同時に働く行が1つも無い（ムドとノノが同席するのは
`惨禍×被弾強化` と `燃焼 (ボルグ×ホタ)` の 2 行だが、そこでも交互作用は 0 だった）。

**採用したのは P2 + P3 なので、実際の変化は ΔP2 + ΔP3 と厳密に一致する**
——`docs/balance.md` の差分 **23 セル / 9 行** は P2 の 19 セル ＋ P3 の 4 セルとちょうど合う。

### 6-2. 拒否権

| 版 | 拒否権1（−10.0pt 以上落ちた行） | (G2) の分解 | 拒否権2（主判定19行の第五波） |
|---|--:|---|--:|
| P0 | — | — | 40.9%（歯止め 33.2） |
| P1 | **3 行** | **3 行とも「壊れ」**（ムド −3.34 / ホタ −3.00・−4.56） | 41.1% ○ |
| **P2** | **0 行** | — | 41.3% ○ |
| **P3** | 1 行 | **制約**（他の4枚が全部 ±0.00。ただしササ自身は判定不能） | 40.8% ○ |
| P4 | 4 行 | 1 行が「壊れ」（ホタ） | 41.0% ○ |
| P5 | **0 行** | — | 41.5% ○ |

**注意（規約 (G9)・拒否ではなく記録）**: 第五波が新たに 95% を超えた行は
P1 / P4 / P5 で `後衛特化+後備え` の 1 行。**P2・P3 は 0 行。**

### 6-3. 自己検査

| 検査 | 実測 | 判定 |
|---|---|:-:|
| 必須1 `compare` 305 セル | 採用前は `docs/balance.md` と **0 件**（BOM 以外の差分なし） | ○ |
| 必須2 `docs/` 10ファイル | 再生成した。差分は §6-4 | ○ |
| 必須3 触っていないノブの既定 | `docs/rules.md` の既定値の列は **33 本が不変**、増えたのは新しい 3 本だけ | ○ |
| 必須4 `ctx.PickOne` の箇所数 | 実呼び出しは HEAD と同数。増えた 1 件は **doc コメントの言及** | ○ |
| (a) ノブが1回も走らない 10 行 × 5波 × 20 seed で P0 と P4 の計数が一致 | 食い違った列 **0 / 8**・勝敗と決着Tの食い違い **0 / 1000 セル** | ○ |
| (b) `Count` 版で 上昇 ÷ 発火 = 3.00 | **3.0000**（発火 7,304） | ○ |
| (c) 繕いの**癒す量**が版で変わらない（1戦監査） | P0・P2 とも `… 棘鎧のカド を繕った（+14）` | ○ |
| (d) 弾きが 1ターン1回以下 | 弾き 2,938 / 延べターン 8,609・**超過 0 件** | ○ |
| (e) `SwapSlots` の既存の呼び出し口が無傷 | 同じ台で移動回数 **1,896 対 1,896** | ○ |
| 追加 `audit` | ずれ **0 件** | ○ |
| 追加 `derive scan` | 欠落 **0** ／ 過剰 **0** ／ `TraitKeyMap` の差 **0 枚**（散開の 1 行を足した後） | ○ |

### 6-4. `docs/` の差分

| ファイル | 変化 |
|---|---|
| `balance.md` | **23 セル / 9 行** |
| `units.md` | **2 行**（ノノ・ササの `PlusText` / `MinusText`。`Flavor` は据え置き） |
| `rules.md` | 新しいノブ 3 本 ＋ 列挙型 `RageMode` ＋ 本数（33 → 36 / 35 → 38 型 / 9 → 10 型） |
| `chain.md` / `ablation.md` / `pulse.md` / `engage.md` / `layout.md` / `reseat.md` / `crossing.md` | 動いた 9 行の下流 |

**波の並び（61行）**: 100 / **69.1** / **75.6** / **64.7** / **42.9**
（主判定19行 100 / 70.9 / 71.6 / 69.3 / **41.1**・歯止め 33.2 との余裕 **+7.9pt**）。
第95期の 100 / 69.9 / 74.8 / 61.9 / 39.5（主判定 38.0・余裕 +4.8）から、
**第4波が +2.8・第5波が +3.4 上がった**（第104期の再行動と、この期のノノ・ササ）。

---

## 7. 予測の答え合わせ

| # | 予測 | 結果 |
|--:|---|---|
| 1 | ムドは上がるが小さい。第2波でいちばん伸びる | **×**（−1.52pt。前提が誤り。下がり方が大きいのは第3〜4波） |
| 2 | ノノは上がる。ボルグ同席でだけ大きい | **○ / △**（+3.67pt。同席は 1 行しかなく判定できない） |
| 3 | ササは勝率が動かない可能性が高い | **×**（唯一の行が −14.5pt・ローカル台では +78.9pt。**どちらにも大きく動いた**） |
| 4 | ヨミが最も得をする | **○**（4枚の読み手のうちヨミだけが +78.9pt） |
| 5 | 拒否権は立たない | **×**（P1 で 3 行・P3 で 1 行。ただし採用した P2 は 0 行） |

**5つ中 2つしか当たっていない。外れた3つはすべて「指示書が引いた過去の数値を確かめずに使った」ことから来ている**
——ムドの攻24（第0期の記憶）、ササの「何も起きない」（第97期は会戦の話で、弾きは単発でも走る）、
拒否権の「既存の駒の中で閉じている」（閉じていても行は落ちる）。

---

## 8. 申し送り（第107期の入口）

- **アセット**: **ムド・ノノ・ササの3枚は決まった。** `docs/units.md` が正。
  **ハリだけが未決**（材料は §5）。
- **ムドの `RageMode.Count` は閉じていない。** P5（`Gain = 5`）は
  **+2.53pt・拒否権 0 行・到達点 36.3（現行 33.0）・到達ターンは +18 で 2.42 → 2.27 と早い**。
  採るなら**測る前に較正を固定した指示書**で1期使うこと——この期は §7 の「掃引しない」に従って採らなかった。
- **`置き去り×分散回復` の席**。ササの弾きで 27.3 → 12.8 に落ちた唯一の行。
  **再生成した `docs/reseat.md` で現行席は 15 位（5波平均 30.2%）に落ちた**
  ——1位は 38.7%（**+8.5pt**）で、第46期の作法では**作法(2) の `confirm`（seed 200..599）にかける行**。
  ただし上位4通りは全部**狙（ガルド前列）を満たさない**（中央がガルド）ので、
  狙を満たす最上位は 4 位の 35.8%（**+5.6pt**）。**この期では動かしていない。**
  **ササは 73 行に 1 行しか無いので、`reseat` は「編成が変わると席が変わるか」を原理的に測れない**
  （第45期「新しい隣接機構は最初から2行以上に入れること」）——**ヨミと組んだ行を1本足すのが筋。**
- **ハリの去就**。**再行動が 0.00 回/戦 しか届かない**のが第85期以来の新しい事実。
  傷軸の払い出し（第104期）は**書き手にしか届かない**ので、
  読み手であるハリを救うには別の払い出しが要る。
- **(G14) は書かなかった**（Q0 が 4/5）。**残った 1 枚（ヒヨ）は器具の欠落ではなく期待の誤り**
  ——`Actions = [Skill]` の駒は手番を持ち、手番を使う。**「手番で殴らない」と「手番を使わない」は別。**
- **第78期の入口・発火口と第80〜83期の独立の広さは、散開が移動の供給者になったぶん動く。数え直していない。**
- **未着手**: ムグの分裂／傷軸の (c)(d)／泥仕合／複数部隊・可変サイズ・作戦層。
