# 作業手順

## バランス調整のたびにやること

1. 数値や特性を変える
2. `dotnet run --project BattleSim -c Release 0 compare > docs/balance.md` で勝率を測り直す
3. `dotnet run --project BattleSim -c Release 0 dump > docs/units.md` で一覧を吐き直す
4. `dotnet run --project BattleSim -c Release 0 layout > docs/layout.md` で配置を探索し直す
5. `git diff docs/` で何が動いたかを確認する
6. docs/ の差分も含めてコミットし、動いた行をコミットメッセージにも書く

`docs/` の3ファイルは生成物。手で編集しても次の生成で消える。

3 を飛ばすと説明文と挙動がずれる。これまでに3回発生している。
2 を飛ばすと勝率表が嘘になる。差分が出ないこと自体が
「触ったがバランスは動いていない」という情報になるので、必ず測り直す。

4 は重い（1コアで40分前後）が、**飛ばすと変更を過大にも過小にも読み違える。**
配置が旧ルール前提のまま残ると、その編成は変更に対応できていないだけなのに
「変更が過剰だった」ように見える（実例: 後衛特化+後備えが旧配置3.5% / 再探索後26.0%）。
仕様変更は必ず配置の再探索とセットにする。

## 受け入れ条件のひな型

変更を測るときは、**動くはずのない編成が動いていないこと**を必ず確認する。
実装ミスの検出器として機能する。`compare` の差分が変更対象を含む編成だけに出ていれば合格。

    例: シオのバフ順序を直した → 変化したのは 移動改(バサ×ヨミ×シオ) のみ、他26編成は完全一致

## レビューのチェックリスト

人でも AI（Codex など）でも、レビューはここを見る。
どれも**実際にこのリポジトリで起きた事故**から来ている。原則の詳細は CLAUDE.md。

**測り直しの確認**

- [ ] `compare` を測り直したか。`docs/balance.md` の差分が**変更対象の編成だけ**に出ているか
- [ ] 純粋な追加（イベント・集計・診断モードの類）なら、`compare` の差分は**ゼロ**か。
      1pt でも動いていたら、記録のつもりで挙動を変えている
- [ ] 特性の挙動を変えたなら `dump` を吐き直し、`PlusText` / `MinusText` を直したか。
      **説明文と挙動のズレは過去4回発生している**（直近はハギの「1ターン1回」が残っていた）
- [ ] 仕様変更（攻撃パターンが変わる等）なら、配置の再探索（`reseat` → `confirm`）とセットか
- [ ] `docs/` を手で編集していないか。あれは BattleSim の出力を置く場所

**壊れやすい約束**

- [ ] `Trait` にインスタンスフィールドで状態を持たせていないか（**全ユニット共有のシングルトン**）。
      状態は `UnitState.Counters` に置く
- [ ] 再入防止フラグを `Trait` の `static` に置いていないか。
      `layout` は戦闘を並列実行するので、別の戦闘同士が互いの割り込みを止め合って非決定になる
- [ ] ダメージが `ApplyDamage` を通っているか。ここを迂回すると「被弾で強くなる」駒が反応しない
- [ ] `Def.Pattern` を直接読んでいないか。必ず `UnitState.CurrentPattern`
- [ ] `LivingMembers` の結果を遅延評価のまま列挙していないか（列挙中に召喚・蘇生で盤面が変わる）
- [ ] `BattleCore` に UI の参照が入っていないか
- [ ] 反撃を `ctx.Reaction` で、ターン外の割り込みを `ctx.Interrupt` で包んだか

**設計の判断**

- [ ] 積み上げる特性に、**積んだ量に比例して増えるコスト**が付いているか
      （溜めが第五波を支配したのは、コストがテンポで積んだ量と独立だったため）
- [ ] 増幅は加算か（乗算にすると毒が発散して30ターン上限に張り付く）
- [ ] 駒の要否を `pulse` の表だけで決めていないか。`ablate` と突き合わせる
- [ ] 新しい攻撃パターンを増やしていないか（4つまで。増やすなら庇う・標的・巻き込みを全部監査）
- [ ] 行動（`UnitDef.Actions`）を足したなら、溜めで `StatusKeys.IdleTurn` を立てていないか
      （据え・号令が無償の毎ターン収入として拾う。README「検証で分かったこと」の `IdleTurn` の項）
- [ ] 手番の倍率を手番外の攻撃（反撃・軋み・追い打ち）にまで掛けていないか

## 代金の原則

**常設のマイナスは符号が反転してよい。それが可変コスト型の核。**
反転してはいけないのは、**能力を制限するために置いた代金**。

> 判定: **その量を失うことをプレイヤーが望む編成があるなら、それは代金として機能しない。**

呪詛（ネル）を禁じないための線引きである。`PerverseTrait` のコメントが
「呪詛の『味方にも漏れる』というマイナスを、そのまま利益に変える駒」と書いているとおり、
常設のマイナスが編成で反転するのはこのゲームで最も価値の高い設計であって、塞ぐ対象ではない。

**見分け方。**

| | マイナス | 代金 |
|---|---|---|
| 払う回数 | 編成時に一度払う通行料 | 使うたびに払うブレーキ |
| 能力との関係 | 効き目を変えない（ネルの漏れが増減しても呪詛は同じだけ効く） | 発動と紐付く（クグの縄1本は大縛りを撃つことでのみ失われる） |

ブレーキが編成によって報酬になると、ブレーキが効かないどころか、**撃つこと自体が目的化する**。

### 代金は、その能力と独立に測れる資源で払わせる

クグは代金をクグ自身の別の出力（味方の縛り）で払ったので、**大縛りの効果と代金の消失が
同じ差分に乗った。** 2周期版の B 非収入型 +34.5 / +46.0 は「敵を縛った効果」ではなく
「味方を縛らずに済んだ効果」で、開戦時版に変えたらほぼ全部消えた（−32.0 / −48.5）。
**効果を測ったつもりで副作用を測っていた。**

## `IdleTurn` の会計

`StatusKeys.IdleTurn` は「その駒がそのターンを差し出した」という記録で、
号令（`RallyTrait`）・据え（`BulwarkTrait`）はこれを見て収入に変える。
何が買えて何が買えないかを、原因ではなく**結果**で決める。

**門は2つ。** 号令・据えが買えるのは、次のどちらでもないターンだけ。

1. 手番を消費した（`Attack` / `Charge` / `Skill`）
2. 手番外で出力を出した（カドの反撃など）

上記以外——のろま・麻痺・拘束・カドの反撃不発——は**買ってよい**。
麻痺を「敵に与えられた罰だから」と除外しない。誰が原因かで扱いを変えると、
味方由来と敵由来の判定を全特性に持ち回ることになる。**結果で解決し、原因で解決しない。**

**1つの `IdleTurn` は複数の駒が同時に買ってよい。** これはバグではなく編成パズルの核である。
正典例は `溜め改 (クグ×バン×ガン)`——縛られた味方1体の失った1ターンに対して、

| 支払い元 | 中身 |
|---|---|
| 縛め（クグ） | 攻撃 +16 |
| 号令（ガン） | 攻撃 +8 |
| 据え（バン） | 被ダメージ −50% |

の3つが同時に払われる。捨てられた欠点が積み重なって価値になるというコンセプトそのものなので、塞がない。

**ただし買えるのは「差し出された本物の空き」だけ。** 最初から自分のターンに振らない型
（`Trait.SurrendersTurn == false`。カドの `Immobile`、ハギの `Pursuer`）からは誰も買えない。
静的なマイナスが毎ターンの収入に化けるため。

### 未解決: 据えは `SurrendersTurn` を見ていない

`ApplyDamage`（`BattleEngine.cs:706`）の判定は `target.Counter(IdleTurn) >= Turn` と
`teammates.Any(Bulwark)` だけで、号令側（`RallyTrait.OnTurnStart`）にある `SurrendersTurn` の
チェックが無い。**ハギ（`Pursuer`）が据えを無償で受けている可能性がある。**

現行コードには「据えは積み上がらない一定の減衰なので制限をかけない」というコメントが
`Traits.cs` 側に残っているが、上の原則に照らせば漏れである。
**今回は直さない**（`compare` が動く独立作業なので、他の測定と交ぜられない）。

記録: この時点（`4a902a7`）の `溜め改 (クグ×バン×ガン)` の勝率は
第1波 100.0% / 第2波 96.0% / 第3波 95.0% / 第4波 100.0% / 第5波 62.0%。

## 差分を嘘にしないための設定

この運用は「`git diff` が何が動いたかの記録である」ことに全面的に乗っているので、
整形の揺れで全行差分が出ると受け入れ条件そのものが機能しなくなる。2つとも実際に起きた。

**行末** — `.gitattributes` で `* text=auto eol=lf` に固定済み。`core.autocrlf` の設定に
依存しない。これが無かった頃は Windows でパッチを当てるたびに25ファイル前後が
「変更あり」になり、中身は1文字も違わなかった（3回発生）。

**インデント** — 全ファイル4スペース。`.editorconfig` に書いてある。
ただし **Godot の内蔵スクリプトエディタはこれを読まない。** 既定がタブなので、
Godot で `.cs` を開いて保存すると全行がタブに変わる（`GodotApp/Main.cs` で1回発生、667行）。
Godot 側で エディター → エディター設定 → テキストエディタ → 動作 → インデント →
「型 = スペース / サイズ = 4」に変えること。

**見分け方** — 「変更あり」が大量に出たら、まず中身が本当に違うのかを確かめる。

    git diff --ignore-all-space --stat    # 空なら行末か空白だけ
    git hash-object <file>                # HEAD 側と一致するならインデックスの持ち越し
    git rev-parse HEAD:<file>             # （その場合は git status を Windows 側で1回流せば消える）

## コミットメッセージ

全表は `docs/balance.md` の差分としてコミットに入る。
メッセージには**動いた行だけ**を書く（24行の表を毎回貼ると `git log` が読めなくなる）。

    カドの反撃量を被弾量参照から攻撃力参照に変更

    第3波: 反撃 (ヒサ×カド)   0.0% → 34.0%
    第4波: 惨禍×被弾強化      0.5% → 99.5%
    （全表は docs/balance.md）

## 触ってよい場所

- 数値だけの調整 → `UnitCatalog.cs`、各 Trait の `const`
- 新しい特性 → `Traits.cs` に Trait を継承したクラスを足し、`TraitId` と `TraitCatalog` に登録
- 盤面全体に効く効果（味方全体の被ダメージ増減など）→ `BattleEngine.ApplyDamage`

## 触ってはいけない場所

- `BattleCore` に UI の参照を足さない。`INotifyPropertyChanged` も `ObservableCollection` も不可
- `PrototypeApp` に戦闘ルールを書かない。ここに書いた瞬間 Godot / Unity へ運べなくなる
- `Def.Pattern` を直接読まない。必ず `UnitState.CurrentPattern` を経由する
- `BattleEvent` を積む処理の中で盤面を変えない。イベントは記録であって戦闘の一部ではない。
  変えた瞬間 `verbose` の有無で結果が変わり、`compare`（verbose=false）と `replay`（verbose=true）が
  別の戦闘を見ることになる。**受け入れ確認は「`compare` の差分がゼロ」で、
  純粋な追加のはずの変更で1ptでも動いたら挙動を変えている**
- `docs/balance.md` と `docs/units.md` を手で書き換えない。BattleSim の出力をそのまま置く場所
- `docs/chain.md`（連鎖の深さ）・`docs/ablation.md`（アブレーション）・`docs/pulse.md`（活動量）・
  `docs/engage.md`（会戦）も同様。
  ただしこれらは
  「数値や特性を変えるたび」の定型手順には含めない。ablate は全編成だと30秒前後かかる診断ツールで、
  勝率が動いていない変更（据え置き判断）では回す意味が薄い。勝率表では判断がつかないとき
  （例: 差し替え候補の勝率が同じで「どちらが噛み合っているか」を見たいとき、
  「消してはいけない」判定の根拠を疑うとき）に個別に回して見る
- **`pulse` の表だけで駒の要否を決めない。** 測っているのは体験の密度で、貢献度ではない。
  呪詛・萎縮・庇いはダメージを経由せず盤面を変えるので `干渉 0` に出る。
  要否は `ablate`（抜いたときの勝率差）と突き合わせて決める
